# 代码质量与 Bug 审查报告

**审查日期**: 2026-09-07
**审查范围**: 全量静态分析 + 未提交改动（`nlp/sentiment_hub.py`、`ruff.toml`、新增 `nlp/ling_judge.py`）
**结论**: 主干质量健康，**2 个 P1 需处理**（1 个类型错误、1 个业务风险），其余为 P2/P3 与环境问题

---

## 一、门禁执行结果

| 检查项 | 口径 | 结果 | 状态 |
|---|---|---|---|
| ruff lint | 全项目 1817 文件 | All checks passed | ✅ |
| ruff bugbear (B) | 全项目 | 0 问题 | ✅ |
| ruff async (ASYNC) | 全项目 | 0 问题 | ✅ |
| mypy 基线门禁 | `utils/`（CI 口径） | 899 vs 基线 957，**-58** | ✅ |
| bandit 安全 | `nlp/` | 0 High / 0 Medium / 2 Low | ✅ |
| 悬挂引用检查 | 全项目 | 无悬挂引用 | ✅ |
| NaN 污染守卫 | 默认目录 | 0 违规 | ✅ |
| 敏感文件入库 | git 索引 | `llmkey.txt` 已 gitignore，未入库 | ✅ |

> **注**: 扩展规则集（ARG/PTH/RET/SIM/PERF）另扫出 5620 项，均为项目未启用的风格/现代化建议，**不计为缺陷**，本报告不展开。

---

## 二、P1 — 需处理

### P1-1　`_compile_rows` 返回类型注解错误（已实测确认）

**位置**: `nlp/sentiment_hub.py:318`

```python
def _compile_rows(
    alerts: dict[str, Any], annotate: dict[str, Any]
) -> tuple[list, list, list, list]:      # ← 第 4 项声明为 list
    ...
    return neg, pos, mid, ling_used      # ← ling_used 实际是 bool
```

**实测输出**:
```
compile_rows -> bool | neg: 1 pos: 1 mid: 0 ling_used: False
```

**影响**:
- mypy 报错 `Incompatible return value type (got tuple[..., bool], expected tuple[..., list])`
- 调用方 `_generate_trend_report` 已用 `bool(ling_used)` 二次转换，说明作者知晓是 bool，**运行时不会崩**
- 但注解与实现不符，误导后续维护者；且 **CI 的 mypy 只覆盖 `utils/`**，`nlp/` 的类型错误永远不会被 CI 捕获

**修复建议**（1 行）:
```python
) -> tuple[list[dict[str, Any]], list[dict[str, Any]], list[dict[str, Any]], bool]:
```

---

### P1-2　Ling 判定会吞掉规则引擎命中的高危负面预警（已实测确认）

**位置**: `nlp/sentiment_hub.py` `_compile_rows` 优先级逻辑

```python
d = it.get("direction")
if d:                                  # Ling 结果优先
    if d == "利空":   neg.append(row)
    elif d == "利好": pos.append(row)
    else:             mid.append(row)  # 中性/混合 → 不进负面表
elif it.get("kw_neg"):                 # 规则兜底：仅当 Ling 无结果
    neg.append(row)
```

**实测**（规则命中 `立案调查`，Ling 判为 `中性`）:
```
Ling=中性 覆盖规则负面命中场景:
  负面表: 0 | 正面表: 0 | 中性表: 1
  -> 规则命中的[立案调查]是否被降级: 是(预警被吞)
```

**风险**: `CRITICAL_NEGATIVE_KEYWORDS` 收录的是**立案、退市、减持**一类高危词。一旦 Ling（LLM）误判为"中性"，该条**彻底不进负面命中表**，预警链路静默失效。这是把风控硬规则让渡给概率模型的典型降级路径。

**修复建议**（需业务确认，提供两档）:
- **保守（推荐）**: 规则命中高危词时，Ling 只能"升级"不能"降级"—— `kw_neg` 命中且 Ling 非"利空"时，仍进负面表，并标注 `方向分歧(规则:利空/Ling:中性)`，保留两方信号供人工判读
- **折中**: 仅当 Ling confidence < 阈值（如 0.7）时，规则命中优先

---

## 三、P2 — 建议处理

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| P2-1 | `nlp/ling_judge.py:241` | 重试后 `parse_json_loose(raw)` 未判空，`raw` 可能为 `None` | 运行时安全（`parse_json_loose` 内有 `if not text` 守卫），类型不严谨，mypy `arg-type` 报错 |
| P2-2 | `nlp/ling_judge.py:172` | payload 声明 `dict[str, object]` 与 `JsonType` 不兼容 | 运行时安全，mypy `arg-type` 报错 |
| P2-3 | `nlp/ling_judge.py:108-114` | `_try_load_json` 全局把 `“”`→`"`；若 brief 值内含中文引号，替换后 JSON 结构被破坏 | 解析失败 → 静默回退规则引擎。**不崩溃，但拉低 Ling 成功率** |
| P2-4 | `sentiment_hub._annotate_items_with_ling` | 时间预算在**调用前**校验，单条 timeout=90s | 总时长可能超 `SENTIMENT_LING_BUDGET_SEC` 近一条的量（晨间任务窗口需留意） |
| P2-5 | `nlp/ling_judge.py:228-232` | 节流锁 `_lock` 仅保护间隔计算，HTTP 请求在锁外 | 多线程并发时节流失效；当前 `sentiment_hub` 为串行调用，**暂无实际影响** |

---

## 四、P3 — 改进项

**P3-1　门禁日志措辞误导**
`scripts/check_nan_pollution.py` 输出 `扫描 0 文件`，但其 `scanned` 计数器只在**文件存在违规**时 +1：

```python
for fpath in files:
    violations = scan_file(fpath)
    if violations:
        all_violations.extend(violations)
        scanned += 1        # ← 统计的是"有违规的文件数"
```

即"扫描 0 文件"真实含义是 **0 违规**，极易被误读为门禁空转失效。建议改为输出"已扫描 N 文件 / 违规 M 文件"。

**P3-2　新增模块零测试覆盖**
`nlp/ling_judge.py` 为新增 261 行，`nlp/sentiment_hub.py` 改动 426 行，但 `tests/` 中 grep `ling_judge|judge_news` **无任何命中**。项目 `.cnb` 规则明确要求"改动必须补测试"。建议至少补：
- `parse_json_loose` 的边界用例（中文引号、markdown 代码块、多 `{` 场景）
- `_compile_rows` 的 Ling/规则优先级用例（**可直接固化 P1-2 的预期行为**）
- `ling_enabled()` 在无 token / `SENTIMENT_LING_ENABLED=0` 下的降级路径

**P3-3　`mypy .` 全项目运行不可用**
`mypy.ini` 的 `exclude` 未覆盖 `backups/`、`_archive/`，全量运行因重复模块名直接中止：

```
backups\28-quant\2026-09-03\config\schema.py: error: Duplicate module named "schema"
Found 1 error in 1 file (errors prevented further checking)
```

CI 只跑 `utils/` 故未暴露，但**本地全量类型检查实际不可用**。建议 `exclude` 增补 `^backups/|^_archive/`。

---

## 五、环境备注（非代码缺陷）

py311 环境 requests 生态依赖不完整，导致 **17 个测试模块无法收集**：
- 缺 `urllib3` → 已补装（本次）
- 缺 `idna` / `certifi` → 仍未装，`tests/unit/test_s5_validation_unit.py` 等 17 模块收集失败

本次为恢复检查能力已安装 `typing_extensions`、`urllib3`（均为缺失依赖的补装，未升级既有包）。建议执行 `pip install -r requirements.txt` 或补齐 `idna`、`certifi` 后复跑全量测试。

---

## 六、结论与建议动作

本次改动整体质量良好：fail-safe 降级链设计完整（Ling 不可用 → 规则引擎 → 不崩溃），节流/重试/预算控制齐全，ruff 与安全扫描全绿。**主要风险不在语法层，而在业务语义层（P1-2）**。

**建议动作（按优先级）**:
1. 修 P1-1 类型注解（1 行，无争议）
2. **确认 P1-2 的处置口径**——高危负面词是否允许被 LLM 降级（这是产品决策，需拍板）
3. 补 P1-2 相关单元测试，固化预期行为
4. 修 P3-1 措辞、P3-3 mypy exclude（低风险）

---

*审查人: WorkBuddy | 方法: ruff / mypy / bandit / 项目门禁脚本 + 未提交改动人工审查 + 运行时实测验证*
