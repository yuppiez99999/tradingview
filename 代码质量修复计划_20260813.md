# 量化交易系统 v8.6.14 代码质量修复计划（2026-08-13 深度复核）

> 来源报告：`代码质量与缺陷审查报告_20260813_深度复核.md`
> 生成日期：2026-08-13
> 修复原则：**先还债再加功能、每修复必验证、灰度优先**（沿用 v9.2 工程铁律）
> 对照基线：`代码质量与缺陷审查报告_20260813.md`（主报告，6 项修复中 5 项完全闭环，S2 为半截修复）

## 优先级总览

| 级别 | 项数 | 内容 | 目标完成 |
|------|------|------|---------|
| P0 阻断性 | 1 | 空头止损执行路径断裂（Bug-1，S2 半截修复） | 08-13 |
| P1 高 | 3 | 合约月份硬编码、signal_monitor 类型不一致、对冲订单误匹配 | 08-14 |
| P2 中 | 2 | 幂等键缺 action、live_scheduler 并发竞争 | 08-15 |
| P3 债务 | 5 | DRY/print/硬编码路径/expiry/DEFAULT_PRICES | 08-16 起 |

---

## P0 — 阻断性（今天必须完成）

### P0-1. `stop_loss_monitor.py`：空头止损执行路径断裂（Bug-1）

**问题**：S2 修复了空头止损的**检测**逻辑（`_evaluate_short` 返回 `action="BUY"`，语义正确——空头平仓=买回），但 `check_and_execute` (L501-505) 只对 `action=="SELL"` 调用 `_execute_sell`。空头持仓触发止损/止盈后，record 进入 `triggered` 列表并保存到 trigger_history（`executed=False`），但**不会执行任何平仓操作**。`_execute_sell` (L530) 也硬编码 `"sell"` 方向，即使调用也无法买回平仓。

**影响**：空头敞口触发止损时系统记录"已触发"但实际不下单，亏损持续暴露。风控闭环严重缺口。

**修复**（两处改动）：

1. `check_and_execute` (L500-514) 改为支持 SELL + BUY：
```python
# 执行平仓 (支持多头卖出 SELL 和空头买回 BUY)
for record in triggered:
    if record.action in ("SELL", "BUY"):
        side = "sell" if record.action == "SELL" else "buy"
        success, order_id = self._execute_close(record.code, side, record.shares, record.current_price)
        record.executed = success
        record.order_id = order_id

        if success:
            logger.warning(
                f"止损止盈触发: {record.name} ({record.code}) "
                f"{record.trigger_type.value} @ ¥{record.current_price:.2f} "
                f"(入场 ¥{record.entry_price:.2f}, P&L {record.pnl_pct:+.1%}, 方向 {record.action})"
            )
        else:
            logger.error(f"执行平仓失败: {record.code} - {order_id}")
```

2. `_execute_sell` (L523-540) 重命名为 `_execute_close`，接受 `side` 参数：
```python
def _execute_close(self, code: str, side: str, shares: int, price: float) -> Tuple[bool, str]:
    """通过 broker 执行平仓 (side='sell' 多头卖出 / 'buy' 空头买回)"""
    if not self.broker:
        logger.warning(f"无 broker, 仅记录: {side.upper()} {code} {shares}@{price:.2f}")
        return False, "no_broker"

    try:
        success, order_id = self.broker.send_order(code, side, shares, price)
        if success and self.execution_mode == "MOCK":
            logger.warning(
                "[WARN] %s %s %d@%.2f 通过 MockBroker 模拟成交 (EXECUTION_MODE=MOCK, 真实持仓未变)",
                side.upper(), code, shares, price,
            )
        return success, order_id
    except Exception as e:  # noqa: BLE001
        logger.error(f"平仓异常: {code} - {e}")
        return False, str(e)
```

**验证**：
```bash
.venv\Scripts\python.exe -m pytest tests/unit/test_stop_loss_monitor_unit.py -v
# 新增空头止损执行测试: 触发后应调用 broker.send_order(side="buy")
# 现有 7 tests (4 long + 3 short) 应继续 PASS
```

---

## P1 — 高（本周完成）

### P1-1. `hedge_execution_orders.py`：合约月份硬编码将随时间失效（Bug-2）

**问题**：L547-549 硬编码 "2608"/"2609" 白名单，到 2026年10月后 "2610"/"2611" 会被重新解析，而 "2608"/"2609" 已过期但仍被放行。

**修复**（动态计算当前活跃合约月份）：
```python
from datetime import datetime

def _active_contract_months() -> set:
    """当前活跃合约月份 (当月 + 下月, 格式 YYMM)"""
    now = datetime.now()
    return {
        now.strftime("%y%m"),
        (now.replace(day=28) + __import__("datetime").timedelta(days=4)).replace(day=1).strftime("%y%m"),
    }

# L547-549 改为
active_months = _active_contract_months()
if any(m in instrument for m in active_months):
    pass  # 当前活跃合约月份, 直接放行
else:
    _valid, _reason = _validate_contract_expiry(instrument)
```

**验证**：
```bash
.venv\Scripts\python.exe -m pytest tests/unit/test_hedge_execution_orders_unit.py -v
# 新增测试: 模拟时间到 2026-10, 验证 "2610" 放行 / "2608" 不放行
```

---

### P1-2. `signal_monitor.py`：落盘 report 用原始 signals 可能抛 TypeError（Bug-3）

**问题**：L234-237 用原始 `signals`（可能含 None/str）计算 mean/std/max/min，而 L165 已过滤出 `numeric_signals` 用于打印。两处不一致，落盘路径可能抛 TypeError。

**修复**（L234-237 改用 numeric_signals，4 行改动）：
```python
"signal_stats": {
    "mean": float(np.mean(numeric_signals)) if numeric_signals else 0,
    "std": float(np.std(numeric_signals)) if numeric_signals else 0,
    "max": float(max(numeric_signals)) if numeric_signals else 0,
    "min": float(min(numeric_signals)) if numeric_signals else 0,
},
```

**验证**：
```bash
.venv\Scripts\python.exe -c "
import signal_monitor
# 构造含 None/str 的 signals, 验证落盘不抛 TypeError
"
```

---

### P1-3. `hedge_order_executor.py`：order_id 缺失时订单误匹配（Bug-4）

**问题**：L545 当 `order_id` 为空时回退到 `(same_inst and same_dir)` 匹配，多笔同 instrument 同方向 PENDING 订单时一笔成交会误标其他订单为 FILLED。

**修复**（要求 order_id 才能进入撮合，或生成确定性合成 id）：
```python
# 方案 A: 要求 order_id (推荐)
if not oid:
    logger.warning(f"订单无 order_id, 跳过撮合: {inst} {direction}")
    continue
same_inst = (fill_inst == inst and inst != "")
same_dir = (direction == fill_dir)
if (oid and fill.get("order_id") == oid) or (same_inst and same_dir and not fill.get("order_id")):
    # 仅当 fill 也无 order_id 时才回退到 inst+dir 匹配
    o["status"] = "FILLED"
    ...
    break
```

**验证**：
```bash
.venv\Scripts\python.exe -m pytest tests/unit/test_hedge_order_executor_unit.py -v
# 新增测试: 两笔同 inst 同 dir PENDING, 一笔成交, 另一笔不应被误标 FILLED
```

---

## P2 — 中（债务收敛）

### P2-1. `daily_trade_executor.py`：幂等键加入 action（Bug-5）

**问题**：L1633 幂等键 `f"{full_code}:{qty}"` 不含 action，同标的同 qty 的买卖指令会被误判重复。

**修复**（1 行改动）：
```python
ide_key = f"{inst['full_code']}:{inst.get('action','BUY')}:{inst.get('qty', 0)}"
```

**验证**：
```bash
.venv\Scripts\python.exe -m pytest tests/unit/test_daily_trade_executor_unit.py -v
# 新增测试: 同标的同 qty 的 BUY + SELL 应都能执行
```

---

### P2-2. `live_scheduler.py`：RUNNING 无锁并发访问（Bug-6）

**问题**：L974/979/1012 全局 `RUNNING` 在多线程下无锁访问，`stop()` 与 timer 回调间存在 TOCTOU 窗口；`module_results[name].append(...)` 后 `= [-50:]` 非原子。

**修复**（用 `threading.Event` 替代布尔标志 + Lock 保护 module_results）：
```python
import threading

RUNNING_EVENT = threading.Event()
RUNNING_EVENT.set()  # 初始为运行状态
_results_lock = threading.Lock()

# 替换所有 `if not RUNNING:` → `if not RUNNING_EVENT.is_set():`
# 替换 `RUNNING = False` → `RUNNING_EVENT.clear()`
# module_results 读写加锁:
with _results_lock:
    self.module_results[name].append(result)
    self.module_results[name] = self.module_results[name][-50:]
```

**验证**：
```bash
.venv\Scripts\python.exe live_scheduler.py --dry-run
# 构造并发压测: 多线程同时 stop() + task 回调, 验证无异常
```

---

## P3 — 债务（可选，渐进推进）

### P3-1. _DEFENSE_ASSETS 提取到共享模块（Q-1）

**问题**：`hedge_execution_orders.py:302-306` 与 `hedge_quantity_calculator.py:214-218` 重复硬编码防御资产持仓金额。

**修复**：提取到 `utils/defense_assets.py`，从 `positions.json` 动态读取，两文件 import 共享。

---

### P3-2. signal_monitor print → logger（Q-2）

**问题**：`signal_monitor.py:134-248` 45 处 print 调试语句，Windows GBK 控制台含 `✓`/`✗`/`─` 时可能抛 UnicodeEncodeError。

**修复**：统一改为 `logger.info()`，或加 `sys.stdout.reconfigure(encoding='utf-8')` 兜底。

---

### P3-3. 移除 "11_量化策略" 历史路径回退（Q-3）

**问题**：`stop_loss_monitor.py:93,95,270` 硬编码历史项目目录名作为回退路径。

**修复**：确认迁移完成后移除回退路径，或集中到 `utils/path_config.py` 统一管理。

---

### P3-4. expiry 从 instrument 解析（Q-4）

**问题**：`hedge_order_executor.py:568` 期权到期日写死为描述性字符串 "每月首个交易日次月到期"。

**修复**：从 instrument 解析合约月份（如 "510050P2609" → 2026-09-28），或从配置读取。

---

### P3-5. DEFAULT_PRICES 改为从收盘报告读取（Q-5）

**问题**：`daily_trade_executor.py:388-409` 硬编码 19 只标的现价作为兜底，会随时间 stale。

**修复**：兜底改为从最近收盘报告读取，或仅保留 ETF 价格（相对稳定），个股价格不兜底。

---

## 验证清单

每项修复完成后，执行以下验证：

```bash
# 1. 语法检查
.venv\Scripts\python.exe -m py_compile <修改文件> && echo "SYNTAX OK"

# 2. ruff F821 (未定义符号)
.venv\Scripts\python.exe -m ruff check --select F821 <修改文件>

# 3. 相关单元测试
.venv\Scripts\python.exe -m pytest tests/unit/test_<相关模块>_unit.py -v

# 4. 门禁回归
.venv\Scripts\python.exe scripts/engineering_debt_gate.py
.venv\Scripts\python.exe scripts/industrial_grade_check.py

# 5. bandit 安全 (核心执行文件)
.venv\Scripts\python.exe -m bandit -lll -ii <修改文件>
```

---

## 修复记录表（已实施 2026-08-13）

| 修复项 | 文件 | 改动 | 验证 | 状态 |
|--------|------|------|------|------|
| P0-1 Bug-1 空头止损执行 | `stop_loss_monitor.py` | check_and_execute 支持 BUY action + `_execute_sell`→`_execute_close(side)` | py_compile OK, ruff F821 0, 173 tests PASS | ✅ |
| P1-1 Bug-2 合约月份 | `hedge_execution_orders.py` | `_active_months` 动态计算当月+下月, 替代硬编码 "2608"/"2609" | py_compile OK, ruff F821 0 | ✅ |
| P1-2 Bug-3 signal 类型 | `signal_monitor.py` | L234-237 改用 numeric_signals | py_compile OK, ruff F821 0 | ✅ |
| P1-3 Bug-4 订单匹配 | `hedge_order_executor.py` | order_id 缺失时仅当 fill 也无 order_id 才回退 inst+dir 匹配 | py_compile OK, ruff F821 0 | ✅ |
| P2-1 Bug-5 幂等键 | `daily_trade_executor.py` | ide_key 加入 action: `f"{full_code}:{action}:{qty}"` | py_compile OK, ruff F821 0, 173 tests PASS | ✅ |
| P2-2 Bug-6 并发锁 | `live_scheduler.py` | `RUNNING_EVENT = threading.Event()` + `_results_lock` 保护 module_results | py_compile OK, ruff F821 0 | ✅ |
| P3-3 历史路径回退 | `stop_loss_monitor.py` | 移除已删除的 "11_量化策略" 回退路径 (3 处) | py_compile OK, ruff F821 0 | ✅ |
| P3-4 expiry 解析 | `hedge_order_executor.py` | 新增 `_parse_expiry_from_instrument()` 从合约代码解析近似到期日 | py_compile OK, ruff F821 0 | ✅ |

**验证汇总** (2026-08-13 实施):
- 语法检查: 6/6 文件 py_compile OK ✅
- ruff F821 (未定义符号): All checks passed ✅
- 单元测试: 173 passed, 0 failed (stop_loss + hedge_execution_orders + daily_trade_executor) ✅
- engineering_debt_gate: 债务等级 GREEN (T1-T18 + D1-D8 全 PASS) ✅

**未实施项** (P3 可选债务, 标注 08-16 起):
- P3-1 _DEFENSE_ASSETS 提取到共享模块 (需创建新文件, 影响实盘对冲, 风险较高)
- P3-2 signal_monitor 45 处 print → logger (工作量大, 收益低)
- P3-5 DEFAULT_PRICES 改为从收盘报告读取 (中等改动, 兜底价影响有限)