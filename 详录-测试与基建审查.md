# 工程基建审查：测试与 CI/依赖/仓库卫生
目标：E:\各种PY程序\28-终极量化交易系统8.4\ ｜ 审查时间：2026-09-09 ｜ 审查方式：抽样 16+ 测试文件、全量统计脚本、git ls-files 抽查、CI/配置文件通读

---

## 测试质量（618 文件 / 195,634 行）

**[P1] tests\test_qmt_audit_fixes.py — 脚本式伪测试，永远不会作为 pytest 用例执行**
- 证据：全文 0 个 `def test_`，54 个 assert 全部位于模块顶层（import 时立即执行）。pytest `python_files = test_*.py` 会收集导入该文件：断言通过 → 报 0 tests（静默"绿"）；断言失败 → collection error。两种情况都**没有任何用例被执行或统计**。文件内自建 `passed/failed` 计数 + print("PASS: ...")，是把验证脚本伪装成测试文件。
- 影响：CI 的 unit-tests job 选择到它时，覆盖率与通过数都是假象。
- 建议：包成 `test_` 函数 / parametrize；或改名为 scripts/verify_qmt_audit_fixes.py 移出 tests\。

**[P1] tests\e2e\test_eod_dry_run.py + tests\test_code_quality_extreme_market.py — 自建测试框架的"巨型 runner"**
- 证据：eod_dry_run 358 行、0 test_ 函数、0 assert，自建 main() + separator() 打印（文件名以 test_ 开头会被 pytest 收集导入，模块级还 mkdir、reconfigure stdout）；code_quality_extreme_market 1103 行只有 3 个 test_ 函数，主体是自建 TestResult/TestCollection 类（重复造 unittest 轮子）。
- 影响：测试金字塔里混入"手写 runner"，无法被 pytest 统计/并行/超时管控（pytest.ini timeout=300 对其无效）。
- 建议：改造为标准 pytest 用例；纯性能脚本移入 benchmarks\。

**[P2] tests 下无 assert 文件 29/618（4.7%），无 test_ 函数文件 34/618**
- 证据：29 个无 assert 文件中约 12 个是 conftest.py / __init__.py / harness / benchmark（合理），其余即上述伪测试与 harness；`assert True` 全仓仅 2 处——空跑几乎为零。
- 建议：为 conftest/helper 文件统一加 `test_` 前缀排除规则或在 pytest.ini `norecursedirs` 收紧。

**[亮点] 抽样 16 文件：真断言为主，mock 用量克制**
- 证据：test_phase_manager.py 8 tests/94 asserts；test_data_contracts.py 16/21；test_tdam_client.py 20/52；test_d6_litellm_router.py 37 tests/74 assert/4 raises。mock 集中在 LLM/网络边界（test_llm_client_unit.py 23 tests 全部 monkeypatch 外部 API，断言真实返回值），属合理隔离而非"测 mock 实现"。约每 test 1.5~2 个真 assert。

**[P2] tests\conftest.py 31,526 字节 — 根 conftest 巨石化**
- 证据：31KB 根 conftest（fixtures 逻辑高度集中），另存在 conftest_benchmark.py。
- 建议：按层级拆分到子目录 conftest / fixtures\。

## 覆盖率真实性

**[P1] 覆盖率口径三处互相矛盾，README 数字不可复现**
- 证据：① `.coveragerc`：`source = utils, ms_strategy`；② CI ci.yml L460：`--cov=utils --cov=v8.3_institutional/src`（与 .coveragerc 不同）；③ README L111 声称 `line_rate=0.833, branch_rate=0.7605 ≥0.80 达标`。
- 关键：当前 `.coverage`（114KB，version 7.15.4）只含 **utils/etf_option_combo 的 11 个文件**——是单次 sprint 运行残留，83.3% 全量声称无法由此验证；core、quant_modules、cloud、根目录 100+ 业务文件全部不在覆盖统计口径内。
- 建议：统一 .coveragerc 与 CI 的 --cov 口径；覆盖率产物（coverage.xml/.coverage）不入库并清理；README 徽章数字标注生成日期与口径。

**[P1] CI 覆盖率门槛形同虚设**
- 证据：ci.yml L479 `--min-line-rate 0.05`（5% 阈值），且失败后 `$global:LASTEXITCODE = 0` 强制放行（注释自认"warning 级 不阻断"）；L460 `--cov-fail-under=60` 旁注释也是"不阻断"。CI 里 unit 测试经 GAP-5 `Select affected tests` 只跑**受影响子集**，全量 618 文件仅在 nightly schedule 兜底。
- 建议：fail-under 提到硬门禁；coverage-trend 阈值至少与 README 声称一致；push 全量 unit 至少每周强制一次并阻断。

## CI

**[亮点] .github\workflows 存在且 push(main) 真实触发**
- 证据：ci.yml 34KB，`push: branches:[main]` + `pull_request` + `schedule 0 2 * * *` + workflow_dispatch；jobs：incremental-static(PR diff) → quality-gate → lint-typecheck(mypy/pylint) → smoke-tests → unit-tests(-n auto 并行 + coverage) → integration-tests(含 Chaos)；PYTHON_VERSION='3.14' 与 .venv(3.14.4) 一致；pytest.ini 规范（strict-markers、timeout=300、filterwarnings error::ResourceWarning）。另有 quality-gate.yml、tdd-guard.yml 等 8 个 workflow。

## 依赖管理

**[P2] 同一依赖声明 4~7 套并存**
- 证据：requirements.txt(37包) 与 requirements-core.txt(32包) 前 4 行完全相同（numpy/pandas/scipy/sklearn）；另有 -cloud/-win/-optional/requirements_dev.txt/requirements_py38_backup.txt + pyproject `[project].dependencies` + `[project.optional-dependencies]` + uv.lock(1.3MB)。pyproject L5 注释自认"requirements.txt 保留为 pip 回退"，但 6 个 txt 无单一真源。
- 建议：pyproject 为唯一真源，requirements*.txt 由工具生成（pip-compile/uv export）并 CI 校验一致；删除 requirements_py38_backup.txt。

**[P2] Python 版本下限未验证**
- 证据：pyproject `requires-python = ">=3.10"`，但 CI 与 .venv 均只用 3.14；声明支持 3.10~3.13 却无任何矩阵验证（CI 单一 3.14）。
- 建议：requires-python 收紧为 ">=3.14" 或加 3.10/3.12 矩阵。

## 仓库卫生

**[P2] 工作区垃圾 749 .bak + 4424 __pycache__ + 根目录散落临时文件（均未被 git 跟踪）**
- 证据：`git ls-files` 共 2736 文件，跟踪 .bak=0、__pycache__=0、llmkey.txt 未跟踪、`git status --porcelain` 为 0（.gitignore 生效）；但磁盘上：.bak* 文件 749 个、__pycache__ 目录 4424 个；根目录散落 _tmp_prep_check.py、logs_s13_klines.log、shadow_account.log、institutional_pipeline_runner.log(0B)、.venv_install_log*.txt ×5、portfolio_return_projection.json.bak_20260824~0909 ×39、v8.6架构图 html ×744KB×2。
- 影响：git 干净但磁盘即"增量备份库"，生产数据（持仓投影）备份混在代码根目录，回滚时极易用错版本。
- 建议：清理 __pycache__（git clean -Xdi 或定期脚本）；程序内备份改写到 backups\ 目录并滚动保留 N 份；_tmp_*.py / *.log 移出根目录。

**[P1] llmkey.txt 裸放仓库根目录（2,868 字节）+ .env 同级**
- 证据：两文件存在于根目录，git 已忽略未跟踪；llmkey.txt 命名即密钥文件。未读取内容以避免密钥外泄。
- 建议：立即迁移到密钥管理器/环境注入；确认该 key 未曾被提交进历史（`git log --all -- llmkey.txt` 为空则只需迁移文件）；轮换该 key 更稳妥。

**[P2] 根目录→utils 的转发 shim 双轨入口**
- 证据：fast_backtest.py、deflated_sharpe.py、automated_execution_system.py、daily_build_and_hedge.py、rebalance_execution_orders.py 共 5 个根文件为纯 re-export shim（"保持 from fast_backtest import ... 兼容"）。
- 建议：发一次弃用周期后删除 shim，统一 `from utils.alpha...` 导入。

**[P2] utils\ 巨石目录：42 子目录 / 206 顶层文件 / 203,214 行，且内含 utils\backup、utils\_legacy**
- 证据：utils 行数是 tests 之外最大的单一目录；内部重名文件仅 9 组（pairs_trading.py ×3、registry.py ×3、base.py ×3、types.py ×2、engine.py ×2 等），说明**重复实现不多，主要问题是"什么都往 utils 塞"**——LLM 网关就有 utils\alpha\llm\ + utils\llm_gateway\ + utils\llm_client.py + utils\local_llm.py 四套并存。
- 建议：按域拆包（llm 统一为一个包）；backup/_legacy 移出 utils。

---

## 统计
- P0：0
- P1：4（伪测试文件、覆盖率口径矛盾、CI 覆盖门槛虚设、llmkey.txt 裸放）
- P2：6（conftest 巨石化、无 assert 文件占比、依赖 4 套并存、Python 版本声明过宽、仓库垃圾堆积、shim 双轨 + utils 巨石[合并项]）
- 亮点：3（真断言为主/空跑仅 2 处、CI 完整真实触发、pytest.ini 规范）
- 总体：测试**内容质量高于平均开源项目**（真断言、mock 克制），但存在 ~12 个"脚本式伪测试"混入 tests\；覆盖率数字有口径漂移不可复现；CI 骨架完整但质量门禁多为 warning 级；git 卫生良好（垃圾全部未跟踪），磁盘卫生差。
