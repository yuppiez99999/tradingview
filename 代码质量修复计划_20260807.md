# 量化交易系统 v8.6 代码质量与缺陷修复计划

来源报告：`代码质量与缺陷审查报告_20260807.md`（OCR + ruff/mypy/bandit/pre-commit 全量扫描）
生成日期：2026-08-07
修复原则：**先还债再加功能、每修复必验证、灰度优先**（沿用 v9.2 工程铁律）。

## 优先级总览

| 级别 | 项数 | 内容 | 目标完成 |
|------|------|------|---------|
| P0 阻断性 | 3 | live_scheduler BASE_DIR 崩溃、UI 页语法错误、再平衡订单执行断链 | 08-07 |
| P1 高 | 5 | SPY 硬编码假订单、cli/modes 未定义名称、对冲订单双路径、0股假FILLED、WT风控前置 | 08-07~08 |
| P2 中 | 5 | 静默吞异常、positions键、stop_loss None、mock降级、.SH回退 | 08-08 |
| P3 债务 | 3 | ruff/mypy baseline、#type:ignore精确化、print收敛 | 08-09 起 |

---

## P0 — 阻断性（今天必须完成）

### P0-1. `live_scheduler.py`：补上 `BASE_DIR` 定义（导入即崩）

**问题**：L50 导入了 `get_project_root` 但从未调用；L77 模块级 `LOCK_FILE = BASE_DIR / ".live_scheduler.lock"` 引用未定义的 `BASE_DIR`，导致 `import live_scheduler` 立即抛 `NameError`，`--live` 模式整体不可用。全文件 13 处引用 BASE_DIR（L77/151/195/203/385/608/613/642/643/675/691/692/804）。

**修复**（在 L56 `LOG_DIR = get_log_dir()` 之后补一行）：
```python
# ============================================================
# 路径初始化 (v8.5+: 通过 path_config 统一管理)
# ============================================================
setup_sys_path()
BASE_DIR = get_project_root()   # ★ 新增：此前缺失导致模块级 LOCK_FILE 立即 NameError
LOG_DIR = get_log_dir()
LOG_DIR.mkdir(exist_ok=True)
```

**验证**：
```bash
.venv\Scripts\python.exe -c "import live_scheduler; print('IMPORT OK', live_scheduler.BASE_DIR)"
.venv\Scripts\python.exe live_scheduler.py --dry-run   # 若能启动调度器则修复成功
```

---

### P0-2. `ui/pages/10_🔬_宏观综合分析.py`：修正缩进（页面无法解析）

**问题**：L116-119 `if adj_data:` 块内 `st.dataframe`（L117）比 `if` 多缩进 8 空格，`else:`（L118）与 `if` 不匹配，触发 `unindent does not match any outer indentation level` 语法错误，整个 Streamlit 页面无法加载。

**修复**：将 L117/118/119 与 L116 `if adj_data:` 对齐到同一缩进级别（24 空格）：
```python
                        if adj_data:
                            st.dataframe(pd.DataFrame(adj_data), use_container_width=True, hide_index=True)
                        else:
                            render_alert_card("权重调整", "无需调整", level="info")
```

**验证**：
```bash
.venv\Scripts\python.exe -m py_compile "ui/pages/10_🔬_宏观综合分析.py" && echo "SYNTAX OK"
# 并启动 streamlit 加载该页确认无语法错误
```

---

### P0-3. 再平衡订单接入执行器（执行断链）

**问题**：`utils/execution/automated_execution_system.py:2202-2233` 的 `_generate_rebalance_orders()` 生成 BUY/SELL 订单后仅 `json.dump` 到 `reports/rebalance_execution_orders_{date}.json`，**无任何模块读取该文件进入撮合**（全库检索确认无消费者）。与历史"期权对冲只生成不执行"断链同源。

**修复方案**（在 `_generate_rebalance_orders` 中，`json.dump` 之后把订单送进撮合路由）：
```python
# ★ 新增：不再只写盘，把有效订单路由进执行队列
valid_orders = report.get("orders") or report.get("valid_orders") or []
for o in valid_orders:
    if not (o.get("code") and o.get("action") and o.get("qty")):
        continue
    self.order_router.route_order(
        {
            "trade_id": f"REBAL_{o['code']}_{o['action']}",
            "instrument": o["code"],
            "direction": "buy" if o["action"] == "BUY" else "sell",
            "trade_size": float(o.get("qty", 0)),
            "urgency": "normal",
            "asset_type": "equity",
        },
        self.current_market_state,
    )
self.order_router.process_execution_queue()
```

**注意**：需先确认 `report` 中订单的实际字段名（`code`/`action`/`qty` 或 `instrument`/`side`/`shares`），与 `utils/execution/rebalance_execution_orders.py` 的 `build_report` 返回结构对齐后再落地。若 `route_order` 对 A 股代码命名有约束（如 `600519.SH`），需在 route 前补全后缀。

**验证**：运行 `automated_execution_system._generate_rebalance_orders()`，断言 `order_router.execution_stats["total_orders"]` 增加、`successful_orders` 匹配有效订单数。

---

## P1 — 高

### P1-1. `automated_execution_system.py`：删除硬编码 SPY 假订单

**问题**：L1627-1655 `trade_info` 硬编码 `{"instrument":"SPY","direction":"buy","trade_size":100000}`，`route_order(execution_plan,...)` 消费的是这个假 SPY 单，与系统自己算出的再平衡/对冲订单完全脱钩。

**修复**：将 `_execute_daily_trading` 步骤 4-7 改为以步骤 11 `_generate_rebalance_orders()` 产出的真实订单构造 `execution_plan` 并逐个 `route_order`；删除硬编码 `trade_info` 与 `select_execution_strategy` 对 SPY 的调用。若步骤 11 尚未产出（P0-3 未完成），此步应返回失败而非路由 SPY 假单。

**验证**：构造单测，断言 `route_order` 收到的 instrument 不为 `"SPY"`，且 trade_size 来自真实再平衡订单。

---

### P1-2. `cli/modes/` 未定义名称补 import

**问题**：多个 CLI 模式运行时 `NameError`（ruff F821）：
- `cli/modes/commodity_fundamentals.py:28,38`：缺 `import sys, os, datetime`
- `cli/modes/enhanced_prediction.py:37,47`：缺 `run_ml_signal_mode`（应导入）、`import pandas as pd`
- `cli/modes/enhanced_training.py:37,38`：缺 `import os`
- `cli/modes/hedge_detail_mode.py:87`：缺 `get_quotes_batch` 导入
- `cli/modes/kommo_monitor.py:26`：缺 `import os`
- `scripts/strategy_evaluator.py:80`：缺 `WalkForwardResult` 导入
- `scripts/sync_covered_call_collateral.py:122`：缺 `data` 定义

**修复**：逐个补齐缺失 import / 从正确的模块导入符号。对 `run_ml_signal_mode`、`get_quotes_batch`、`WalkForwardResult` 需先确认其定义模块再 `from ... import`。

**验证**：`ruff check cli/modes --select F821` 归零；逐个 `python cli/modes/xxx.py --help` 或调用对应入口确认不再 NameError。

---

### P1-3. 对冲订单双路径统一

**问题**：`automated_execution_system._generate_hedge_execution_orders`（L1942-1987）写的 `hedge_execution_orders_*.json` 无撮合消费；真正的撮合器 `hedge_order_executor.py` 从 `trade_plan_{date}.json` 的 `hedge_execution`/`futures_options_hedge` 字段读。两条路径各自独立。

**修复**：以 `trade_plan` 为唯一真相源——`_generate_hedge_execution_orders` 产出的订单回写进 `trade_plan["hedge_execution"]["active_orders"]`（按 `_update_positions_state` 的嵌套字典形状：`put_protection`/`covered_call`），由 `hedge_order_executor._collect_pending_orders` 撮合；`hedge_execution_orders_*.json` 降级为归档/审计用途，不再作为撮合输入。

**验证**：构造含对冲字段的 trade_plan，跑 `hedge_order_executor.py --date <d>`，断言 PENDING 期权订单被撮合、组合 Beta 下降、`hedge_execution_fill_*.json` 落盘。

---

### P1-4. `daily_trade_executor.py`：修复 0 股假 FILLED

**问题**：L1313-1409 `_execute_single_instruction` 当 `fill_amount < exec_price` 时 `actual_qty` 取整为 0（L1365），但 `actual_qty == 0` 仍返回 `status: "FILLED"` 并计入结果，造成静默假成交。

**修复**：在 L1366 后加守卫——`actual_qty <= 0` 时返回 `{"status": "SKIPPED", "reason": "fill_amount_below_min_unit"}`，不进 `execution_results`/`daily_records`，不更新 positions/progress。

**验证**：单测传入 `fill_amount < exec_price` 的指令，断言返回 `SKIPPED` 而非 `FILLED`，且 positions.shares 不变。

---

### P1-5. `daily_trade_executor.py`：WT 风控盘前阻断

**问题**：L657-679 `_run_wt_risk_precheck` 盘前仅打印风险评分不阻断执行，高风险指令仍落入人工确认清单。

**修复**：在 `_run_wt_risk_precheck` 中，当 `risk_summary["risk_score"]` 超过阈值（与 `_run_wt_risk_block_check` 一致，通常集中度/回撤超限）时返回阻断标记，`generate_instructions` 据此过滤或标注高风险指令；并确保 `wt_modules` 初始化失败（返回 None）时显式告警而非静默放行。

**验证**：构造超阈值组合，断言盘前生成指令数下降或带 HIGH_RISK 标记，`utils/notify.send_alert` 被调用。

---

## P2 — 中

### P2-1. 7 处静默吞异常收敛
`daily_trade_executor.py:85,457,1547`；`live_scheduler.py:491,780,1091,1114`。将 `except:`/`except Exception` 改为具体异常类型 + `logger.warning(..., exc_info=True)`，不静默吞掉。

### P2-2. `utils/execution/rebalance_execution_orders.py:45` 缺键崩溃
`data["positions"]` → `data.get("positions", {}) or {}`。

### P2-3. `stop_loss_monitor.py:317,321` None 除崩
`rule.get("stop_loss_pct", -12.0)` → `(rule.get("stop_loss_pct") or -12.0)`，并对非数值加 `float()` 保护。

### P2-4. `stop_loss_monitor.py` mock 静默降级
无真实 broker 时显式标记 `EXECUTION_MODE=MOCK` 并告警/拒绝，避免"看似已执行实为模拟"。

### P2-5. `daily_trade_executor.py:1293` 硬编码 `.SH` 回退
`f"{code}.SH"` → 用 `_infer_suffix(code)` 动态推断后缀，避免 `.SZ`/`.BJ` 标的补同步失败。

---

## P3 — 债务收敛（CI baseline）

### P3-1. ruff baseline
全项目 13274 错误（T201 print 5266、ANN 缺注解 ~2815、BLE001 宽泛 except 667、F401 未用导入 445 为主）。建议先生成 baseline 白名单（`# noqa` 或 `per-file-ignores`），恢复 CI `--exit-non-zero` 门禁，再按类别分批清零。**优先级：先清 F821(35) 与 invalid-syntax(10)（已是 P0/P1 项）→ F541 f-string 误用(257) → F401 → BLE001 → T201。**

### P3-2. `# type: ignore` 精确化
全项目 483 处，mypy 报大量 `unused-ignore`/错误码不匹配。改为带具体错误码的精确忽略（`# type: ignore[assignment]`），启用 `warn_unused_ignores`，逐步把核心执行文件 mypy 错误（230）降到可接受基线。

### P3-3. print 收敛
1541 处 print 改用 `logging`（统一 `PYTHONIOENCODING=utf-8`），避免 Windows GBK 下含货币符号（¥）抛 `UnicodeEncodeError`。

### P3-4. bandit 误报标注
`utils/last30days_adapter.py:344`、`utils/tdam_client.py:828` 的 MD5 用于缓存键/查询哈希（非安全用途），加 `# nosec B324` 注释；`utils/console_encoding.py:50` `chcp shell=True` 命令为常量列表，加注释说明无注入风险。

---

## 实施顺序与回归

1. **第一批（P0-1, P0-2）**：两处"文件级阻断"直接修复并 `py_compile` 验证。
2. **第二批（P0-3, P1-1, P1-3）**：执行链断链——涉及订单路由/撮合，需先跑 `pytest` 确认不破坏现有撮合闭环。
3. **第三批（P1-2, P1-4, P1-5）**：CLI 名称修复 + 假成交/风控逻辑，配单测。
4. **第四批（P2）**：边界健壮性。
5. **回归**：每批完成后运行
   ```bash
   .venv\Scripts\python.exe -m pytest --collect-only -q          # 确认 0 collection error
   ruff check <改动文件> --select F821,F401,invalid-syntax      # 确认新增归零
   .venv\Scripts\python.exe scripts/check_exception_policy.py    # 异常策略门禁
   ```
6. **commit 约定**：`[G{编号}] {描述}`（沿用 v9.2 工业级 gate 标记体系），如 `[G15] fix live_scheduler BASE_DIR undefined`。

## 风险提示

- P0-3/P1-3 涉及撮合链路改造，落地前必须确认 `rebalance_execution_orders` 的 `report` 结构与 `order_router.route_order` 的入参契约，避免字段名不匹配导致新断链。
- P1-5 风控阻断阈值应与 `_run_wt_risk_block_check` 保持一致，防止盘前盘后判定矛盾。
- P3 baseline 生成后要纳入 `.opencodereview/rule.json` 或 ruff 配置持久化，防止 CI 回退。
