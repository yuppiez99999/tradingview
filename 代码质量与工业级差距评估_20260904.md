# 代码质量与工业级差距评估报告 — 2026-09-04

> **评估范围**: `28-终极量化交易系统8.4/` 全量代码
> **工具链**: industrial_grade_check / engineering_debt_gate / assert_data_validity / check_dangling_refs / ruff / mypy / bandit / pytest (venv Python 3.14)
> **对照基线**: v9.1 工业级达标计划 (2026-08-06) + 代码质量审查报告 (2026-08-27)

---

## 1. 执行摘要

| 维度 | 实测结果 | 评级 | 趋势(vs 08-27) |
|------|---------|------|----------------|
| 工业级判据 (C1-C12) | 11 PASS / 1 WARN / 0 FAIL | A- | 持平 |
| 数据有效性 (D1-D12) | 12 PASS / 0 FAIL | A | 持平 |
| 悬挂引用 | 0 处 | A | 持平 |
| 测试健康 | 16709 tests collected / 0 errors; etf_option_combo 126 passed | A | 持平 |
| 覆盖率 | 0.8330 (基线持平) | A | 持平 |
| **工程债务门禁** | **RED (T6 + D11)** | **C** | **退化** |
| **mypy 类型基线** | **FAIL: +22 净新增** | **C** | **退化** |
| ruff 存量 | 122 处 (含 F821×2 生产代码) | B- | 退化(75→122) |
| bandit 安全 | 0 HIGH / 0 MEDIUM (仅 LOW 1295) | A | **改善**(2 HIGH 已清零) |
| v8.7 发布门禁 | **BLOCK (D11)** | — | 未解除 |

**总体评价**: 生产链路（执行闭环/期权对冲/数据契约/环境隔离）持续保持健康，安全高危已清零。**但 8 月底新增的 `utils/etf_option_combo/` 期权组合模块未过类型门禁即合入，导致 mypy 基线净增 22 个 error 并出现 2 处生产代码 F821（未定义名）**——这是本轮最主要的质量退化，违反"新模块落地必须同轮 ruff+black+pytest 全绿"的项目铁律。工程债务门禁因 T6(31>30) 与 D11(样本 11/20) 双触发降级为 RED，v8.7 发布持续阻断。

---

## 2. 门禁实测快照 (2026-09-04 19:05)

### 2.1 industrial_grade_check (11 PASS / 1 WARN / 0 FAIL)

| 判据 | 状态 | 说明 |
|------|------|------|
| C1 执行闭环 | **WARN** | QmtBrokerAPI 已实现但 broker.enable=false / dry_run=true，真实下单未接线 (xtquant 未安装=物理阻塞) |
| C2 数据管道分层 / C3 环境隔离 / C4 信号执行分离 / C5 测试可运行 / C6 CI 可运行 / C7 告警 / C8 成交回报落盘 / C9 期权对冲执行链 / C10 fills 新鲜度 / C11 PnL 桥接 / C12 期权执行链集成 | OK | 全部通过 |

### 2.2 engineering_debt_gate: **RED** (2 项失败)

| 编号 | 状态 | 实测 | 判定逻辑 |
|------|------|------|---------|
| T6 fail-safe 宽捕获 | **FAIL** | 31 处 (> 阈值 30) | 仅超阈值 1 处即翻红， YELLOW→RED 边界 |
| T7 裸 except Exception | OK | 198 处 (≤250) | — |
| D11 PhaseB shadow | **FAIL** | 稳定 11/7 天 ✓，但样本 11/20 ✗ | **不是不稳定，是样本量不足**：连续稳定天数已达标签要求，total_samples<20 触发阻断 |
| 其余 T1-T5/T8-T18/D1-D10 | OK | — | 含覆盖率 0.8330、超大文件均 ≤2000 行 |

**v8.7 发布门禁: BLOCK** — 唯一阻断项 D11。PhaseB enabler 显示当前 orchestrator 阶段观察期 31/21 天已满、健康检查 PASS，Stage 1-3 均 DONE。

### 2.3 mypy 基线门禁: **FAIL**

```
[mypy-baseline] 当前 error 数: 982 | 基线: 960 | 增量: +22
```

新增 error 的文件分布（与基线差集 109 行，净 +22）：

| 文件 | 典型问题 |
|------|---------|
| `utils/etf_option_combo/combo_base.py` | **F821: `GreekExposure`/`OptionDataFetcher` 未定义** + Returning Any ×N |
| `utils/etf_option_combo/cash_secured_put.py / collar.py / combo_backtest.py` | Need type annotation / "object" has no attribute |
| `utils/alpha/drift_monitor.py` (5 处) | Returning Any / Optional 属性访问 |
| `utils/chaos/fault_injector.py:40` | Name `NonMonotonicTimestampError` 未定义 |
| `lgb_trainer/trainer.py:833` | Name "adaptive_optimize" already defined |
| `quant_modules/ai_hedge_fund/utils/progress.py:76` | Too many arguments |

### 2.4 ruff 存量 (122 处)

| 规则 | 数量 | 说明 |
|------|------|------|
| T201 print | 22 | P0 门禁外文件 |
| F401 unused-import | 20 | 可自动修 |
| I001 unsorted-imports | 17 | 可自动修 |
| W292 缺末尾换行 | 14 | 可自动修 |
| **F821 undefined-name** | **2** | **生产代码 `combo_base.py`，阻断级** |
| E741/N806/N803 | 21 | 命名风格 |
| 其余 (F841/B905/ANN/UP009/C901/B009) | ~25 | 低风险 |

### 2.5 门禁脚本自身 bug（新发现）

`engineering_debt_gate.py` 运行时输出中出现子进程读取线程崩溃：
```
Exception in thread Thread-1 (_readerthread):
UnicodeDecodeError: 'gbk' codec can't decode byte 0xab
```
门禁内部某处 `subprocess.run` 未指定 `encoding='utf-8'`，Windows 默认 GBK 解码含中文的子进程输出失败。本次未影响判定结果输出，但属于门禁工具自身的可靠性缺陷（违反"门禁自身 fail-closed"原则）。

---

## 3. 缺陷清单（按严重度）

### P0 — 阻断级（须立即修复）

| # | 缺陷 | 位置 | 影响 | 修复方案 |
|---|------|------|------|---------|
| P0-1 | **F821 未定义名 ×2**：`GreekExposure`、`OptionDataFetcher` 在字符串注解中被引用但模块内未导入（docstring 声称复用 `utils.greek_hedge_manager` 但无 import） | `utils/etf_option_combo/combo_base.py:125,240` | 当前因 `from __future__ import annotations` 不在运行时崩溃，但任何 `typing.get_type_hints()`/序列化框架消费该 dataclass 即 `NameError`；类型检查已实打实报错 | 加 `if TYPE_CHECKING: from utils.greek_hedge_manager import GreekExposure` 及 OptionDataFetcher 对应导入（或定义本地 Protocol） |
| P0-2 | **mypy 基线净增 22**：etf_option_combo 新模块带类型债合入，基线门禁 FAIL，CI 类型防线失守 | `utils/etf_option_combo/*` 等 | 允许类型退化累积；对照项目铁律"新模块落地必须同轮 ruff+black+pytest"，缺 mypy 环节 | 修复该模块全部新增 error 至 ≤ 基线；**禁止用 `--update` 直接冻结掩盖**（先修复存量再谈重冻结） |
| P0-3 | **门禁脚本子进程 GBK 崩溃** | `scripts/engineering_debt_gate.py`（内部 subprocess 调用处） | 门禁自身可靠性缺陷，极端情况下误判 | 所有内部 `subprocess.run(...)` 补 `encoding="utf-8", errors="replace"` |

### P1 — 高风险（本迭代内修复）

| # | 缺陷 | 实测 | 影响 | 修复方案 |
|---|------|------|------|---------|
| P1-1 | D11 样本量不足致 v8.7 发布阻断 | 11/20 样本（稳定天数已达标） | 发布被阻断 | 无需代码修复：继续每日 shadow 跑积累样本至 20；按当前节奏（观察期 31 天积累 11 样本≈每周 2-3 个交易日样本）预计 2-3 周解除。**注意核对 min_samples=20 的口径是否过严**（31 天观察期 vs 20 样本，比例偏高） |
| P1-2 | T6 宽捕获 31 处超阈值 | > 30 翻红 | 债务等级 RED，建议冻结新功能 | 精确化至少 2 处（缩小捕获范围或改窄异常类型）即回到 ≤30；扫描命令见 §5 |
| P1-3 | C1 QMT 真实下单未接线 | broker.enable=false + xtquant 未安装 | 实盘前置阻塞（12月底上实盘计划的最大技术依赖） | 安装 xtquant → account 配置 → enable=true + dry_run 影子期 → 灰度（按原计划 Phase 4，勿提前裸实盘） |

### P2 — 中风险（排期修复）

| # | 缺陷 | 说明 |
|---|------|------|
| P2-1 | **压力测试疑似回退为模拟持仓**：assert D1 显示 `stress_test_SIMULATED_20260824.json (is_simulated=true)`。G3 曾于 08-06 修复为读 positions.json 真实持仓（26 标的 actual_pnl 非零），现最近报告为 SIMULATED 且断言跳过校验。需核查 stress_test_runner 运行入口是否退回 `--simulate` 模式 |
| P2-2 | ruff 存量 122 处（56 处可 `--fix` 自动修复），T201×22 集中在门禁豁免名单外文件 |
| P2-3 | `utils/chaos/fault_injector.py:40` Name `NonMonotonicTimestampError` 未定义（mypy 新增） |
| P2-4 | `lgb_trainer/trainer.py:833` 重复定义 `adaptive_optimize`（疑似合并代码时残留） |

---

## 4. 与工业级程序的差距对比

对照 v9.1 达标计划 15 项差距的当前状态：

| # | 差距项 | v9.1 状态 | **当前实测** | 结论 |
|---|--------|----------|-------------|------|
| G1 | 真实券商下单 | 未接线 | **仍为 WARN**（C1） | ❌ 残留（xtquant 物理阻塞） |
| G2 | 再平衡执行断链 | — | C8 OK，FillsStore 接入执行链 | ✅ 已闭合 |
| G3 | 压力测试空持仓 | 08-06 已修复 | **最新报告为 SIMULATED** | ⚠️ 疑似回退，需核查 |
| G4 | 成交回报驱动 PnL | — | C11 OK | ✅ 已闭合 |
| G5 | 研究/生产隔离 | 08-11 清零 | C3 OK | ✅ 已闭合 |
| G6 | mypy 基线 | 1967 基线 | 960 基线（大幅收敛）但 **+22 净增** | ⚠️ 曾收敛，正在退化 |
| G7 | 覆盖率 | 无产物 | **0.8330 ≥ 0.80** (D9 OK) | ✅ 已闭合 |
| G8 | 告警接入 | 已接入 | C7 OK | ✅ 已闭合 |
| G9 | FeatureStore 物理分层 | 缺 | 未在本轮检查范围（架构级） | ➖ 维持原计划 |
| G10 | 核心热路径 C++/Rust | 缺 | 未变 | ➖ 维持原计划 |
| G11 | CVaR 蒙特卡洛 | 解析近似 | USE_FINENG_PATH_SIM=True 已启用 | ✅ 基本闭合 |
| G14 | 多源交叉校验 | 弱 | 未在本轮检查范围 | ➖ 维持原计划 |
| — | 安全扫描 | 2 HIGH (B324 MD5) | **0 HIGH / 0 MEDIUM** | ✅ 本轮改善 |
| — | 发布门禁 | — | **v8.7 BLOCK (D11)** | ❌ 阻断中 |

**工业级差距结论**：工程基础层（数据契约/执行闭环/测试/覆盖率/隔离/告警/安全）已达 A 级、与工业级标准的"流程性差距"基本闭合。剩余差距分三类：
1. **实盘前置**（G1 QMT 接线 + 影子期）——需硬件/账号资源，纯技术外依赖；
2. **发布纪律**（D11 样本积累 + T6 债务阈值）——时间是主要变量；
3. **架构纵深**（G9 FeatureStore / G10 热路径重写 / G14 多源校验）——中长期项，不阻断当前发布。

---

## 5. 修复方案（按优先级）

### P0 立即修复（预计 0.5 天）

```powershell
cd "e:\各种PY程序\28-终极量化交易系统8.4"

# 1. 修 combo_base.py F821: 在 import 区加
#    if TYPE_CHECKING:
#        from utils.greek_hedge_manager import GreekExposure
#        from <fetcher真实模块> import OptionDataFetcher
# 验证:
$env:PYTHONIOENCODING="utf-8"; & .\.venv\Scripts\python.exe -m ruff check utils\etf_option_combo --select F821
# 期望: 0 errors

# 2. 修 etf_option_combo 其余类型 error (combo_backtest/cash_secured_put/collar/drift_monitor/fault_injector/trainer)
# 验证 (mypy 基线门禁):
$env:PYTHONUTF8="1"; & .\.venv\Scripts\python.exe scripts\mypy_baseline_gate.py
# 期望: PASS (增量 <= 0)

# 3. 修 engineering_debt_gate.py 内部 subprocess 加 encoding="utf-8", errors="replace"
# 验证: 重跑无 Thread-1 UnicodeDecodeError traceback
```

### P1 本迭代内（1-3 周）

```powershell
# 4. D11: 确认 shadow 每日采样任务在跑，等待样本到 20
& .\.venv\Scripts\python.exe scripts\phase_b_progressive_enabler.py --check
# 同时建议复核 min_samples=20 口径是否合理（31天观察期对应比例）

# 5. T6: 定位 31 处宽捕获，精确化至少 2 处
# 扫描逻辑见 scripts/engineering_debt_gate.py:_check_fail_safe_broad_except()
# 验证: engineering_debt_gate.py 输出 T6 [OK] (<= 30)

# 6. P2-1 压力测试回退核查: 确认定时任务入口未退回 --simulate
& .\.venv\Scripts\python.exe stress_test_runner.py   # 不带 --simulate, 应读 positions.json 真实持仓
```

### P2 排期（本月内）

```powershell
# 7. ruff 存量清理 (56 处可自动修)
& .\.venv\Scripts\python.exe -m ruff check . --fix
# 剩余 66 处人工处置 (T201/E741/N806 等)

# 8. fault_injector.py:40 / trainer.py:833 两个 mypy Name error
```

### G1 实盘接线（按既定 Phase 4 计划，12 月窗口前置）

安装 xtquant → `system_config.json` broker 账号配置 → `enabled=true` + `dry_run=true` 影子期 ≥ 2 周 → 灰度 10%/50%/100% → C1 WARN 转 PASS。

### 防复发机制建议

1. **新模块合入清单加 mypy 项**：现铁律为 ruff+black+pytest，补 `mypy_baseline_gate.py` 为第四件套——本次 etf_option_combo 带 22 个类型 error 合入正说明缺这一环；
2. **门禁脚本自身纳入测试**：engineering_debt_gate 的 subprocess 编码崩溃若在 CI 环境发生，可能静默产生不完整判定（fail-open），违反门禁 fail-closed 原则；
3. **D11 样本量与观察期口径绑定**：min_samples 应与观察期天数有明确换算关系，避免"观察期已满但样本永远不够"的死锁。

---

## 6. 附录：本报告全部复现命令

```powershell
cd "e:\各种PY程序\28-终极量化交易系统8.4"
$env:PYTHONIOENCODING="utf-8"; $env:OPENBLAS_NUM_THREADS="1"; $env:NO_PROXY="*"

& .\.venv\Scripts\python.exe scripts\industrial_grade_check.py      # 11P/1W/0F
& .\.venv\Scripts\python.exe scripts\engineering_debt_gate.py        # RED (T6, D11)
& .\.venv\Scripts\python.exe scripts\assert_data_validity.py         # 12P/0F
& .\.venv\Scripts\python.exe scripts\check_dangling_refs.py          # OK
& .\.venv\Scripts\python.exe scripts\mypy_baseline_gate.py            # FAIL +22
& .\.venv\Scripts\python.exe -m ruff check . --statistics            # 122 errors
& .\.venv\Scripts\python.exe -m bandit -r utils -q                   # 0 HIGH/MEDIUM
& .\.venv\Scripts\python.exe -m pytest tests -q --co                 # 16709 collected / 0 errors
& .\.venv\Scripts\python.exe scripts\phase_b_progressive_enabler.py --check
```

---

## 7. 修复执行记录 (2026-09-04 当日完成)

> 本报告 §3-§5 中 P0 全部三项 + P1 的 T6 已当日修复并验证；P1 其余项与 P2 维持原方案。

### 7.1 已修复项

| 原编号 | 修复内容 | 改动文件 | 验证结果 |
|--------|---------|---------|---------|
| P0-1 | F821×2 未定义名 → TYPE_CHECKING 导入 `GreekExposure`(utils.greek_hedge_manager) / `OptionDataFetcher`(utils.option_data_fetcher) | `utils/etf_option_combo/combo_base.py` | `ruff --select F821` 0 errors |
| P0-2 | mypy 基线净增 +22 → 修复 etf_option_combo 全部新增类型 error（35 处：返回类型精确化 cast/state_manager 类型从 object 改为 ComboStateManager 并贯通 5 个子类/Returning Any 加 bool()/cast） | combo_base / combo_state / combo_orchestrator / combo_backtest / covered_call / collar / cash_secured_put / vertical_spread / calendar_spread / option_data_fetcher.py | **mypy 门禁 PASS：982→957（基线 960，-3 存量收敛中）** |
| P0-2 附带 | **真 bug**：`run_backtest` 的 `etf_prices: dict[str, list[dict]]` 被当 `list[dict]` 直接迭代——传入真实价格数据时 `bar.get` 必然 AttributeError 崩溃 | `utils/etf_option_combo/combo_backtest.py:61` | 修复为 `(etf_prices.get(underlying) if etf_prices else None) or 合成序列` |
| P0-2 附带 | `fault_injector.py:40` / `trainer.py:833` 的 no-redef（try/except import fallback 标准模式）补 `# type: ignore[no-redef]` | 2 文件 | mypy error 消除 |
| P0-3 | engineering_debt_gate 内部 subprocess 未指定编码 → 补 `encoding="utf-8", errors="replace"` | `scripts/engineering_debt_gate.py:957` | 重跑门禁 **UnicodeDecodeError traceback 消失** |
| P1-2 | T6 宽捕获 31→29：精确化 `system_check.py:804`（__import__ 捕获缩至 ImportError/OSError/RuntimeError/ValueError/SyntaxError）与 `rss_feed_fetcher.py:63`（feedparser.parse 缩至 OSError/ValueError/TypeError） | 2 文件 | **T6 [OK] 29 处 (≤30)** |
| 附带 | ruff --fix 修复改动文件 import 排序等 6 处；删除 combo_base.py 未使用变量 `empty_greeks` | 13 文件 | ruff F821/F401/I001/W292/E501 on 改动文件 0 新增 |

### 7.2 修复后门禁快照 (2026-09-04 19:30)

| 门禁 | 修复前 | 修复后 |
|------|--------|--------|
| industrial_grade_check | 11P/1W/0F | 11P/1W/0F（无回归，C1 WARN 维持） |
| assert_data_validity | 12P/0F | 12P/0F（无回归） |
| mypy baseline gate | **FAIL +22** | **PASS -3** |
| engineering_debt_gate T6 | **FAIL 31>30** | **OK 29≤30** |
| engineering_debt_gate 等级 | RED (T6+D11) | RED (仅 D11 样本 11/20，需自然积累) |
| etf_option_combo 测试 | 126 passed | 126 passed |

注：`test_performance.py::test_state_persistence_latency` 出现 51-59ms vs 50ms 阈值的边缘失败——git diff 证实改动仅涉及 load/get 读取路径，save/fsync 热路径零改动，同一份代码一次通过三次边缘超标，属 Windows fsync 环境抖动，与本轮修复无关。

### 7.3 剩余待办（更新后）

1. **D11 PhaseB shadow 样本积累至 20**（当前 11/20，无代码动作）——v8.7 发布唯一阻断项；
2. P2 项维持原方案：压力测试 SIMULATED 回退核查 / ruff 存量人工处置（自动修部分已完成于改动文件）；
3. G1 QMT 实盘接线按既定 Phase 4 计划推进。

### 7.4 v8.7 发布前置检查单（2026-09-04 追加）

**D11 解除策略（用户已确认）: 自然积累** —— 0 代码改动，每交易日照常 EOD 采样 +1。

解除判据（须全部满足）:
1. `reports/evolution/phase_b_status.json` → `daily_health_log` 样本 ≥ 20/20（`min_shadow_samples=20`）；
2. `consecutive_stable_days` ≥ 7（当前 11/7 已达标）；
3. `python scripts/engineering_debt_gate.py` 输出 D11 [OK]、无 BLOCK。

预计解除日: **约 2026-09-21**（当前 11/20，余 9 个交易日；09-04 当日 healthy 样本已入账）。

采样链路可靠性（2026-09-04 核验）:
- 每日 EOD: `15_每日工作流/run_daily_eod_workflow.py` → `scripts/phase_b_b4_shadow_runner.py` 写 `daily_health_log`，每交易日记 1 条；
- 最近入账 2026-09-04（healthy，cumulative_stable_days=11），连续 11 交易日 healthy，无空档；
- 断链已于 2026-09-01 修复并确认可靠。**自检规则：若连续 3 个交易日无新样本入账，视为链路中断，须核查 EOD 定时任务后再谈发布。**

v8.7 发布前置检查命令（全部通过方可发版）:

```powershell
cd "e:\各种PY程序\28-终极量化交易系统8.4"
$env:PYTHONUTF8="1"; $env:PYTHONIOENCODING="utf-8"
& .\.venv\Scripts\python.exe scripts\engineering_debt_gate.py          # D11 [OK] (样本≥20), T6 [OK]
& .\.venv\Scripts\python.exe scripts\phase_b_progressive_enabler.py --check
& .\.venv\Scripts\python.exe scripts\mypy_baseline_gate.py             # RC=0 (类型债不增)
& .\.venv\Scripts\python.exe scripts\industrial_grade_check.py         # 无新 FAIL
& .\.venv\Scripts\python.exe scripts\assert_data_validity.py           # 无 FAIL
```

2026-09-04 附加修复（mypy 基线门禁接入 CI + 本地，防类型债再次零拦截合入）:
- `ci.yml` `lint-typecheck` job: mypy utils/ 从 warning 级吞错 → `scripts/mypy_baseline_gate.py` 阻断式调用（前置 `$env:PYTHONUTF8="1"`），error > 基线即阻断 PR/push；
- `scripts/pre_commit_check.py`: 新增 mypy 基线门禁段（暂存区含 `utils/` .py 改动时触发，timeout 300s，沿用 `_utf8_env()` 容错模式）；
- 基线重冻结: `docs/mypy_baseline_v9.2.txt` 960 → **957**（与当前收敛态一致，新增 1 个即 FAIL）；
- 验证: 新增 `tests/unit/test_mypy_baseline_gate.py` 6 passed（mock 判定逻辑，不实跑全量 mypy）；真实负向验证——临时降基线 1 行 → gate FAIL RC=1 拦截，恢复后校验一致（957 行）。

### 7.5 T6 余量巩固评估结论（2026-09-04, 记录缓做）

目标: 余量 ≥5（≤25 处）。实测: **29/30（OK，不阻断）**，较上轮 31 已降 2。

评估过程: 枚举全部 29 处站点（utils 17 + scripts 10 + quant_modules/ai_decision 0），逐类审查：

| 类别 | 站点 | 判定 |
|------|------|------|
| fail-open 观测路径 | `utils/infra/ecl/sinks.py`(L134/144)、`bypass.py`(L143-188×4)、`embeddings.py`(L46/120) | 决策链路 sink 注释明言"禁止抛出/静默降级"; 嵌入后端探测须兼容任意模型加载失败 → **不可收窄** |
| 强制兜底 | `utils/risk_guard_integrator.py:926`（"P0-7: fail-safe 兜底必须捕获所有异常"） | **不可收窄**（风控链路, 收窄=风险回归） |
| chaos 演练 | `utils/chaos/fault_injector.py`(L72/79/363) | 演练不得崩溃 → **不可收窄** |
| 数据源探测/降级 | `web_content_extractor.py`、`real_inputs_builder.py`(L273/535)、`run_s12_shadow.py`、`factor_approval_executor.py` | 观测路径 fail-open, 收窄可能漏捕 → **风险高** |
| 门禁/运维脚本兜底 | `scripts/ruff_incremental_gate.py:98`、`review_gate.py:477`、`launch_shadow_30day.py`、`s6_paper_trading_runner.py`、`register_task_health_check.py:38` | 门禁顶层 fail-close/运维幂等 → 属脚本自身稳健性设计, 收益低 |
| 上轮已精确化 | `system_check.py:804`、`rss_feed_fetcher.py:63`（31→29） | ✅ 已做完最明显的"业务操作型"宽捕获 |

**缓做结论**: 剩余站点均为有意的设计性宽捕获（fail-open 观测/探测/演练/门禁兜底），无一可零风险收窄。强收窄将在真实异常路径漏捕关键错误，收益（余量 1→5 缓冲）与风险不成比例。且 T6 属**告警级不阻断债**（never RED），当前 OK。
**替代约束**: 不得用删除 `# noqa: BLE001`/`# fail-safe` 标记把站点转入 T7 裸债口径（阈值 250）——那只是掩盖债务类别，非治理。
**建议**: 维持 29/30；未来新增 fail-safe 站点时优先写具体异常类型（本轮 CI 无新增防线, 但 pylint T02 已对 utils/infra|risk|execution|data 阻断 broad-except 新增）。

