# 量化交易系统 v8.6 代码质量与缺陷审查报告（OCR + ruff/mypy/bandit/pre-commit 全量扫描）

审查对象：`e:\各种PY程序\28-终极量化交易系统8.4`（生产级量化策略系统 v8.6 统一版）
审查方式：open-code-review (OCR) Delegation Mode 规则集（`system / **/*.py`）+ ruff 0.15.16 + mypy 2.3.0 + bandit 1.9.4 + 项目自定义 pre-commit 钩子脚本 + 人工精读关键执行链。
审查日期：2026-08-07
审查范围：核心生产代码（根目录执行模块、`utils/execution/`、`utils/`、`ai_decision/`、`scripts/`、`cli/modes/`、`ui/pages/`），排除 `_archive/`、`tests/`、`ms_strategy/`、`research/`、`external/`、`qlib*` 等第三方/归档。

## 执行摘要

本次审查在昨日（08-06）报告基础上，改用 OCR Delegation Mode + 完整工具链（ruff/mypy/bandit/pre-commit 钩子脚本）全量扫描，并重点精读每日交易执行链、对冲/期权执行链、工作流调度链。结果发现 **3 处 Critical（含 1 处导入即崩溃的 `live_scheduler.py` BASE_DIR 未定义、1 处 `ui/pages/10_🔬_宏观综合分析.py` 语法错误导致页面无法加载、1 处再平衡/对冲订单生成后无执行器消费的执行断链）、若干 High（自动化执行系统硬编码 SPY 假订单、`cli/modes/` 多文件未定义名称运行期 NameError、对冲订单双路径不一致、风控前置阻断缺失、0 股假 FILLED），以及大量工具链层面的债务**（ruff 13274 错误、mypy 核心文件 230 错误、483 处 `# type: ignore`、1541 处 print）。昨日报告确认的 `hedge_order_executor.py:233` active_orders 形状缺陷**已修复**（现已正确处理嵌套字典形状）。bandit HIGH 级别在核心交易路径 0 缺陷，安全性整体良好。

## 审查方法与工具链

- **OCR**：`ocr delegate rule` 确认所有被审 Python 文件均落入 `system / **/*.py` 规则组。因仓库为全新未提交仓库（git "No commits yet"），无法以 diff 模式运行，采用全量精读 + 启发式扫描交叉验证。
- **ruff 0.15.16**：全项目 `check` 得 **13274 条错误**，按规则统计（见下表）。
- **mypy 2.3.0**：核心 3 文件（含传递依赖）230 错误，错误码以 `misc`(64)/`index`(18)/`assignment`(15)/`operator`(2) 为主，多源于 `# type: ignore` 滥用。需 `PYTHONUTF8=1` + `--config-file mypy.ini` 绕过 `pyproject.toml` GBK 编码坑。
- **bandit 1.9.4**：`-lll -ii`（HIGH 严重度/置信度）扫描核心交易路径 0 缺陷；扫描 `utils/` 得 3 处 High（见后，均为低实际风险）。
- **pre-commit 钩子脚本**：`check_exception_policy.py` 通过；`_claude_hook_quality_check.py` 在核心文件发现 **7 处静默吞异常**（P1）。pre-commit 框架本体因系统 Python 3.8 site-packages `.pth` GBK 解码 bug 无法启动（环境问题，非代码问题），改用钩子脚本直跑。
- **项目内置扫描**：`_scan_system_quality.py` 扫描 493 文件得 2177 违规（P0:0, P1:497, P2:1680），其中 print_debug 1541、type_ignore 482、sys_path 139、assert_in_prod 15。`_scan_except_remaining.py` 确认裸 `except:` 已全部清零。

### ruff 错误规则分布（Top）

| 规则 | 数量 | 性质 |
|------|------|------|
| T201 print | 5266 | 可维护性（Windows GBK 下含货币符号会崩溃） |
| W293 空白行尾空格 | 1299 | 风格 |
| ANN001/201/202/003/002 缺注解 | ~2815 | 类型债务 |
| BLE001 宽泛 except | 667 | 异常吞没风险 |
| F401 未使用导入 | 445 | 死代码/维护 |
| F541 f-string 无占位符 | 257 | **多为真实 bug（误用 f-string）** |
| E402 模块级 import | 207 | 可维护性 |
| **F821 未定义名称** | **35** | **真实 NameError bug** |
| F841 未使用变量 | 76 | 潜在逻辑问题 |
| **invalid-syntax 语法错误** | **10** | **文件无法解析** |
| F811 重定义 | 8 | 潜在遮蔽 bug |
| E722 裸 except | 5 | 异常吞没 |
| **B010 setattr 常量属性** | 8 | 多为第三方，忽略 |

## 主要发现

### Critical（阻断性）

1. **`live_scheduler.py`：`BASE_DIR` 未定义，导入即 `NameError` 崩溃**
   `live_scheduler.py` 第 50 行从 `utils.path_config` 导入了 `get_project_root, get_v8_src_dir, get_log_dir, setup_sys_path`，但从未调用 `get_project_root()` 定义 `BASE_DIR`。而第 77 行模块级语句 `LOCK_FILE = BASE_DIR / ".live_scheduler.lock"` 直接引用 `BASE_DIR`。由于该语句在模块加载时执行，**任何 `import live_scheduler` 都会立即抛 `NameError: name 'BASE_DIR' is not defined`**。全文件共 13 处引用 `BASE_DIR`（L77/151/195/203/385/608/613/642/643/675/691/692/804）。这意味着统一入口的 `--live` 模式（依赖 live_scheduler）完全不可用。修复：在 L56 后加 `BASE_DIR = get_project_root()`（或 `Path(__file__).resolve().parents[0]`）。

2. **`ui/pages/10_🔬_宏观综合分析.py:116-119`：缩进错误，页面无法解析**
   第 116-117 行 `if adj_data:` 块多缩进了一级，导致第 118 行 `else:` 与 `if` 不匹配，触发 `unindent does not match any outer indentation level` 语法错误。该 Streamlit 页面（"宏观综合分析" 页）**整体无法加载**，Streamlit 应用加载此页时直接报语法错误。修复：将 116-117 行缩进与 118 行 `else:` 对齐（`if adj_data:` 块内的 `st.dataframe` 与 `else` 同级）。

3. **再平衡订单生成后无任何执行器消费（执行断链）** —— `utils/execution/automated_execution_system.py:2202-2233`
   `_generate_rebalance_orders()` 调用 `generate_rebalance_orders(...)` 生成 BUY/SELL 订单，但 `orders` 只被传入 `build_report` 并 `json.dump` 到 `reports/rebalance_execution_orders_{date}.json`，**再无任何代码读取该文件**（全库检索确认无消费者）。订单从未进入 `order_router.route_order()` 或任何撮合引擎。这与历史"期权对冲 generate_hedge_orders 只生成不执行"的断链缺陷同源，本次发生在股票再平衡订单上。修复：将 orders 接入 `OrderRouter.route_order()` / `process_execution_queue()`，或建立与 `hedge_order_executor.py` 等价的再平衡订单撮合执行器。

### High（高）

4. **`automated_execution_system.py` 实际路由硬编码 SPY 假订单** —— L1627-1655
   `_execute_daily_trading` 中 `trade_info` 硬编码为 `{"instrument":"SPY","direction":"buy","trade_size":100000}`，随后 `route_order(execution_plan, ...)` 消费的正是这个假 SPY 单（L1655），与系统自己算出的对冲 plan（步骤 3）和再平衡 report（步骤 11）**完全脱钩**。即使修复 C3，此处仍会把写死的 SPY 单送进执行队列。修复：用 `_generate_rebalance_orders()` 产出的订单构造 `execution_plan` 并逐个 `route_order`，删除硬编码 trade_info。

5. **`cli/modes/` 多文件未定义名称（运行期 NameError）**
   - `commodity_fundamentals.py:28,38`：使用 `sys`、`os`、`datetime` 但未导入
   - `enhanced_prediction.py:37,47`：`run_ml_signal_mode`、`pd` 未定义
   - `enhanced_training.py:37,38`：`os` 未定义
   - `hedge_detail_mode.py:87`：`get_quotes_batch` 未定义
   - `kommo_monitor.py:26`：`os` 未定义
   - `scripts/strategy_evaluator.py:80`：`WalkForwardResult` 未定义
   - `scripts/sync_covered_call_collateral.py:122`：`data` 未定义
   这些文件均在 `cli/modes/__init__.py` 模式注册表登记，CLI 调用对应模式时立即抛 `NameError`。

6. **对冲订单双生成/消费路径不一致，`hedge_execution_orders_*.json` 无撮合消费** —— `automated_execution_system.py:1942-1987` vs `hedge_order_executor.py:200-260`
   `_generate_hedge_execution_orders` 生成的 `hedge_execution_orders_*.json` 仅被 `run_daily_eod_workflow.py:109` 当作归档收集，不进入撮合；而修复"只生成不执行"的 `hedge_order_executor.py` 消费的是 `trade_plan_{date}.json` 里的 `hedge_execution`/`futures_options_hedge` 字段。两套对冲订单路径各自独立，`automated_execution_system` 那条仍"只写盘不撮合"。若下游依赖该报告文件撮合则漏单。修复：统一对冲订单唯一真相源，`_generate_hedge_execution_orders` 产物应回写进 `trade_plan` 供 `hedge_order_executor` 撮合。

7. **`daily_trade_executor.py` 的"执行"是纯记账，无撮合，且 0 股也标记 FILLED** —— L1313-1409
   `_execute_single_instruction` 全文件无 `send_order`/`place_order`/broker 调用（`SimulatedBroker` 仅出现在第 20 行 docstring），成交价、成交数均在内存按 `ref_price×(1+滑点)` 推算后直接改写 positions.json。当 `fill_amount < exec_price` 时 `actual_qty` 取整为 0（L1365），但 `actual_qty == 0` 仍返回 `status: "FILLED"`、计入 `execution_results`——**静默假成交**。修复：接通真实 broker 撮合，`actual_qty <= 0` 时返回未成交并跳过。

8. **WT 风控盘前仅打印不阻断（风控前置缺失）** —— `daily_trade_executor.py:657-679`
   `_run_wt_risk_precheck` 盘前调用，仅打印风险评分日志不阻断执行；盘后才用 `_run_wt_risk_block_check` 一票否决。若组合集中度/回撤超限，盘前仍生成含过高仓位的指令落入人工确认清单。建议盘前也做阻断级校验。

### Medium（中）

9. **7 处静默吞异常（宽泛 except 无日志无 re-raise）**：`daily_trade_executor.py:85,457,1547`；`live_scheduler.py:491,780,1091,1114`。均为 `except Exception`（或 `except:`）既不 log 也不 raise，异常被静默吞掉，调用方无法定位。建议收敛为具体异常类型并记录日志。

10. **再平衡 `load_positions` 直接下标访问 `data["positions"]`** —— `utils/execution/rebalance_execution_orders.py:45`。若 `positions.json` 缺 `"positions"` 键直接 `KeyError`，且被外层 `except` 吞掉静默返回 None。建议 `data.get("positions", {}) or {}`。

11. **`stop_loss_monitor.py:317,321` `dict.get(key, default)` 在值为 `None` 时除零/TypeError**：`rule.get("stop_loss_pct", -12.0) / 100` 当 YAML 显式写 `stop_loss_pct: null` 时返回 `None`，`None / 100` 抛 `TypeError`，该持仓止损检查崩溃。建议 `(rule.get("stop_loss_pct") or -12.0) / 100`。

12. **止损卖出经 Mock 券商静默降级** —— `stop_loss_monitor.py:113-115,205-215`：未注入真实 broker 时默认 `_create_mock_broker()`，触发后"看似已执行"实则仅模拟，真实持仓永不下单但日志显示 SELL 已执行。建议无真实 broker 时显式标记 `EXECUTION_MODE=MOCK` 并告警/拒绝。

13. **`daily_trade_executor.py:1293` full_code 回退硬编码 `.SH`**：`_sync_positions_idempotent` 中匹配不到时回退 `f"{code}.SH"`，而标的可能是 `.SZ`/`.BJ`，导致补同步失败、当日成交静默丢失。建议用 `_infer_suffix(code)`。

### bandit High（低实际风险）

- `utils/console_encoding.py:50` `subprocess.run(['chcp','65001'], shell=True)`：命令为常量列表，无注入风险，违反最小权限原则（昨日报告已记录）。
- `utils/last30days_adapter.py:344`、`utils/tdam_client.py:828` 使用 `hashlib.md5`：用于缓存键/查询哈希，**非安全用途**，属误报（可加 `# nosec` 或注释说明）。

### 昨日缺陷修复确认

- `hedge_order_executor.py:243-253`：已正确区分 `active_orders` 的嵌套字典形状（`put_protection`/`covered_call` 子列表）与旧版扁平列表，**昨日 H1 缺陷已修复**。
- 裸 `except:` 已全项目清零（`_scan_except_remaining.py` 确认）。

## 工具链债务概况

- **ruff**：13274 错误，其中约 5000+ 为 print（T201）、2800+ 为缺类型注解（ANN）、667 为宽泛 except（BLE001）。真正需要优先处理的是 F821（35）、invalid-syntax（10）、F541（257）、F401（445）、BLE001（667）。CI 若以 `--exit-non-zero` 全量跑 ruff 将永远失败，需先建立 baseline 再渐进清零。
- **mypy**：核心文件 230 错误（misc 64 为主，多为 `# type: ignore` 与实际错误码不匹配/`unused-ignore`），叠加全项目 483 处 `# type: ignore`，类型安全名存实亡。建议：将 `# type: ignore` 全部替换为带具体错误码的精确忽略，并启用 `warn_unused_ignores`。
- **print 调试**：1541 处，在 Windows GBK 控制台含非 ASCII 货币符号（¥）时抛 `UnicodeEncodeError`（历史已记录），建议统一 `logging` + `PYTHONIOENCODING=utf-8`。

## 结论

系统工程质量较 08-06 有一定改善（期权执行器形状缺陷已修复、裸 except 清零、bandit 核心路径 0 High），但仍存在 3 处 Critical：`live_scheduler.py` 的 `BASE_DIR` 未定义会导致 `--live` 模式导入即崩；`ui/pages/10_🔬_宏观综合分析.py` 缩进错误导致整个页面无法加载；以及再平衡/对冲订单"只生成不执行"的执行断链（与历史期权断链同源，本次出现在股票再平衡）。此外 `automated_execution_system.py` 硬编码 SPY 假订单、`cli/modes/` 多文件未定义名称、`daily_trade_executor` 0 股假 FILLED 均为需要修复的真实缺陷。建议按优先级推进，并建立 ruff/mypy baseline 以恢复工具链门禁的可执行性。

## 修复优先级建议

- **P0（立即）**：`live_scheduler.py` BASE_DIR 定义；`ui/pages/10_🔬_宏观综合分析.py` 缩进修复；再平衡订单接入执行器（C3）。
- **P1（本周）**：删除 `automated_execution_system` 硬编码 SPY（H4）；`cli/modes/` 未定义名称补 import；对冲订单双路径统一（H6）；0 股假 FILLED（H7）；7 处静默吞异常（M9）。
- **P2（债务收敛）**：ruff/mypy baseline；`# type: ignore` 精确化；print 收敛；bandit MD5 误报标注。

## 局限性

本审查以静态分析 + 人工精读为主，未执行系统（无实时行情/券商连线，`--dry-run` 下成交回报不落盘）。pre-commit 框架因系统 Python 3.8 的 GBK `.pth` 编码 bug 无法启动，改用钩子脚本直跑，与框架行为基本一致但未覆盖 `ruff-format`/`vulture` 两个钩子。`ms_strategy/`、`external/`、`qlib*`、`_archive/` 等目录未纳入深度审查。F821/F541 等批量项仅抽查了部分文件确认，未逐条核对运行时可达性。

## 参考

1. [open-code-review 规则集（Python）](e:/各种PY程序/open-code-review/internal/config/rules/rule_docs/python.md)
2. [OCR 规则配置文件（ruff.toml）](e:/各种PY程序/28-终极量化交易系统8.4/ruff.toml)
3. [OCR pre-commit 配置](e:/各种PY程序/28-终极量化交易系统8.4/.pre-commit-config.yaml)
4. [live_scheduler.py](e:/各种PY程序/28-终极量化交易系统8.4/live_scheduler.py)
5. [自动化执行系统 automated_execution_system.py](e:/各种PY程序/28-终极量化交易系统8.4/utils/execution/automated_execution_system.py)
6. [每日交易执行器 daily_trade_executor.py](e:/各种PY程序/28-终极量化交易系统8.4/daily_trade_executor.py)
7. [宏综合 UI 页](e:/各种PY程序/28-终极量化交易系统8.4/ui/pages/10_🔬_宏观综合分析.py)
8. [昨日审查报告（20260806）](e:/各种PY程序/28-终极量化交易系统8.4/代码质量与缺陷审查报告_20260806.md)
9. [系统质量扫描脚本](e:/各种PY程序/28-终极量化交易系统8.4/scripts/_scan_system_quality.py)
10. [bandit 配置](e:/各种PY程序/28-终极量化交易系统8.4/bandit.yaml)
