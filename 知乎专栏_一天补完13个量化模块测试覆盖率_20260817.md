# 量化交易系统测试覆盖率补全与开源生态集成：一次单日工程实践记录

> **系统**：A 股量化交易系统 v8.6.14　**日期**：2026-08-17　**作者**：yuppiefree2025
>
> 本文记录在单一工作日内完成的三项工程任务：将 13 个核心模块的单元测试覆盖率从 0 提升至 60%–97%、完成 3 个 Sprint 的开源项目集成、以及自我进化框架观察期决策文档的占位符预填充。重点不在产出量，而在三项任务背后的工程方法论——测试债的分批偿还策略、开源依赖的能力缺口评估准则、以及插件化重构的可逆性设计。

## 一、工作概览

| 任务编号 | 主线 | 产出 | 质量门禁 |
|---|---|---|---|
| T2 | 单元测试覆盖率提升 | 4 批次 / 13 模块 / 365 tests | 全 GREEN |
| docs/1 | 开源生态项目集成 | Sprint A + Sprint B + Sprint C W.C.3 / 135 tests | 全 GREEN |
| T3 | 决策文档占位符预填充 | 5 项观测数据 | 数据闭环 |

全部门禁通过：ruff All checks passed、mypy 无报错、pytest 500 passed（365 + 135）。

## 二、T2：测试覆盖率的分批偿还

### 2.1 技术债的成因

量化策略代码与测试代码存在天然的迭代速率差异：策略层（因子、信号、风控规则）因研究驱动而高频变更，而单测需构造行情、持仓、订单等 mock 数据，编写成本显著高于普通业务系统。长期累积的结果是 `utils/` 下 155 个 Python 模块中，相当一部分单元覆盖率长期为 0%——仅在回测流程中被间接执行，从未建立独立的契约约束。

此类技术债的直接代价是**回归风险不可控**。一个实际案例：`run_evolution_eval.py` 中观察期天数被硬编码为 14，后续配置调整为 `OBSERVATION_DAYS = 21` 时，该硬编码未同步修改，导致 `eval_status.next_action` 输出 "11/21 天"——分母已更新而分子仍由旧逻辑计算。此类缺陷在缺乏单测的条件下无法被静态捕获。

### 2.2 分批执行策略

按模块风险与依赖复杂度由低到高排序，分 4 批执行，每批完成后运行 ruff + pytest 确认 GREEN 方进入下一批：

| 批次 | 模块数 | 测试数 | 覆盖率区间 |
|---|---|---|---|
| 第 13 批 | 4 | 164 | 61.00% – 97.44% |
| 第 14 批 | 4 | 84 | 64.86% – 95.00% |
| 第 15 批 | 2 | 43 | 79.30% – 86.22% |
| 第 16 批 | 3 | 74 | 89.82% – 90.00% |

分批的工程依据：单次编写 365 个测试时，任一 import 错误或 fixture 污染都会使调试上下文膨胀，定位成本随批大小超线性增长。分批本质上是将"验证延迟"从会话级压缩到批次级，以频繁的小步验证换取低返工率。

### 2.3 关键模块的测试设计

**`market_impact_model`（0% → 97.44%）**　Almgren-Chriss 线性冲击模型与 Square-Root 非线性冲击模型，属执行层核心——预测自身订单对价格的冲击。两者均为纯数学函数，输入输出契约明确，mock 成本低，故覆盖率可达 97%。

**`var_backtest`（0% → 91.20%）**　VaR 回测三件套：Kupiec POF 检验（无条件覆盖）、Christoffersen 检验（独立性 + 条件覆盖）、Basel 交通灯分区。测试难点在于构造"恰好违反 Kupiec 但不违反 Christoffersen"的边界违反序列，采用参数化测试（parametrize）一次性覆盖多分支。

**`portfolio_optimizer`（0% → 61.00%）**　覆盖率未追求更高，原因在于 `run_offline_pipeline` 依赖外部因子文件，单测环境无法提供。可测的 `load_factor_signals` / `adjust_target_weights` / `apply_risk_management` 均已覆盖。**对不可单测的外部 IO 边界强行 mock，会引入与实现强耦合的脆弱测试，维护成本高于其价值。**

**`drawdown_controller`（0% → 94.00%）**　四级回撤响应（0–5% / 5–10% / 10–15% / >15%），采用 fail-closed 设计——阈值配置缺失时默认触发最严格响应。对风控类模块，测试必须显式覆盖"配置缺失"与"阈值边界"两条路径，仅覆盖正常路径不构成合格契约。

### 2.4 伴随缺陷修复

`run_evolution_eval.py` 硬编码 14 → `OBSERVATION_DAYS`（21）。根因为魔法数字未抽离为命名常量。修复后补加断言 `assert eval_status.observation_days == OBSERVATION_DAYS`，以测试契约约束未来变更。

## 三、docs/1：开源生态项目集成

集成输入为此前梳理的 29 个高价值 GitHub 量化项目清单，本次选取 8 项制定集成计划，按风险递增划分为 Sprint A / B / C。

### 3.1 Sprint A：低风险高 ROI 层（3 项目 / 62 tests）

**W.A.1 OpenBiliClaw 舆情信号源**　MediaCrawler 7 平台采集 → NewsSentimentEngine 打分 → SignalResult 第 6 信号源。原系统含 5 个信号源（技术面 / 资金面 / 基本面 / 筹码 / 事件），本次扩展舆情维度。新增 365 行，17 tests。

**W.A.2 code-graph-rag Python 封装**　将 code-review-graph MCP 工具的图检索能力封装为 Python 可调用接口，并为 `ai_coordinator` 新增 `record_decision_with_impact` 方法（opt-in，不修改高频路径 `record_decision` 的签名），用于记录 AI 决策对代码节点的影响半径。25 tests。

**W.A.3 HTML 图表生成器**　`html_chart_generator.py`（352 行），支持 mermaid flowchart、echarts graph、gantt 及 4 个项目特定图表（对冲五阶段 / 数据源降级 / AI 路由 / 信号融合）。20 tests，含 HTML well-formed 校验。

### 3.2 Sprint B：依赖评估与团队记忆

**W.B.1 Switchyard 评估 → SKIP**　Switchyard 为 LLM 路由项目，但本系统已接入 LiteLLM，具备 6-provider fallback、场景路由与成本分流能力。Switchyard 的接入仅增加运维面而不带来能力增量。

> **开源依赖接入准则**：评估标准应为"是否填补当前能力曲线的缺口"，而非项目 star 数或社区活跃度。Switchyard 在本系统的能力缺口评估中不满足接入条件。

**W.B.2 TencentDB-Agent-Memory 团队级共享记忆中枢**　原架构中各分析师 agent 独立维护教训库，缺乏跨 agent 的知识共享。新增 `team_memory_hub.py`（434 行），基于 SQLite `team_lessons` 表实现 `share_lesson` / `query_relevant_lessons` / `get_agent_profile` / `build_lessons_prompt_block`，检索排序采用相似度与时序衰减的复合权重。

关键设计为**对 23 个分析师文件的零侵入**：历史教训通过 `state["data"]["historical_lessons"]` 注入，分析师从 state 读取，无需修改 prompt 模板。`USE_TEAM_MEMORY_HUB` feature-flag 默认 false，支持灰度启用。23 tests。

### 3.3 Sprint C W.C.3：协调器插件化重构

`ai_coordinator.py` 的 `route()` 原为硬编码 if-else（按 TaskType 与 budget 路由至不同 LLM），`resolve_conflicts()` 为多数投票。插件化目标是将上述硬编码抽象为可插拔插件，且**不破坏现有调用方契约**。

实施方案：

1. **新建插件包** `utils/ai_coordinator_plugins/`（5 文件）：`base.py`（抽象基类 Plugin / RoutingPlugin / ConflictDetectionPlugin + RoutingContext / ConflictContext / RoutingResult / ConflictResult dataclass）、`registry.py`（PluginRegistry：注册 / 卸载 / 按 priority 降序 / `resolve_routing` / `resolve_conflict` / YAML 加载 / 全局单例）、`routing_plugin.py`（5 路由插件：BudgetGuard p100 / Intraday p90 / DeepResearch p80 / MacroAnalysis p70 / Default p10）、`conflict_detection_plugin.py`（MajorityVote p50 / WeightedVote p60）。
2. **协调器改造**：`route()` 拆分为 `_route_legacy()` 与 `_route_via_plugins()`，`resolve_conflicts()` 同理。保留原 API 完全向后兼容。
3. **Shadow 比对**：插件路径执行时同步运行旧路径，结果不一致仅记 warning 日志不阻塞主流程。
4. **Feature-flag 灰度**：`USE_PLUGIN_COORDINATOR` 默认 false，插件路径异常自动回退至旧路径。
5. **测试覆盖**：9 个参数化测试覆盖 shadow 比对（route: 5 TaskType × 3 budget 档；resolve_conflicts: 3 标的 × 3 source），全部一致。50 tests。

该方案的核心设计目标是**可逆性**：任一阶段故障均可回退至旧路径，不影响线上运行。相较于一次性重写，可逆重构将风险从"发布即不可回退"降级为"发布后可观测、可回退"。

## 四、T3：决策文档占位符预填充

自我进化框架的观察期决策文档含 5 项占位符，本次以实测数据填充：

| 占位符 | 实测数据 | 工程解读 |
|---|---|---|
| §1.4 DriftMonitor 误报率 | 7 报告 / 612 alerts / 误报率 0.00% / 可解释率 100.00% | 25 个 critical 均为真实漂移（PSI=3.92），无误报 |
| §4.1 样本量风险 | 15/20 样本（75.0%）/ 有效率 100% / 时间跨度 15/21 天 | 样本量满足统计要求 |
| §4.2 误报率风险 | 总体 / PSI / KS / ADWIN 误报率均 0.00% | 多检验口径一致 |
| §4.3 Public/Private 分离 | public=0.0 vs private=0.4716 / reward_hacking=0.0 / pit_violations=0 | 公开指标未被优化，无 reward hacking 迹象 |
| §4.4 shadow 预热 | 2/3 天 / diff_rate=0.0088 / flag 不变式保持 | 新旧路径差异率 0.88%，在容许区间内 |

各项数据均由对应脚本生成，非主观赋值。占位符预填充的工程意义在于：**决策文档若长期停留在"待填"状态，等同于决策未发生。** 数据闭环是决策前置的必要条件。

## 五、方法论小结

1. **测试债分批偿还**　按模块风险与依赖复杂度排序，每批以 GREEN 验证为进入下一批的前置条件。批次粒度的选择应使单批调试上下文保持可控。

2. **覆盖率目标的合理边界**　对依赖外部 IO 的模块（如 `portfolio_optimizer` 的 61%、`vol_target_controller` 的 64.86%），强行 mock 外部边界会引入实现耦合型脆弱测试。覆盖率应作为充分性指标而非充分必要性指标。

3. **开源依赖的能力缺口评估**　接入准则为"是否填补能力曲线缺口"，而非项目流行度。Switchyard 的 SKIP 决策与 TencentDB-Agent-Memory 的接入决策均基于此准则。

4. **插件化重构的可逆性优先**　shadow 比对 + feature-flag 默认 false + 异常自动回退的组合，将重构风险从"不可回退"降级为"可观测、可回退"。重构的第一目标是保持行为等价，而非结构优雅。

5. **决策文档的数据闭环**　占位符必须以实测数据填充，"待填"状态下的决策文档不构成有效决策。

## 六、后续工作

- **Sprint C 剩余**：W.C.1 unsloth 微调（依赖 GPU 资源）、W.C.2 EchoBird CLI 切换
- **T3 决策文档**：待 08-22–08-23 观察期结束后最终确认，并补充 §5.4 建议

---

*工作记录已沉淀至 `cairn/LOG.md`（2026-08-17 条目），集成计划详见 `docs/1设计计划集成到系统内并能完整运行_20260817.md`，Sprint A 知识专题见 `cairn/docs1-integration-sprint-a-20260817.md`。*
