# 安全与配置审计报告 — 28-终极量化交易系统8.4

- 审计时间：2026-09-09 20:19–20:35 (Asia/Shanghai)
- 审计范围：根目录密钥文件、.gitignore/git 跟踪状态、计划任务 PS1 脚本、日志泄露抽查、网络服务、配置管理
- 方法：PowerShell 直读 E:\ 盘 + git 命令 + 日志正则抽查；所有密钥值脱敏（只披露类型与前 4 字符）

---

## 发现清单

### [P1-1] 位置：`E:\各种PY程序\28-终极量化交易系统8.4\.env`（3413 B，2026-09-07 修改）
**问题**：本机明文存储 2 个真实 API 密钥。`.env` 已被 `.gitignore`（第 81 行）覆盖且未被 git 跟踪（`git ls-files` 仅含 `.env.example`；`git check-ignore -v` 确认），但文件本体明文落盘在工程目录内，任何可读该目录的进程、备份任务、云同步、AI 工具上下文收集均可直接读取。

**证据（脱敏）**：
| 变量 | 类型特征 | 前4字符 | 长度 | 判定 |
|---|---|---|---|---|
| `WIND_API_KEY` | Wind 万得金融数据 API key（`ak_` 前缀） | `ak_T` | 35 | **真实密钥** |
| `MODELSCOPE_TOKEN` | ModelScope 平台 token（`ms-` 前缀） | `ms-5` | 39 | **真实密钥** |
| `DEEPSEEK_API_KEY` | 占位符（以占位词开头，非 sk- 格式） | 已抑制 | 32 | 占位符，非真实 |

文件内注释链自证矛盾：头部注释写"请勿在此文件明文保存真实凭证…真实凭证请走系统密钥管理或环境变量注入"，但 MODELSCOPE_TOKEN 段注释（2026-09-07）明确指示"轮换: token 失效/需更换时改此行；勿提交(本文件已 gitignore)"——即本机明文文件实际承担了密钥存储职能。另存在三层 fallback 解析链（进程环境变量 → 根 .env → 子目录 .env），进一步放大路径复杂度。

**修复建议**：
1. WIND_API_KEY / MODELSCOPE_TOKEN 视为"半暴露"状态，按用户级（`[Environment]::SetEnvironmentVariable(..., 'User')`）或 DPAPI 加密注入，随后从 .env 删除并在两家平台轮换；
2. 若保留 .env 作为 fallback，至少将其移出工程目录（如 `%USERPROFILE%\.quant-secrets\`）并收紧 ACL（仅当前用户可读）；
3. 确认工程未被任何云盘/备份工具整目录同步。

### [P1-2] 位置：`E:\各种PY程序\28-终极量化交易系统8.4\llmkey.txt`（2868 B，2026-09-01）
**问题**：`.env` 的明文副本（54 行，与 .env 同构：同含 DEEPSEEK/WIND/TRADING_ENV/SHADOW/TDAM 段），属冗余密钥落盘面，使泄露面×2。文件头注释与 2026-09-01 的 .gitignore 注释（"审计发现: llmkey.txt 含真实 API key, 禁止入库"）证明其历史上曾存放真实 key。git 状态安全：`.gitignore` 第 403 行 `llmkey*.txt` 覆盖，`git ls-files` 未跟踪。

**证据（脱敏）**：第 42 行 `WIND_API_KEY=<30+字符opaque token>`；第 28 行 `DEEPSEEK_API_KEY=<10+字符token>`（旧版占位）。

**修复建议**：直接删除 `llmkey.txt`（单一事实源只留 .env 或环境变量）；若需保留模板，另存无密钥的 `llmkey.example.txt`。可用 `trash` 回收站方式删除以便恢复。

### [P2-1] 位置：`register_intraday_task.ps1`
**问题**：LLM 盘中决策引擎计划任务使用 `-LogonType S4U -RunLevel Highest`（最高权限、免密 S4U 登录），每 15 分钟运行。无明文密码（✓ 良好），但 RunLevel Highest 超出该任务实际需求，任务被劫持时影响面放大。

**证据**：`$principal = New-ScheduledTaskPrincipal -UserId "$env:USERDOMAIN\$env:USERNAME" -LogonType S4U -RunLevel Highest`

**修复建议**：降为 `RunLevel Limited`（对照 `register_hn_task.ps1` 已用 Limited，是正确基线）；若必须 Highest，需文档说明理由并加任务执行白名单校验。其余 6 个 PS1（register_daily_trading_tasks / register_hn_task / register_trade_executor_tasks / repair_cn_tasks / repair_scheduled_tasks / service_manager）未发现明文密码/credential 硬编码 ✓。

### [P2-2] 位置：`config\`、`configs\`、根目录配置文件
**问题**：配置管理分裂，单一事实源未彻底落地：
- `config\`（24+ 文件）与 `configs\`（10 文件）两套目录并存，`feature_flags.yaml` 两处都有（12104 B vs 10898 B，内容已分叉）；
- `system_config.json`（根，10214 B）自述"本段自 config/system_config.json 合并至此（单一事实源 = 根）"，但 legacy 目录与文件仍在，存在被误读风险；
- 根目录 `portfolio_return_projection.json` 积累了 **40 个** `.bak_*` 时间戳备份、`config\market_returns.json` 有 7 个 `.bak`，运行时写配置+备份混入源码树。

**正面证据**：`system_config.json` 内所有敏感字段（api_key / token / account / broker.* / llm_config.*.api_key）均为空值，并带 `_security_note` 注明"必须通过环境变量注入，禁止在此文件存储"；broker.enabled=false + dry_run=true 默认安全。

**修复建议**：合并 config/ 与 configs/ 为单目录（迁移脚本 + grep 全量改引用路径）；feature_flags.yaml 保留一份，另一份改为 symlink 或生成物；将 *.bak 迁至 backups/（gitignore）并加保留期清理。

### [P2-3] 位置：`.env.example` vs 实际 `.env`
**问题**：模板与实际变量集漂移。example 含 GLM/MOONSHOT/CLAUDE/OPENAI/APIZERO_API_KEY（实际 .env 均无）；实际 .env 含 TDAM_TEAM_ID/TDAM_AGENT_ID/TDAM_BASE_URL/MODELSCOPE_TOKEN（example 全无）。新密钥接入无模板引导，运维易直接抄注释里的说明把真实 key 写进 .env（P1-1 的成因之一）。

**修复建议**：.env.example 补齐 MODELSCOPE_TOKEN、TDAM_* 段（全部留空+注释）；CI 加一个简单 drift 检查（对比两文件变量名集合）。

---

## 正面确认（无问题项）

1. **网络服务**：全仓库唯一自建 FastAPI 服务 `utils\execution\qmt_rpc_server.py` 安全基线良好——默认绑定 `127.0.0.1`（`--host` 默认取 `QMT_RPC_HOST` 或 127.0.0.1）、`_assert_safe_bind` fail-closed（非回环地址且无白名单则拒绝启动，防 0.0.0.0 暴露，HIGH-1 修复项）、X-Token 恒定时间比较 + 按 IP 暴破封禁。**未发现无鉴权监听 0.0.0.0 的服务**。uvicorn.run/app.run/Flask 自建监听 0 命中（unsloth_compiled_cache 中的 0.0.0.0 仅为第三方训练库 vllm 默认参数文档，非本系统服务）。
2. **日志抽查**：logs\ 下 6 个近期文件（quant_system.log 7.0MB、data_provider.log 7.9MB、daily_eod_20260909.log、quant_strategy_system_20260909.log、auto_retrain_2026-09-09.log、shadow_account.log）正则扫描 `sk-*/api_key/token/password/Bearer/Authorization` 均无命中，无密钥/账号打印 ✓。
3. **git 卫生**：.env、llmkey.txt 均被 .gitignore 覆盖且未被跟踪；被跟踪的仅 .env.example（占位）✓。
4. **主会话全局 grep 复核引用**：verify=False 仅存于备份目录 skills\ifind-finance-data-1.3.0.bak\call.py（2 处）；shell=True 0；os.system 0；eval() 均为 model.eval()；pickle.load 均带 SHA256 校验；api_key/password/secret 硬编码 0 ✓。

---

## 统计

| 严重度 | 数量 | 编号 |
|---|---|---|
| P0 | 0 | — |
| P1 | 2 | .env 明文真实密钥（WIND_API_KEY、MODELSCOPE_TOKEN）、llmkey.txt 冗余密钥副本 |
| P2 | 3 | 计划任务 RunLevel Highest、config/configs 分裂+40个.bak、.env.example 漂移 |
| 合计 | 5 | — |

最高优先级行动：轮换并移出 WIND_API_KEY 与 MODELSCOPE_TOKEN；删除 llmkey.txt。
