# 代码质量审计报告 — 28-终极量化交易系统 8.4（v8.6.14）

- 审计时间：2026-09-09 20:01–20:45（Asia/Shanghai）
- 审计对象：`E:\各种PY程序\28-终极量化交易系统8.4\`
- 审计方式：独立静态扫描（ruff 0.15.16 独立严格规则集，与项目自带 ruff.toml 不同）+ 安全模式全局匹配 + 四路并行深度人工审查（执行链路 / 策略与回测 / 安全与配置 / 测试与基建）+ 与项目自评报告交叉验证
- 严重度定义：P0 = 可直接造成资金损失/数据损毁；P1 = 高风险逻辑缺陷或风控失效；P2 = 质量/可维护性/健壮性问题

---

## 1. 结论速览

**总评级：B-（工程化程度远超个人项目水准、量化方法论扎实、自我修复闭环活跃；但执行链路存在 5 项风控一致性问题与密钥管理缺陷，属切实盘前的阻断项）**

| 指标 | 实测值 |
|---|---|
| 一方 Python 代码 | 约 56.8 万行（含测试 19.5 万、脚本 4.5 万；生产代码约 32.8 万） |
| git 活跃度 | 560 次提交，近 30 天 455 次、近 7 天 139 次 |
| 本次发现 | **P0 = 0，P1 = 10，P2 = 23（共 33 项）** |
| 测试 | 618 文件 / 195,634 行；抽样真断言为主，`assert True` 全仓仅 2 处 |
| 覆盖率 | 自称 line_rate=0.833 —— **口径矛盾，本次不可复现**（详见 P1-9） |
| 安全扫描 | bandit 0 HIGH/0 MEDIUM（项目自评）；本次独立 grep：无 shell=True、无 os.system、硬编码密钥 0 命中 |
| CI | 8 个 workflow，push(main) 真实触发；但覆盖率门槛 5% 且失败强制放行 |

## 2. 系统画像

- **模块构成**：根目录入口 43 文件 2.1 万行（统一入口单文件 2,700 行）；`utils/` 41 个子模块 20.3 万行（最大巨石目录）；`ms_strategy/` 2.16 万行；`quant_modules/ai_hedge_fund/` 68 文件（自研多 Agent 投研，非引入库）；`tests/` 19.56 万行；引入的 qlib 分叉 6.2 万行已正确隔离在 third_party 边界外。
- **工程成熟度信号（正面）**：pyproject/uv.lock/8 个 CI workflow/pre-commit/mypy+pylint 配置齐全；`ci_lookahead_guard.py` 提供 CI 级前视偏差门禁（扫描 bfill/shift(-N)/train_test_split/全样本标准化）；自评报告体系完整（本项目已有多轮自我审查留痕，本次确认 2026-09-04 报告的 3 个回测 P0 已全部修复）。
- **工程成熟度信号（负面）**：磁盘垃圾 749 个 .bak + 4,424 个 __pycache__ + 根目录 40 个配置 .bak（git 未跟踪，但构成"磁盘即备份库"的回滚风险）；依赖声明 4–7 套并存；config/ 与 configs/ 双目录分叉。

## 3. P1 高风险发现（10 项，切实盘前必修）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| P1-1 | utils/execution/order_router.py:125,236 | 执行队列 `deque(maxlen=50)` 超限**静默挤出**队头订单，`route_order` 仍返回 success | 批量再平衡 >50 切片时静默丢单；切实盘后即 P0 |
| P1-2 | utils/execution/order_router.py:359-372 | 失败单标记 pending 后被无条件 popleft，**全仓库无重新入队路径**，"重试 3 次"为死代码 | 失败订单永久滞留内存，无对账 |
| P1-3 | daily_trade_executor.py:832-848 | WT 风控分析器**缺失时 fail-open**（warning 后放行），与同文件 DTE-2"决策路径 fail-close"铁律自相矛盾 | 风控防线恰在初始化失败场景失效 |
| P1-4 | institutional_pipeline_runner.py:330-337 | V72 牛市 5% 封顶风控异常时仅 log 后继续，未封顶决策直接流入下游 | 回测验证过的风控上限静默失效 |
| P1-5 | system_integration.py:867-879 | 重训冷却锁**不校验训练结果**就写入（失败也算成功）；训练前无锁可并发重复重训 | 失败标的被冷却锁压制数天无法重训 |
| P1-6 | .env（根目录） | 明文存放 **WIND_API_KEY、MODELSCOPE_TOKEN 两个真实密钥**（已 gitignore 且未跟踪，但任何可读目录的进程/备份/同步均可读取） | 密钥半暴露；需轮换并移出 |
| P1-7 | llmkey.txt（根目录） | .env 的明文副本（同构 54 行），泄露面 ×2；git 已忽略未跟踪 | 建议直接删除，单一事实源 |
| P1-8 | tests/test_qmt_audit_fixes.py 等 ~12 个文件 | 脚本式伪测试混入 tests\：全文 0 个 `test_` 函数（54 个 assert 在模块顶层，import 时执行），pytest 收集为 0 tests 静默"绿" | CI 通过数/覆盖率出现假象 |
| P1-9 | .coveragerc vs ci.yml L460 vs README L111 | 覆盖率口径三处互相矛盾（utils,ms_strategy / utils,v8.3_institutional / 未说明）；当前 .coverage 仅含 etf_option_combo 11 个文件 | README 0.833 数字不可复现 |
| P1-10 | ci.yml L479/L460 | 覆盖率门槛 `--min-line-rate 0.05`（5%），失败后 `$LASTEXITCODE=0` 强制放行；`--cov-fail-under=60` 注明"不阻断" | 质量门禁形同虚设 |

## 4. P2 发现摘要（23 项）

**执行链路（13 项）**：live_scheduler 单实例锁 TOCTOU 竞态；定时任务无超时挂死即停摆；module_last_run 写入无锁；幂等补同步 json.load 无保护；滑点 10bp/ADUV=1,000,000 硬编码魔法数字；幂等键 (code,action,qty) 粒度不足误杀同日合法重复指令；影子账户状态非原子写+损坏即静默重置；`config/positions.json` 相对路径依赖 CWD；`main()` 593 行巨型函数 + 45 处"fail-safe 待后续精确化"注释债（含 4 处静默 pass）；hedge_order_executor 读对冲持仓后即丢弃（死代码）；`_atomic_write_json` 四处重复实现行为不一致；停机标志裸 bool（stop 最长阻塞 5 分钟）+ 演示参数硬编码；执行链路为"保证成交"模拟且缺实盘切换一致性防线。

**安全与配置（3 项）**：盘中 LLM 计划任务 `RunLevel Highest` 超出需求（对照 register_hn_task.ps1 的 Limited 是正确基线）；config/ 与 configs/ 双目录并存且 feature_flags.yaml 已分叉 + 运行时 .bak 混入源码树；.env.example 与实际 .env 变量集漂移。

**测试与基建（6 项）**：根 conftest.py 31,526 字节巨石化；29/618 文件无 assert（约 12 个为伪测试，其余为合理 harness）；依赖声明 4–7 套并存无单一真源；`requires-python >=3.10` 但 CI/venv 均只用 3.14（下限未验证）；磁盘垃圾 749 .bak + 4,424 __pycache__ + 根目录散落临时文件/日志/40 个配置 .bak；5 个根目录转发 shim 双轨入口 + utils/ 巨石目录（LLM 网关四套并存）。

**策略与回测（1 项）**：utils/alpha/strategy_evaluator.py 使用 train_test_split，需确认是否作用于时序数据（另两处命中分别为守卫脚本自身与非时序场景，合理）。

## 5. 量化静态扫描画像（独立严格规则集，ruff 0.15.16，39,545 条原始发现）

筛选高信号项（已剔除 NaN 惯用法、变量命名等误报）：

| 规则类别 | 数量 | 含义与风险 |
|---|---|---|
| DTZ 时区朴素 datetime | **约 1,835** | datetime.now()/utcnow()/today() 无 tzinfo —— 交易系统执行窗口/日切逻辑的正确性隐患 |
| T201 print | 5,732 | 日志纪律缺失（应统一 logging） |
| BLE001 盲捕获 + S110 try-except-pass + S112/138 | 613+101+158 | 约 870+ 处异常吞没点（重点文件抽查确认 0 裸 `except:`，但 `except Exception: pass` 仍大量存在） |
| C901 复杂结构 / PLR0912 分支数 / PLR0915 语句数 | 493/278/270 | 复杂度热点（与巨型文件重合） |
| PLC0415 函数内 import | 3,333 | 启动性能与依赖图不可静态分析 |
| PLR0913 参数过多 / PLW0603 global | 382/181 | 接口与状态管理债务 |
| subprocess 无 check / 无 shell 参数 / 部分路径 | 108/100/46 | 外部调用健壮性 |
| NPY002 numpy legacy random | 668 | 可复现性风险（应 RandomState/default_rng） |

**correctness 层意外干净**：F821 未定义名 0 处（自评报告 09-04 的 2 处已修复）、F811 1 处、F841 1 处、无用导入仅 60 处。`v == v`/`PASS`/`model.eval()` 等静态告警经人工核验均为惯用法误报。

## 6. 正面观察（本次审计确认的保持项）

1. **daily_trade_executor 幂等设计质量高**：幂等键持久化 + "先 progress 后 positions"的 fail-closed 写序 + 原子写 + 崩溃窗口注释。
2. **11 个执行链重点文件 0 裸 `except:`**，异常类型已系统性收敛（多轮修复留痕与代码一致）。
3. **Purged TimeSeriesSplit + embargo**（lgb_tscv_trainer，n_splits=5, embargo 1%）正确切断标签重叠泄漏。
4. **Deflated Sharpe Ratio 真实接入主流程**：institutional_pipeline_runner、shadow_account_system、ai_decision 自动研究、etf_option_backtest、ms_strategy metrics/combinatorial_purged_cv 均实际引用。
5. **成本模型完整**（cost_aware_backtest）：佣金万 2.5、印花税千 1 仅卖出、过户费、Almgren-Chriss 冲击系数 0.142。
6. **fast_backtest 的"诚实边界"**：明确声明不做 walk-forward（传入 strategy_fn 即抛错）、DSR 须显式声明试验次数 —— 这种自我设限在同类项目中罕见。
7. **CI 级前视偏差门禁**（ci_lookahead_guard.py）+ combinatorial purged CV 存在。
8. **网络服务安全基线好**：qmt_rpc_server 默认 127.0.0.1、非回环 fail-closed、X-Token 恒定时间比较 + IP 封禁；日志抽查无密钥泄露；shell=True/os.system 全仓为 0。
9. **自我修复闭环活跃**：2026-09-04 回测审查的 3 个 P0（胜率恒 100%、Sharpe 剔零日、信号成交同日同价）已全部实修（realized_pnl 字段、无风险利率扣减、样本标准差 N-1 均已验证在位）；2 处生产 F821 已清零。

## 7. 修改方案（三批次，合计约 25 人日）

### 批次一 · 切实盘阻断项（本周，约 3 人日）

1. order_router：去掉 maxlen 或溢出显式报错；失败单重入队/独立重试队列 + pending 超时对账（P1-1/2，0.5d）
2. 风控 fail-close 统一：analyzer 缺失返回 blocked；V72 封顶异常时阻断或降级保守决策（P1-3/4，0.5d）
3. 密钥处置：轮换 WIND_API_KEY/MODELSCOPE_TOKEN → 移入用户级环境变量或 DPAPI → 删除 llmkey.txt → 收紧目录 ACL（P1-6/7，0.5d）
4. 伪测试改造：test_qmt_audit_fixes.py 等包装成 `test_` 函数或移出 tests\（P1-8，1d）
5. 重训冷却锁按 result 状态分别写 + 训练前原子"进行中"标记（P1-5，0.5d）

### 批次二 · 可信度与可维护性（两周，约 8 人日）

6. 覆盖率口径统一（.coveragerc = CI --cov），fail-under 提为硬门禁，README 数字标注日期与口径（P1-9/10，1d）
7. 执行链 P2 修复包：幂等键加指令指纹、影子账户原子写、PROJECT_ROOT 路径、atomic_write_json 收敛到公共模块（2d）
8. 时区治理：以执行窗口/日切相关模块为先，DTZ 清零（分批，2d）
9. 执行链路 print → logging 分批替换（2d）
10. 仓库卫生脚本：.bak/配置备份迁 backups\ 滚动保留、__pycache__ 清理、根目录临时文件归位（1d）

### 批次三 · 结构性改善（一个月，约 14 人日）

11. 巨型文件拆解：risk_guard_integrator.py（113KB）、统一入口 main()（593 行）、daily_workflow.py（86KB）、daily_trade_executor.py（85KB）（5d）
12. utils 按域拆包：LLM 四套收敛为一个包、backup/_legacy 移出（3d）
13. 依赖单一真源：pyproject 生成 requirements*.txt + CI 一致性校验；requires-python 收紧或加版本矩阵（1d）
14. 样本外验证主链路：将 ms_strategy 的 combinatorial purged CV 真正接入 fast_backtest/主训练流程（fast_backtest 目前诚实声明"未实现"）（3d）
15. 模拟/实盘对账任务 + broker.enable 切换门禁（环境变量 fail-closed 校验在执行器入口强制引用）（2d）

---

## 7bis. 修复进展追踪（滚动更新，最新在上）

> 状态口径：✅ 完成（附 commit / 实证）/ 🟡 部分 / ⛔ 调查后判定不修（附理由）/ ☐ 未启动。
> 明细沉淀见 `cairn/audit-p2-execution-chain-20260910.md`（批次二）与 `cairn/audit-p3-broker-gate-reconciliation-20260910.md`（批次三 item 15）。

### 2026-09-10 · D1 压力测试空仓缺陷真正修复（附项闭环）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 附·D1 压力测试 `actual_pnl` 全 0（空仓） | ✅ | **根因非"持仓读取失败"而是资产类别分类全线失效**：`_run_scenario` 只按 `strategy` 英文关键词 + 极窄 style 白名单判断，而真实 `positions.json` 是 `type=ETF/STOCK` + `style=科技/宽基/金融...`，且 CLI 映射不透传 `type` → 26 持仓全落 `other`、`impact_pct=0`。修复：新增 `classify_asset_class()` 确定性映射（`asset_class` > `strategy` > `type` > `style/sector` > 名称含 ETF > `other`）＋ `build_positions_from_positions_json()` 透传 `type/style/sector`（可测函数）＋ 未分类金额写报告 `unclassified_amount` 并告警（不再静默记 0）＋ `scope_note` 声明覆盖范围 |
| 附·D1 门禁「空场景假 PASS」（本轮新发现，同类缺陷） | ✅ | 修完上项后 D1 输出 `0 个场景, 0 个为零` → 查得 `tests/unit/test_stress_test_runner_unit.py::TestSaveReport::test_save_report` 按**当日日期**把空场景报告写进 `reports/stress_test_{today}.json`，**覆盖真实报告**，而原 D1 对 `len(scenarios)==0` 判为通过。修复：D1 空场景即 FAIL；两个测试文件加 autouse fixture 把 `REPORT_DIR` 重定向 `tmp_path`，单测不再触碰生产报告目录 |
| 附·D1 实证 | ✅ | 真实 26 持仓 → `unclassified_amount=0`、`positions_amount=2,735,784`；四场景 `actual_pnl` = -982,916 / -332,078 / -343,529 / -634,595；`assert_data_validity` **11P/1F → 12P/0F**；新增回归 51 passed（含 D1 门禁负向用例：空场景 FAIL／全零 FAIL／非零 PASS／SIMULATED 跳过）；mypy 317=317；`industrial_grade_check` 11P/1W/0F 持平 |
| 附·D1 遗留 | 🟡 | 报告口径为"证券账户股票/ETF 端**未含对冲**"，与含对冲假设的 `expected_portfolio_dd` 不同口径（已在 `scope_note` 写明）→ `crash_2015` 实际 -19.7% 超 -15% 限额、`all_pass=False` 属真实暴露；`hedge_positions` 期权腿未计入（场景 `options_tail` 为多头保护腿口径，套空头备兑 Call 会给出错误符号），是否需要方向感知建模待定 |

### 2026-09-10 · 批次三 item 15 收口（broker 门禁 fail-closed + 对账任务）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 15. broker 门禁 fail-closed（`get_broker` 实盘就绪不再静默降级） | ✅ | 新增 `LiveBrokerUnavailableError` + `is_live_intent()` / `is_live_broker()`；`_degrade_or_raise()` 统一"降级 or 抛"分支；`_build_qmt` / `_build_remote_qmt` 增 `live=` 参数并 `except LiveBrokerUnavailableError: raise` 防被宽捕获吞掉；`tests/unit/test_p15_live_broker_gate_20260910.py` |
| 15. 主链路装配点 fail-closed（不再 `OrderRouter(broker=None)` 继续跑） | ✅ | `utils/execution/automated_execution_system.py`：捕获 → `logger.critical` + `send_alert` → `raise`；`_live_intent_fallback()` 兜底（broker_factory 整体不可用且实盘就绪 → 拒绝启动） |
| 15. 模拟/实盘对账任务接入（T13 订单级 + T17 持仓 drift） | ✅ | 新增 `utils/risk/trade_reconciliation_runner.py`（计划单提取 / 成交载入 / 关联模式判定 / 报告落盘 / 严格退出码）+ `scripts/run_trade_reconciliation.py`（CLI）+ EOD 阶段 4.93；T13/T17 由"有组件无任务"变可调度真实任务 |
| 15. · 对账精度诚实口径（订单级覆盖率不足即降级，不假 PASS） | 🟡 | 成交侧 `meta.order_id` 覆盖率 < 50% → 自动降级 `(symbol, side)` 聚合对账，报告标 `linkage_mode="symbol_side"` + `linkage_degraded=True`；`status ∈ {no_plan,no_planned_orders,no_fills}` 任一缺数据即如实标记且 `verified=False`。根因 = 计划单 `morning/afternoon_orders` 无 `order_id`（须生成器与执行器同源盖章），属后续项 |

### 2026-09-10 · 批次二收尾（执行链 P2 剩余项）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 7. 执行链 P2 修复包 · live_scheduler 单实例锁 TOCTOU | ✅ | `_acquire_lock()` 原子建锁（`O_CREAT\|O_EXCL`）+ stale 接管；`tests/unit/test_p2_execution_chain_20260910.py::test_acquire_lock_is_atomic` |
| 7. · 定时任务挂死即停摆 | ✅ | `_run_task` 提交 executor + `future.result(timeout)`，超时标 TIMEOUT 且调度链继续；`stop()` 有界等待（30s）；`::test_run_task_times_out_without_blocking_schedule` |
| 7. · module_last_run 写入无锁 + 幂等补同步重复触发 | ✅ | `_record_module_result()` 单临界区 + `get_status()` 快照 + `check_and_run` 改"锁内先占位再执行"；`::test_get_status_no_runtime_error_under_concurrency` |
| 7. · hedge_order_executor 死代码（读对冲持仓即丢弃 → 漏单） | ✅ | 接入 `fallback_active_orders` + 日期护栏；负向实证：修复前 0 单 / 修复后 1 单 |
| 7. · 滑点 10bp / ADV=1,000,000 硬编码魔法数字 | ✅ | `_cost_param()` 三级取值（YAML `cost_model` > `QUANT_<KEY>` > 内置默认，默认逐位一致）；`::test_cost_param_priority_config_then_env_then_default` |
| 7. · 幂等键加指令指纹 / PROJECT_ROOT / atomic_write 收敛 | ✅ | `66694ebb`（ref_price 指纹）+ `fef8b708`/`d3f44b55`（positions.json 路径 8 文件）+ `a064479e`（atomic_write 收敛） |
| 7. · 影子账户状态非原子写 | ⛔ | 调查结论：`_save_json_atomic` 用 `shutil.move` 绕 Windows 安全软件 hook `MoveFileEx`(WinError5)，公共 `atomic_write_json` 的 `_replace_with_retry` 只重试 `PermissionError`，不覆盖该场景 |
| 8. 时区治理 | 🟡 | DTZ003 全清零（`b298d678`/`6d02d910`/`cd147a41`/`d1a7bb90`/`91f4681f`/`b4412ab4`）+ DTZ005 `utils/` 394→0（`7767ef9d`）+ 防回退门禁（`9f40b70f`）；全库剩余 ~968 处待分批 |
| 9. print → logging | ✅ | `558236e9` 收尾（执行链自检块 6 处转换 + T201 豁免收紧）。注：审计原始"5732 处"为严格规则集口径，经核多为非执行链噪声 |
| 10. 仓库卫生脚本 | ✅ | `664c14aa`（`__pycache__` 清理 + `.bak`/配置备份迁 `backups\` 滚动保留 + 根目录临时文件归位） |
| 13. 依赖单一真源 | ✅ | `08d9558e`（pyproject 生成 requirements*.txt + CI 一致性门禁）+ `42d2b4b0`/`d5f39e74`（补漏登）+ `7fb56add`（幽灵 lockfile 引用） |

### 2026-09-09 · 批次一（切实盘阻断项）9/10

| 报告项 | 状态 | 证据 |
|---|---|---|
| 1. order_router 队列溢出静默丢单 + 失败单无重入队（P1-1/2） | ✅ | `664c14aa` |
| 2. 风控 fail-close 统一（P1-3/4） | ✅ | `664c14aa` |
| 3. 密钥处置（P1-6/7） | 🟡 | `llmkey.txt` 已删（单一事实源）；`.env` 内 WIND_API_KEY / MODELSCOPE_TOKEN **轮换属人工动作，待用户执行** |
| 4. 伪测试改造（P1-8） | ✅ | `664c14aa` |
| 5. 重训冷却锁按 result 状态写 + 原子"进行中"标记（P1-5） | ✅ | `664c14aa` |
| 6. 覆盖率口径统一 + 硬门禁（P1-9/10） | ✅ | `664c14aa` |
| 附：D1 压力测试"假 PASS"消除 | ✅ | `fa7cfcf0`（排除 `stress_test_SIMULATED_*` + 改按日期排序）→ 修复后**如实暴露**真实 FAIL：`stress_test_20260909.json` 4 场景 `actual_pnl` 全 0（空仓）。**该真实缺陷已于 2026-09-10 修复（见 §7bis 顶部「D1 压力测试空仓缺陷真正修复」），D1 现 12P/0F** |

### 批次三（结构性改善）— 部分启动

| 报告项 | 状态 |
|---|---|
| 11. 巨型文件拆解 | ☐ |
| 12. utils 按域拆包 | ☐ |
| 14. 样本外验证主链路接入 | ☐ |
| 15. 模拟/实盘对账 + broker.enable 切换门禁 | ✅（见上表；遗留：计划单补 `order_id` 以达成订单级对账精度） |

## 8. 方法与局限

- 四路并行审查由主会话 + 子代理完成；策略与回测维度的子代理连续两次遇到 LLM 请求失败，该维度由主会话亲自补查（wt_backtest_engine 修复验证、DSR 引用面、CV 方式、成本模型、前视门禁均已覆盖），但未做到 ms_strategy 84 文件全量精读。
- 静态扫描使用独立严格规则集（select ALL 减去文档/注释类），与项目自带 ruff.toml（仅 7 项发现）口径完全不同，数字不可直接对比。
- 未复跑 pytest 全量（自评称 16,709 用例可收集）；覆盖率声称未重新生成验证。
- 详录见随附三份分册报告（执行链路 18 项 / 安全与配置 5 项 / 测试与基建 10 项，含文件:行号证据）。

---

*审计：AutoClaw · 2026-09-09*
