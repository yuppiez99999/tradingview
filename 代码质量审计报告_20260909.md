# 代码质量审计报告 — 28-终极量化交易系统 8.4（v8.6.14）

- 审计时间：2026-09-09 20:01–20:45（Asia/Shanghai）
- 审计对象：`E:\各种PY程序\28-终极量化交易系统8.4\`
- 审计方式：独立静态扫描（ruff 0.15.16 独立严格规则集，与项目自带 ruff.toml 不同）+ 安全模式全局匹配 + 四路并行深度人工审查（执行链路 / 策略与回测 / 安全与配置 / 测试与基建）+ 与项目自评报告交叉验证
- 严重度定义：P0 = 可直接造成资金损失/数据损毁；P1 = 高风险逻辑缺陷或风控失效；P2 = 质量/可维护性/健壮性问题

---

## 1. 结论速览

**总评级：B-（工程化程度远超个人项目水准、量化方法论扎实、自我修复闭环活跃；但执行链路存在 5 项风控一致性问题与密钥管理缺陷，属切实盘前的阻断项）**

| 指标 | 实测值 |
|---|---|
| 一方 Python 代码 | 约 56.8 万行（含测试 19.5 万、脚本 4.5 万；生产代码约 32.8 万） |
| git 活跃度 | 560 次提交，近 30 天 455 次、近 7 天 139 次 |
| 本次发现 | **P0 = 0，P1 = 10，P2 = 23（共 33 项）** |
| 测试 | 618 文件 / 195,634 行；抽样真断言为主，`assert True` 全仓仅 2 处 |
| 覆盖率 | 自称 line_rate=0.833 —— **口径矛盾，本次不可复现**（详见 P1-9） |
| 安全扫描 | bandit 0 HIGH/0 MEDIUM（项目自评）；本次独立 grep：无 shell=True、无 os.system、硬编码密钥 0 命中 |
| CI | 8 个 workflow，push(main) 真实触发；但覆盖率门槛 5% 且失败强制放行 |

## 2. 系统画像

- **模块构成**：根目录入口 43 文件 2.1 万行（统一入口单文件 2,700 行）；`utils/` 41 个子模块 20.3 万行（最大巨石目录）；`ms_strategy/` 2.16 万行；`quant_modules/ai_hedge_fund/` 68 文件（自研多 Agent 投研，非引入库）；`tests/` 19.56 万行；引入的 qlib 分叉 6.2 万行已正确隔离在 third_party 边界外。
- **工程成熟度信号（正面）**：pyproject/uv.lock/8 个 CI workflow/pre-commit/mypy+pylint 配置齐全；`ci_lookahead_guard.py` 提供 CI 级前视偏差门禁（扫描 bfill/shift(-N)/train_test_split/全样本标准化）；自评报告体系完整（本项目已有多轮自我审查留痕，本次确认 2026-09-04 报告的 3 个回测 P0 已全部修复）。
- **工程成熟度信号（负面）**：磁盘垃圾 749 个 .bak + 4,424 个 __pycache__ + 根目录 40 个配置 .bak（git 未跟踪，但构成"磁盘即备份库"的回滚风险）；依赖声明 4–7 套并存；config/ 与 configs/ 双目录分叉。

## 3. P1 高风险发现（10 项，切实盘前必修）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| P1-1 | utils/execution/order_router.py:125,236 | 执行队列 `deque(maxlen=50)` 超限**静默挤出**队头订单，`route_order` 仍返回 success | 批量再平衡 >50 切片时静默丢单；切实盘后即 P0 |
| P1-2 | utils/execution/order_router.py:359-372 | 失败单标记 pending 后被无条件 popleft，**全仓库无重新入队路径**，"重试 3 次"为死代码 | 失败订单永久滞留内存，无对账 |
| P1-3 | daily_trade_executor.py:832-848 | WT 风控分析器**缺失时 fail-open**（warning 后放行），与同文件 DTE-2"决策路径 fail-close"铁律自相矛盾 | 风控防线恰在初始化失败场景失效 |
| P1-4 | institutional_pipeline_runner.py:330-337 | V72 牛市 5% 封顶风控异常时仅 log 后继续，未封顶决策直接流入下游 | 回测验证过的风控上限静默失效 |
| P1-5 | system_integration.py:867-879 | 重训冷却锁**不校验训练结果**就写入（失败也算成功）；训练前无锁可并发重复重训 | 失败标的被冷却锁压制数天无法重训 |
| P1-6 | .env（根目录） | 明文存放 **WIND_API_KEY、MODELSCOPE_TOKEN 两个真实密钥**（已 gitignore 且未跟踪，但任何可读目录的进程/备份/同步均可读取） | 密钥半暴露；需轮换并移出 |
| P1-7 | llmkey.txt（根目录） | .env 的明文副本（同构 54 行），泄露面 ×2；git 已忽略未跟踪 | 建议直接删除，单一事实源 |
| P1-8 | tests/test_qmt_audit_fixes.py 等 ~12 个文件 | 脚本式伪测试混入 tests\：全文 0 个 `test_` 函数（54 个 assert 在模块顶层，import 时执行），pytest 收集为 0 tests 静默"绿" | CI 通过数/覆盖率出现假象 |
| P1-9 | .coveragerc vs ci.yml L460 vs README L111 | 覆盖率口径三处互相矛盾（utils,ms_strategy / utils,v8.3_institutional / 未说明）；当前 .coverage 仅含 etf_option_combo 11 个文件 | README 0.833 数字不可复现 |
| P1-10 | ci.yml L479/L460 | 覆盖率门槛 `--min-line-rate 0.05`（5%），失败后 `$LASTEXITCODE=0` 强制放行；`--cov-fail-under=60` 注明"不阻断" | 质量门禁形同虚设 |

## 4. P2 发现摘要（23 项）

**执行链路（13 项）**：live_scheduler 单实例锁 TOCTOU 竞态；定时任务无超时挂死即停摆；module_last_run 写入无锁；幂等补同步 json.load 无保护；滑点 10bp/ADUV=1,000,000 硬编码魔法数字；幂等键 (code,action,qty) 粒度不足误杀同日合法重复指令；影子账户状态非原子写+损坏即静默重置；`config/positions.json` 相对路径依赖 CWD；`main()` 593 行巨型函数 + 45 处"fail-safe 待后续精确化"注释债（含 4 处静默 pass）；hedge_order_executor 读对冲持仓后即丢弃（死代码）；`_atomic_write_json` 四处重复实现行为不一致；停机标志裸 bool（stop 最长阻塞 5 分钟）+ 演示参数硬编码；执行链路为"保证成交"模拟且缺实盘切换一致性防线。

**安全与配置（3 项）**：盘中 LLM 计划任务 `RunLevel Highest` 超出需求（对照 register_hn_task.ps1 的 Limited 是正确基线）；config/ 与 configs/ 双目录并存且 feature_flags.yaml 已分叉 + 运行时 .bak 混入源码树；.env.example 与实际 .env 变量集漂移。

**测试与基建（6 项）**：根 conftest.py 31,526 字节巨石化；29/618 文件无 assert（约 12 个为伪测试，其余为合理 harness）；依赖声明 4–7 套并存无单一真源；`requires-python >=3.10` 但 CI/venv 均只用 3.14（下限未验证）；磁盘垃圾 749 .bak + 4,424 __pycache__ + 根目录散落临时文件/日志/40 个配置 .bak；5 个根目录转发 shim 双轨入口 + utils/ 巨石目录（LLM 网关四套并存）。

**策略与回测（1 项）**：utils/alpha/strategy_evaluator.py 使用 train_test_split，需确认是否作用于时序数据（另两处命中分别为守卫脚本自身与非时序场景，合理）。

## 5. 量化静态扫描画像（独立严格规则集，ruff 0.15.16，39,545 条原始发现）

筛选高信号项（已剔除 NaN 惯用法、变量命名等误报）：

| 规则类别 | 数量 | 含义与风险 |
|---|---|---|
| DTZ 时区朴素 datetime | **约 1,835** | datetime.now()/utcnow()/today() 无 tzinfo —— 交易系统执行窗口/日切逻辑的正确性隐患 |
| T201 print | 5,732 | 日志纪律缺失（应统一 logging） |
| BLE001 盲捕获 + S110 try-except-pass + S112/138 | 613+101+158 | 约 870+ 处异常吞没点（重点文件抽查确认 0 裸 `except:`，但 `except Exception: pass` 仍大量存在） |
| C901 复杂结构 / PLR0912 分支数 / PLR0915 语句数 | 493/278/270 | 复杂度热点（与巨型文件重合） |
| PLC0415 函数内 import | 3,333 | 启动性能与依赖图不可静态分析 |
| PLR0913 参数过多 / PLW0603 global | 382/181 | 接口与状态管理债务 |
| subprocess 无 check / 无 shell 参数 / 部分路径 | 108/100/46 | 外部调用健壮性 |
| NPY002 numpy legacy random | 668 | 可复现性风险（应 RandomState/default_rng） |

**correctness 层意外干净**：F821 未定义名 0 处（自评报告 09-04 的 2 处已修复）、F811 1 处、F841 1 处、无用导入仅 60 处。`v == v`/`PASS`/`model.eval()` 等静态告警经人工核验均为惯用法误报。

## 6. 正面观察（本次审计确认的保持项）

1. **daily_trade_executor 幂等设计质量高**：幂等键持久化 + "先 progress 后 positions"的 fail-closed 写序 + 原子写 + 崩溃窗口注释。
2. **11 个执行链重点文件 0 裸 `except:`**，异常类型已系统性收敛（多轮修复留痕与代码一致）。
3. **Purged TimeSeriesSplit + embargo**（lgb_tscv_trainer，n_splits=5, embargo 1%）正确切断标签重叠泄漏。
4. **Deflated Sharpe Ratio 真实接入主流程**：institutional_pipeline_runner、shadow_account_system、ai_decision 自动研究、etf_option_backtest、ms_strategy metrics/combinatorial_purged_cv 均实际引用。
5. **成本模型完整**（cost_aware_backtest）：佣金万 2.5、印花税千 1 仅卖出、过户费、Almgren-Chriss 冲击系数 0.142。
6. **fast_backtest 的"诚实边界"**：明确声明不做 walk-forward（传入 strategy_fn 即抛错）、DSR 须显式声明试验次数 —— 这种自我设限在同类项目中罕见。
7. **CI 级前视偏差门禁**（ci_lookahead_guard.py）+ combinatorial purged CV 存在。
8. **网络服务安全基线好**：qmt_rpc_server 默认 127.0.0.1、非回环 fail-closed、X-Token 恒定时间比较 + IP 封禁；日志抽查无密钥泄露；shell=True/os.system 全仓为 0。
9. **自我修复闭环活跃**：2026-09-04 回测审查的 3 个 P0（胜率恒 100%、Sharpe 剔零日、信号成交同日同价）已全部实修（realized_pnl 字段、无风险利率扣减、样本标准差 N-1 均已验证在位）；2 处生产 F821 已清零。

## 7. 修改方案（三批次，合计约 25 人日）

### 批次一 · 切实盘阻断项（本周，约 3 人日）

1. order_router：去掉 maxlen 或溢出显式报错；失败单重入队/独立重试队列 + pending 超时对账（P1-1/2，0.5d）
2. 风控 fail-close 统一：analyzer 缺失返回 blocked；V72 封顶异常时阻断或降级保守决策（P1-3/4，0.5d）
3. 密钥处置：轮换 WIND_API_KEY/MODELSCOPE_TOKEN → 移入用户级环境变量或 DPAPI → 删除 llmkey.txt → 收紧目录 ACL（P1-6/7，0.5d）
4. 伪测试改造：test_qmt_audit_fixes.py 等包装成 `test_` 函数或移出 tests\（P1-8，1d）
5. 重训冷却锁按 result 状态分别写 + 训练前原子"进行中"标记（P1-5，0.5d）

### 批次二 · 可信度与可维护性（两周，约 8 人日）

6. 覆盖率口径统一（.coveragerc = CI --cov），fail-under 提为硬门禁，README 数字标注日期与口径（P1-9/10，1d）
7. 执行链 P2 修复包：幂等键加指令指纹、影子账户原子写、PROJECT_ROOT 路径、atomic_write_json 收敛到公共模块（2d）
8. 时区治理：以执行窗口/日切相关模块为先，DTZ 清零（分批，2d）
9. 执行链路 print → logging 分批替换（2d）
10. 仓库卫生脚本：.bak/配置备份迁 backups\ 滚动保留、__pycache__ 清理、根目录临时文件归位（1d）

### 批次三 · 结构性改善（一个月，约 14 人日）

11. 巨型文件拆解：risk_guard_integrator.py（113KB）、统一入口 main()（593 行）、daily_workflow.py（86KB）、daily_trade_executor.py（85KB）（5d）
12. utils 按域拆包：LLM 四套收敛为一个包、backup/_legacy 移出（3d）
13. 依赖单一真源：pyproject 生成 requirements*.txt + CI 一致性校验；requires-python 收紧或加版本矩阵（1d）
14. 样本外验证主链路：将 ms_strategy 的 combinatorial purged CV 真正接入 fast_backtest/主训练流程（fast_backtest 目前诚实声明"未实现"）（3d）
15. 模拟/实盘对账任务 + broker.enable 切换门禁（环境变量 fail-closed 校验在执行器入口强制引用）（2d）

---

## 7bis. 修复进展追踪（滚动更新，最新在上）

> 状态口径：✅ 完成（附 commit / 实证）/ 🟡 部分 / ⛔ 调查后判定不修（附理由）/ ☐ 未启动。
> 明细沉淀见 `cairn/audit-p2-execution-chain-20260910.md`（批次二）与 `cairn/audit-p3-broker-gate-reconciliation-20260910.md`（批次三 item 15）。

### 2026-09-11 · 批次三 item 12 第一增量 — LLM 孤儿入口归档 + `_legacy` 空壳移除 + 边界门禁补盲区

| 步骤 | 状态 | 证据 |
|---|---|---|
| 1. 消费图实测 | ✅ | 精确 AST import 扫描（2282 个 .py）：`utils/llm_finetune.py` **0 导入方**；`utils/local_llm.py` 仅自身测试（`etf_flow_decision` 的 `_local_llm_client` 是变量名，实际加载 `15_每日工作流/llm_client.py`）；`utils/_legacy/` 仅 447B docstring 占位（T1.2），**全文件类型（py/yaml/toml/md/json/cfg/ini/bat/ps1/txt/sh）零路径引用**，17 处 `_legacy` 字样全是标识符（`_load_legacy`/`passthrough_to_legacy` 等）|
| 2. 归档（可回滚） | ✅ | 4 文件 → `_archive/dead_code/2026-09-11/`（保留原相对路径 + `manifest.json` 含 sha1）：`utils/llm_finetune.py`、`utils/local_llm.py`、`utils/_legacy/__init__.py`、`tests/unit/test_local_llm_unit.py`；护栏断言归档 blob 可读且哈希一致 |
| 3. 保护规则同步 | ✅ | `archive_dead_code.py` 设计原则 #5 的「utils/_legacy 严禁删除」改为移除记录（规则与事实不再漂移，护栏有断言）|
| 4. 边界门禁补盲区 | ✅ | `check_llm_boundary.py` 的 `LLM_MODULE_PREFIXES` 补 `utils.alpha.llm`（多 provider 栈 deepseek/doubao/ds4/glm/ollama/siliconflow + consensus + audit 此前不在清单）；扩清单后实测执行链 **47 文件 0 违例** |
| 5. 现存入口口径 | ✅ | 单一事实源写入护栏与 docstring：`utils/glm5_client.py`+`utils/llm_gateway/`（GLM-5 路径，`engineering_debt_gate.py:841` 依赖 gateway **不可移**）、`utils/alpha/llm/`+`utils/alpha/llm_router.py`（研究侧多 provider）、`utils/llm_client.py`（统一门面）、`utils/llm_evolution/`（自演化子系统，非 client 入口，gate + phase_b 依赖）|
| 6. 新护栏 | ✅ | `tests/unit/test_item12_llm_entry_consolidation_20260911.py` **8 passed**（`_legacy` 防复活 / 孤儿防复活 / manifest完整性+sha1 / 现存入口齐 / 边界清单覆盖 / 保护规则同步）|
| 7. 护栏负向验证 | ✅ | 生产树注入 `import utils.local_llm` 探针 → **1 failed**（`test_no_production_import_of_archived_modules` 抓到），移除探针后复绿 |
| 8. `config/` vs `configs/` | 🟡 只测不动 | `configs/` **0 文件被 git 跟踪**（整目录 gitignore = 机器本地）；仅 3 文件名重叠且**全部分歧**（sha1 零相同）：`feature_flags.yaml`/`settings.yaml` 的 configs 版**零生产消费**（死副本）、`portfolio.yaml` 为唯一真实双源冲突 —— 4 处硬编码主读（`morning_info_runner:230` / `evolution_orchestrator:728` / `vol_regime_weighter:1024` / `auto_trading_system:446`）vs 声明口径「config/ 唯一事实源」，且 `vol_regime_weighter` 的 8 类风格字段与 configs 版对齐 ⇒ **合并=业务配置取舍，须拍板**，详见 `cairn/item12-config-dual-source-audit-20260911.md` |
| 9. 残留 | 🟡 | D1（删死副本）/ D2（portfolio 以哪个版本为准）/ D3（4 处硬编码改走 `ConfigManager.get_portfolio_config()`）三个决策点待拍板；LLM 两个 GLM-5 路径（`glm5_client` vs `alpha/llm`）的最终归一未做 |

> **复现**：`pytest tests/unit/test_item12_llm_entry_consolidation_20260911.py -q` → 8 passed；`python scripts/check_llm_boundary.py` → 0 违例。

### 2026-09-11 · 批次三 item 11 续做（四）— `v8.3_institutional/daily_workflow.py` 实现簇 mixin 拆解（2180 → 1538 行）

| 步骤 | 状态 | 证据 |
|---|---|---|
| 1. 切割面 | ✅ | 该文件**已做过一轮 phase 拆分**: 多数 `phase_*` 方法是委托 shim (真逻辑在 `workflow/phases/*`)。本轮迁出的是仅存的两大**真实现**簇: 装载/环境感知 (804-1112, 309 行) + 模拟执行 (1484-1821, 338 行); **编排层留在宿主** (`__init__` 287 行 / 全部 `phase_*` shim / `_build_context` / `run` / `main` 229 行) |
| 2. 落点 | ✅ | `v8.3_institutional/workflow_mixins/{context_loader,sim_execution}.py` (341/370 行)。**严禁** `daily_workflow/` 同名目录: 带 `__init__.py` 的同名包会**抢先**于同名模块被 import (负向实证: 创建后 `import daily_workflow` 解析到包, `DailyWorkflow` 消失, 3 个护栏用例全红) |
| 3. 为何无需 `_h()` | ✅ | 迁出方法只引用 `Any/os/json/logger` (AST 实测)。注意: 探测脚本**必须把嵌套在 Try 块内的可选导入也计入** (初版漏了 `SmartOrderRouter/MockBroker/AlgoType`, ruff F821 当场抓出) |
| 4. 可选导入语义保持 | ✅ | `SmartOrderRouter/MockBroker/AlgoType` 属 V75 可选导入 (Try 块, 失败置 `V75_READY=False` 降级): 注解走 `TYPE_CHECKING`、`AlgoType` 运行时在 `_execute_order_batch` 内局部导入 —— 模块级导入会在可选模块缺失时炸掉宿主整体导入 |
| 5. 等价性 | ✅ | 同一测试集 (eod_dry_run + 4 个单测) 原宿主 vs 新宿主 **均 83 passed**; 含新护栏 **95 passed / 0 failed**; `v87_release_gate` 的 `daily_workflow_lines` 门禁 1538 ≤ 3000 |
| 6. 新护栏 | ✅ | `tests/unit/test_daily_workflow_mixin_split_20260911.py` **12 passed** (结构层 AST + 运行时 MRO 身份 + 可选导入语义 + logger 名 + 行数上限) |
| 7. 护栏负向验证 | ✅ | ① mixin 模块级导入可选符号 → **1 failed**; ② 创建 `daily_workflow/` 同名包 → **3 failed** 且实证 `import daily_workflow` 解析到包 / `DailyWorkflow` 消失 |
| 8. 残留 | 🟡 | 宿主 `__init__` 仍 287 行、`main` 229 行; 13 个 Try 导入块 (~320 行) 因导入期副作用 + 测试 patch 模块级名 (`CircuitBreaker`/`NTPSync`/`RiskManager`/`_run_calibration`) 必须留在宿主。item 12 未启动 |

> **复现**：`pytest tests/unit/test_daily_workflow_mixin_split_20260911.py -q` → 12 passed。

### 2026-09-10 · 批次三 item 11 续做（三）— `daily_trade_executor.py` 盘前指令簇拆解（2275 → 1385 行，迁出 `executor/premarket.py` 976 行）

| 步骤 | 状态 | 证据 |
|---|---|---|
| 1. 切割面选择 | ✅ | 47 个扁平函数 / 无类。执行簇（~530 行）耦合 9 个具名常量 + 7 个门面函数（含券商调用）；**指令生成簇爆炸半径更小且纯写文件** ⇒ 选后者 |
| 2. **决定性约束** | ✅ | 5 个测试文件以 `monkeypatch.setattr(daily_trade_executor, NAME, ...)` 替换 **21 个宿主名字**（路径常量 + 函数）。若迁出侧写成 `from 宿主 import NAME` 取**值**，补丁将**静默失效**（经典 monkeypatch 盲区）⇒ 迁出代码一律 `_h().NAME` 属性式读取 |
| 3. 迁出范围 | ✅ | `executor/premarket.py` **976 行**（20 函数 + `DEFAULT_PRICES`）；宿主 **2275 → 1385 行**；脚本化 AST 切片落 **59 处 `_h().`** 重写。AST 守卫：重写目标不得是参数 / 局部变量 / lambda 参数（实测 `shadow conflicts: []`） |
| 4. **刻意留在宿主** | ✅ | 预测价簇 `_load_prediction_prices` / `_get_prediction_prices_index` / `_reset_prediction_prices_index` + `fetch_prediction_signals` —— `test_load_prediction_prices_b25_unit.py` 用 `spec_from_file_location` 造 **rogue 实例（未注册 `sys.modules`）** 并 patch 其 `PROJECT_ROOT`；若迁出，`_h()` 经 `sys.modules` 会命中另一实例 ⇒ 顺序相关的假绿 / 假红 |
| 5. 宿主重导出 | ✅ | 文件末尾 21 条 `from executor.premarket import X as X`（ruff 认可的显式重导出写法，不报 F401）⇒ `dte.NAME` / `from daily_trade_executor import NAME` 对外契约不变 |
| 6. 等价性实证 | ✅ | 12 个引用该模块的测试文件：**原宿主 vs 新宿主跑同一集合均为 279 passed / 3 failed / 1 skipped**（3 failed = 既存 Py3.14 `pathlib` stub 缺 `case_sensitive` 参数，**已用原宿主复跑同一集合证实与本次无关**） |
| 7. 新护栏 | ✅ | `tests/unit/test_daily_executor_premarket_split_20260910.py` **9 passed**（结构层 AST + 行为层 monkeypatch 语义） |
| 8. 护栏负向验证 | ✅ | 临时把 `_h().TRADE_PLAN_FILE` / `_h().is_trading_day` 改回**导入期取値绑定** ⇒ **2 条行为用例立即失败**（2 failed / 7 passed），证明护栏真的在拦而非空转 |
| 9. 门禁同步 | ✅ | P0 名单新增 `executor/premarket.py`（`scripts/check_no_print_p0.py` + `scripts/quality_snapshot.py`）；前者 `resolve_targets` 修子目录路径解析（原按 basename 拼回 `repo_root` 会**丢子目录**）。负向：注入 1 条 `print(` ⇒ 全量 / argv 两模式均 **RC=1** |
| 10. 残留 | 🟡 | 宿主仍 **1385 行**（执行簇 + CLI + `main()` 未拆）；`v8.3_institutional/daily_workflow.py` **2180** ☐；item 12 未启动 |

> **踩坑（值得记住）**：`ast` 的 `col_offset` 是 **UTF-8 字节偏移**，不是字符偏移。按 `str` 直接切片会把含中文的行切坏 —— 本次 f-string 内的 `ACCUMULATION_START/END` 被切成 `ACCUMULATI_h().ACCUMULATION_STARTULATI_h().ACCUMULATION_END`。**脚本化切片必须按 `bytes` 切片后再 decode。**
> **复现**：`pytest tests/unit/test_daily_executor_premarket_split_20260910.py -q` → 9 passed；`python scripts/check_no_print_p0.py executor/premarket.py` → RC=0。

### 2026-09-10 · 批次三 item 11 续做（二）— 统一入口 `main()` 拆解（593 → 13 行；入口 1995 → 1457 行）

| 步骤 | 状态 | 证据 |
|---|---|---|
| 1. 诊断 | ✅ | `main()` 593 行中 ~480 行是纯声明（`MODES` 170 + epilog 60 + `add_argument` 250），逻辑仅 etf_combo 分支 64 + 分发循环 30 |
| 2. 迁出三模块 | ✅ | `cli/handlers/cli_parser.py`(378) / `etf_combo_runner.py`(77) / `mode_dispatch.py`(58)；入口 **+48 / −586** ⇒ **1995 → 1457 行**，`main()` **593 → 13 行**(L1433-1445) |
| 3. 循环导入规避 | ✅ | 37 handler 中 19 个导入 / 18 个入口本地定义 ⇒ `MODE_SPECS` 只留 `(flag,dest,help)`，绑定表 `MODE_HANDLERS` 留入口（**代价 = 新漂移面**） |
| 4. 漂移双保险 | ✅ | 运行期 `dispatch()` 未绑定 dest → **fail-closed `sys.exit(1)`**；测试期 `test_entry_main_split_20260910.py` AST 断言两表 dest 集合同构 |
| 5. 等价性 | ✅ | HEAD vs AFTER `--help`：**选项集合 93 vs 93 IDENTICAL** + **正文 210 行逐行 IDENTICAL**（须剔除 usage 段：prog 名长度不同导致 argparse 折行假差异）；`--daily --live` rc 2/2；`--help` rc 0/0 |
| 6. 门禁/回归 | ✅ | `ruff check` **All checks passed**（`I001 --fix` 重排 + `F401 --fix` 清掉随迁出悬空的 `sys`/`time`/`_log_execution_summary`）；新测试 **9 passed**、既有 **62 passed**、`py_compile` rc=0 |
| 7. 防回退护栏 | ✅ | 测试断言 `main() ≤ 40 行` + 禁 `main()` 内 `add_argument` + 断言 `build_parser`/`dispatch` 被调用 |
| 8. 残留 | 🟡 | `daily_trade_executor.py` **2275** ☐、`v8.3_institutional/daily_workflow.py` **2180** ☐、item 12 未启动 |

> **顺带发现（未修，待定级）**：模式互斥组 `required=True` 但 `--etf-combo` 不在组内 ⇒ `--etf-combo` **单独使用被 argparse 拒绝**(rc 2)，须叠加任一模式 flag 才能进 `run_etf_combo()`。本次保持原行为不变（等价性优先）。

### 2026-09-10 · 批次三 item 11 续做 — 统一入口 main() 拆解（`cli/handlers/`，2784 → 1995 行）

| 步骤 | 状态 | 证据 |
|---|---|---|
| 1. 迁出三模块 | ✅ | 新增 `cli/handlers/{support,helpers,deprecated_modes}.py` + `__init__.py`；入口 diff **+135 / −924** ⇒ **2784 → 1995 行**（迁出侧 304/597/89/12 行） |
| 2. 语义等价 | ✅ | HEAD 版 vs 工作版 `--help`（锁 `COLUMNS=200`）选项集合比对 **93 vs 93 全等，零增零失**；`--help` rc=0 —— 纯迁出类重构应用此法替代"声称零行为变更" |
| 3. 门禁 | ✅ | `ruff check cli/handlers/ 量化策略系统_统一入口_v8.6.py`（项目全规则）**All checks passed**；`ruff.toml` 加 `cli/handlers/**/*.py = [BLE001, E402, N806, C901]`（与源文件同集），**F401 不豁免**：`support.py` 用 59 名 `__all__` 固化契约（无 F401/F822） |
| 4. DTZ005 阻塞项 | ✅ | 入口存量 5 处裸 `datetime.now()`（报告名日期 / 压力测试时间戳 / 对冲+再平衡 `trade_date` 缺省 / 启动日志，均业务时间语义）→ `now_bj()`，`from datetime import datetime` 同步换 `from utils.datetime_utils import now_bj`；`ruff --select DTZ005` 由 FAIL → pass |
| 5. 回归 | ✅ | `pytest tests/unit/test_unified_entry_audit_regression_20260824.py tests/unit/test_g7_ms_strategy_live_scheduler_boost.py` = **62 passed**；`py_compile` 4 文件 rc=0 |
| 6. 残留 | 🟡 | `main()` 仍 **593 行**（L1391-1983）未拆；`daily_trade_executor.py` **2275** / `v8.3_institutional/daily_workflow.py` **2180** 未动（`utils/risk_guard_integrator.py` 已拆至 **455 行**） |

> 提交耦合：`ruff.toml` 的 `cli/handlers/**` 豁免须与代码同提交，否则 CI 对新增目录报 E402/C901/BLE001/N806。

### 2026-09-10 · 批次一 item 6 二轮收口 — gate-hardening（子集口径由「无记录」改为「显式落盘豁免」）

> 一轮（`388236a7`）把门禁从"形同虚设"改为"**全量**真阻断"，但**子集口径仍是裸 `exit 0`**：CI 在 `run_full != true` 时既不调用门禁、也不写任何产物 —— 与"缺数据 = 通过"同类的**无记录 = 通过**。本轮补齐该缺口，并顺带堵住"口径未知"的第三种形态。

| 报告项 | 状态 | 证据 |
|---|---|---|
| 6. 子集口径「无记录 = 通过」（本轮新发现） | ✅ | `ci.yml` 原 `if ($env:RUN_FULL -ne 'true') { Write-Host "...跳过"; exit 0 }` —— 不调用门禁、不落盘。现：子集分支必须调用 `--scope subset` 并落盘 `reports/ci/coverage_trend.json`，其中 `waived` / `waiver_reason` / `waived_checks` 均可机器读取；子集豁免时 CI 打 `::warning title=coverage-gate-waived::` ⇒ 绿灯不再等于"覆盖率已过全量判定" |
| 6. 豁免边界：结构性错误**不豁免** | ✅ | 新增 `CovResult.waivable` 区分"覆盖率测量类"与"结构性配置错误"：报告缺失/不可解析（COV-0）、关键模块路径陈旧、关键模块不在 `.coveragerc source` 内 ⇒ `waivable=False`，**子集口径也硬阻断**（缺数据 / 配置错误 ≠ 通过） |
| 6. 「口径未知 = 子集」第三形态（本轮新发现） | ✅ | 旧写法把 `RUN_FULL` **缺失 / null / 异常** 一并与 `-ne 'true'` 归入子集 ⇒ 静默豁免（select-tests 步骤失败、输出未回填、新增调用方都能触发）。现只有**明确** `run_full=false` 才允许豁免；其余（含空值）一律按 `--scope full` 硬门禁，并打 `::warning title=coverage-scope-unknown::` 保留可见性（fail-closed 且不静默） |
| 6. 子集**严禁**固化基线 | ✅ | 子集覆盖率与全量不可比 ⇒ 子集通过时不写 `coverage_baseline.json`（防用子集口径污染基线），并打印明确原因；全量通过才固化 |
| 6. 阈值单一事实源 | ✅ | `--min-line-rate` 缺省改取 `.coveragerc [report] fail_under`（原 argparse 硬编码 0.80，与 CI 的 0.38 不一致 ⇒ 本地绿 / CI 红互相矛盾，最终被当噪声忽略＝等价放行）；内置 0.80 仅作最后兜底 |
| 6. CI 产物可审计 | ✅ | 上传件由 `coverage.xml`（门禁**并不读**的路径）改为 `reports/coverage.xml`（门禁实际读取）+ `reports/ci/coverage_trend.json`（判定结果与豁免记录）+ `htmlcov/` |
| 6. 实证（脚本层） | ✅ | `pytest tests/unit/test_coverage_trend_gate_20260910.py` = **34 passed**（含修复前必红：子集必须落盘 `waived` / 同一报告全量必须阻断 / 子集仍拦报告缺失与关键模块配置错误 / 子集不写基线而全量写 / 口径未知按 full / 缺省阈值跟随 `.coveragerc`） |
| 6. 实证（实跑二口径） | ✅ | 同一份 `reports/coverage.xml`：`--scope full` → **RC=1**（`blocking_fail=1`，`waived=false`）；`--scope subset` → **RC=0** 且报告含 `waived=true` / `waived_checks=["COV-utils/risk/trade_reconciliation_runner.py"]` / `waiver_reason` 全文；`ruff` / `py_compile` / YAML 解析全绿 |
| 6. 说明 · 本地该条 FAIL 非真实空洞 | ✅ | 本地 `reports/coverage.xml` 为**陈旧**产物（早于 `utils/risk/trade_reconciliation_runner.py` 由 item 15 创建）⇒ 报"不在报告中"。已核实 `tests/unit/test_p15_reconciliation_runner_20260910.py` 确实导入该模块，CI 全量运行时它会出现在报告中；门禁此处的方向正确（宁可误报也不放过） |
| 6. 残留 | 🟡 | PR 事件仍走子集 ⇒ PR 阶段覆盖率**不由本门禁把关**（设计如此，已在 `::warning` 中明示）；全量硬阻断依赖 `push(main)` / nightly `schedule` 变体 |

### 2026-09-10 · 批次一 item 4 补齐（伪测试二件套收口：静默绿 → 真用例）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 4. `tests/e2e/test_eod_dry_run.py` 伪测试 | ✅（本轮补齐） | 原 357 行 / **0 个 `def test_`** / 0 `assert`，断言与 IO 全在模块顶层 + `main()`；pytest 因 `python_files = test_*.py` 会收集并导入它 ⇒ 通过报 "0 tests collected"（**静默绿**），失败则 collection error，两种情况都**没有任何用例被统计**。现：副作用（导入即 `mkdir` 报告目录 + `reconfigure(stdout)`）移入 fixture；抽出 `run_eod_dry_run()` 返回结构化结果（不打印、不 `sys.exit`）；新增 **6 个真断言用例**（13 phase 全执行且 ERROR=0 / 每 phase 有状态 / 18 门面齐全 / 产物契约 / 拆单失败=0 / verdict=PASS）；`main()` 保留为手工诊断入口 |
| 4. · 第二层静默绿：**目录名触发**的跳过（本轮新发现） | ✅ | 仅去掉 `e2e` 标记**无效**：`tests/conftest.py::pytest_collection_modifyitems` 按**关键字**判定，而 pytest 会把**祖先目录名**并入 `item.keywords` ⇒ 只要文件位于 `tests/e2e/`，`"e2e"` 必然命中，除非 `--run-integration` 一律 skip；而 CI nightly 的 `pytest tests -v` **不带**该开关 ⇒ 这些用例在 CI **永不执行**（换了个理由的静默绿）。故文件迁至 `tests/test_eod_dry_run.py`，保留 `slow` 标记以隔离快速通道 `-m "not slow"` |
| 4. · 断言必须按真实契约而非猜测 | ✅ | 首版断言 `workflow_state_{YYYYMMDD}.json` **不存在** → 用例红；回查 `v8.3_institutional/workflow/phases/report.py` 真实落盘为 `<report_dir>/<TRADE_DATE>/v75_daily_workflow_<YYYYMMDD>.{json,md}`（内容 = `ctx.state`，含 `trade_date/phases/orders/risk_status/hedge_status/trade_plan_loaded/...`）→ 断言改为该契约（`dry_run is True` + `phases` 非空 + md 存在 + JSON 可解析） |
| 4. `tests/test_code_quality_extreme_market.py` 空检查集假 PASS | ✅ | 该文件本已有 3 个真用例 + `tc` fixture 把子检查失败转成 pytest 失败（并非完全不工作），但 fixture 只查 `failures()` ⇒ 用例提前 return / 逻辑跳过导致**一个子检查都没跑**时 `total=0` → 静默通过。现 `total == 0` 即 `pytest.fail`；新增 `test_tc_fixture_rejects_empty_collection` 直接驱动 fixture 生成器，做**删除即红**负向证明 |
| 4. · 顺带修仓库卫生 | ✅ | 该伪测试的模块级副作用把运行时产物写进源码树并**误入库**：根 `eod_dry_run_summary_20260812.json` + `tests/e2e/eod_reports/2026-08-12/v75_daily_workflow_20260812.{json,md}` → 已 `git rm --cached`（磁盘保留）+ `.gitignore` 拦截复发；CLI 摘要改为写入报告目录 |
| 4. · 实证 | ✅ | `pytest tests/test_eod_dry_run.py tests/test_code_quality_extreme_market.py` = **10 passed / 47.1s**（改造前该文件收集 **0** 用例）；`ruff_incremental_gate` 3 文件 0 新增；`py_compile` OK |

> 历史文档不改：`cairn/LOG.md` 旧条目、`cairn/daily-workflow-split-plan.md`、`cairn/phase-execute-field-access-fix-20260812.md`、`详录-测试与基建审查.md` 中对 `tests/e2e/test_eod_dry_run.py` 的引用属**历史记录**，保持原样（"零残留"判据只适用于 import 路径，不回溯改史）。

### 2026-09-10 · 批次一 item 6 补齐（覆盖率门禁由「形同虚设」改为真阻断）

> 本轮复核发现：原标 ✅ 的 item 6 只完成了阈值口径（5%→0.38）与趋势脚本去兜底，**门禁本身仍未真正生效**。三条独立缺陷如下，均已修复。

| 报告项 | 状态 | 证据 |
|---|---|---|
| 6. CI 写/读的 `coverage.xml` 路径不一致（本轮新发现） | ✅ | `ci.yml` 的 pytest 写 `根/coverage.xml`，而 `_check_coverage_trend.py` 默认读 `reports/coverage.xml`（与 `engineering_debt_gate` T8 同约定）→ 门禁读到的**不是本次 CI 产物**（缺失即 COV-0 FAIL，或读到历史残留）。现统一为 `--cov-report=xml:reports/coverage.xml`（unit + contract 两层）并由 CI 显式 `--coverage-xml reports/coverage.xml` |
| 6. 关键模块校验**从未生效**（本轮新发现，门禁假 PASS） | ✅ | `CRITICAL_MODULES` 8 条中 4 条指向**并不存在**的 `scripts/*.py`；其余条目键形态与 coverage.py 的 filename 不一致（coverage 以 `.coveragerc` `source` 根为基准，`utils/execution/fills_store.py` 实际写作 `execution/fills_store.py`）→ `cls_map.get(旧键)` 恒为 None → 全部落入 "not in report → **non-blocking PASS**" 宽容分支。现改为真实路径 + 后缀匹配 + **缺失即 FAIL**；实跑真实报告：6 条真实匹配（fills_store 0.716 / fills_pnl_bridge 0.8864 / automated_execution_system 0.8423 / broker_factory 0.5581 / rebalance_execution_orders 0.2824 / daily_build_and_hedge 0.9638） |
| 6. 阈值口径守卫 + 子集/全量口径区分 | ✅ | `--min-line-rate` 不在 `(0,1]` → 退出码 2 拒绝执行（防 item 14 "不可达阈值" 同类陷阱）；覆盖率硬门禁仅在 `run_full=true` 生效（GAP-5 子集覆盖率与全量不可比，显式跳过并打印原因），全量硬阻断由 `push(main)` 与 nightly `schedule` 保证（二者无 `base_ref` ⇒ 全选） |
| 6. 实证 | ✅ | 新增 `tests/unit/test_coverage_trend_gate_20260910.py` **17 passed**（含修复前必红用例：后缀匹配 / 陈旧路径 FAIL / 越界阈值 exit 2 / 存在但不在报告 FAIL / CI 接线一致性）；`ruff_incremental_gate` 0 新增；mypy 317=317 |

### 2026-09-10 · 批次三 item 14 收口（样本外验证真接入 + 闸门三处假 PASS 修复）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 14. 新增真实样本外内核 | ✅ | `utils/alpha/walk_forward.py`：`build_windows()` 滚动窗口（**purge 间隔为硬约束**，运行期断言 `train_end <= test_start - purge_days`）；`walk_forward_evaluate()` 逐窗口 Sharpe/回撤 + OOS 拼接 `oos_sharpe`/`oos_sharpe_cv`/正收益窗口占比 + 前后半段衰减；**数据不足 fail-closed**（`n_windows=0` + `is_stable=False` + `insufficient_reason`，绝不返回"看起来稳定"的空结果）；`cpcv_path_distribution()` 委托 `ms_strategy` 的 `CombinatorialPurgedCV` 真实内核并以 `available=False` 明确不可用 |
| 14. `fast_backtest` 真接入 | ✅ | 新增显式入口 `FastBacktest.run_walk_forward()` / `run_cpcv()`；根 `fast_backtest.py` shim 同步 re-export。**"本模块不执行 WF/CPCV"的旧声明已重写**——`run()` 保持单段静态（`strategy_fn`/`data` 仍 fail-fast 防误用），真实滚动验证走新入口 |
| 14. · 闸门假 PASS #1（WF 模块不存在 → 步骤永远跳过） | ✅ | `backtest_gate.py` 原 `from utils.alpha.purged_kfold import PurgedKFold` —— **该模块在仓库中从未存在** → `_has_purged_kfold` 恒 False → `_run_walk_forward` 永不执行且 `walk_forward_passed` 取默认 True。现改指向真实内核；闸门启用时**必须真验证**，内核缺失/历史不足/异常一律 fail-closed + `details["walk_forward_status"]` 记录原因 |
| 14. · 闸门假 PASS #2（压力测试空持仓 + 读错字段） | ✅ | 原 `run_all_scenarios([])` 传空持仓（各场景 pnl=0）且读取**不存在的** `result["max_drawdown"]`（真实键 `worst_dd`）→ 回撤恒 0 恒通过。现读 `config/positions.json` 真实持仓 + 正确字段；无持仓/0 场景即 fail-closed |
| 14. · 闸门假 PASS #3（自造 DSR + 不可达阈值） | ✅ | 原自造公式 `SR*sqrt(0.5) - sqrt(2ln n)/sqrt(252)`（不属任何已发表口径）+ 失败默认 1.5，而 `min_dsr` 默认 1.0（对 DSR 概率值域 [0,1] **不可达**）→ 恒通过。现改用 `utils.backtest.deflated_sharpe` 的 Bailey 公式（`gate.dsr` = raw 概率）；`PipelineConfig.min_dsr` **1.0 → 0.5**；判定处新增 `min_dsr >= 1.0` 口径守卫 → 报错拒绝（不静默松弛） |
| 14. 附带发现 · `ms_strategy` WF 零间隔泄漏（本轮新发现） | ✅ 已参数化+告警 | `ms_strategy/src/backtest/walk_forward.py` 硬编码 `test_start = train_end` → train/test **同日重叠**，标签含未来 horizon 时即标签泄漏。已加 `purge_months` 参数（默认 0 保持历史行为）+ `purge_months<=0` 时显式 WARNING，docstring 注明建议 ≥ 标签窗口 |
| 14. 实证 | ✅ | 新增 `tests/unit/test_walk_forward_20260910.py` 29 用例（purge 硬断言／数据不足 fail-closed／稳定判正／regime shift 判不稳／CPCV 分布／`run()` fail-fast 未被放开／闸门 4 项负向）→ 合计 **145 passed**（含 ms_strategy 覆盖）；mypy 317=317；ruff 增量门禁 9 文件通过；`industrial_grade_check` 11P/1W/0F；`assert_data_validity` 12P/0F。注：`test_pipeline.py` 6 个 pytdx 用例为**既存失败**（基线同样失败，TDX 服务器不可达），非本次引入 |
| 14. 遗留 | 🟡 | 本次交付=**验证侧**真实样本外能力 + 闸门接线；模型**训练侧**仍用 `lgb_tscv_trainer` 的 Purged TimeSeriesSplit + embargo 1%（审计已列为正面项），**未**改为 CPCV 多路径 —— 属可选增强 |

### 2026-09-10 · D1 压力测试空仓缺陷真正修复（附项闭环）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 附·D1 压力测试 `actual_pnl` 全 0（空仓） | ✅ | **根因非"持仓读取失败"而是资产类别分类全线失效**：`_run_scenario` 只按 `strategy` 英文关键词 + 极窄 style 白名单判断，而真实 `positions.json` 是 `type=ETF/STOCK` + `style=科技/宽基/金融...`，且 CLI 映射不透传 `type` → 26 持仓全落 `other`、`impact_pct=0`。修复：新增 `classify_asset_class()` 确定性映射（`asset_class` > `strategy` > `type` > `style/sector` > 名称含 ETF > `other`）＋ `build_positions_from_positions_json()` 透传 `type/style/sector`（可测函数）＋ 未分类金额写报告 `unclassified_amount` 并告警（不再静默记 0）＋ `scope_note` 声明覆盖范围 |
| 附·D1 门禁「空场景假 PASS」（本轮新发现，同类缺陷） | ✅ | 修完上项后 D1 输出 `0 个场景, 0 个为零` → 查得 `tests/unit/test_stress_test_runner_unit.py::TestSaveReport::test_save_report` 按**当日日期**把空场景报告写进 `reports/stress_test_{today}.json`，**覆盖真实报告**，而原 D1 对 `len(scenarios)==0` 判为通过。修复：D1 空场景即 FAIL；两个测试文件加 autouse fixture 把 `REPORT_DIR` 重定向 `tmp_path`，单测不再触碰生产报告目录 |
| 附·D1 实证 | ✅ | 真实 26 持仓 → `unclassified_amount=0`、`positions_amount=2,735,784`；四场景 `actual_pnl` = -982,916 / -332,078 / -343,529 / -634,595；`assert_data_validity` **11P/1F → 12P/0F**；新增回归 51 passed（含 D1 门禁负向用例：空场景 FAIL／全零 FAIL／非零 PASS／SIMULATED 跳过）；mypy 317=317；`industrial_grade_check` 11P/1W/0F 持平 |
| 附·D1 遗留 | 🟡 | 报告口径为"证券账户股票/ETF 端**未含对冲**"，与含对冲假设的 `expected_portfolio_dd` 不同口径（已在 `scope_note` 写明）→ `crash_2015` 实际 -19.7% 超 -15% 限额、`all_pass=False` 属真实暴露；`hedge_positions` 期权腿未计入（场景 `options_tail` 为多头保护腿口径，套空头备兑 Call 会给出错误符号），是否需要方向感知建模待定 |

### 2026-09-10 · 批次三 item 15 收口（broker 门禁 fail-closed + 对账任务）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 15. broker 门禁 fail-closed（`get_broker` 实盘就绪不再静默降级） | ✅ | 新增 `LiveBrokerUnavailableError` + `is_live_intent()` / `is_live_broker()`；`_degrade_or_raise()` 统一"降级 or 抛"分支；`_build_qmt` / `_build_remote_qmt` 增 `live=` 参数并 `except LiveBrokerUnavailableError: raise` 防被宽捕获吞掉；`tests/unit/test_p15_live_broker_gate_20260910.py` |
| 15. 主链路装配点 fail-closed（不再 `OrderRouter(broker=None)` 继续跑） | ✅ | `utils/execution/automated_execution_system.py`：捕获 → `logger.critical` + `send_alert` → `raise`；`_live_intent_fallback()` 兜底（broker_factory 整体不可用且实盘就绪 → 拒绝启动） |
| 15. 模拟/实盘对账任务接入（T13 订单级 + T17 持仓 drift） | ✅ | 新增 `utils/risk/trade_reconciliation_runner.py`（计划单提取 / 成交载入 / 关联模式判定 / 报告落盘 / 严格退出码）+ `scripts/run_trade_reconciliation.py`（CLI）+ EOD 阶段 4.93；T13/T17 由"有组件无任务"变可调度真实任务 |
| 15. · 对账精度诚实口径（订单级覆盖率不足即降级，不假 PASS） | 🟡 | 成交侧 `meta.order_id` 覆盖率 < 50% → 自动降级 `(symbol, side)` 聚合对账，报告标 `linkage_mode="symbol_side"` + `linkage_degraded=True`；`status ∈ {no_plan,no_planned_orders,no_fills}` 任一缺数据即如实标记且 `verified=False`。根因 = 计划单 `morning/afternoon_orders` 无 `order_id`（须生成器与执行器同源盖章），属后续项 |

### 2026-09-10 · 批次二收尾（执行链 P2 剩余项）

| 报告项 | 状态 | 证据 |
|---|---|---|
| 7. 执行链 P2 修复包 · live_scheduler 单实例锁 TOCTOU | ✅ | `_acquire_lock()` 原子建锁（`O_CREAT\|O_EXCL`）+ stale 接管；`tests/unit/test_p2_execution_chain_20260910.py::test_acquire_lock_is_atomic` |
| 7. · 定时任务挂死即停摆 | ✅ | `_run_task` 提交 executor + `future.result(timeout)`，超时标 TIMEOUT 且调度链继续；`stop()` 有界等待（30s）；`::test_run_task_times_out_without_blocking_schedule` |
| 7. · module_last_run 写入无锁 + 幂等补同步重复触发 | ✅ | `_record_module_result()` 单临界区 + `get_status()` 快照 + `check_and_run` 改"锁内先占位再执行"；`::test_get_status_no_runtime_error_under_concurrency` |
| 7. · hedge_order_executor 死代码（读对冲持仓即丢弃 → 漏单） | ✅ | 接入 `fallback_active_orders` + 日期护栏；负向实证：修复前 0 单 / 修复后 1 单 |
| 7. · 滑点 10bp / ADV=1,000,000 硬编码魔法数字 | ✅ | `_cost_param()` 三级取值（YAML `cost_model` > `QUANT_<KEY>` > 内置默认，默认逐位一致）；`::test_cost_param_priority_config_then_env_then_default` |
| 7. · 幂等键加指令指纹 / PROJECT_ROOT / atomic_write 收敛 | ✅ | `66694ebb`（ref_price 指纹）+ `fef8b708`/`d3f44b55`（positions.json 路径 8 文件）+ `a064479e`（atomic_write 收敛） |
| 7. · 影子账户状态非原子写 | ⛔ | 调查结论：`_save_json_atomic` 用 `shutil.move` 绕 Windows 安全软件 hook `MoveFileEx`(WinError5)，公共 `atomic_write_json` 的 `_replace_with_retry` 只重试 `PermissionError`，不覆盖该场景 |
| 8. 时区治理 | 🟡 | DTZ003 全清零（`b298d678`/`6d02d910`/`cd147a41`/`d1a7bb90`/`91f4681f`/`b4412ab4`）+ DTZ005 `utils/` 394→0（`7767ef9d`）+ 防回退门禁（`9f40b70f`）；全库剩余 ~968 处待分批 |
| 9. print → logging | ✅ | `558236e9` 收尾（执行链自检块 6 处转换 + T201 豁免收紧）。注：审计原始"5732 处"为严格规则集口径，经核多为非执行链噪声 |
| 10. 仓库卫生脚本 | ✅ | `664c14aa`（`__pycache__` 清理 + `.bak`/配置备份迁 `backups\` 滚动保留 + 根目录临时文件归位） |
| 13. 依赖单一真源 | ✅ | `08d9558e`（pyproject 生成 requirements*.txt + CI 一致性门禁）+ `42d2b4b0`/`d5f39e74`（补漏登）+ `7fb56add`（幽灵 lockfile 引用） |

### 2026-09-09 · 批次一（切实盘阻断项）9/10

| 报告项 | 状态 | 证据 |
|---|---|---|
| 1. order_router 队列溢出静默丢单 + 失败单无重入队（P1-1/2） | ✅ | `664c14aa` |
| 2. 风控 fail-close 统一（P1-3/4） | ✅ | `664c14aa` |
| 3. 密钥处置（P1-6/7） | 🟡 | `llmkey.txt` 已删（单一事实源）；`.env` 内 WIND_API_KEY / MODELSCOPE_TOKEN **轮换属人工动作，待用户执行** |
| 4. 伪测试改造（P1-8） | ✅ | `664c14aa` |
| 5. 重训冷却锁按 result 状态写 + 原子"进行中"标记（P1-5） | ✅ | `664c14aa` |
| 6. 覆盖率口径统一 + 硬门禁（P1-9/10） | ✅ | `664c14aa`（阈值口径 5%→0.38 + 趋势脚本去兜底）＋ **2026-09-10 一轮 `388236a7`**：CI 写/读 `coverage.xml` 路径统一、关键模块校验改 fail-closed（原 8 条**从未生效**）、阈值口径守卫与子集/全量口径区分 ＋ **2026-09-10 二轮 gate-hardening**：子集口径由裸 `exit 0`（无记录 = 通过）改为必须落盘显式豁免（`waived`/`waiver_reason`/`waived_checks`）+ 口径未知按 full fail-closed —— 见 §7bis 顶部两则 |
| 附：D1 压力测试"假 PASS"消除 | ✅ | `fa7cfcf0`（排除 `stress_test_SIMULATED_*` + 改按日期排序）→ 修复后**如实暴露**真实 FAIL：`stress_test_20260909.json` 4 场景 `actual_pnl` 全 0（空仓）。**该真实缺陷已于 2026-09-10 修复（见 §7bis 顶部「D1 压力测试空仓缺陷真正修复」），D1 现 12P/0F** |

### 批次三（结构性改善）— 2/5（item 11 进行中）

| 报告项 | 状态 | 2026-09-10 实测 |
|---|---|---|
| 11. 巨型文件拆解 | 🟡 进行中 | ① `utils/risk_guard_integrator.py` **2344 → 455 行**（拆至 `utils/risk/guards/` 10 文件）✅；② `量化策略系统_统一入口_v8.6.py` **2784 → 1457 行**（拆至 `cli/handlers/` 六模块：support/helpers/deprecated_modes + cli_parser/etf_combo_runner/mode_dispatch），**`main()` 593 → 13 行** ✅（见 §7bis 顶部两条）；③ `daily_trade_executor.py` **2275 → 1385 行**（盘前指令簇拆至 `executor/premarket.py` 976 行）✅（见 §7bis 顶部三条）；④ `v8.3_institutional/daily_workflow.py` **2180 → 1538 行**（真实现簇拆至 `workflow_mixins/` 两 mixin）✅（见 §7bis 顶部四条） |
| 12. utils 按域拆包 | 🟡 第一增量（`a` 见 §7bis 顶部） | 实测后：LLM 入口收敛第一步 = **孤儿归档**（`utils/llm_finetune.py` 全树 0 导入方、`utils/local_llm.py` 仅自身测试消费）+ `_legacy` 空壳移除（纯 docstring 占位、全文件类型零路径引用）+ `check_llm_boundary.py` 补 `utils.alpha.llm` 盲区（扩清单后执行链 0 违例）；`config/` vs `configs/` 已实测（`configs/` 0 文件被 git 跟踪、仅 3 文件名重叠且全分歧，`portfolio.yaml` 为唯一真实双源冲突、4 处硬编码主读待业务拍板，见 `cairn/item12-config-dual-source-audit-20260911.md`） |
| 13. 依赖单一真源 | ✅ | `08d9558e` + `42d2b4b0`/`d5f39e74` + `7fb56add`（见批次二表） |
| 14. 样本外验证主链路接入 | ✅（遗留：训练侧仍用 Purged TSCV） | `utils/alpha/walk_forward.py` 真实 WF/purge 内核 + `FastBacktest.run_walk_forward()`/`run_cpcv()` 显式入口 + `BacktestGate` 三处假 PASS 全改 fail-closed；见 §7bis 顶部 item 14 条目 |
| 15. 模拟/实盘对账 + broker.enable 切换门禁 | ✅ | `253a28ef`（见上表；遗留：计划单补 `order_id` 以达成订单级对账精度） |

### P2 分册项现状（2026-09-10 全量核实，回答"是否都已完成"）

| P2 项 | 状态 | 实测 |
|---|---|---|
| 执行链 13 项 | ✅ 已修 10 / ⛔ 1 / 🟡 2 | 见批次二表；⛔ 影子账户非原子写（判定不修）；🟡 残项见下 |
| 安全与配置 · 计划任务 `RunLevel Highest` 超需 | ✅ | `register_intraday_task.ps1:24` 已改 `-RunLevel Limited`（P2 修复 2026-09-09），对齐 `register_hn_task.ps1` 基线 |
| 安全与配置 · `.env.example` 与 `.env` 变量集漂移 | ☐ 未收敛 | 实测 `.env.example` 20 个 `KEY=` 条目 vs `.env` 15 个 —— 仍漂移 |
| 安全与配置 · `config/` 与 `configs/` 双目录并存 | ☐ 未收敛 | 两目录均存在；`feature_flags.yaml` 分叉问题并入 item 12 |
| 测试与基建 · `conftest.py` 巨石化（31,526 字节） | ☐ 未拆分 | 审计原文记"根 conftest.py"，**实际位置 = `tests/conftest.py`**，实测仍 31,526 字节（未拆） |
| 测试与基建 · 依赖声明多套并存 | ✅ | 并入 item 13（`08d9558e` 单一真源 + CI 一致性门禁） |
| 测试与基建 · `requires-python >=3.10` 下限未验证 | 🟡 | `pyproject.toml:17-19` 仅补"待办"注释；**加 CI 3.10/3.12 版本矩阵或收紧至 `>=3.12` 未做** |
| 测试与基建 · 磁盘垃圾（749 .bak + 4,424 `__pycache__`） | ✅ | 实测 `.bak` = 54、`__pycache__` = 50（排除 `.venv`）→ 基本清干净，属正常运行期增量，非回退 |
| 测试与基建 · 根目录转发 shim 双轨入口 + utils 巨石 | ☐ | 根目录仍 43 个 `.py`；`量化策略系统_统一入口_v8.6.py` 内 `_deprecated_mode_stub` 系列 shim 保留；utils 巨石并入 item 12 |
| 策略与回测 · `strategy_evaluator.py` 的 `train_test_split` | ✅ 复核后确认合理 | 实为 `checker.check_train_test_split(train_end, test_start, min_gap_days=...)` 的**前视偏差检查器调用**，非对时序数据做随机切分 → 无数据泄漏，该疑问关闭 |
| 静态扫描 · DTZ 时区（约 1,835 处） | 🟡 | 并入 item 8：DTZ003 全清零 + DTZ005 `utils/` 清零 + 防回退门禁；全库残余 ~968 处待分批 |
| 静态扫描 · T201 print（5,732 处） | ✅ 按执行链口径收口 | 并入 item 9（审计原数为严格规则集口径，多为非执行链噪声） |
| 静态扫描 · BLE001/S110/S112 异常吞没（~870 处） | ☐ 未做 | 未纳入任何批次（批次二仅按文件收窄了少数几处） |
| 静态扫描 · C901/PLR0912/PLR0915 复杂度热点 | ☐ 未做 | 与 item 11 巨型文件同源，随拆解一并处理 |
| 静态扫描 · PLC0415 函数内 import（3,333）/ PLR0913 / PLW0603 | ☐ 未做 | 未纳入批次 |
| 静态扫描 · subprocess 无 check（108）/ NPY002 legacy random（668） | ☐ 未做 | 未纳入任何批次 |

## 8. 方法与局限

- 四路并行审查由主会话 + 子代理完成；策略与回测维度的子代理连续两次遇到 LLM 请求失败，该维度由主会话亲自补查（wt_backtest_engine 修复验证、DSR 引用面、CV 方式、成本模型、前视门禁均已覆盖），但未做到 ms_strategy 84 文件全量精读。
- 静态扫描使用独立严格规则集（select ALL 减去文档/注释类），与项目自带 ruff.toml（仅 7 项发现）口径完全不同，数字不可直接对比。
- 未复跑 pytest 全量（自评称 16,709 用例可收集）；覆盖率声称未重新生成验证。
- 详录见随附三份分册报告（执行链路 18 项 / 安全与配置 5 项 / 测试与基建 10 项，含文件:行号证据）。

---

*审计：AutoClaw · 2026-09-09*
