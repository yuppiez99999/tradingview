# 回测程序缺陷审查报告

**审查日期**：2026-09-04
**审查范围**：回测链路全部实现与入口
**审查方式**：静态代码审查 + 签名/依赖可达性验证

## 审查对象

| 文件 | 行数 | 角色 |
|---|---|---|
| `utils/wt_backtest_engine.py` | 767 | 主事件驱动回测引擎（WonderTrader 风格） |
| `utils/alpha/fast_backtest.py` | 653 | ML 策略评估引擎（DSR / IC_IR / Sharpe CV） |
| `cli/modes/backtest.py` | 62 | CLI 回测入口 |
| `backtest_engine.py` / `fast_backtest.py` | 11 / 12 | 兼容 shim |
| `backtests/etf200w_20260903/portfolio_etf200w_backtest.py` | 260 | 200 万 ETF 月度再平衡回测 |
| `backtests/v510_portfolio_20260903/portfolio_v510_backtest.py` | 347 | 23 标的组合阈值再平衡回测 |

---

## 一、致命缺陷（P0）—— 回测数字不可信

### P0-1 胜率统计完全错误，恒等于 100%

`utils/wt_backtest_engine.py:400-405`

```python
winning_trades = [t for t in self.trades if t["action"] == "SELL"]
if winning_trades:
    win_count = sum(1 for t in winning_trades if t["total_revenue"] > 0)
    win_rate = win_count / len(winning_trades)
```

`total_revenue = execution_price * qty - commission`（L196），只要成交额大于 5 元佣金就恒为正。
**判断"是否盈利"却只看了卖出回款，从未与持仓成本比较。**

- 后果：任何策略的胜率都接近 100%，指标完全失效。
- 正确口径：`total_revenue - avg_cost * qty > 0`，且需要在 trade 记录中落 `realized_pnl` 字段。
- 引擎的 `BaseTradeDict` 根本没有已实现盈亏字段（L46-57），这是根因。

### P0-2 Sharpe 比率被系统性高估

`utils/wt_backtest_engine.py:373-387`

```python
daily_returns = [d["daily_return"] for d in self.daily_pnl if d["daily_return"] != 0]
```

**过滤掉了全部零收益日。** 零收益日（空仓、停牌、持仓未变动）是真实的风险暴露期，剔除后：

- 样本量 N 缩小 → 分母（标准差）被压缩
- 均值被抬升（移除了 0 值拖累）

双重作用下 Sharpe 被显著放大，**空仓时间越长的策略失真越严重**——恰好是择时策略最需要准确评估的场景。

叠加问题：
- 未扣减无风险利率（`fast_backtest.py` 扣了，此处没扣，两套口径不一致）
- 用总体标准差（除以 N 而非 N-1），小样本下进一步低估波动

### P0-3 前视偏差：信号与成交同日同价

`utils/wt_backtest_engine.py:302-359`

```python
for _i, day_data in enumerate(data):
    prices = day_data.get("prices", {})
    self.update_prices(prices)          # 用当日价格估值
    signals = strategy_func(day_data, self.positions)   # 用当日数据产生信号
    ...
    success = self.buy(code, price, qty)   # 用同一个 price 成交
```

信号生成与成交发生在同一天、同一个价格上。若策略函数读取了当日任何行情（收盘价、当日资金流信号），就等于**用已知结果成交**。

对比参照：`backtests/` 下的组合回测已正确实现"收盘产生信号 → 次日开盘执行"（见 `portfolio_v510_backtest.py:274-284` 的 `pending_rebalance` 机制）。
**主引擎与组合回测的成交口径不一致，是同一项目内两套标准。**

缺失机制：无 `execution_delay`、无 `next_open` 成交、无信号时间戳校验。

### P0-4 CLI 回测模式是死代码，两条分支都跑不通

`cli/modes/backtest.py:23-56`

```python
try:
    from fast_backtest import run_fast_backtest
    run_fast_backtest()                    # ① 缺必填参数
except ImportError:                        # ② 捕获类型错误 ≠ ImportError
    from backtest_engine import BacktestEngine
    engine = BacktestEngine(portfolio, settings)   # ③ 签名完全不匹配
    result = engine.run_backtest(...)              # ④ 方法不存在
```

逐条验证结果：

| 位置 | 问题 | 实际后果 |
|---|---|---|
| ① | `run_fast_backtest(returns, n_trials=1, ...)` 的 `returns` 是必填位置参数 | `TypeError`，而 `except ImportError` 捕获不到 → 直接抛出 |
| ③ | 真实签名是 `__init__(initial_capital, commission_rate, slippage_rate, min_commission)` | `PortfolioConfig` 对象被当作 `initial_capital`，settings dict 被当作 `commission_rate` |
| ④ | `BacktestEngine` 只有 `run()`，**没有 `run_backtest()`** | `AttributeError` |

结论：`python 量化策略系统_统一入口_v8.6.py backtest` 不可能产出回测结果。

### P0-5 `FastBacktest` 宣传的 Walk-Forward / PurgedKFold 根本没实现

`utils/alpha/fast_backtest.py`

模块 docstring 第一行声称整合 Walk-Forward Analysis 与 PurgedKFold，但：

- `run()` 的 `strategy_fn` 和 `data` 两个参数（L214-215）**接收后全程零引用**
- `n_windows` 由 `_estimate_n_windows()`（L524-535）按"21 天 = 1 月"**估算**，不是真实滚动次数
- `window_details` 恒为空列表（L288）
- 全文无 `purge`、无 `embargo`、无 KFold 逻辑

**该模块实际只算了一组静态绩效指标**，却被冠以 walk-forward 验证的名义，DSR/IC_IR/Sharpe CV 的可信度被包装放大。

### P0-6 DSR 默认值导致 V9 门槛恒失败，且显示与判定口径不一致

- L396-397：`n_trials <= 1` 时 `_compute_dsr` 直接 `return 0.0`
- 默认 `n_trials=1` → DSR 恒为 0 → `dsr_scaled = 0 < 5` → **V9 永远 FAIL**（用户不显式传 `n_trials` 时）
- L176 打印：`DSR: {self.dsr:.2f} (阈值 >= 5)` —— 打印的是 0~1 原始值，判定用的是 `dsr * 10`（L568）

**用户看到的数字和判定依据对不上**，是典型的误导性输出。

---

## 二、严重缺陷（P1）

| 编号 | 位置 | 缺陷 | 影响 |
|---|---|---|---|
| P1-1 | `wt_backtest_engine.py:349-352` | 无 T+1 约束，同一循环内可当日买入再当日卖出 | 日内反转类策略回测结果虚高 |
| P1-2 | `wt_backtest_engine.py:141-146` | 滑点固定 0.1%，与成交量、订单规模完全脱钩，无冲击成本 | 大资金策略成本被严重低估 |
| P1-3 | `wt_backtest_engine.py:136-139` | 费用模型缺印花税（股票卖出 0.05%）、缺过户费；ETF 也套用最低 5 元佣金 | 与 `portfolio_v510_backtest.py`（已实现印花税）口径冲突 |
| P1-4 | `wt_backtest_engine.py:413-438` | 无基准对比、无 Alpha/Beta、无信息比率、无超额收益 | 无法判断策略是否真有 alpha |
| P1-5 | 同上 | 无换手率（turnover）指标 | 无法评估交易成本侵蚀 |
| P1-6 | `wt_backtest_engine.py:418-422` | 年化 `(1+R)^(252/N)-1` 对短窗口无下限保护 | 回测 10 天被放大约 25 倍 |
| P1-7 | `fast_backtest.py:325-326` | 期末权益 ≤ 0 时 `max_dd = 0.0` | **爆仓策略报告 0 回撤，V9"回撤≤10%"反而 PASS**——最坏情况被判为最优 |
| P1-8 | `wt_backtest_engine.py:480` | `equity = day_data.get("equity", 1000000.0)`，与引擎真实权益脱钩 | 仓位按固定 100 万计算，资金曲线失真 |
| P1-9 | `wt_backtest_engine.py:598-606` | `except (...): continue` 静默吞掉所有数据加载异常，无计数无告警 | 回测在残缺数据上运行且用户不知情 |
| P1-10 | 两个 `portfolio_*_backtest.py:31-38` | 硬编码 `sys.path.insert` 到插件缓存目录导入 `export_results` | 插件升级/缓存清理后回测不可复现 |
| P1-11 | `portfolio_etf200w_backtest.py:102-115` | 买入不校验现金余额，`cash` 可为负；停牌日价格为 NaN 直接污染权益 | 透支成交、权益曲线断裂 |
| P1-12 | `portfolio_v510_backtest.py:307-318` | docstring 承诺"写入脚本所在目录"，但 `export_results` 未传 `output_dir`（对比 etf200w 传了 `output_dir=HERE`） | 产物散落到当前工作目录 |
| P1-13 | 两个组合回测 | 用**当前**持仓清单回溯 2023-09 至今 | 幸存者偏差，历史收益系统性高估 |
| P1-14 | 组合回测 | 无滑点（只有佣金）、无涨跌停不可成交建模 | 成本低估 |

---

## 三、一般缺陷（P2）

**代码质量**
- `wt_backtest_engine.py:489` — `signal_info.get("inflow", 0)` 返回值被丢弃，死代码
- `wt_backtest_engine.py:632-634` — 函数内 `import random` + `random.seed(42)`，污染全局随机状态
- `wt_backtest_engine.py:260-273` — `_is_suspended` 的"价格缺失"分支永不触发（`run()` 已提前 `continue`）
- `portfolio_v510_backtest.py:151` — `_ = label  # 仅注释用途`，参数未使用
- `portfolio_v510_backtest.py:240` — 用 `assert` 做数据完整性校验，`python -O` 下失效
- `portfolio_etf200w_backtest.py:26` — `import math` 未使用

**性能与兼容性**
- `fast_backtest.py:479-483` — `rolling().apply()` Python 级回调，O(n×21)，长序列极慢
- `fast_backtest.py:486-488` — `groupby().apply()` 在 pandas 2.2+ 触发 deprecation
- `fast_backtest.py:342` — Sortino 可能返回 `float('inf')`，污染 JSON 序列化

**方法论缺口**
- 无样本外滚动验证（主引擎）
- 无随机策略基准对照（random baseline）
- 无参数敏感性 / 稳健性检验
- `_normalize_returns` 不校验索引单调性与重复值

---

## 四、架构层面问题

### 1. 四套回测实现互不兼容

| 实现 | 成交价口径 | 费用口径 | 滑点 | T+1 |
|---|---|---|---|---|
| `wt_backtest_engine.py` | 当日同价 | 佣金+最低5元 | 0.1% | 无 |
| `fast_backtest.py` | 输入收益率序列 | 无 | 无 | N/A |
| `portfolio_v510_backtest.py` | 次日开盘 | 佣金+印花税 | 无 | 天然满足 |
| `portfolio_etf200w_backtest.py` | 次日开盘 | 佣金 | 无 | 天然满足 |

同一策略走不同入口会得到不同结果，**无法横向比较**。

### 2. 无统一回测结果 Schema

各实现返回结构完全不同（`dict` / `BacktestResult` / CSV+JSON），无公共契约，无法做统一的策略对比与归档。

### 3. 无复现机制

`backtests/` 下结果以时间戳目录命名（`etf200w_20260903`、`v510_portfolio_20260903`），**无参数快照、无数据版本指纹、无 git commit 记录** → 历史结果无法复现。

### 4. 测试存在盲区

`tests/unit/test_wt_backtest_engine_unit.py`、`tests/unit/test_fast_backtest.py` 存在，但对 P0-1、P0-2 的断言仅是"字段存在"：

```python
for field in ["initial_capital", "final_equity", "total_return",
              "sharpe_ratio", "max_drawdown", "win_rate"]:
    assert field in report
```

**只验证键名存在，从不验证数值正确** → 统计错误既未被发现，也未被保护。

---

## 五、修复优先级建议

**第一批（阻断性，必须立即修）**
1. P0-1 胜率口径 — 增加 `realized_pnl` 字段，改为与持仓成本比较
2. P0-2 移除零收益日过滤，补无风险利率与样本标准差
3. P0-3 引入成交延迟机制（`signal_date` → `execution_date`），默认次日开盘
4. P0-4 重写 CLI 回测入口，或标注为 deprecated
5. P0-7 权益 ≤ 0 时回撤应为 -100%，不得置 0

**第二批（指标可信度）**
6. P0-5 实现真实的 walk-forward，或删除文档中的虚假声明
7. P0-6 统一 DSR 显示与判定口径，`n_trials` 默认值改为 >1 并强制要求调用方声明
8. P1-1 T+1 约束
9. P1-2/P1-3 滑点按成交量建模，补齐印花税与过户费
10. P1-4/P1-5 增加基准对比与换手率

**第三批（工程化）**
11. P1-10 将 `export_results` 内联到项目内，去掉插件缓存依赖
12. P1-12 补 `output_dir`
13. 统一四套实现的成交与费用口径，定义公共 `BacktestResult` Schema
14. 为每个回测产物附加参数快照 + 数据指纹，实现可复现
15. 补充"数值正确性"测试（胜率、Sharpe、回撤的解析解对照）

---

## 六、结论

当前回测程序的**核心统计指标（胜率、Sharpe）在数学上是错的**，且主引擎存在**前视偏差**。这意味着：

- 已生成的回测报告（胜率、Sharpe 相关结论）需要重新核算
- 主引擎（`wt_backtest_engine.py`）的结果不能直接用于策略筛选
- 相对可信的是 `backtests/` 下的两个组合回测——它们的成交时点处理正确，但仍缺滑点与幸存者偏差校正

**建议：在 P0 项修复完成前，不要用现有回测结果做任何实盘资金决策。**

---

*本报告基于静态代码审查，未执行回测（运行环境缺少 pandas/numpy 依赖）。所有结论均给出代码位置，可逐条复核。*
