# v8.6 集成升级最优方案（2026-08-21）

> 知识沉淀来源（2026-08-21 全量基线，含当日新增）：
> - 旧排期：`github_trending_高价值统计与升级计划_20260807.md`（W33-W38+）
> - 本周热榜：`GitHub本周热榜统计_20260818.md`（14 Top + 1 用户指定）
> - 失败测试：`failed_tests.txt`（44 失败，**8/21 重跑确认全部已修复**，归档 `failed_tests_过时_20260812.txt`）
> - ai_decision/ 17 文件全貌（当前孤立，待 C 轨集成）
> - AGENTS.md v8.6.14 + ROADMAP.md（Wave 7 启动，4 Sprint 至 12-31 v8.7）
> - **[8/21 新增] 第三方项目集成**：`cairn/third-party-integration-20260821.md`（S3→S2→A2→A1→S1 全部完成，98 单测全绿）
> - **[8/21 新增] ECC 赋能评估**：`docs/ECC赋能量化系统工作计划_20260821.md`（修正初始判断，ECC 增量价值有限，仅 3 项最小化接入）
> - **[8/21 新增] 十五五专项规划**：`cairn/Reference/十五五专项规划正文汇编_20260821.md`（10 个规划获正文，权重复核建议）
> - **[8/21 新增] duckduckgo-mcp**：deep-research 免费替代（npm v1.0.1，无 API key）
> - **[8/21 新增] EOD 三重保障**：`cairn/eod-fallback-guarantee-20260820.md`（PYTHONPATH 隔离修复，08-21 验证 exit 0）
> 设计原则：选方案 → 回测 → 审指标 → 调优 → 定版 → 部署生产；每阶段含 shadow 验证，不干扰实盘调度
> 硬约束：① 不破坏数据源优先级链（Wind→TDX→AKShare→新浪→缓存→兜底）② 通过 pre-commit T201/pyflakes 门禁 ③ 保持不可变性 + 单文件 <800 行 ④ 不改变现有 35+ CLI 模式行为

---

## 一、知识沉淀盘点

### 1.1 旧排期（W33-W38+，截至 2026-08-07）

| Phase | 项目 | 周次 | 状态（8/21 = W34） |
|---|---|---|---|
| 0 | code-review-graph 升级 | W33 (8/11-8/15) | 应已完成，需核实 |
| 1 | loopx → live_scheduler | W34-W35 (8/18-8/29) | 进行中（W34） |
| 2a | pdf-inspector → OCR 流水线 | W36 (9/1) | 待启动 |
| 2b | skills 仓库补质量门禁 | W37 (9/8) | 待启动 |
| 3a | cloudflare/computer 评估 | W38+ (9/15) | 待评估 |
| 3b | TencentMemory 评估 | W38+ (9/15) | 待评估 |

### 1.2 本周热榜新发现（2026-08-18，14 个 Top + 1 个用户指定）

| 项目 | 本周★ | 语言 | 与 v8.6 契合点 | 价值 |
|---|---|---|---|---|
| **antirez/ds4**（用户指定） | 21.5k | C | DwarfStar 推理引擎，原生支持 **GLM 5.2** + DeepSeek V4 Flash，可替换 `utils/glm5_client.py` 的 Ollama 后端 | 🔴 P0 |
| vitali87/code-graph-rag | 1,135 | Python | monorepo RAG + 知识图谱，可增强 `cairn/` 知识层与 code-review-graph | 🟡 P1 |
| anthropics/skills | 2,714 | Python | Anthropic 官方 Skills 仓库，可注入 `skills/` 与 FinClaw 互补 | 🟡 P1 |
| unslothai/unsloth | 3,329 | Python | 本地 LLM 训练 UI，可增强 `ml_enhanced_trainer.py` 微调管道 | 🟡 P2 |
| semantica-agi/semantica | 4,746 | Python | 图原生 AI 基础设施，可增强 `ai_decision/rag_context.py` | ⚪ P3 评估 |
| PrimeIntellect-ai/prime-agent | 4,328 | TS | 自我进化 RLM 智能体，自治编码场景，与实盘交易无关 | ⚪ P3 评估 |
| cathrynlavery/diagram-design | 16,260 | HTML | Claude Code 图表，可增强 `ui/pages/` 报告可视化 | 🟡 P2 |
| 3b1b/manim | 1,724 | Python | 数学动画，可用于策略回测可视化 | ⚪ P3 评估 |
| cactus-compute/needle | 3,627 | Python | 14MB 基础模型，边缘部署场景，与机构管道无关 | ⚪ 不接入 |
| macro-inc/macro | 2,724 | Rust | 团队工作台，与单机量化系统架构不匹配 | ⚪ 不接入 |
| megadose/holehe | 1,416 | Python | 邮箱反查 OSINT，与交易无关 | ⚪ 不接入 |
| lightningpixel/modly | 1,338 | TS | 本地 3D 生成，与交易无关 | ⚪ 不接入 |
| basecamp/omarchy | 1,477 | Shell | Linux 桌面配置，系统是 win32 | ⚪ 不接入 |

### 1.3 失败测试技术债（44 个，13173 通过，544s）

| 类别 | 数量 | 根因 | 阻塞 |
|---|---|---|---|
| iFinD 残尸测试 | ~22 | AGENTS.md 已剔除 iFinD（2026-08-03），但 `test_g7_etf_flow_monitor_boost.py` / `test_g7_hedge_engine_boost_v2.py` 仍引用 `_fetch_ifind_*` / `_exec_ifind` / `ifind_mcp_available` / `IFIND_TOKEN` | CI 门禁 |
| limit_pool_provider 缓存属性 | ~11 | `LimitPoolProvider` 缺 `_cache_ttl` / `_cache_lock` 属性（重构未同步测试） | CI 门禁 |
| transformer_encoder torch | 2 | `test_force_torch_raises` 期望 RuntimeError 未抛出；`test_auto_fallback_numpy` 期望 False | CI 门禁 |
| tdx_data_source | 1 | `test_graceful_failure` 期望 False 实际 True（优雅降级逻辑反转） | CI 门禁 |
| qlib signal adapter | 3 | `test_local_lightgbm_signal` 返回 None，fallback 路径未实现 | CI 门禁 |
| institutional_optimizer | 1 | `test_empty_positions` 期望 None 实际 `array([0.])` | CI 门禁 |
| system_check env | 1 | 期望 `IFIND_TOKEN` 在 critical env vars（已剔除但测试未更新） | CI 门禁 |
| limit_pool akshare | 1 | akshare 已安装但测试期望未安装场景 | CI 门禁 |
| transformer encoder 其他 | 2 | DID NOT RAISE RuntimeError | CI 门禁 |

### 1.4 ai_decision/ 模块全貌（17 文件，当前孤立）

| 文件 | 职责 | 当前集成状态 |
|---|---|---|
| `cli.py` | CLI 入口 | 独立运行，未挂到统一入口 v8.6 |
| `orchestrator.py` | 任务编排 | 未接入 institutional_pipeline |
| `debate_engine.py` | 多模型辩论 | 未接入信号融合 |
| `decision_gate.py` | 决策门控 | 未接入再平衡 |
| `consensus_aggregator.py` | 共识聚合 | 未接入信号融合 |
| `eod_review.py` | EOD 复盘 | 未接入 EOD 管道报告阶段 |
| `execution_bridge.py` | 执行桥接 | 未接入 daily_trade_executor |
| `backtest_replay.py` | 回测回放 | 独立 |
| `rag_context.py` | RAG 上下文 | 未接入 cairn 知识层 |
| `auto_research_skill.py` | 自动研究 | 独立 skill |
| `auto_research_defaults.py` | 默认配置 | 配套 |
| `providers.py` | LLM provider | 未与 `utils/glm5_client.py` 统一 |
| `models.py` | 数据模型 | 配套 |
| `config.py` | 配置 | 配套 |
| `dashboard.py` | 仪表盘 | 未接入 UI |
| `health.py` | 健康检查 | 未接入 01_系统概览 |
| `__init__.py` | 包初始化 | — |

---

## 二、最优方案选型

### 2.1 候选方案对比

| 方案 | 描述 | 优点 | 缺点 | 评分 |
|---|---|---|---|---|
| **A. 单轨串行** | 按旧排期逐周推进，不并行 | 风险低，验收简单 | 周期长（W34→W40+），技术债持续阻塞 CI | 6/10 |
| **B. 双轨并行** | 旧排期 + 失败测试修复 | 解锁 CI，进度快 | ai_decision 孤立问题未解 | 7/10 |
| **C. 三轨并行**（**最优**） | A 技术债清偿 + B 外部高价值集成 + C ai_decision 全链路集成 | 最大化吞吐，三轨互不依赖 | 协调复杂，需严格阶段门禁 | **9/10** |
| D. 全量重写 | 推翻 ai_decision 重做 | 架构最优 | 风险极高，违反"不改变现有行为"约束 | 3/10 |

### 2.2 选型结论：**方案 C — 三轨并行**

三轨互不依赖：
- **A 轨**（技术债）只改测试与已剔除模块的残留引用，不动生产代码
- **B 轨**（外部集成）只新增模块 + feature flag，不改既有管道
- **C 轨**（ai_decision 集成）只在管道挂接点加 hook，不改 ai_decision 内部

任一轨失败可独立回滚，不影响其他两轨。

---

## 三、A 轨：技术债清偿（W34-W35，8/18-8/29）

> **[2026-08-21 状态更新]** ✅ **A 轨全部完成（零代码改动）** — `failed_tests.txt` 是 8/12 过时数据，重跑 pytest 确认 44 失败**全部已修复**（iFind 22 测试已 `@pytest.mark.skip` + limit_pool/transformer/tdx/qlib 等 22 测试已修复），归档 `failed_tests_过时_20260812.txt`。下方 §3.1-§3.7 详细动作**未执行**（因测试已修复），保留作历史记录与根因分析；实际验收见 §3.8 + §十一。

### 3.1 iFinD 拥尸清理（最高优先，22 个测试）

**根因**：AGENTS.md §6 已于 2026-08-03 剔除 iFinD，但测试文件未同步清理。

**动作**：
1. 删除 `tests/unit/test_g7_etf_flow_monitor_boost.py::TestFetchIFindFundFlow` 整个类（6 测试）
2. 删除 `tests/unit/test_g7_etf_flow_monitor_boost.py::TestInitDataSources` 中 iFind 相关 3 测试
3. 删除 `tests/unit/test_g7_etf_flow_monitor_boost.py::TestGetEtfFundFlow::test_wind_none_ifind_used`（已断言 `price_momentum == ifind_mcp` 失败，证明 fallback 已切到 price_momentum）
4. 删除 `tests/unit/test_g7_hedge_engine_boost_v2.py::TestHedgeEngineFuturesPrices` 中 6 个 `_exec_ifind` / `fetch_futures_prices_from_ifind` 测试
5. 删除 `tests/unit/test_g7_hedge_engine_boost_v2.py::TestIntegratorModuleFunctions` 中 5 个 `_get_ifind_prices_batch` / `_exec_ifind` 测试
6. 修改 `tests/unit/test_g7_system_check_boost.py::test_critical_env_vars_windows`：从期望列表移除 `IFIND_TOKEN`
7. 全仓 grep `ifind` / `iFinD` / `IFIND_TOKEN` 残尸引用，确认 `utils/hedge_engine.py` / `utils/hedge_rebalance_integrator.py` / `ETFRealTimeTracker` 已无 iFind 代码路径

**验收**：iFinD 相关 22 测试全部移除，剩余测试不引用 iFind 符号

### 3.2 limit_pool_provider 缓存属性（11 测试）

**根因**：`LimitPoolProvider` 重构后缺 `_cache_ttl` / `_cache_lock`，测试仍引用旧属性。

**动作**：
1. 读 `utils/limit_pool_provider.py`（或对应路径）确认当前缓存实现
2. 二选一：
   - 若重构后用 `functools.lru_cache`：改测试断言为 `hasattr(provider, 'get_pool')` + 调用验证
   - 若需保留显式缓存：在 `__init__` 补 `self._cache_ttl = ...` / `self._cache_lock = threading.Lock()`
3. `test_get_limit_up_pool` / `test_get_limit_down_pool` / `test_get_broken_pool` / `test_get_pools_batch*` / `test_clear_cache` / `test_cache_info` / 便捷函数 2 测试同步修复

**验收**：limit_pool 11 测试通过

### 3.3 transformer_encoder torch fallback（2 测试）

**根因**：`test_force_torch_raises` 期望 torch 不可用时抛 RuntimeError，实际未抛；`test_auto_fallback_numpy` 期望 False 实际 True。

**动作**：
1. 读 `utils/transformer_encoder.py`（或 `ms_strategy/src/alpha/` 下）的 `build_factor_encoder(force_torch=...)` 实现
2. 若 `force_torch=True` 时 torch 不可用确实未抛异常：补 `raise RuntimeError("torch required but not available")` 分支
3. `test_auto_fallback_numpy`：确认 auto 模式在 torch 缺失时返回 numpy 路径，修正断言方向

**验收**：transformer_encoder 2 测试通过

### 3.4 tdx_data_source 优雅失败（1 测试）

**根因**：`test_graceful_failure` 期望 `False` 实际 `True`，说明优雅降级逻辑反转。

**动作**：
1. 读 `quant_modules/tdx_connector.py` 的 `init_connection` 失败路径
2. 确认失败时返回 `False`（连接失败）而非 `True`；或修正测试期望为 `True`（若设计意图是"优雅降级成功即返回 True"）

**验收**：tdx 1 测试通过

### 3.5 qlib signal adapter（3 测试）

**根因**：`test_local_lightgbm_signal` / `test_generate_signal_fallback` / `test_local_lightgbm_signal_dataframe_input` 均 `assert None is not None`，说明 `QlibSignalAdapter` 在本地 LightGBM 路径返回 None。

**动作**：
1. 读 `ms_strategy/src/` 下 `QlibSignalAdapter` 实现
2. 确认本地 LightGBM 模型加载路径（`models/lgb_enhanced/` 或 `models/ml_v510/`）
3. 修复 `local_lightgbm_signal` 在模型存在时返回 signal，不存在时走 fallback 并返回非 None 占位

**验收**：qlib signal 3 测试通过

### 3.6 institutional_optimizer 空持仓（1 测试）

**根因**：`test_empty_positions` 期望 `None` 实际 `array([0.])`，说明空持仓返回零向量而非 None。

**动作**：修正测试断言为 `assert result is not None and result.sum() == 0`（或修正实现返回 None）— 倾向改测试，因零向量是合理返回

**验收**：1 测试通过

### 3.7 akshare 安装场景（1 测试）

**根因**：`test_akshare_not_installed` 期望 akshare 未安装，但环境已安装。

**动作**：改测试为 `importlib.util.find_spec('akshare')` 检测 + skipif 装饰器，而非硬断言未安装

**验收**：1 测试通过

### 3.8 A 轨总验收

```
pytest tests/unit/ -q
# 期望：44 failed → 0 failed，13173+ passed
# CI 门禁：pre-commit + pyflakes + ruff T201 全绿
```

---

## 四、B 轨：外部高价值项目集成（W34-W40，8/18-10/2）

### 4.0 已完成项（2026-08-21，第三方项目集成 + ECC 评估）✅

> **本小节为 2026-08-21 方案同步时回填** — 以下集成在方案初版（8/21 上午）之后当日完成，原方案未纳入，现补录以消除"方案待执行 vs 实际已完成"脱节。
> 详见 `cairn/third-party-integration-20260821.md` + `docs/第三方项目集成总体计划_20260821.md` + `docs/ECC赋能量化系统工作计划_20260821.md`

#### 4.0.1 第三方项目集成（S3→S2→A2→A1→S1，98 单测全绿）✅

| 编号 | 项目 | 集成产出 | 单测 | 状态 |
|---|---|---|---|---|
| S3 | ai-berkshire 决策纪律层 | `utils/value_discipline_layer.py` + 子包，集成 `glm5_decision_engine.py` | 26 | ✅ |
| S2 | TimesFM 2.5 时序预测头 | `utils/timesfm_predictor.py`，集成 `lgb_tscv_trainer.py --predictor` | 14 | ✅ |
| A2 | SkillSpector 安全门禁 | `scripts/skill_security_scan.py`，集成 `pre_commit_check.py` | 18 | ✅ |
| A1 | codebase-memory-mcp | tree-sitter 知识图谱 v0.10.8 + Codex CLI 配置 | — | ✅（3 agent 配置失败不影响二进制） |
| S1 | TradingAgents 新闻模块 | `utils/news_intelligence.py` + 信号源适配器，集成 `signal_fusion.py` | 40 | ✅ |
| **合计** | | **4 项代码集成 + 2 项环境启用** | **98** | **✅ 全绿** |

**可复用经验**（沉淀于 `cairn/third-party-integration-20260821.md`）：
- 信号源适配器模式（复用 `sentiment_signal_source` 模板）
- fail-safe 降级链（显式异常元组，非裸 `except Exception`）
- TimesFM 2.0.2 API 变更适配（`TimesFM_2p5_200M_torch` 工厂函数）
- ruff.toml per-file-ignores 豁免模式
- 不可变性（`@dataclass(frozen=True)`）+ feature-flag 幂等注册

#### 4.0.2 ECC 赋能评估结论（修正初始判断）✅

**初始判断（错误）**：ECC 可补齐 ML 工程化/测试纪律/前视偏差检测短板。
**调研真相**：v8.6.14 已全面覆盖且更专业（482 自有测试 / 6-job CI / 229 处前视偏差检测 / cairn 几十专题），ECC 增量价值有限。
**最小化接入（3 项）**：
1. ✅ 借鉴 Iteration Compact 模板格式 → `cairn/strategy-iteration-compact-template.md`
2. ✅ ECC python rules 作为 CI 对照基线（非替代）→ `docs/ecc-python-rules-crosscheck_20260821.md`
3. ✅ deep-research 接入可行性评估 → `docs/deep-research接入可行性_20260821.md`
**不接入**：ECC agents/rules 不接入 CI（避免回退）；不替代前视偏差/反回归框架。

#### 4.0.3 其他 8/21 当日完成项

- **十五五专项规划正文**：10 个规划获详尽正文（`cairn/Reference/十五五专项规划正文汇编_20260821.md`），权重复核建议：绿色低碳 15→18-20% / 健康中国 10→11-12% / 安全发展 10→11-12%
- **duckduckgo-mcp 安装**：deep-research 免费替代（`npm install -g duckduckgo-mcp`，无 API key），`.mcp.json` 已配置，重启后生效
- **EOD 三重保障建立**：`cairn/eod-fallback-guarantee-20260820.md`（PYTHONPATH 隔离修复，08-21 验证 v84_PostMarket exit 0）

### 4.1 ds4 接入 GLM-5 决策引擎（P0，最高价值，W34-W36）

**契合点**：系统已用 `utils/glm5_decision_engine.py` + `utils/glm5_client.py`（多模型路由：豆包→GLM-5→DeepSeek→Ollama）。ds4 原生支持 **GLM 5.2**（系统当前模型）+ DeepSeek V4 Flash，可替换 Ollama 本地后端，加速盘前决策。

> **[2026-08-21 集成点修正]** `glm5_client.py` 已重构为 LiteLLMRouter 薄包装（W7.4.3），ds4 真实集成点是 `utils/alpha/llm/router.py` LLMRouter provider 链，**非** `glm5_client.py`。下方"原方案"保留作历史记录，"实际执行"为 W34 真实改动。

**原方案（已过时，集成点错误）**：
1. **W34**：在 `utils/glm5_client.py` 新增 `DS4Provider` 类（与现有 `OllamaProvider` 同接口）
   - 通过 subprocess 调用 ds4 HTTP server（ds4 内置 HTTP server，见热榜文档）
   - 配置：`config/settings.yaml` 新增 `ds4: {host: 127.0.0.1, port: 8080, model: glm-antirez-q4}`
   - feature flag：`GLM5_DS4_ENABLED=0` 默认关闭，shadow 验证后开启
2. **W35**：在 `utils/glm5_decision_engine.py` 路由表插入 ds4 优先级（豆包→**ds4**→GLM-5 云端→DeepSeek→Ollama）
   - shadow 模式：ds4 与 Ollama 并行推理，对比响应一致性，不影响实盘
3. **W36**：定版 + 接入 `ai_decision/providers.py` 统一 provider 注册

**实际执行（W34 已完成，见 §十一）** ✅：
1. **W34** ✅：新建 `utils/alpha/llm/providers/ds4.py`（~80 行，复用 `openai_compatible_chat`）
   - `utils/alpha/llm/providers/__init__.py` 导出 `call_ds4`
   - `utils/alpha/llm/router.py`：import + 默认 chain 2 处 + `_register_default_providers` + `_call_ds4` 薄代理方法
   - fallback 链：`omniroute → deepseek → doubao → glm → siliconflow → **ds4** → ollama`
   - feature flag：`GLM5_DS4_ENABLED=0` 默认关闭
   - 测试：`tests/unit/test_llm_router.py` 断言更新（6→7 provider，chain 加 ds4）→ **91 passed**
2. **W35**（待执行）：shadow 运行 7 天（需合格硬件 Mac ≥96GB 或 CUDA GPU，见 §4.1.1 硬件评估）
3. **W36**（待执行）：定版 + 接入 `ai_decision/providers.py` 统一 provider 注册

**验收**：
- ds4 本地启动 `./ds4 --model glm-antirez-q4 --port 8080` 成功
- `GLM5_DS4_ENABLED=1` 时盘前决策延迟 < Ollama 模式 50%
- shadow 运行 7 天，ds4 与 Ollama 决策一致率 ≥ 95%

**回滚**：`GLM5_DS4_ENABLED=0` 即切回 Ollama

**风险**：ds4 是 Beta（热榜文档明确），需 Mac ≥96GB 或 CUDA GPU；若本地无合格硬件，降级为远程 ds4 服务或保持 Ollama

#### 4.1.1 ds4 shadow 硬件可行性评估（2026-08-21）

> **[2026-08-21 评估完成]** 本机硬件实测：win32 + NVIDIA RTX 3060 Laptop GPU **6GB 显存**（空闲 5877 MiB，Ampere compute 8.6，驱动 596.21）。
>
> **❌ 本机 POC 不可行 — 双重阻断**：
> - **阻断 1（编译）**：ds4 Makefile 仅支持 macOS Metal / Linux CUDA / ROCm / CPU，**无 Windows 原生编译**（需 WSL2，Beta 未验证）
> - **阻断 2（显存）**：ds4 2-bit 量化仅压缩 routed MoE experts，dense parts（attention/shared experts/projections）保持 Q8/F32 原精度，GLM 5.2 dense parts 估计 10-20GB+ ≫ 6GB；IQ2_XXS 已是最激进量化；SSD streaming 也无法救（dense parts 必须驻留）

| 项 | 实测 | ds4 要求 | 判定 |
|---|---|---|---|
| CUDA GPU | ✅ RTX 3060 Laptop | 需要 | 满足 |
| 显存 | 6GB（空闲 5877 MiB） | GLM 5.2 dense parts 10-20GB+ | ❌ 远不够 |
| OS | win32 | ds4 无 Windows 编译支持 | ❌ 需 WSL2 |
| 量化 | — | IQ2_XXS 已是最激进 | ❌ 无更小选项 |

**降级方案评估（已全部评估）**：
1. ~~首选：本机 CUDA shadow~~ → ❌ 6GB 显存不够 + 无 Windows 编译
2. ~~更小量化 / CPU 模式~~ → ❌ 已是 IQ2_XXS 最激进；CPU 需 WSL2 + 速度极慢无实用价值
3. ~~远程 ds4 服务~~ → ⚪ 当前无远程 GPU（≥24GB）或 Mac（≥96GB）资源
4. **✅ 保持 Ollama fallback**：ds4 代码集成保留（91 passed），`GLM5_DS4_ENABLED=0` 永久保持，fallback 链不变

**结论**：ds4 W34 代码完成 ✅，**W35 硬件 POC 已评估 — 本机不可行**。代码保留 feature flag 永久关闭，待远程硬件（≥24GB GPU 或 ≥96GB Mac）触发重新评估。**不阻塞 B 轨其他项和 C 轨**。详见 `cairn/ds4-integration.md` §九。

### 4.2 code-graph-rag 接入 cairn 知识层（P1，W37）

**契合点**：v8.6.14 AGENTS.md 已用 Project Cairn 组织知识，code-graph-rag 的 monorepo RAG + 知识图谱可增强 `cairn/` 跨模块查询。

**动作**：
1. 评估 code-graph-rag 与已集成的 code-review-graph MCP 的功能重叠
2. 若互补：在 `cairn/code-graph-rag-guide.md` 沉淀使用指南，接入 `ai_decision/rag_context.py`
3. 若重叠：跳过，记录决策到 `cairn/LOG.md`

**验收**：决策记录入 cairn，若接入则 RAG 查询延迟 < 2s

### 4.3 anthropics/skills + addyosmani/skills 统一注入（P1，W37）

**契合点**：AGENTS.md §4.4 已列 FinClaw 1031 个 skills，anthropics/skills 是官方仓库，可与 FinClaw 互补。

**动作**：
1. 从 anthropics/skills 选 `code-review` / `testing` / `refactoring` 类技能
2. 从 addyosmani/agent-skills 选 `error-handling` / `python-patterns` 类技能
3. 适配现有 `.ruff` / `mypy` / `pre-commit` 门禁，写入 `skills/` 并更新 `skills-lock.json`
4. 与旧排期 Phase 2b 合并执行

**验收**：CI 门禁通过率提升，新增技能不与 FinClaw 冲突

### 4.4 unsloth 本地训练 UI 评估（P2，W38）

**契合点**：`utils/ml_enhanced_trainer.py` 已做四维优化 + 贝叶斯超参，unsloth 可增强本地 LLM 微调（用于 `ai_decision/debate_engine.py` 的辩论模型微调）。

**动作**：
1. POC：用 unsloth 微调 Qwen3.8 on 历史交易决策日志
2. 评估微调后辩论模型与基础模型在回测中的 Sharpe 差异
3. 决策是否定版，记录入 `cairn/unsloth-eval.md`

**验收**：POC 报告 + 决策

### 4.5 diagram-design 接入 UI 报告（P2，W38）

**契合点**：`ui/pages/` 19 页报告可视化可用 diagram-design 的 27 种图表类型增强（纯 HTML+SVG 自包含，无 Mermaid 依赖）。

**动作**：在 `ui/components/report_viewer.py` 集成 diagram-design 模板，替换部分 matplotlib 图表

**验收**：UI 报告渲染性能不劣化，图表类型 ≥ 27

### 4.6 semantica / prime-agent / manim 评估（P3，W39+）

- **semantica**：评估图原生 AI 是否增强 `ai_decision/rag_context.py` 的知识图谱
- **prime-agent**：评估自治编码是否用于测试自动生成（与实盘交易无关，低优先）
- **manim**：评估策略回测动画可视化（锦上添花，低优先）

**产出**：各 POC 报告 + 决策入 `cairn/`

### 4.7 旧排期延续（W34-W38+）

| 旧 Phase | 周次 | 与本方案关系 |
|---|---|---|
| 1. loopx → live_scheduler | W34-W35 | 并行执行，不冲突 |
| 2a. pdf-inspector → OCR | W36 | 并行执行 |
| 2b. skills 补强 | W37 | **合并到 4.3** |
| 3a. cloudflare/computer | W38+ | 并行评估 |
| 3b. TencentMemory | W38+ | 并行评估 |

---

## 五、C 轨：ai_decision 全链路集成（W35-W37，8/25-9/12）

### 5.1 集成目标

把 17 个孤立模块串成 EOD 管道内的决策链，不改变 ai_decision 内部实现，只在管道挂接点加 hook。

### 5.2 集成数据流

```
institutional_pipeline_runner.py
  ├─ phase: data      → DataConnectorManager（不变）
  ├─ phase: factors   → AlphaFactor（不变）
  ├─ phase: signals   → SignalFusionEngine（不变）
  │    └─ [NEW hook] ai_decision/rag_context.py 注入 cairn 知识上下文
  ├─ phase: review    → [NEW] ai_decision/eod_review.py EOD 复盘
  │    └─ ai_decision/debate_engine.py 多模型辩论
  │    └─ ai_decision/consensus_aggregator.py 共识聚合
  │    └─ ai_decision/decision_gate.py 决策门控（风控/合规/成本）
  ├─ phase: execute   → DailyTradeExecutor（不变）
  │    └─ [NEW hook] ai_decision/execution_bridge.py 桥接执行
  └─ phase: report    → Markdown 报告
       └─ [NEW hook] ai_decision/dashboard.py 仪表盘嵌入
```

### 5.3 集成动作（W35-W37）

**W35：管道 hook 注入**
1. 在 `institutional_pipeline_runner.py` 的 `phase_signals` / `phase_review` / `phase_execute` / `phase_report` 方法中加 feature flag hook
2. flag：`AI_DECISION_INTEGRATED=0` 默认关闭，shadow 验证后开启
3. 每个 hook 用 try/except 包裹，失败不阻塞主管道（优雅降级）

**W36：CLI 入口统一**
1. 在 `量化策略系统_统一入口_v8.6.py` 新增 `--ai-decision-full` 模式
2. 调用 `ai_decision/cli.py` 的 orchestrator，传入管道上下文
3. `ai_decision/providers.py` 与 `utils/glm5_client.py` 统一 provider 注册（含 ds4）

**W37：UI 与健康检查接入**
1. `ui/pages/13_🤖_AI决策与ML信号.py` 嵌入 `ai_decision/dashboard.py` 仪表盘
2. `ui/pages/01_🏠_系统概览.py` 嵌入 `ai_decision/health.py` 健康检查卡片
3. `ai_decision/backtest_replay.py` 接入 `ui/pages/` 回测回放页

### 5.4 C 轨验收

- `AI_DECISION_INTEGRATED=1` 时 EOD 管道全流程通过
- shadow 运行 7 天，集成前后交易指令一致率 ≥ 99%（允许 1% 决策差异）
- `--ai-decision-full` CLI 模式可独立运行
- UI 仪表盘 + 健康检查卡片渲染正常
- 单文件 <800 行，pre-commit 门禁全绿

### 5.5 C 轨回滚

`AI_DECISION_INTEGRATED=0` 即切回原管道，零影响

### 5.6 与 ROADMAP Wave 7 协调（2026-08-21 补充）

> **[2026-08-21 补充]** 原方案 §五 未说明 C 轨与 ROADMAP Wave 7（4 Sprint 至 12-31 v8.7）的关系，可能导致任务重叠或遗漏。本小节明确边界。

**ROADMAP Wave 7 概况**（见 `cairn/ROADMAP.md`）：
- 时间：2026-08-13 ~ 12-31，4 Sprint，目标 12-31 v8.7 发布
- 范围：Wave 1-5 剩余任务 + 工业级差距 + 工程基础层 Phase 0-3 + AutoResearch + ocr/ECC

**C 轨与 Wave 7 任务边界**：

| C 轨任务 | 周次 | Wave 7 Sprint | 关系 | 协调策略 |
|---|---|---|---|---|
| 管道 hook 注入 | W35 (8/25) | Sprint 1 (08-13~09-12) | 重叠 | C 轨改 `institutional_pipeline_runner.py` hook，Wave 7 Sprint 1 改 daily_workflow 拆分 — **改不同文件，并行安全** |
| CLI 统一 | W36 (9/1) | Sprint 1 后半 | 重叠 | C 轨加 `--ai-decision-full` 模式，Wave 7 不改 CLI 入口 — **无冲突** |
| UI/健康接入 | W37 (9/8) | Sprint 2 (09-13~10-11) | 衔接 | C 轨 W37 结束时 Sprint 2 刚启动 — **顺序衔接** |

**与其他 ROADMAP 任务的协调**：

| ROADMAP 任务 | 时间 | 与本方案关系 |
|---|---|---|
| ETF期权对冲 Phase 2 回测 | 08-21~09-05 | 与 B 轨 ds4 shadow 并行，改 `etf_option_hedge_rebalancer.py`，不冲突 |
| ETF期权对冲 Phase 3 shadow | 09-06~10-05 | 与 C 轨 UI 接入并行，独立子组合，不冲突 |
| ETF期权对冲 Phase 4 灰度 | 10-06~11-05 | **前置依赖 Wave 7 Sprint 3 实盘验证四件套** — 本方案不涉及，由 ROADMAP 推进 |
| MVSK P5 生产接入 | 09-05~11-12 | 挂 Wave 7 Sprint 1-3，改 `portfolio_builder.py` — 与 C 轨改 `institutional_pipeline_runner.py` 不同文件，并行安全 |
| Phase B 渐进启用 | 08-20~09-05 | B1 已启用（08-20），B2-B4 与 A/B/C 轨并行，feature flag 独立 |

**结论**：C 轨 W35-W37 全部落在 Wave 7 Sprint 1-2 内，改文件集与 Wave 7 任务**无重叠**（C 轨改 `institutional_pipeline_runner.py` + `量化策略系统_统一入口_v8.6.py` + `ui/pages/`，Wave 7 改 `daily_workflow.py` + `portfolio_builder.py` + 工程基础层）。三轨并行 + Wave 7 可四线并行推进，12-31 v8.7 发布时 C 轨已完成（W37=9/12 早于 v8.7）。

---

## 六、排期甘特视图

> **[2026-08-21 状态]** A 轨 W34 已 ✅ 完成（零代码改动，原 W35 排期取消）；B 轨 ds4 W34 代码完成待 W35 shadow；C 轨待 W35 启动。下方甘特图为初版排期，实际进度见 §十一。

```
三轨\周次        W34   W35   W36   W37   W38   W39   W40
                 8/18  8/25  9/1   9/8   9/15  9/22  9/29

A 技术债清偿      ✅DONE(零改动)
  iFinD清理       ✅
  limit_pool      ✅
  transformer     ✅
  tdx/qlib/其他   ✅

B 外部集成
  ds4 GLM-5       ████████████████
  loopx(旧)       ████████████
  pdf-inspector              ████
  code-graph-rag             ████
  anthropics/skills               ████
  unsloth评估                         ████
  diagram-design                      ████
  旧Phase3评估                            ░░░░░░░░░░
  semantica/prime/manim                      ░░░░░░░░░░

C ai_decision集成       ████████████████
  管道hook注入         ████
  CLI统一                   ████
  UI/健康接入                    ████

████ = 执行    ░░░░ = 评估
```

---

## 七、风险与回滚

| 风险 | 轨 | 影响 | 缓解 | 回滚 |
|---|---|---|---|---|
| ds4 Beta 不稳定 + 需大内存 GPU | B | 盘前决策失败 | shadow 运行 + feature flag | `GLM5_DS4_ENABLED=0` |
| loopx 配额拖慢实时调度 | B | 交易延迟 | shadow 隔离验证 | feature flag 切回原 scheduler |
| pdf-inspector Rust FFI 增构建复杂度 | B | 打包部署 | CLI 子进程起步，稳定后 FFI | 移除 FFI 依赖 |
| ai_decision hook 异常阻塞管道 | C | EOD 失败 | try/except 优雅降级 | `AI_DECISION_INTEGRATED=0` |
| 三轨并行改动冲突 | A+B+C | 回归风险 | 三轨改不同文件集，每轨独立验收 | 各 flag 独立回滚 |
| iFinD 测试删除误删生产代码 | A | 功能丢失 | 仅删测试，grep 确认生产代码已无 iFind | git revert |
| unsloth 微调过拟合 | B | 辩论模型退化 | POC + 回测 Sharpe 对比 | 不定版 |

---

## 八、验收门禁

每阶段必须通过：

1. **CI 门禁**：`pre-commit run --all-files` 全绿（T201 / hardcoded-path / pyflakes / ruff）
2. **测试门禁**：`pytest tests/unit/ -q` 失败数单调下降，A 轨完成时 44 → 0
3. **不可变性**：新代码用 `.assign()` / `dataclasses.replace()`，无原地修改
4. **文件规模**：单文件 <800 行，UI 页面 <200 行，组件 <80 行
5. **数据源链**：不引入新数据源，遵守 Wind→TDX→AKShare→新浪→缓存→兜底
6. **CLI 兼容**：现有 35+ CLI 模式行为不变，新模式以新 flag 添加
7. **shadow 验证**：B/C 轨每个外部集成前 shadow 运行 ≥ 7 天，不影响实盘
8. **cairn 沉淀**：每阶段完成后在 `cairn/LOG.md` 顶部追加条目 + 指针

---

## 九、配套沉淀

> **[2026-08-21 状态核对]** 逐项核实文档实际存在情况，标注 ✅已建 / ❌待建。

| 产出 | 路径 | 维护 | 实际状态（8/21） |
|---|---|---|---|
| 本方案 | `v86集成升级最优方案_20260821.md` | 一次性，执行时按周更新进度 | ✅ 已建（本文件，8/21 已优化同步） |
| 周进度 | `cairn/LOG.md` 顶部追加 | 每周 | ✅ 已建（8/21 W34 条目已追加） |
| ds4 集成指南 | `cairn/ds4-integration.md` | W34-W36 沉淀 | ✅ 已建（W34 POC 指南 + shadow 步骤） |
| loopx 集成方案 | `cairn/loopx-integration.md` | W34-W35 沉淀 | ✅ 已建（8/21 调研完成） |
| **ai_decision 集成指南** | `cairn/ai-decision-integration.md` | W35-W37 沉淀 | ✅ 已建（8/21，17 模块接口扫描 + 5 挂接点设计 + 行数预警 + ds4 阻塞） |
| **技术债清偿记录** | `cairn/tech-debt-cleanup.md` | A 轨完成时 | ✅ 已建（8/21，44 测试修复确认 + 经验沉淀） |
| 第三方集成经验 | `cairn/third-party-integration-20260821.md` | 8/21 沉淀 | ✅ 已建（§4.0.1 引用） |
| 失败测试归档 | `failed_tests_已清偿_20260821.txt` | A 轨完成时归档原 44 项 | ✅ 已归档为 `failed_tests_过时_20260812.txt` |
| 旧排期更新 | `github_trending_高价值统计与升级计划_20260807.md` 标注进度 | 持续 | ✅ 已标注（8/21，§二 加进度回写表） |
| **方案同步记录** | `cairn/v86-plan-sync-20260821.md` | 8/21 方案优化沉淀 | ✅ 已建（8/21，7 处脱节修正 + 同步机制） |

**待建文档创建时机**：
- ✅ 全部配套沉淀文档已建（10/10），无待建项

---

## 十、执行顺序建议

1. **✅ W34 已完成（8/21）**：A 轨技术债清偿（零代码改动，44 测试已修复）+ B 轨 ds4 代码集成（91 passed）+ 旧 Phase 1 loopx 调研 + 第三方集成 S1-S3/A1-A2（98 单测）+ ECC 评估
2. **W35（8/25 起下一步）**：~~B 轨 ds4 硬件 POC~~ ✅ 已评估（❌ 本机不可行，待远程硬件）+ C 轨管道 hook 注入 + 旧 Phase 1 loopx POC
3. **W36**：C 轨 CLI 统一 + B 轨 ds4 定版 + 旧 Phase 2a pdf-inspector
4. **W37**：C 轨 UI 接入 + B 轨 skills 补强（合并旧 2b）+ code-graph-rag
5. **W38+**：B 轨评估性接入（unsloth / diagram-design / cloudflare / TencentMemory / semantica / prime / manim）

**关键路径**（W34 后更新）：~~A 轨 iFinD 清理~~ ✅ → ~~B 轨 ds4 硬件 POC~~ ✅（❌ 不可行，待远程硬件）→ C 轨管道 hook → C 轨 CLI 统一

---

*设计时间：2026-08-21*
*知识沉淀基线：见文档头部（2026-08-21 全量基线，含 8/21 当日 5 项新增沉淀）*

---

## 十一、W34 执行进度（2026-08-21 实际执行）

### A 轨：技术债清偿 — ✅ 完成（零代码改动）

**关键发现**: `failed_tests.txt` 是 8/12 过时数据。重跑 pytest 确认 44 失败**全部已修复**：
- iFind 22 测试：已用 `@pytest.mark.skip` / `pytest.skip()` 修复（源码已移除 iFinD 数据源）
- limit_pool 11 + transformer 2 + tdx 1 + qlib 3 + 其他 5 测试：均已修复
- 验证：246 passed/19 skipped + 245 passed/3 skipped
- 归档：`failed_tests.txt` → `failed_tests_过时_20260812.txt`

### B 轨：ds4 集成 — ✅ 代码完成（❌ 本机 POC 不可行，待远程硬件）

**集成点修正**: `glm5_client.py` 已重构为 LiteLLMRouter 薄包装（W7.4.3），ds4 真实集成点是 `utils/alpha/llm/router.py` LLMRouter provider 链。

**代码改动**:
- 新建 `utils/alpha/llm/providers/ds4.py`（~80 行，复用 `openai_compatible_chat`）
- `utils/alpha/llm/providers/__init__.py` 导出 `call_ds4`
- `utils/alpha/llm/router.py`：import + 默认 chain 2 处 + `_register_default_providers` + `_call_ds4` 薄代理方法
- `tests/unit/test_llm_router.py`：断言更新（6→7 provider，chain 加 ds4）
- fallback 链：`omniroute → deepseek → doubao → glm → siliconflow → ds4 → ollama`
- feature flag：`GLM5_DS4_ENABLED=0` 默认关闭

**验证**: `pytest test_llm_router.py test_d6_litellm_router.py` → **91 passed**

### 旧 Phase 1：loopx 调研 — ✅ 调研完成（待 W34-W35 POC）

**loopx 仓库调研**: Python 3.11+ 零依赖，`pip install loopx`，核心 tick `quota should-run / todo claim / todo update / refresh-state / quota spend-slot`，集成点 `live_scheduler.py` 配额感知唤醒。

### W34 沉淀

- `cairn/ds4-integration.md` — ds4 POC 指南 + shadow 验证步骤
- `cairn/loopx-integration.md` — loopx 集成方案 + 验收标准
- `cairn/LOG.md` — W34 进度条目

### W34 → W35 衔接

- B 轨 ds4：❌ 本机不可行（6GB 显存 + 无 Windows 编译），代码保留 feature flag 永久关闭，待远程硬件（≥24GB GPU 或 ≥96GB Mac）触发
- 旧 Phase 1 loopx：`pip install loopx` + `loopx connect` POC
- C 轨 ai_decision 集成：W35 启动管道 hook 注入

---

*W34 完成时间：2026-08-21*
*下一步：W35 shadow 验证 + loopx POC + C 轨启动*

---

## 十二、方案↔执行同步机制（2026-08-21 建立）

> **[2026-08-21 建立]** 本方案 8/21 上午初版与当日实际执行严重脱节（第三方集成已完成但方案未纳入、ds4 集成点错误、A 轨状态分裂等 7 处）。为防止未来再脱节，建立以下同步机制。

### 12.1 同步触发点

| 触发事件 | 同步动作 | 责任 |
|---|---|---|
| 每周 W 结束（周五 EOD 后） | 对照 `cairn/LOG.md` 本周条目，更新方案对应周次状态（§十一 模式） | 方案维护者 |
| 重大集成完成（第三方项目/外部模块/新 CLI 模式） | 立即在方案对应小节回填"已完成项"（如 §4.0），标注 ✅ + 单测数 + 沉淀指针 | 集成执行者 |
| ROADMAP 里程碑变更（Wave 推进/新 Phase/排期调整） | 更新方案 §5.6 协调表，确认时间线无冲突 | 方案维护者 |
| 每月 1 日 | 扩展头部"知识沉淀来源"，纳入上月新增 cairn 专题 + docs | 方案维护者 |
| 方案与实际偏差 ≥1 处 | 触发"方案同步"任务（如本次 8/21），按阶段 0→1→2→3 修正 | 任意发现者 |

### 12.2 同步检查清单（每次同步时逐项核对）

- [ ] 头部"知识沉淀来源"是否涵盖最新 cairn/LOG.md 所有条目
- [ ] A/B/C 三轨每个"待执行"小节是否仍待执行（未悄然完成）
- [ ] 每个"已完成"小节是否有对应的 cairn 沉淀指针
- [ ] §九 配套沉淀表格"实际状态"列是否最新
- [ ] §5.6 协调表是否与 ROADMAP 当前 Sprint 一致
- [ ] 排期甘特图（§六）是否与实际周次对齐
- [ ] 验收门禁（§八）是否有新增项需补录

### 12.3 防脱节原则

1. **方案是活文档**：执行进展实时回写，不是"写完即冻"
2. **沉淀先行**：每完成一项，先入 cairn，再回填方案（避免方案成为唯一事实源）
3. **状态显式**：每个小节开头标注 ✅已完成 / ⏳进行中 / ❌待执行 / ⚠️阻塞，不用"待执行"含糊带过
4. **指针必带**：每个"已完成"小节末尾带 `cairn/<topic>.md` 指针，可追溯
5. **偏差即修**：发现方案与实际偏差 ≥1 处，立即触发同步，不积累

---

*同步机制建立时间：2026-08-21*
*本次同步修正：7 处脱节（3 严重 + 4 中等），见 `cairn/v86-plan-sync-20260821.md`*
