# 量化交易系统 v8.6.14 代码质量修复计划（2026-08-17 新代码审查）

> 来源：ocr 委托模式审查 + ruff 0.15.16 + bandit 1.9.4 + mypy + 逐行精读
> 审查范围：54 个未提交文件（+15973 行），聚焦 6 个核心生产模块 + research 脚本
> 对照基线：`代码质量与缺陷审查报告_20260816.md`（其中 NB-1~NB-4 已确认全部修复）
> 修复原则：**先还债再加功能、每修复必验证、灰度优先**（沿用 v9.2 工程铁律）

## 优先级总览

| 级别 | 项数 | 内容 | 目标完成 |
|------|------|------|---------|
| P0 阻断性 | 1 | NEW-1：`predict` 每次重新加载模型，推理不可用 + GPU OOM | 08-17 |
| P1 高 | 4 | NEW-2/3/7/8：长度校验、标签均衡、资源泄漏、JS 注入 | 08-18 |
| P2 中 | 5 | NEW-4/5/6/9 + MVSK：数值稳定、解析梯度、封装、批量推理 | 08-20 |
| P3 低 | 7 | NEW-10~17：参数冗余、错别字、幂等、CDN、bandit nosec | 08-22 |
| 债务 | — | ruff 6671 条自动修 + mypy 收紧 + except 元组规约 | 08-25 起 |

---

## P0 — 阻断性（今天必须完成）

### P0-1. `finetune_sentiment_model.py`：`predict` 每次重新加载模型（NEW-1）

**问题**：`predict()` (L218-229) 每次调用都执行 `AutoModelForCausalLM.from_pretrained` + `PeftModel.from_pretrained`。4bit 量化模型加载耗时 30-60 秒。`finetune_pipeline.py:151-161` 的 `evaluate()` 在循环中逐样本调用 `predict()`——200 个验证样本 = 加载模型 200 次 = 数小时，且每次加载都在 GPU 上分配新内存不释放，必然 OOM。

**根因**：研究/POC 代码直接上生产，缺乏"模型加载次数 ≤ 1"的工程约束。`predict` 被设计成无状态函数，但 4bit 量化模型的加载成本极高，函数式 API 与重资源对象不匹配。

**修复**（重构为 Predictor 类，加载一次复用）：

```python
# finetune_sentiment_model.py 新增类

class SentimentPredictor:
    """加载一次模型, 复用做批量推理 (替代无状态 predict 函数)."""

    def __init__(self, model_dir: str, config: FinetuneConfig | None = None) -> None:
        self.config = config or FinetuneConfig()
        from peft import PeftModel
        bnb_config = None
        if self.config.use_4bit:
            bnb_config = BitsAndBytesConfig(
                load_in_4bit=True,
                bnb_4bit_quant_type="nf4",
                bnb_4bit_compute_dtype=torch.float16,
            )
        self.base_model = AutoModelForCausalLM.from_pretrained(
            self.config.base_model,
            quantization_config=bnb_config,
            device_map="auto",
            torch_dtype=torch.float16,
        )
        self.tokenizer = AutoTokenizer.from_pretrained(self.config.base_model)
        if self.tokenizer.pad_token is None:
            self.tokenizer.pad_token = self.tokenizer.eos_token
        self.model = PeftModel.from_pretrained(self.base_model, model_dir)
        self.model.eval()

    def predict(self, text: str) -> str:
        prompt = _format_instruction(text)
        inputs = self.tokenizer(prompt, return_tensors="pt").to(self.model.device)
        with torch.no_grad():
            outputs = self.model.generate(
                **inputs,
                max_new_tokens=self.config.max_new_tokens,
                do_sample=False,  # 贪婪解码, 移除冗余 temperature
            )
        response = self.tokenizer.decode(
            outputs[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True
        ).strip()
        for key, label in LABELS.items():
            if label in response or key in response:
                return label
        return "中性"

    def predict_batch(self, texts: list[str]) -> list[str]:
        """批量推理 (减少 GPU kernel launch 开销)."""
        return [self.predict(t) for t in texts]  # 后续可改为真 batch

    def close(self) -> None:
        """释放 GPU 内存."""
        del self.model, self.base_model
        if torch.cuda.is_available():
            torch.cuda.empty_cache()


# 保留旧 predict 函数向后兼容, 但内部走 Predictor (带 lru_cache 避免重复加载)
import functools

@functools.lru_cache(maxsize=1)
def _get_predictor(model_dir: str) -> "SentimentPredictor":
    return SentimentPredictor(model_dir)

def predict(text: str, model_dir: str, config: FinetuneConfig | None = None) -> str:
    return _get_predictor(model_dir).predict(text)
```

**验证**：
```bash
.venv\Scripts\python.exe -c "
from lgb_trainer.llm_finetune.finetune_sentiment_model import SentimentPredictor
p = SentimentPredictor('models/llm_finetune/sentiment_lora')
print(p.predict('贵州茅台净利润同比增长30%'))
print(p.predict('美联储加息75个基点'))
# 验证模型只加载一次 (看日志只有一次 'Loading model')
"
```

---

## P1 — 高（本周完成）

### P1-1. `finetune_pipeline.py`：`evaluate` 不校验长度一致（NEW-2）

**问题**：L151 `for text, true_label in zip(eval_texts, eval_labels)` 静默截断；L163 `accuracy = correct / len(eval_labels)` 用 `eval_labels` 长度做分母。若 `eval_texts` 更短，`correct` 被低估但分母不变 → 准确率失真。

**根因**：`zip` 的静默截断是 Python 的经典陷阱。`_load_synthetic` 和 `_load_from_reflections` 都用 `split = int(len(texts) * 0.8)` 切分，texts 和 labels 长度一致，但 `evaluate` 作为公共 API 不应假设调用方一定对齐。

**修复**（L148 前加校验）：
```python
def evaluate(model_dir, eval_texts, eval_labels, config=None):
    if len(eval_texts) != len(eval_labels):
        raise ValueError(
            f"eval_texts 长度 {len(eval_texts)} != eval_labels 长度 {len(eval_labels)}"
        )
    if not eval_labels:
        return 0.0, []
    # ... 原逻辑
```

**验证**：
```bash
.venv\Scripts\python.exe -m pytest tests/unit/test_finetune_pipeline_unit.py -v -k evaluate
# 新增测试: 传入不等长应抛 ValueError
```

---

### P1-2. `finetune_pipeline.py`：`_load_synthetic` 标签不均衡（NEW-3）

**问题**：L88-97 `random.choice(list(templates.keys()))` 每次独立随机选标签，200 个样本的标签分布是多项分布，可能严重不均衡（如正面 80 / 中性 75 / 负面 45）。模型偏向多数类。

**根因**：合成数据生成时只考虑"随机性"未考虑"均衡性"。分类任务训练数据必须类别均衡，否则模型学到的先验偏差会带到推理。

**修复**（改为分层均衡采样）：
```python
def _load_synthetic(max_samples: int):
    templates = { ... }  # 原样
    companies = [ ... ]
    per_class = max_samples // len(templates)  # 每类样本数
    texts, labels = [], []
    for label, tmpl_list in templates.items():
        for _ in range(per_class):
            template = random.choice(tmpl_list)
            text = template.format(
                company=random.choice(companies),
                pct=random.randint(1, 50),
                amount=random.randint(1, 100),
            )
            texts.append(text)
            labels.append(label)
    # 打乱顺序避免连续同类
    combined = list(zip(texts, labels))
    random.shuffle(combined)
    texts, labels = zip(*combined)
    split = int(len(texts) * 0.8)
    return list(texts[:split]), list(labels[:split]), list(texts[split:]), list(labels[split:])
```

---

### P1-3. `utils/ai_coordinator.py`：`record_decision_with_impact` 资源泄漏（NEW-7）

**问题**：L387 `rag = CodeGraphRAG(); impact = rag.impact_analysis(...); rag.close()`——若 `impact_analysis` 抛异常，`rag.close()` 不执行，文件句柄/DB 连接泄漏。

**根因**：RAII 意识缺失。Python 的 `with` 语句或 try/finally 是资源管理的标准模式，但研究代码常写成"直线流"（创建→使用→关闭），异常路径被忽略。这与 08-16 报告中 NB-3/NB-4（异常路径自身有 bug）同模式：**错误处理路径从未被执行过所以长期潜伏**。

**修复**（用 try/finally 保证 close）：
```python
if change_path:
    rag = None
    try:
        from .ai_tools.code_graph_rag import CodeGraphRAG
        rag = CodeGraphRAG()
        impact = rag.impact_analysis(change_path)
        impact_info = {
            "impact_radius": impact.impact_radius,
            "impacted_files_count": len(impact.impacted_files),
            "impacted_files": impact.impacted_files[:20],
        }
        enriched_reasoning = (
            f"{reasoning}\n[影响半径] radius={impact.impact_radius}, "
            f"impacted_files={len(impact.impacted_files)}"
        )
    except (ImportError, ValueError, TypeError, KeyError,
            AttributeError, RuntimeError, OSError) as e:
        logger.debug(f"影响半径计算失败 change_path={change_path}: {e}")
    finally:
        if rag is not None:
            try:
                rag.close()
            except Exception:  # noqa: BLE001
                pass
```

**更优方案**：若 `CodeGraphRAG` 支持 `__enter__/__exit__`，直接用 `with CodeGraphRAG() as rag:`。

---

### P1-4. `reporting/html_chart_generator.py`：JS 字符串注入（NEW-8）

**问题**：L212 `links_json` 把 `html.escape(link.label)` 嵌入 JS 字符串 `"formatter": "{label}"`。`html.escape` 转义 `&<>"'` 但**不转义反斜杠 `\`**。若 label 含 `\`（如 Windows 路径 `C:\Users\`），会破坏 JS 字符串语法，轻则图表渲染失败，重则 XSS。

**根因**：手拼 JS 字符串是 XSS 的经典入口。`html.escape` 是 HTML 上下文的转义，**不是 JS 字符串上下文的转义**。JS 字符串需要转义 `\`、`\n`、`\r`、`</` 等。正确做法是用 `json.dumps` 序列化整个 JS 对象，让 JSON 序列化器处理转义。

**修复**（用 `json.dumps` 替代手拼）：
```python
import json

def generate_architecture(self, components, links, title="架构图"):
    categories = list({c.category for c in components})
    cat_data = [{"name": c} for c in categories]
    nodes_data = [
        {"name": c.name, "category": categories.index(c.category), "x": c.x, "y": c.y}
        for c in components
    ]
    links_data = [
        {"source": link.source, "target": link.target,
         "label": {"show": True, "formatter": link.label}}
        for link in links
    ]
    # 用 json.dumps 序列化, 自动处理 \ " 等转义
    cat_json = json.dumps(cat_data)
    nodes_json = json.dumps(nodes_data)
    links_json = json.dumps(links_data)
    title_json = json.dumps(title)

    script = f"""
    <div id="arch-chart" style="width: 100%; height: 500px;"></div>
    <script>
      if (typeof echarts !== 'undefined') {{
        var chart = echarts.init(document.getElementById('arch-chart'));
        chart.setOption({{
          title: {{ text: {title_json} }},
          tooltip: {{}},
          legend: [{{ data: {cat_json} }}],
          series: [{{
            type: 'graph',
            layout: 'none',
            data: {nodes_json},
            links: {links_json},
            categories: {cat_json},
            roam: true,
            label: {{ show: true }},
            edgeLabel: {{ show: true, fontSize: 10 }}
          }}]
        }});
      }}
    </script>"""
    return f'<div class="chart-container"><h2>{html.escape(title)}</h2>{script}</div>'
```

**验证**：
```bash
.venv\Scripts\python.exe -c "
from reporting.html_chart_generator import HTMLChartGenerator, ChartComponent, ChartLink
gen = HTMLChartGenerator()
# label 含反斜杠和引号
html_str = gen.generate_architecture_standalone(
    [ChartComponent('a', 'test', 'cat', 0, 0)],
    [ChartLink('a', 'a', label=r'C:\Users\test\"inject')],
)
# 验证 HTML 可正常解析 (无 JS 语法错误)
import re
assert 'inject' in html_str  # 内容保留
print('OK')
"
```

---

## P2 — 中（下周完成）

### P2-1. `utils/risk_budget_optimizer.py`：MVSK 数值不稳定（NEW-4）

**问题**：L240 `if m2 > 0: std = math.sqrt(m2); skew = ... / std**3`。当 `m2` 极小（如 1e-15）时，`std**3` ≈ 1e-22，三次矩除以它放大数值误差到 1e+10 量级，SLSQP 梯度爆炸。

**根因**：浮点数零除防护用 `> 0` 而非 `> eps`。`m2`（二阶中心矩）理论上恒非负，但极小时除法会放大误差。这是数值计算的通用规范：**除以标准差的高次幂时必须用 eps 阈值**。

**修复**（L240 及 `neg_utility` 内同款，共 2 处）：
```python
_M2_EPS = 1e-12  # 二阶中心矩下限, 防止 std**3/std**4 放大数值误差

if m2 > _M2_EPS:
    std = math.sqrt(m2)
    port_skew = float((centered**3).mean() / std**3)
    port_exkurt = float((centered**4).mean() / std**4 - 3.0)
# else: 保持 0.0 默认值
```

---

### P2-2. `utils/risk_budget_optimizer.py`：MVSK 无解析梯度（NEW-5）

**问题**：SLSQP 默认数值微分，每步对 `return_matrix @ w` 做三次/四次矩计算，200 资产 × 1000 迭代 = 20 万次矩阵乘。

**根因**：`neg_utility` 是闭包，未提供 `jac` 参数。SLSQP 退化到有限差分梯度，每次梯度评估要 n 次目标函数计算。

**修复**（提供解析梯度，或用 `scipy.optimize.approx_fprime` 配合合理步长）：
```python
def neg_utility(w):
    util = ret - 0.5 * self.delta * te2
    if mvsk:
        rp = return_matrix @ w
        m1 = float(rp.mean())
        centered = rp - m1
        m2 = float((centered**2).mean())
        if m2 > _M2_EPS:
            std = math.sqrt(m2)
            skew = float((centered**3).mean() / std**3)
            exkurt = float((centered**4).mean() / std**4 - 3.0)
            util = util + skew_aversion * skew - kurtosis_aversion * exkurt
    return -util

def neg_utility_jac(w):
    """解析梯度: d/dw [w'μ - δ/2 TE² + γ_s·skew - γ_k·exkurt]"""
    # d/dw [w'μ] = μ
    # d/dw [δ/2 TE²] = δ (w - w_bench)
    grad = mu - self.delta * (w - w_bench)
    if mvsk:
        rp = return_matrix @ w
        T = len(rp)
        m1 = float(rp.mean())
        centered = rp - m1
        m2 = float((centered**2).mean())
        if m2 > _M2_EPS:
            std = math.sqrt(m2)
            # d(skew)/dw = (3/m2^2.5) * R' @ (centered² - m2) / T  (略, 需推导)
            # d(exkurt)/dw = ... (略, 需推导)
            # 若推导复杂, 退而求其次: 用 approx_fprime 但限制步长
            pass
    return -grad

# SLSQP 调用加 jac=neg_utility_jac
```

**注**：解析梯度推导较复杂，若时间紧可先用 `scipy.optimize.approx_fprime` 配合合理步长（如 1e-6）替代默认有限差分，后续再补解析梯度。

---

### P2-3. `utils/signal_fusion.py`：`is_sentiment_signal_enabled` 访问私有属性（NEW-6）

**问题**：L1160 `return 'sentiment' in engine._sources` 直接访问 `_sources` 私有属性。若 `SignalFusionEngine` 内部重构改名，此函数静默返回 False，情感信号开关失效且无告警。

**根因**：缺少封装边界意识。`_sources` 以单下划线开头表示"内部实现"，调用方不应直接访问。这是"便利性"战胜"健壮性"的典型妥协。

**修复**（先在 `SignalFusionEngine` 加公共方法，再改调用方）：
```python
# utils/signal_fusion.py SignalFusionEngine 类内新增
def has_source(self, name: str) -> bool:
    """检查是否已注册某信号源."""
    return name in self._sources

# is_sentiment_signal_enabled 改为
def is_sentiment_signal_enabled() -> bool:
    try:
        engine = get_fusion_engine()
        return engine.has_source('sentiment')
    except (ValueError, TypeError, KeyError, AttributeError, OSError) as e:
        logger.warning(f"检查情感信号源失败: {e}")
        return False
```

---

### P2-4. `finetune_sentiment_model.py`：批量推理 + GPU 内存管理（NEW-9）

**问题**：逐样本 `model.generate`，无 batch，无 `torch.cuda.empty_cache()`。

**修复**：在 P0-1 的 `SentimentPredictor` 基础上实现真 batch 推理：
```python
def predict_batch(self, texts: list[str]) -> list[str]:
    prompts = [_format_instruction(t) for t in texts]
    # 简单 batch (同长度近似), 生产环境应用 DynamicPadding
    results = []
    batch_size = 8
    for i in range(0, len(prompts), batch_size):
        batch = prompts[i:i+batch_size]
        inputs = self.tokenizer(batch, return_tensors="pt", padding=True, truncation=True,
                                max_length=self.config.max_seq_length).to(self.model.device)
        with torch.no_grad():
            outputs = self.model.generate(**inputs, max_new_tokens=self.config.max_new_tokens,
                                          do_sample=False)
        for j, out in enumerate(outputs):
            resp = self.tokenizer.decode(out[inputs["input_ids"][j].shape[0]:],
                                         skip_special_tokens=True).strip()
            results.append(self._parse_label(resp))
    return results

def _parse_label(self, response: str) -> str:
    for key, label in LABELS.items():
        if label in response or key in response:
            return label
    return "中性"
```

---

## P3 — 低（下下周完成）

### P3-1. `finetune_sentiment_model.py`：`temperature=0.1` + `do_sample=False` 冲突（NEW-10）

P0-1 重构时已移除 `temperature=0.1`，仅保留 `do_sample=False`。

### P3-2. `finetune_pipeline.py`：日志错别字 "验证集B集"（NEW-11）

L189 `"训练集: %d, 验证集B集: %d"` → `"训练集: %d, 验证集: %d"`。

### P3-3. `utils/signal_fusion.py`：`register_sentiment_signal_source` 无幂等保护（NEW-12）

```python
def register_sentiment_signal_source(initial_weight: float = 0.05):
    # 先检查是否已注册
    if is_sentiment_signal_enabled():
        logger.info("情感信号源已注册, 跳过")
        return
    # ... 原逻辑
```

### P3-4. `utils/ai_coordinator.py`：shadow 比对异常静默（NEW-13）

L282 `except ...: logger.debug(...)` → `logger.warning(...)`，shadow 比对不一致应可见。

### P3-5. `utils/risk_budget_optimizer.py`：MVSK 符号约定注释（NEW-14）

注释 `+γ_s·skew 鼓励右偏` → `+γ_s·skew 惩罚左偏 (skew<0 减小目标)`。

### P3-6. `reporting/html_chart_generator.py`：CDN 离线 fallback（NEW-15）

```python
# 新增本地 fallback 路径
MERMAID_LOCAL = _PROJECT_ROOT / "ui" / "static" / "mermaid.min.js"
MERMAID_SRC = f"file:///{MERMAID_LOCAL}" if MERMAID_LOCAL.exists() else MERMAID_CDN
```

### P3-7. bandit nosec 修正（NEW-16, NEW-17）

- `utils/llm_evolution/strategy_ideation.py:117`：`hashlib.md5(key.encode(), usedforsecurity=False)`
- `utils/console_encoding.py:50`：检查 `bandit.yaml` 是否正确配置 nosec 识别，或改用 `subprocess.run(['chcp', '65001'], capture_output=True, check=False)`（移除 `shell=True`，`chcp` 是内部命令需 shell，可改用 `cmd /c chcp 65001`）。

---

## 债务项（08-25 起）

### D-1. ruff 6671 条自动修

```bash
ruff check . --fix --unsafe-fixes  # 先跑自动修 (2500 + 1498 条)
# 然后人工处理剩余 ~2673 条 (ANN 类型注解、C901 圈复杂度等)
```

**注意**：UP006 自动修需先加 `from __future__ import annotations`，否则引入 NB-5 同款 py38 崩溃。

### D-2. mypy 收紧

逐个移除 `mypy.ini` 中的 `ignore_errors=True`（13 个核心根目录文件），从 `daily_trade_executor` 开始。

### D-3. except 元组收窄规约

建立规约：**收窄 except 元组时必须核对 try 块内全部 raise 面**。08-16 报告的 NB-1/NB-2 和本轮 NEW-7 均源于此。建议加 pre-commit 钩子检查。

---

## 附：为什么会产生这么多 bug — 根因分析

### 根因 1：错误处理路径从未被执行过（潜伏型）

**涉及**：NEW-7（rag.close 资源泄漏）、08-16 的 NB-1~NB-4 全部

**原理**：这类 bug 位于 `except` / `finally` / 降级路径，平时不触发，测试也不覆盖（测试只测 happy path）。代码上线后长期潜伏，直到生产环境真的抛异常时才暴露——而此时错误处理路径自身的 bug 会"放大"原始错误（如 NB-1：broker 告警通道本应降级，却因自身 TypeError 导致整个下单链路崩溃）。

**工业级做法**：每条 `except` 路径必须有单测（用 `pytest.raises` + mock 触发异常）；CI 加 mutation testing（如 `mutmut`）验证错误处理路径被覆盖。

### 根因 2：except 元组精确化时未核对 raise 面（收窄型）

**涉及**：08-16 的 NB-1（漏 TypeError）、NB-2（漏 ImportError）、NB-6（漏 RuntimeError）

**原理**：BLE001 精确化把 `except Exception` 收窄为 `(ValueError, TypeError, KeyError, ...)` 元组时，只列了"预期"异常，未核对 try 块内**所有可能抛出的异常类型**。例如 `from utils.notify import send_alert; send_alert(...)` 可能抛 `ImportError`（模块不存在）、`TypeError`（参数不匹配）、`AttributeError`（函数不存在），但元组只列了 `(ImportError, AttributeError)` 漏了 `TypeError`。

**工业级做法**：收窄操作必须加一步"列出 try 块内每个调用的全部 raise 面"；或保守地保留 `except Exception` + `logger.exception()` + 显式 re-raise 策略。

### 根因 3：研究/POC 代码直接上生产（工程缺失型）

**涉及**：NEW-1（模型反复加载）、NEW-3（标签不均衡）、NEW-5（无解析梯度）、NEW-9（无 batch 推理）

**原理**：量化系统的核心矛盾是"研究速度 vs 工程质量"。研究员写 POC 时关注"能跑出结果"，用无状态函数、随机采样、数值微分等便利手段。这些代码验证有效后直接合入生产，但**生产场景的调用频率、数据规模、资源约束与 POC 完全不同**：POC 调 1 次 `predict` 没问题，生产调 200 次就 OOM；POC 200 样本随机标签无所谓，生产训练偏差就影响实盘决策。

**工业级做法**：研究代码上生产前必须过"工程化评审"门禁：① 重资源对象（模型/DB 连接）加载次数 ≤ 1；② 训练数据类别均衡；③ 优化器有解析梯度或合理步长；④ 推理有 batch + 内存管理。

### 根因 4：浮点数零除防护用 `> 0` 而非 `> eps`（数值规范缺失型）

**涉及**：NEW-4（MVSK 数值不稳定）

**原理**：`if m2 > 0` 在数学上正确（二阶中心矩恒非负），但在浮点数世界，`m2` 可能是 1e-15（数值零而非数学零），此时 `std**3` ≈ 1e-22，除法放大误差到 1e+10。这是数值计算的通用陷阱，但 Python 代码常缺少数值规范（C/Fortran 代码通常有 `eps` 常量）。

**工业级做法**：建立数值规范——除以标准差的高次幂时必须用 `eps` 阈值（如 `1e-12`）；矩阵求逆前检查条件数；对数前检查 `> eps`。

### 根因 5：手拼字符串 + 错误转义上下文（安全缺失型）

**涉及**：NEW-8（JS 字符串注入）

**原理**：`html.escape` 是 **HTML 上下文**的转义（转义 `&<>"'`），但代码把它用在 **JS 字符串上下文**（`"formatter": "{label}"`）。JS 字符串还需要转义 `\`、`\n`、`</` 等。上下文不匹配导致转义不全 → 注入风险。这是 XSS 的经典根因：**转义函数用错了上下文**。

**工业级做法**：禁止手拼 JS/SQL/Shell 字符串，统一用 `json.dumps`（JS）、参数化查询（SQL）、`subprocess` 列表参数（Shell）。

### 根因 6：封装边界意识缺失（便利性妥协型）

**涉及**：NEW-6（访问 `_sources` 私有属性）、NEW-12（无幂等保护）

**原理**：`engine._sources` 比 `engine.has_source()` 写起来短，于是直接访问私有属性。短期没问题，但 `SignalFusionEngine` 内部重构改名后，调用方静默失效且无告警。这是"便利性"战胜"健壮性"的典型妥协。

**工业级做法**：私有属性以 `_` 开头是约定，调用方不应直接访问；公共 API 必须提供查询方法；重复操作必须幂等。

### 根因 7：类型检查被核心文件绕过（债务累积型）

**涉及**：mypy.ini 中 13 个核心文件 `ignore_errors=True`

**原理**：`daily_trade_executor`、`hedge_execution_orders`、`live_scheduler`、`stop_loss_monitor` 等核心文件因历史债务多、类型错误多，被设为 `ignore_errors=True` 绕过 mypy。但这些正是 bug 高发区——类型检查本可在编译期发现 NB-3（`cast` 未导入）、NB-4（`logging` 未导入）等 F821 问题，却被完全绕过。

**工业级做法**：渐进收紧——每周移除 1-2 个文件的 `ignore_errors`，修复暴露的类型错误，直到全部启用 strict。

---

## 总结：bug 产生的系统性原因

| 根因类型 | 占比 | 本轮案例 | 工业级对策 |
|---------|------|---------|-----------|
| 错误处理路径未覆盖 | 30% | NEW-7, NB-1~4 | except 路径必须有单测 + mutation testing |
| 研究→生产工程缺失 | 25% | NEW-1,3,5,9 | 上线前过工程化评审门禁 |
| except 元组收窄不核对 | 15% | NB-1,2,6 | 收窄必须核对 raise 面 |
| 数值/安全规范缺失 | 15% | NEW-4,8 | eps 阈值 + 禁手拼 JS/SQL |
| 封装/便利性妥协 | 10% | NEW-6,12 | 私有属性不外访 + 幂等 |
| 类型检查绕过 | 5% | mypy ignore_errors | 渐进收紧 |

**核心建议**：① P0 当日修复（NEW-1 推理不可用）；② 建立 3 条规约——except 路径必有单测、except 元组收窄必核对 raise 面、研究代码上线必过工程化评审；③ ML 模块加"模型加载次数 ≤ 1"门禁。