# v8.7 Release Control Board(ROADMAP)优化改进评审报告

| | |
|---|---|
| 评审对象 | `ROADMAP.md` · v8.7 Release Control Board(roadmap = r9.3,2026-09-05 R-1 重组版,约 300 行) |
| 评审日期 | 2026-09-07 |
| 评审视角 | ① 事实与内部一致性 ② 数字与指标口径 ③ 排期与依赖逻辑 ④ 治理结构 |
| 核验方式 | 全文通读 + 日期与算术断言 Python 独立复算(不依赖文档自述) |

---

## 执行摘要

**总评:这份控制板在同类个人量化项目文档中属于高纪律水准。** R-1 重组(875 → 约 300 行、单一事实源 YAML、历史下沉 LOG)、Release Gate ≠ Research Gate 铁律、Release Blocker 白名单制、软件发布与生产切换解绑(R-3)、日度稳定盈利口径的数学证伪(夏普 26 论证)——这些都是正确且少见的治理机制,本次评审的全部建议均以**不破坏这些机制**为前提。

问题不在结构,在**口径收尾与机制闭环**:共 18 项发现——**P0 × 1**(发布门禁层自相矛盾)、**P1 × 9**(口径分裂 / 目标自相矛盾 / 关键节点断档 / kill 判据未量化)、**P2 × 8**(容量 / 流程 / 卫生)。全部可通过局部修订解决,无需再次重组。

| 严重度 | 数量 | 定义 |
|---|---|---|
| P0 | 1 | 发布门禁层自相矛盾或事实错误 |
| P1 | 9 | 口径分裂 / 目标不自洽 / 关键节点断档 / 机制未闭环,将实际影响 09-14 ~ 01-09 的决策质量 |
| P2 | 8 | 改进项,不影响近期决策正确性 |

**程序化核验通过项**(文档可信度的正面证据):25 个周几标注全部正确;D11 样本推演(11/20 @ 09-04,每交易日 +1 → 20/20 @ 09-17)精确吻合;B4 warmup(4/7 @ 09-04 → 7/7 @ 09-09)吻合;G-2 观察窗 12-10 起算 21 天(至 12-30)自洽。

---

## 一、P0 · 硬伤(1 项,09-18 D11 复验前必须清除)

### F01 G-2「0 FAIL 持续中」与 assert_data_validity 1 FAIL 三处口径打架,且被引用的「注」不存在

**定位**:§RELEASE GATES G-2 行 / §CURRENT STATE `gates_trio` / §开放问题 3

**证据**(原文摘录):

> `assert_data_validity: "11 PASS / 1 FAIL (D1 压力测试遗留, 独立跟踪)"` —— CURRENT STATE
> `G-2 | 门禁三件套 0 FAIL(12-31 前 21 天观察窗自 12-10 冻结起算)| 0 FAIL 持续中` —— RELEASE GATES
> `D1 压力测试遗留 — assert_data_validity 1 FAIL 独立跟踪(不阻塞发布,见 RELEASE GATES G-2 注)` —— 开放问题 3

**问题拆解**:
1. G-2 把"门禁三件套 0 FAIL"定义为发布 blocker,状态列却写"**0 FAIL 持续中**";而 gates_trio 明确记录 assert_data_validity **当前 1 FAIL**。"持续中"把"12-10 观察窗起算后的目标状态"提前写成了现状。
2. 开放问题 3 说 D1"不阻塞发布,见 RELEASE GATES **G-2 注**"——G-2 行内没有任何关于 D1 的注,指针悬空。
3. 三处合读产生两种相反解读:D1 FAIL 阻塞 12-31(白名单字面义)vs 不阻塞(开放问题义)。矛盾恰好出现在整个文档**最不能含糊的位置**——发布门禁白名单。

**修复建议**(推荐 a):
- **a.** G-2 状态改写为"观察窗 12-10 起算,窗口内 0 FAIL;D1 遗留 FAIL 在窗口外,需 12-10 前闭环或在决策登记表显式豁免(登记豁免编号)";开放问题 3 的"见 G-2 注"改为指向该豁免编号。
- **b.** 若确认 D1 不修:将 assert_data_validity 的 D1 子项从 G-2 观察口径显式剥离,单列 D1 观察行,白名单保持纯净。
- **c.** 若 D1 计划 12-10 前修:在 NEXT 14 DAYS 之后补 D1 修复节点,让"0 FAIL 持续中"成为有依据的预测。

---

## 二、P1 · 口径、目标与关键机制(9 项)

### F02 月度胜率目标自相矛盾:≥70% 与「12 个月中 8-9 个月正」不能同时成立

**定位**:§CURRENT STATE `performance_targets`

**证据**:

> `monthly_win_rate: "≥ 70% (12 个月中 8-9 个月正)"`
> `daily_target: "废止 — 日度稳定盈利数学上不存在 (等价年化夏普 26)..."`

**问题**:8/12 = 66.7% < 70%;≥70% 要求 ≥9/12。括号口径与主口径打架。另外,文档用"夏普 26"的专业论证废止了日度目标,但 70% 月度胜率本身隐含年化夏普 ≈ Φ⁻¹(0.70) × √12 ≈ **1.8**——可行但属高目标,应对等透明地公布推算口径,而非只对日度目标做数学检验。

**修复**:统一为"≥9/12 个月正(75%)"或"月度胜率 ≥2/3(期望 8/12)";注明"月正 = 费后净收益 > 0"、是否含期货端。

### F03 「天」的两种语义并存:影子 30 天窗口有效交易日仅约 14~15 个,统计功效未声明

**定位**:§CURRENT STATE `phase_b`(B4)/ `shadow_lines` / §策略六线 资金线

**证据**:

> `B4_USE_MLOPS_PIPELINE: shadow_running # warmup 4/7 交易日 (09-01~09-04), 达标约 09-09`
> `mvsk_qlib_30day: cron_registered # Shadow30Day_EOD 16:35, 首触发 09-07, 窗口 09-13~10-12`
> 资金线:`Sprint3-2(100 万 shadow 30 天)`

**问题**:B4 的"7 天"按**交易日**计(09-01~09-04 四个交易日 = 4/7);而 MVSK/qlib/GNN 与 Sprint3-2 的"30 天"按**自然日**计。09-13~10-12 自然日 30 天,扣周末(9 天)、国庆假期(约 5 个工作日)、中秋(09-25)后**有效交易日约 14~15 个**(2026 年官方假期安排以国务院通知为准)——统计功效较名义窗口缩水一半,却无任何声明。跨项目对比(B4 vs shadow 三线)口径也不一致。

**修复**:全文统一标注口径,如"30 天(自然日,含假期,实际交易日 ~14)";Kill/验收判据写明按交易日还是自然日计;Sprint3-2 补口径说明。

### F04 资金线灰度三个 Sprint 只有一个有锚点

**定位**:§策略六线 资金线 / §NEXT 14 DAYS

**证据**:

> `Sprint3-1(D11 全绿后 20 万)→ Sprint3-2(100 万 shadow 30 天)→ Sprint3-3 切换窗 01 月初(R-3)`
> `09-18(五)| D11 复验(预期 PASS)→ ... → 解锁 Sprint3-1(20 万测试,冻结豁免)`

**问题**:Sprint3-1 无持续时长与完成判据;Sprint3-2 无启动判据(3-1 满足什么才升 100 万)、无起止日期锚点。倒排:若 3-2 要在 12-10 冻结前产出充分材料,30 自然日窗口**最晚 11 月上旬启动**;若拖到 12 月,窗口尾部撞 RC-1/RC-2/rehearsal 全链。资金线是四项切换里体量最大的一项,却是六线里锚点最稀的一条。

**修复**:给 Sprint3-1 定"最短 N 交易日 + 无 P0 事故 + NAV 对账连续 N 日"式判据(可引用 `docs/sprint3_capital_upgrade_gate_20260905.md`);给 3-2 定启动日锚点(建议不晚于 2026-11-09,留两周评估缓冲)。

### F05 「统一评估周」三种日期口径并存,且 10-10 是周六、早于窗口收尾

**定位**:§CURRENT QUARTER Stage B / `mvsk_p5` / `etf_option_submodel` / Research Kill 表

**证据**:

> `★ 统一评估周 10-09~10-16(P3.3 + MVSK/qlib Δ夏普 + GNN S6 观察 + Sprint 2 收尾 → 一份合并评估报告...)`
> `p5_3_switch: 判定于 10-13 统一评估周`
> `p3_3_evaluation: script_ready (run_p33_evaluation.py, 评估约 10-10; ...)`
> qlib kill:`不优于当前 V9(10-13 评估)`

**问题**:同一评估周存在 10-09~10-16、10-13、约 10-10 三种口径;10-10 是**周六**,且三线窗口 10-12 才收尾——周六评估拿不到完整 30 天数据。"约 10-10"若指 P3.3/S12(其 shadow 自 09-14 起算 30 天也到 10-13 附近),同样不成立。

**修复**:统一为"窗口收尾 10-12(EOD)→ 数据落库与计算 10-13 → 判定 10-14~10-16";P3.3 的"约 10-10"改到 10-13 后,与合并评估报告对齐。

### F06 Research Kill Criteria 多数未量化、无判定程序

**定位**:§CURRENT QUARTER Research Kill Criteria 表

**证据**:

> `GNN CHAIN_MOM_60D | S6 观察期 CPCV/稳定性不显著`
> `MVSK P5-3 | Δ夏普 ≤ 0 或异常换仓`
> `新因子 | ICIR < 阈值(S1-S7 门禁)` / `新模型 | live degradation > 阈值(模型退役标准)`

**问题**:阈值数值、判定人、证据格式三缺;**最小效应量**缺失——Δ夏普 = +0.01 也满足"> 0",但为此切换生产信号源的风险收益完全不成比例。"触发即归档不续期"是好机制,判据模糊会让它在执行时被争议拖垮。

**修复**:每条补"阈值 + 判定人 + 证据文件"三件套;MVSK 加最小效应量(例:Δ夏普 ≥ +0.15 且统计显著)与成本项(换手/容量)。

### F07 「每日查看路径」指向不存在的 DECISION NEEDED;控制板缺待决策队列

**定位**:文末页脚

**证据**:

> `每日查看路径:CURRENT STATE → NEXT 14 DAYS → RELEASE GATES → shadow 产出 → DECISION NEEDED。`

**问题**:"DECISION NEEDED"节不存在(重组遗留);"shadow 产出"也不是节名。更深一层:决策登记表是**事后记录**,控制板没有 **pending 决策队列**——D1 怎么处置、C1 WARN 是否豁免、qlib 窗口是否停跑,这些"等拍板"的事在每日查看路径上无处可见。

**修复**:页脚改为实际节名;新增 `## DECISION NEEDED(事项 / 决策人 / 截止日)`小节承载 pending 队列,与决策登记表(事后)形成闭环。

### F08 已废止的 V9 命名仍活在活跃判据里

**定位**:§ARCHIVED DECISIONS D-2 行 / Research Kill 表

**证据**:

> 头部铁律:`preset = p9_200w(200 万生产组合配置,原 "v9.0 preset" 命名废止)`
> `D-2 | qlib 信号源切换 V9→qlib_lgb_v2 同 D-1 口径`
> kill 表:`不优于当前 V9(10-13 评估)`

**问题**:命名废止后,活跃的 kill 判据与决策登记仍用旧名"V9",新读者无法解码它指向哪个实体(preset?信号源?模型版本?)。

**修复**:改为"当前生产信号源(原称 V9)"或直接落 preset 名,保持三线命名铁律的全文一致性。

### F09 「组合口径」未定义:8%~18% 的分母与「跑赢通胀」的基准缺失

**定位**:§CURRENT STATE `performance_targets` / `s12_defense_acceptance`

**证据**:

> `accounts: "证券 200 万 (p9_200w_preset) + 期货 100 万 (对冲/套利载体)"`
> `annual_return: "8% ~ 18% (组合口径, 期望值非承诺)"`
> `s12_defense_acceptance: "收益正向(跑赢通胀) / 30日滚动回撤 ≤5%..."`

**问题**:"组合口径"的分母不明——仅证券 200 万?300 万合并?期货端盈亏计不计?回撤 ≤10% 预算线是证券端 NAV 还是合并 NAV?"跑赢通胀"未给基准(CPI?哪个口径?多少?)。绩效目标是全文被引用最多的数字,分母不明会让 2027 年的验收争议无从裁决。

**修复**:写明计算式(例:annual_return =(证券 PnL + 期货 PnL)/ 期初总权益 300 万);"跑赢通胀"改为"≥ 同期 CPI(统计局口径)"或给固定下限(如 ≥3%)。

### F10 qlib 已自判「统计上无意义、10-13 判 FAIL、D-2 不切换」,却仍跑满 30 天窗口并占用评估周与切换窗席位

**定位**:§CURRENT STATE `qlib_lgb_v2.known_gap` / §NEXT 14 DAYS / Stage B / 生产切换窗

**证据**:

> `⚠ 双设计缺陷:①接线错配(消费端 4 只 ETF vs 模型 86 只个股...)②信号静态(...)→ W7.2.9 的 Δ夏普对比统计上无意义, 10-13 评估判 FAIL(设计缺陷口径) + D-2 不切换`
> `09-14(一)| shadow 三线窗口首交易日(MVSK P5-2 / qlib W7.2.9 / GNN S6...)`
> 生产切换窗:`...MVSK P5-3 / qlib 切换——四项独立 Go/No-Go`

**问题**:结论已定(设计缺陷口径 FAIL + 不切换),仍安排:30 天 cron 跑满、评估周出 Δ夏普材料、切换窗保留第四席。cron 资源成本小,但评估注意力与切换窗席位是真成本;且"FAIL(设计缺陷口径)"不是模型证伪——模型本身尚未被真正检验。现状是"不归档、不修复、照跑"三头不靠。

**修复**(三选一,09-14 窗口起跑前拍板):**a.** 停跑 W7.2.9,qlib 按 Kill 流程归档,评估周与切换窗席位释放给 MVSK/GNN;**b.** 冻结窗允许 shadow——先修接线错配与每日推理,修完重开 30 天窗口(评估与切换顺延至 2027 Q1);**c.** 维持现状,但把评估周中 qlib 材料的定位从"决策材料"降级为"缺陷存档记录",切换窗第四席显式标注"预置 No-Go(设计缺陷)"。

---

## 三、P2 · 改进项(8 项)

### F11 生产切换窗实际仅 5 个交易日,三项活体逐项实施+回滚密度高,且无顺延滑移规则

2027-01-02(六)~ 01-09(六):8 个自然日,扣除周末与窗外元旦,实际交易日 **01-04 ~ 01-08 共 5 个**。四项中 qlib 已预置 No-Go,活体三项(200 万资金升级 / S12 并入 / MVSK P5-3)各需"实施 + 观察 + 可回滚",平均 1.5 天/项,大资金升级后的观察期明显不足。**修复**:明确"每项实施后 ≥2 交易日观察再进下一项"(5 天仅够 2 项)→ 扩窗至 01-15 或分两批;写明滑移规则(单项顺延不压缩其他项观察期)。

### F12 三个 GitHub Wave 与切换窗同日(01-04)启动

Wave 9-GH(01-04~04-30)、Wave 11-B(01-04~02-14)、Wave 12-B(01-04~03-21)均以 01-04 为起点 = 切换窗首个交易日。切换期是生产最高敏感期,同期启动三个集成波次分走审查与合并注意力。**修复**:Waves 统一后置至 01-11 起(切换收尾 + 复盘后),或至少错峰。

### F13 2027 无容量预算

Q4 有"支线合计 ≤2 人天/周",2027 仅 Wave 12-B 标了"~26 人天";v8.7.1 Core(G4 01 月 + G3 02~03 月)+ 四个 Wave + GNN S7 入库 + ERL Stage 2→3 + S12 参数优化同期展开,无人天上限,排期可信度打折。**修复**:2027 PLAN 补季度容量预算(人天/周),各 Wave 标预算,超限自动降入候选池。

### F14 Invariants 10 条仅 1 条有机器检查标注——锚点 ≠ 验证

10 条不变量都有"实现锚点"(代码位置),但只有 I-02 标注 ◆ 机器检查(G5 双门禁)。锚点回答"在哪实现",不回答"是否还在守着"。I-04(fail-closed)、I-09(审计链)这类"出事才知道没守住"的不变量最需要周期性验证。**修复**:逐条补"验证方式 + 频率",分三档:◆ 机器检查 / ▲ 周期演练(chaos)/ ○ 人工审计;v8.7.1 P2 的 CI 门禁范围相应扩展。

### F15 期货 100 万账户:有目标、无运营

期货账户只在 `performance_targets.accounts` 出现("对冲/套利载体,IC/IM/IF Beta + 基差,杠杆 ≤2 倍");策略六线无期货线,Stage 锚点与切换窗无其开户/接入/验收/切换安排,CURRENT STATE 无状态位。若 2027 年初才启动,它对 8~18% 组合目标的贡献/拖累无人跟踪;若已就位,控制板看不见。**修复**:六线加第七线"对冲线(期货 100 万)"或在资金线内补节点;CURRENT STATE 加期货账户状态键。

### F16 100 万 → 200 万之间仅约 14~20 个交易日实证,容量复核未设为 Go/No-Go 必答项

`acceptance_basis` 明确"真实资金影子绩效(20万→100万→200万灰度),非回测",但 3-2(100 万)仅 30 天(实际交易日 14~20 个,取决于是否跨假期)即再翻倍;开放问题 2 自认"扩大规模需重新评估冲击成本模型"。100 万与 200 万对冲击成本的敏感度不同量级。I-05 只管 NAV 对账,不管容量验证。**修复**:Sprint3-3 的 Go/No-Go 材料增设"容量/冲击成本复核"必答项(引用开放问题 2 的重评结论);3-2 期间强制记录成交滑点分布作为 3-3 输入。

### F17 文件卫生:frontmatter 转义破坏解析;C1 WARN 无 12-10 前处置口径

① frontmatter `project\_topic`、`ai\_generated` 及 related 列的下划线转义 + `***` 分隔符会破坏 YAML/frontmatter 机器解析(若非上传链路自动转义,应修原件)。② `industrial_grade_check` 的 C1 WARN("xtquant 未装=物理阻塞")在 12-10 冻结前若无环境变更,将带 WARN 进入观察窗;G-2 只看 FAIL,白名单外的 WARN 无处置口径。**修复**:修 frontmatter;给 C1 一行处置口径(12-10 前装 xtquant,或登记豁免 + 影响评估)。

### F18 Change Budget 例外无判定程序;NPC 任务池验收/优先级未约定

① 例外"bug 修复 / 风险与性能优化"无判定权与判定标准——"修复"与"新增行为"的边界正是渐进重构容易钻的口子。② NPC 任务池"推分支建 PR(标题 AUTO-x)"但未约定 PR 合并次序、与主线任务的冲突优先级、验收人;D12 机械检查 09-19 才激活,激活前 NPC 产出与 Change Budget 的关系未写死(NPC 任务全为纯代码/测试/文档,风险低,但口径应显式)。**修复**:补"例外判定三问"(修的是既有行为还是新增?有无对应 issue/事故单?谁签?);NPC 节补"PR 验收 = 云端验证命令绿 + 人工抽查,冻结期只收 [稳定性] 标签"。

---

## 四、保持不破坏的基线(评审确认做对了的)

1. **Release Control Board 重组本身**——875 → 300 行、CURRENT STATE 单一事实源、历史下沉 LOG,方向正确;
2. **Release Gate ≠ Research Gate 铁律 + Blocker 白名单(5 项)**——防止研究线隐式绑架发布,机制正确(F01 只是状态列笔误级矛盾,机制本身没问题);
3. **软件发布与生产切换解绑(R-3 拆批次)**——正确且关键,四项独立 Go/No-Go 的粒度也对;
4. **日度稳定盈利口径废止(夏普 26 论证)**——诚实且专业;本报告 F02 只是把同样的数学纪律应用到月度目标;
5. **Kill Criteria / Invariants / Change Budget 成文**——从口头纪律到成文治理的实质跃迁,量化补全(F06/F14)是收尾不是推翻;
6. **旧章节兼容映射 + 修改纪律**——重组期的工程化处理,避免存量引用断裂;
7. **日期纪律**——25 个周几标注全对、D11/B4 推演精确,在同类文档中罕见。

---

## 五、修复路线(按文档自身时间线倒排)

| 时点 | 应完成 | 对应发现 |
|---|---|---|
| **本周(09-08 ~ 09-12)** | G-2 口径澄清;统一评估周口径;qlib 三选一拍板 | F01、F05、F10 |
| **09-14 窗口起跑时** | 影子窗口口径标注(自然日/交易日 + 实际交易日数) | F03 |
| **10-13 评估周前** | 月度胜率口径统一;Kill 判据量化;组合口径定义 | F02、F06、F09 |
| **12-10 冻结前** | Sprint3 锚点(3-2 最晚 11 月上旬启动);C1 WARN 处置;V9 残留清理 | F04、F17、F08 |
| **2027 规划期** | 切换窗扩容/滑移规则;Waves 错峰;容量预算;Invariants 验证三档;期货线补位;灰度容量复核;例外与 NPC 流程 | F11 ~ F16、F18 |

---

## 六、方法与局限

- **方法**:四视角(一致性 / 数字口径 / 排期逻辑 / 治理)通读;全部日期与算术断言经 Python 独立复算(周几标注 25 个全对;D11 20/20 = 09-17 吻合;B4 7/7 = 09-09 吻合;切换窗交易日 = 01-04 ~ 01-08 共 5 个;影子窗口自然日 30 / 工作日 21 / 扣国庆中秋后约 14~15 个)。
- **局限 1**:交叉复核 Agent 因运行超时未回传,本报告为**单源评审**;P0/P1 各条建议合入前由第二位读者按本文引用抽查一遍(所有断言均附原文摘录,可直接对验)。
- **局限 2**:ROADMAP 指向的 docs/、cairn/ 专项文档(如 `qlib_w729_gap_analysis_20260905.md`)未逐一打开核验,本报告仅针对主文档的内部一致性;外部文档与主文档的一致性属另一轮审查范围。
- **严重度口径**:P0 = 发布门禁层自相矛盾或事实错误;P1 = 将实际影响 09-14 ~ 01-09 决策质量;P2 = 不影响近期决策正确性的改进项。
