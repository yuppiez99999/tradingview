# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST.txt

# Virtual Environment
venv/
ENV/
env/
.venv
qlib_env/
qlib_env/**
qlib/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
*.log.*

# Data & Cache
data/
cache/
*.cache
*.parquet
*.pkl
*.pkl.*
*.jsonl
*.db
*.sqlite

# Models
models/
*.pkl
*.joblib
*.h5
*.pt
*.pth

# Test coverage
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Environment variables
.env
.env.local
.env.*.local

# Large files
*.whl
*.tar.gz
*.zip
*.7z

# Backup files
*.bak_*

# MLflow
mlruns/

# Reports (keep structure, ignore content)
reports/*
!reports/.gitkeep
!reports/README.md
# CI 基线文件必须提交, 否则 ruff 增量门禁与 Phase 3-B 静态分析无法通过
!reports/ruff_baseline.json
!reports/ci/coverage_baseline.json
v7.5_institutional/reports/*
!v7.5_institutional/reports/.gitkeep

# Daily reports archive
每日报告归档/*
!每日报告归档/README.md

# Trade plans (keep structure, ignore content)
trade_plans/*
!trade_plans/.gitkeep
!trade_plans/README.md

# v8.3/v7.5 trade plans (运行时生成, 不入版本库)
v8.3_institutional/trade_plans/*.json
v7.5_institutional/trade_plans/*.json
!v8.3_institutional/trade_plans/.gitkeep
!v7.5_institutional/trade_plans/.gitkeep

# Hedge execution fill 报告 (运行时生成)
v8.3_institutional/reports/hedge_execution_fill_*.json

# Trade instructions
trade_instructions/*
!trade_instructions/.gitkeep

# Qlib data (large binary files)
qlib_data/

# Simulation snapshots
sim_snapshots/

# Config files (contains live positions and sensitive data)
config/*
configs/

# Allow portfolio template to be tracked
!v7.5_institutional/config/portfolio.yaml
!config/brinson_attribution.yaml
!config/factor_attribution.yaml

# Hedge execution runtime reports (含交易指令, 运行时产物)
utils/execution/reports/hedge_execution_orders_*.json
# P2: 价格表是无敏感配置, 允许入版本库
!config/llm_pricing.yaml
# S6 纸交易配置 (无敏感数据, 因子跟踪配置)
!config/gnn_factor/
!config/gnn_factor/s6_paper_trading.yaml

# Trade executor generated files
daily_trade_executor.log
daily_trade_executor_pid.txt

# Portfolio projection files
portfolio_return_projection.json

# Skill state files
.agents/skills/wind-mcp-skill/scripts/update-state.json
skills/wind-mcp-skill/scripts/update.lock

# IDE workdir
.workbuddy/

# Screenshots and UI captures
v7.5_institutional/*.png
v7.5_institutional/*/*.png
v7.5_institutional/*/*/*.png

# Qlib binary feature/price data lives under qlib_data/ (dir-ignored).
# 2026-08-29: ms_strategy 已合并进父仓库 (删除其独立 .git), 不再排除 — 84 文件纳入 git 跟踪 + CI 覆盖。
# Vibe-Trading 仍为孤儿 gitlink, 保留排除。
_archive/research_references_quarantine/references/Vibe-Trading/
# ms_strategy/dataset/ qlib 二进制数据集 (27151 文件 / 278 MB, 可再生, 不入 git)
ms_strategy/dataset/

# Pip download cache
pip-cache/

# Data caches (regenerable parquet/cache)
data_cache/
data_cache/**/*.parquet

# Runtime output directory
output/

# Monitoring / health reports (runtime generated)
monitor_reports/

# Root-level debug / stderr-stdout dumps from automation
_etf_*.log
_etf_*.txt

# Python compiled / caches (safety net)
*.pyc
__pycache__/

# === GitHub 周榜项目集成 (2026-07-26) ===

# graphify 输出目录（运行时生成，不入版本库）
graphify-out/

# codebase-memory-mcp 二进制与索引（273MB 二进制，不入版本库）
tools/codebase-memory-mcp/codebase-memory-mcp.exe
tools/codebase-memory-mcp/index.db
tools/codebase-memory-mcp/.mcp.json.backup
tools/codebase-memory-mcp/mcp_config_new.json

# Vibe-Trading 参考副本（浅克隆，不入版本库）
research/references/Vibe-Trading/
# Added by code-review-graph
.code-review-graph/

# === v8.6.9 金融多 Agent 协调器 (Shadow Mode) ===
# Agent 决策审计日志 (运行时生成, 不入版本库)
data/agent_orchestrator_audit/

# === v8.6.9 研究蒸馏信号 (RIA--TV++ 量化版) ===
# 蒸馏信号快照 (运行时生成, 不入版本库)
data/distilled_signals/

# === v8.6.9 awesome-llm-apps 模式提取文档 ===
# 仅文档, 不 clone 源码
research/references/awesome-llm-apps-patterns/

# === v8.7 IDE 内部目录不入版本库 ===
.trae-cn/
.trae/
.claude/skills/.trae-cn/

# === 运行时 kill_switch 报告不入版本库 ===
research/vibe_trading_factor_analysis/reports/kill_switch/

# === AI agent 内部记忆/缓存不入版本库 ===
.agents/
.memory/
graphify-out/

# === OCR 代码审查工具配置（含 API Key 等敏感信息） ===
.ocr_home/
.opencode*/

# === v8.7.2 临时调试/清理脚本不入版本库 (2026-07-27) ===
# 一次性临时输出文件
_tmp.txt
_tmp_*.txt
_mypy_out_tmp.txt
_pylint_out_tmp.txt
# 一次性 C 盘清理/扫描脚本（本地维护用，与项目无关）
check_procs.ps1
cleanup_c.ps1
cleanup_fast.ps1
list_jobs.ps1
scan_fast.ps1
scan_junk.ps1
scan_large.ps1
scan_roaming.ps1
# 通用临时调试输出
debug_*.txt
debug_*.log
tmp_*.txt
out_*.txt

# === P0 安全网基线补充 (2026-08-08) ===
# ruff/mypy 缓存大文件（非源码，禁止入版本库）
.ruff_*.txt
.ruff_*.json
.mypy_*.txt
.mypy_*.json
# venv 安装/测试日志转储（含本地路径，非源码）
.venv_install_log*.txt
.venv_test_*.txt
tests_unit_log.txt
tests_*.log
cov_log*.txt
pip_error.txt
scan_*.txt
# qlib_env 虚拟环境兜底（第29行规则未覆盖 exe 时的双保险）
qlib_env/**/*.exe
qlib_env/**/*.dll
qlib_env/**/*.so
.tmp_pip/
.pip_cache/
.codebuddy/
.codex/
.claude/
.trae-cn/
.trae/
.omc/
.streamlit/
.agents/
.cairn/
# 根级临时/备份
backups/
temp/
*.bak
*_backup.txt
# P0 会话产生的调试/临时产物
pre_commit_debug.txt
pre_commit_py_debug.txt
temp_tdam_verify.txt
board_preview.html
github_weekly_report_*.html
research_plan_*.md
research_report_*.md

# === v8.6.14 复审 R12 临时产物补充 (2026-08-12) ===
# Excel / Office 临时锁文件 (如 ~$高价值GitHub项目清单.xlsx)
~$*.xlsx
~$*.xls
~$*.docx
~$*.pptx
# 根级 / 临时输出转储 (CI 调试产物)
_*.out.txt
_*.out.json
_ms_*.txt
# 临时 Python 调试脚本
_tmp_*.py
_tmp_*.pyc
# 临时 pip 下载缓存兜底 (已被 .tmp_pip/ 覆盖, 双保险)
.tmp_*.pip/

# P0 pre-commit 验证标记
test
test3

# === 工作区清理 (2026-08-14) ===
# 非量化项目目录 (游戏项目, 保留本地不纳入 git)
game/
games/
# IDE/Agent 工具目录
.arts/
.zcode/
# 临时调试脚本 (brace 修复等一次性脚本)
check_*.py
fix_*.py
debug_*.py
print_*.py
replace_*.py
count_*.py
find_*.py
# 临时截图
screenshot_*.png
# 临时覆盖率输出
coverage_output.txt

# === 工作区清理 (2026-08-15) ===
# loopx agent 运行时 handoff 目录
.loopx_handoffs/

# ============================================================
# 白名单例外 (2026-08-29)
# 背景: 通用忽略规则 data/ models/ cache/ config/* reports/* 会误伤同名
# 生产代码目录, 导致这些源码从未入库 —— 本地存在但 CI checkout 拿不到,
# 表现为 "No module named 'utils.data'" 类恒定失败, 且本地磁盘故障即永久丢失。
# gitignore 语义铁律: 父目录被排除时, 文件级否定无效, 必须先否定目录本身;
# 多条规则冲突时"最后匹配者胜", 故否定必须写在被否定规则之后 (即本文件末尾)。
# ============================================================
# 写法约定: 只"取消目录排除 + 放行 *.py", 绝不用 !dir/** ——
# 后者会覆盖全局 *.parquet / *.db / __pycache__ 等规则, 把大文件与缓存一起放行。
# --- 生产源码目录 (被 data/ 规则误伤, 父目录未被排除, 直接否定即可) ---
!utils/data/
!utils/data/*.py
!quant_modules/ai_hedge_fund/data/
!quant_modules/ai_hedge_fund/data/*.py
!ms_strategy/src/data/
!ms_strategy/src/data/*.py
# --- 生产源码目录 (被 models/ 规则误伤) ---
!utils/fineng/models/
!utils/fineng/models/*.py
# --- 配置 schema (被 config/* 误伤, config 目录本身未被排除) ---
!config/schema.py
!config/feature_flags.yaml
# --- 运行必需配置 (Issue#1 glm-5.3 指出未入库; 非 secrets, 放行) ---
!config/etf_option_subportfolio.yaml
!config/s12_shadow_config.json
!config/trade_execution.yaml
# --- ETF 期权对冲回测脚本 (被根目录 data/ 误伤, 需先放行目录再收窄) ---
# data/etf_option_backtest 下还有 26 个生成报告(.md) 与大量回测产物(.json/.txt),
# 这些是运行产物不该入库, 故先整体排除再只放行 *.py。
!data/
data/*
!data/etf_option_backtest/
data/etf_option_backtest/*
!data/etf_option_backtest/*.py
# --- cache/ 下的工具脚本 (被 cache/ 误伤) ---
!cache/
cache/*
!cache/*.py
# --- CI 冻结基线: reports/ci 目录被 reports/* 排除, 须先取消目录排除,
#     再排除运行时产物, 最后放行冻结基线 (与 reports/ruff_baseline.json 同性质) ---
!reports/ci/
reports/ci/*
!reports/ci/coverage_baseline.json
# --- 运维工具脚本 (被 check_*.py 临时脚本规则误伤) ---
!scripts/check_eod_status.py
!scripts/check_trade_logic.py

# --- 密钥文件 (2026-09-01 审查发现: llmkey.txt 含真实 API key, 禁止入库) ---
llmkey.txt
llmkey*.txt

# --- 临时测试报告目录 (一次性产物) ---
_test_report_*/
