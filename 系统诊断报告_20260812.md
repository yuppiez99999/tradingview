# ZCodeProject v8.6 系统诊断报告

> 诊断时间：2026-08-12 09:25 (GMT+8)｜诊断环境：`.venv` (Python 3.14.4)｜执行人：WorkBuddy 回测明算

## 一、结论速览

| 维度 | 结果 | 说明 |
|------|------|------|
| 主入口加载 | ✅ PASS | `量化策略系统_统一入口_v8.6.py` 正常导入并解析 CLI（exit 0） |
| 系统健康 `--check` | ⚠️ 降级运行 | 无数据源连接器（Wind MCP / Bloomberg / GLM 全部断开）→ **离线模式** |
| **P0 语法错误** | 🔴 **已修复** | `utils/ifind_client.py` 字典字面量缺 3 个逗号，整模块不可导入 → 已补逗号，现可编译+导入 |
| 测试套件 (unit+smoke) | ⚠️ 467 通过 / 5 失败 / 10 跳过 | 4356 个用例被 `-m` 反选；另有 1 个收集错误 |
| 静态检查 (ruff) | ⚠️ 67 处 | 主入口文件为主，多为缺失类型注解 (ANN) + 少量风格 (E701/E702) |
| mypy 配置 | ⚠️ 配置告警 | `mypy.ini` 含无效选项 `error_limit = 0`（不影响运行，仅告警） |

**最关键结论**：系统在离线（无实时数据）下可启动，但存在 1 个真实 P0 阻断性 bug（已修复）与若干测试/代码漂移问题。集成测试（integration/e2e）依赖 Wind MCP / Bloomberg / GLM API，当前连接器全部断开，本次未运行。

---

## 二、关键发现（按严重程度）

### 🔴 P0 — `utils/ifind_client.py` 语法错误（已修复）
- **位置**：第 619、630、639 行，一个期货/现货行情字典字面量。
- **问题**：连续 3 行 `... else None` 末尾缺少逗号，导致 `SyntaxError: invalid syntax`，**整个 `utils/ifind_client.py` 模块无法被任何代码导入**。
- **影响**：所有走 iFinD 数据供应路径的功能（基本面批量拉取、期货行情）在导入时即崩溃。
- **修复**：补上 3 个缺失逗号。修复后 `py_compile` 通过、`importlib.util.find_spec('utils.ifind_client')` 返回可导入。
- **根因推测**：该文件由 LLM/批量生成，字典条目过长导致漏逗号；`py_compile` 未被纳入预提交门禁（见第四节建议）。

### 🟡 测试失败（5 项，均为 测试/代码漂移）
| 用例 | 失败断言 | 漂移方向 |
|------|----------|----------|
| `tests/unit/test_finance_agent_orchestrator_unit.py::test_init_creates_5_default_agents` | 期望 5 个 agent，实际 6 个（新增 `weather`） | 代码新增 WeatherAgent，测试未更新 |
| `tests/unit/test_finance_agent_orchestrator_unit.py::test_init_default_weights` | 期望 `value=0.25`，实际 `0.22` | 权重已重调，测试硬编码旧值 |
| `tests/unit/test_shadow_threshold_gates.py::test_two_layer_threshold_values` | 期望 `DEFAULT_REQUIRED_DAYS=14`，实际 `21` | 阈值由 14 天改为 21 天，测试未同步 |
| `tests/test_morning_info_runner.py::TestTaskIfindAnalysis::test_copies_source_file_when_exists` | 期望报告含 `'iFinD 源报告'`，实际为 `'iFinD 自动标的研判报告'` | 报告模板文案已改 |
| `tests/test_morning_info_runner.py::TestRunDailyMorningPhaseRouting::test_info_phase_runs_before_trading_day_check` | 期望存在"交易日检查代码块"，实际缺失 | 晨间流程逻辑变更 |

> 这些失败多为"行为已演进、测试未跟进"，属于低风险漂移；但 `shadow_threshold_gates` 的 14→21 天阈值变更需你确认是**有意调参**还是**回归**，建议优先核对。

### 🟡 测试收集错误（1 项，已知设计取舍）
- `tests/unit/backtest/test_result_converter.py` 在收集阶段报 `ImportError: qlib.__spec__ 被 E2E 测试拦截 (避免 scipy 崩溃链)`。
- 这是 conftest 中一处**全局拦截**（为避免 scipy 崩溃链），副作用是连累了 1 个本应可收集的 unit 文件。属有意设计，但建议把该拦截收窄到 e2e 作用域，避免误伤 unit 收集。

### 🟡 `--check` 健康检查报告本身存在"探针漂移"
`--check` 模式（`cli/modes/quick_check.py`，v5.10 时代遗留）探测了若干当前 v8.6 对象**已不再提供的方法**，产生误报：
- `ConfigManager` 无 `get_all` 方法 → 报 ERROR
- `StrategyRegistry` 无 `list` 方法 → 跳过
- 连接器 `get_status` 缺失 / `GracefulFallback.is_fallback_mode` 缺失 → 跳过
- 模块可用性 ❌ 项（数据提供层 / 再平衡引擎 / 每日报告 / 止损止盈监控 / 时序预测模型 / Wind 数据供应器）多因 `core.context` 降级导入未接上新模块路径，而非功能真的缺失。

> 即：`--check` 现在更多是"探针自检"，不能直接当作模块健康度。建议把 `quick_check.py` 对齐到 v8.6 的真实 API（或标注为 deprecated）。

### 🟡 质量门禁
- **ruff**（主入口示例）：67 处，主要为 `ANN001/ANN201`（缺参数/返回类型注解）+ 少量 `E701/E702`（一行多语句）。CI 采用"增量零新增"门禁（基线冻结于 `scripts/ruff_baseline_gen.py`），所以存量忽略、只看增量。
- **mypy.ini**：第 40 行 `error_limit = 0` 非 mypy 合法选项 → 每次 mypy 启动告警 `Unrecognized option`。建议删除该行（mypy 对应能力是 `--error-summary` / 无 error_limit）。

---

## 三、本次执行的命令与产物

| 动作 | 命令 | 结果 |
|------|------|------|
| 入口冒烟 | `python 量化策略系统_统一入口_v8.6.py --help` | exit 0 |
| 系统自检 | `python 量化策略系统_统一入口_v8.6.py --check` | exit 0（降级报告） |
| 语法全扫 | `py_compile` 全仓 `.py`（排除 venv/qlib_env/external） | 仅 `ifind_client.py` 报错（已修）|
| 单元+冒烟测试 | `pytest -q -m "unit or smoke" --ignore=tests/unit/backtest/test_result_converter.py` | 467 passed / 5 failed / 10 skipped / 4356 deselected |
| 主入口 lint | `ruff check 量化策略系统_统一入口_v8.6.py` | 67 errors |
| 类型门禁 | `mypy 量化策略系统_统一入口_v8.6.py` | 触发 `mypy.ini` 非法选项告警 + `ifind_client.py` 语法错（已修）|

---

## 四、修复优先级建议

1. **【已做】** `utils/ifind_client.py` 三处缺逗号 → 已补，模块恢复可导入。
2. **【建议立即】** 把 `py_compile` / `python -m compileall` 加入 pre-commit 与 CI，杜绝语法错误入库（本次若非手动全扫，该 P0 会被遗漏）。
3. **【建议】** 核对 `shadow_threshold_gates` 的 14→21 天阈值，确认后同步更新测试或回滚。
4. **【建议】** 批量更新 5 个漂移测试以对齐当前行为（新增 weather agent、权重、报告文案）。
5. **【建议】** 删除 `mypy.ini` 第 40 行 `error_limit = 0`。
6. **【建议】** 将 `quick_check.py` 对齐 v8.6 API，或标注 deprecated；当前其输出具误导性。
7. **【可选】** 收窄 qlib 全局拦截到 e2e 作用域，恢复 `test_result_converter.py` 的 unit 收集。

---

## 五、未覆盖项（需联网/实时数据）
- `tests/integration`、`tests/e2e`：依赖 Wind MCP / Bloomberg API / GLM API，当前连接器全部 `disconnected`，未运行。
- `--live` / `--ai-decision` / `--etf-flow` 等实时模式：需数据源上线后方可端到端验证。

> ⚠️ 以上内容由 AI 基于公开信息整理生成，仅供参考，不构成任何投资建议或个股推荐。投资有风险，决策需谨慎。
