# weight_diff_l2 阈值 0.30 口径复核材料(2026-09-08)

> type: decision_material · 状态: **待业务拍板**(不擅自改阈值)
> 结论沉淀: cairn/shadow-30day-validation.md · LOG: 2026-09-08 条目
> 复核触发: LOG 09-08 审查口径风险登记 + 09-13 评估窗启动前必答

## 一、阈值消费点(代码事实)

`scripts/launch_shadow_30day.py:412-431` `_check_fail_fast()`:

```python
# MVSK: weight_diff_l2 健康值 ~0.0-0.1, >0.30 = 组合权重完全背离
MVSK_DIFF_THRESHOLD = 0.30     # ← 复核对象
QLIB_DIFF_THRESHOLD = 0.80

if daily_result.mvsk_weight_diff_l2 > MVSK_DIFF_THRESHOLD:
    # → fail-fast latch(一次性), 30 天验证 overall_pass=False, 需人工介入重跑
```

注意: `shadow_30day_evaluator.py` 的 MVSK 判定(Δ夏普>0 + 换仓成本)不含 L2 阈值;
L2 只进 fail-fast latch(launcher 每日检查)→ 一次误触发即杀死整个 30 天窗口。

## 二、阈值制定口径 vs 当前口径(问题本质)

| 项 | 阈值制定时(≤09-06) | 当前(治理⑤ 09-07 起) |
|---|---|---|
| L2 坐标系 | 全组合含防御仓(511010 国债 ~51.5%) | **仅可优化子集归一化**(权重和=1.0) |
| 防御仓贡献 | 单点 0.4154(恒定本底, 曾致 L2 恒 ~0.45 误报) | 0(已剔除) |
| 同一物理分歧的 L2 | 基准 | **放大 ≈ 1/subset_total 倍**(LOG 估 4.25×) |
| "健康值 ~0.0-0.1"注释 | 基于旧口径/等权合成数据(L2=0.0) | **已失真** |

数学: 子集归一化 w'ᵢ = wᵢ/subset_total, 同一优化器输出差异 Δw 在子集坐标下
Δw'ᵢ = Δwᵢ/subset_total → L2' = L2/4.25(若 subset_total≈0.235; 生产实际以
positions.json 为准, 09-07 实测子集 25 标的)。

## 三、实测证据(reports/shadow/mvsk_p5_daily_diff.jsonl)

| 日期 | L2 | 口径 |
|---|---|---|
| 2026-08-19 | 0.0 | 等权合成(无差异, 无参考价值) |
| 2026-08-24 | 0.0 | 同上 |
| 2026-09-04 | 0.0 | 同上 |
| **2026-09-07** | **0.2423** | **真实优化, 子集口径(25 ETF)** |

**0.2423 = 阈值 0.30 的 80.8%**。子集口径健康值一个样本就贴线, 单日收益率矩阵
扰动(优化器敏感性)即可能越线 → 09-13 窗内误触发概率显著。

## 四、选项(供拍板)

| 选项 | 内容 | 优点 | 风险 |
|---|---|---|---|
| **A(推荐)** | 0.30 → **0.50**, 注释同步改口径说明 | 0.2423 实测 + 4.25× 放大 → 旧口径等价 ~0.118, 0.50/4.25≈0.118 恰在旧健康区间上限; 仍远低于完全背离(L2≈1.0) | 掩盖真实大分歧的灵敏度略降(可用 max_weight_diff_l2 报表补偿) |
| B | 维持 0.30, 观察窗内实测 | 不改代码 | 一次误触发 latch → 30 天验证作废重跑(时间成本 > 冻结窗纪律) |
| C | 动态阈值(滚动均值 + kσ) | 自适应 | 工程复杂; 冻结窗前不引入新机制 |

## 五、拍板后执行清单

1. `scripts/launch_shadow_30day.py:424` 阈值与注释修改(选项 A 为一行)
2. `cairn/shadow-30day-validation.md` §3.3 追记口径说明(更正注记, 不静默覆盖)
3. `tests/unit/` 若有 `_check_fail_fast` 阈值断言同步更新(grep `MVSK_DIFF_THRESHOLD`)
4. LOG 登记决策编号

## 六、拍板结果与复审安排(2026-09-08 拍板: 暂缓, 选项 D)

**决议**: 阈值暂不改动; 再收集一周 shadow 真实数据后复审。
**复审截止**: 2026-09-12 EOD (09-13 评估窗启动前必须完成决策, 无再延空间)。

### 复审判据 (数据驱动, 复审日执行下方命令出数)

新增样本 = 09-09 ~ 09-12 各交易日 EOD 的 `mvsk_p5_daily_diff.jsonl` 记录
(全部为治理⑤后子集口径 + 真实优化路径, 与 09-07 的 0.2423 同口径可比):

| 复审观测 | 结果 → 决策 |
|---|---|
| 样本 max < 0.30 且 mean ≤ 0.25 | 0.30 勉强可行 → 仍推荐 A (0.50), B 需接受贴线运行风险 |
| 样本 max ≥ 0.30 (≥1 次越线, 排除数据异常日) | **0.30 实证不可行** → 强制 A (0.50) 或同步调优 max_tracking_error |
| 样本 mean ≤ 0.10 (旧注释健康区间) | 口径放大假设过悲观 → 维持 0.30 (B) 有据 |
| 样本 < 2 条 (数据缺口) | fail-closed: 优先补数据链路, 决策顺延需升级至 ROADMAP DECISION NEEDED |

### 复审一键出数命令 (只读, 09-12 EOD 后执行)

```powershell
Get-Content reports/shadow/mvsk_p5_daily_diff.jsonl |
  Where-Object { $_ -match '"date": "2026-09-(09|10|11|12)"' } |
  ForEach-Object { ($_ | ConvertFrom-Json) } |
  Select-Object date, weight_diff_l2 |
  Format-Table -AutoSize
# 判读: 对照上表四行 → 按材料§四选项执行§五清单
```

> 拍板: 用户 2026-09-08 · 关联: ROADMAP DECISION NEEDED · 复审责任: 下次会话/09-12 EOD

> **2026-09-08 二次拍板更正**: 用户越过复审窗口直接执行 **选项 A**
> (MVSK_DIFF_THRESHOLD 0.30→0.50 已落地, 含注释/测试/知识文档四件套,
> 见 `cairn/LOG.md` 2026-09-08 条目)。**本节暂缓复审安排作废**;
> §四/§五 判据与清单保留作为决策链路存档。

