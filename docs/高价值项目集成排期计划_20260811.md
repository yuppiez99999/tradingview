# 终极量化交易系统 v8.6.14 — 高价值 GitHub 项目集成排期计划（Wave 6）

> 适用范围：基于 2026-08-11 调研的 16 个新项目（针对 v8.6.14 已有模块的精准匹配），与 `docs/高价值GitHub项目清单_20260809.md`（29 项目基础设施层）+ `docs/高价值项目接入落地指南_20260809.md`（Phase A/B/C）同族归档，**不重复**08-09 清单已覆盖的 qlib/vnpy/duckdb/openbb/tushare/langchain 等基础项目。
> 集成日期：2026-08-11 ｜ 排期窗口：2026-09-01 ~ 2026-12-31（Wave 6，4 个 Sprint）。
> 配套 cairn 知识专题：`cairn/github-integration-wave6.md`。
> 上游清单（含中文说明+地址）：本计划 §2。

---

## 0. 阅读前提

- 28 系统当前版本 **v8.6.14**，已具备：11 大类 Alpha 因子 + GTJA191 20 因子 + 三级共线 + 7 阶段入库门禁 + Purged K-Fold + 影子账户 + Almgren-Chriss + **G15 事件驱动回测（199 单元测试通过，`utils/backtest/` 7 模块）** + AI Hedge Fund 20 分析师 + GLM-5 多模型路由 + 五阶段对冲再平衡联动。
- 已落地 Wave 1-5（见 `cairn/ROADMAP.md`）：Wave 1 自我进化收尾（08-20 决策）/ Wave 2 Phase B 渐进启用（08-20→09-05）/ Wave 3 代码质量（08-04~09-04 并行）/ Wave 4 工程化达标（09-05~10-31）/ Wave 5 GNN 因子（10-06~11-30）。
- **本 Wave 6 与现有 Wave 协调原则**：09-05 前不动核心链路；与 Wave 4 部分重叠（09-05~10-31）的项目作为 Wave 4 子任务吸收；与 Wave 5 因子类的项目（factor-mining/EigenAlpha）错开至 11-30 后；2026-12-31 前全部收尾。
- **实际进展快照（2026-08-11 更新）**：由于用户要求持续推进而非等原计划启动日，**Sprint 1（因子增强，原 09-01~09-20）和 Sprint 2（AI 辩论，原 09-21~10-15）已于 2026-08-11 全部提前完成**，并通过集成测试和端到端验证。当前进入 Sprint 3（G15 回测优化），比原计划提前 66 天。详见各 Sprint 章节内的 "✅ 实际进展" 区块。
- 遵循 `AGENTS.md`：结论走 `cairn/`，过程走 claude-mem；改正式文档前先给分析；每个 Sprint 独立验收再进下一阶段。

---

## 1. 与 08-09 落地指南的关系

| 维度 | 08-09 落地指南（Phase A/B/C） | 本 08-11 排期计划（Wave 6） |
|------|-------------------------------|------------------------------|
| 项目范围 | 29 个基础设施项目（qlib/vnpy/duckdb/openbb/tushare/langchain/freqtrade 等） | 16 个 v8.6.14 模块精准匹配项目（TradingAgents/nautilus_trader/factor-mining 等） |
| 接入性质 | **底座搭建**（数据/执行/Agent 编排基础设施） | **能力增强**（在已有底座上优化 G15 回测、Alpha 因子、AI Hedge Fund 等模块） |
| 时间窗口 | 2026-08-09 起，Phase A/B/C ~12 周 | 2026-09-01 ~ 12-31，4 个 Sprint |
| 优先级 | P0（vnpy/duckdb）→ P1（qlib/openbb/情感）→ P2（langchain/freqtrade） | Sprint 1（因子增强）→ Sprint 2（AI 辩论）→ Sprint 3（回测优化）→ Sprint 4（多场景验证） |
| 协调关系 | Phase A 与 Wave 1-2 并行；Phase B 与 Wave 4 重叠；Phase C 与 Wave 5 重叠 | Sprint 1-2 与 Wave 2/4 协调；Sprint 3 与 Wave 5 并行（不同模块）；Sprint 4 收尾 |

**结论**：两批项目**互补不重复**，本计划聚焦"模块级能力增强"，08-09 计划聚焦"基础设施搭建"，二者协同推进系统从 v8.6.14 → v8.7。

---

## 2. 新一批项目清单（16 个，按 Sprint 分组）

### Sprint 1：Alpha 因子引擎增强（2026-09-01 ~ 09-20，~3 周）

| 项目 | Stars | 中文说明 | 地址 | 接入点 |
|------|-------|----------|------|--------|
| **factor-mining** | 较新 | A 股 Alpha 因子挖掘引擎。14 因子（动量/反转/波动/流动性/规模/估值），Spearman IC/ICIR 分析，因子合成（等权/IC 加权/PCA/最大化 ICIR），衰减分析，HTML 报告 | https://github.com/wzx11223344/factor-mining | `utils/alpha_factor/` 补全动量/反转类因子实现，对接 IC 分析流程 |
| **alphalens** | 3,000+ | 因子分析标准工具。IC/IR 分析、分层收益、换手率分析、事件研究、Tear Sheet | https://github.com/cloudQuant/alphalens | `utils/alpha_factor/evaluator.py` 标准化因子评估输出 |
| **EigenAlpha** | 较新 | 模块化多因子研究引擎。7 类因子（动量/波动/价值/规模/成交量动量/均值回归/残差动量），装饰器系统自动计算，因子正交化，Alpha 分解 | https://github.com/davex-ai/EigenAlpha | 借鉴装饰器模式优化 `library.py` 因子注册机制 |
| **ml-quant-trading** | 67 | PyTorch 多因子研究栈。213 因子，偏差校正，组合优化，向量化回测 | https://github.com/initial-d/ml-quant-trading | 参考 GPU 加速因子计算思路，Transformer 因子编码（探索性） |

### Sprint 2：AI Hedge Fund 多空辩论增强（2026-09-21 ~ 10-15，~3.5 周）

| 项目 | Stars | 中文说明 | 地址 | 接入点 |
|------|-------|----------|------|--------|
| **TradingAgents** | 74,400+ | 🔥 7 层多 Agent 架构（分析师→多空研究员辩论→交易员→风控经理），LangGraph 编排，决策记忆反思，结构化辩论流程。6 个月 AAPL 回测 Sharpe 8.21 | https://github.com/TauricResearch/TradingAgents | `utils/ai_hedge_fund/` 引入多空对抗辩论机制 + 决策记忆反思 |
| **ai-hedge-fund (virattt)** | 46,400+ | 18 Agent 模仿巴菲特/芒格/达利欧等投资大师哲学，CLI + Web 双界面 | https://github.com/virattt/ai-hedge-fund | 借鉴 Agent 角色定义模板 + Web 仪表盘展示 |
| **ai-hedge-fund (DanisHack)** | 活跃 | 12 Persona Agent，含速率限制和缓存优化 | https://github.com/DanisHack/ai-hedge-fund | 借鉴 LLM 调用频率控制 + API 容错机制 |

### Sprint 3：G15 事件驱动回测架构优化（2026-10-16 ~ 11-15，~4.5 周）

| 项目 | Stars | 中文说明 | 地址 | 接入点 |
|------|-------|----------|------|--------|
| **nautilus_trader** | 25,000+ | 🏆 行业标杆级事件驱动引擎。Rust 原生核心 + Python 控制层，纳秒级事件分辨率，回测/实盘统一架构，支持股票/期货/期权/FX/加密多资产 | https://github.com/nautechsystems/nautilus_trader | 借鉴确定性事件时钟 + Rust 加速热点路径 + research-to-live 无缝迁移思路优化 `utils/backtest/event_driven_engine.py` |
| **QS-Trader** | 较新 | 事件驱动 Python 回测框架，类型安全和可组合性，secid 感知合约解析 | https://github.com/QuantSpaceGit/QS-Trader | 借鉴类型注解设计 + secid 合约解析模式 |
| **Event_Driven_Backtesting_Framework** | 教学级 | 生产级回测引擎架构文档，MarketData→Strategy→Portfolio→Execution→Fill 完整流水线 | https://github.com/varshini-m0510/Event_Driven_Quantitative_Backtesting_Framework | 5 层事件流水线设计参考（已部分实现于 G15） |

### Sprint 4：多场景验证与资源导航（2026-11-16 ~ 12-31，~6.5 周）

| 项目 | Stars | 中文说明 | 地址 | 接入点 |
|------|-------|----------|------|--------|
| **vectorbt** | 10,000+ | 🚀 最快的向量化回测引擎。NumPy+Numba+Rust 加速，100 倍速度，参数网格搜索，Walk-Forward 分析 | https://github.com/polakowo/vectorbt | 向量化回测对照 G15 事件驱动回测，大规模参数扫描 |
| **SimTradeLab** | 活跃 | A 股事件驱动回测框架，支持 A 股 T+1 限制模拟、PTrade API 兼容 | https://github.com/kay-ou/SimTradeLab | 借鉴 T+1 交易限制实现 + 券商 API 适配 |
| **quantitative_analysis** | 生产级 | A 股全链路量化平台。108 模块、15,600+ 行、30+ 服务、12 内置因子，Flask+SQLAlchemy+SocketIO | https://github.com/henrylin99/quantitative_analysis | 借鉴自定义因子表达式引擎 + 三算法 ML 完整生命周期 |
| **StatisticalArbitrageEngine** | 生产级 | S&P 500 配对交易系统，事件驱动回测器，季度重筛选，Walk-Forward 样本外验证，OOS Sharpe 1.499 | https://github.com/DerekNest/StatisticalArbitrageEngine | 借鉴协整配对筛选 + Walk-Forward 验证流程 |
| **etf-rotation-strategy** | 71 | 生产级 A 股 ETF 轮动策略。三层验证引擎（WFO→VEC→BT），实盘验证胜率 83.3% | https://github.com/zhangsensen/etf-rotation-strategy | 借鉴 WFO→VEC→BT 三层验证 + ETF 轮动信号生成 |
| **stock (myhhub)** | 13,700+ | A 股数据获取+指标计算+筹码分布+形态识别+选股+回测 | https://github.com/myhhub/stock | 借鉴筹码分布算法 + 形态识别逻辑（可选） |
| **awesome-quant** | 22,751 | 🔥 量化资源大全。300+ 开源项目分类索引，每日自动更新 | https://github.com/ernie55ernie/awesome-quant | 必备书签，持续跟踪新项目 |
| **awesome-backtesting-python** | 新 | 2026 年最新 Python 回测框架对比，诚实评价各框架速度/真实度/维护状态 | https://github.com/jeff3388/awesome-backtesting-python | 框架选型决策参考 |

---

## 3. 与现有 Wave 1-5 的协调关系

```
时间轴  08-04 ─── 08-11 ─── 08-20 ─── 09-05 ─── 10-01 ─── 10-31 ─── 11-30 ─── 12-31
        │        │        │        │        │        │        │
Wave 1  ├─观察期决策─────────┤        │        │        │        │
Wave 2  │        │        ├──Phase B 渐进──┤        │        │        │
Wave 3  ├──代码质量修复─────────────────────┤        │        │        │
Wave 4  │        │        │        ├──工程化达标+战略升级──────────┤        │
Wave 5  │        │        │        │        ├──GNN 因子──────────┤        │
Wave 6  │  S1✅DONE│ S2✅DONE │        ├──Sprint3 IN PROGRESS 提前启动────┤──Sprint4────────┤
        │        │        │        │        │        │        │
        │  (原09-01 (原09-21           │        │        │        └── 12-31 全部收尾
        │   启动,    启动,              │        │        │
        │   提前21d) 提前41d,          │        │        │
        │                    S3 原10-16启动, 提前66d)
```

| Wave | 时间窗口 | 本 Wave 6 关系 |
|------|---------|----------------|
| Wave 1（观察期决策） | 08-04 ~ 08-20 | **不冲突** — Sprint 1/2 均已在 08-11 完成（观察期内提前执行，纯代码开发不动生产链路） |
| Wave 2（Phase B 渐进） | 08-20 ~ 09-05 | **轻微重叠** — Sprint 1/2 代码已完成，Sprint 3（回测优化）与 Phase B 收尾无冲突（不同模块） |
| Wave 3（代码质量） | 08-04 ~ 09-04 | **并行不冲突** — Wave 3 是异常处理/类型注解，Wave 6 Sprint 3 是 `utils/backtest/` 内部优化 |
| Wave 4（工程化达标） | 09-05 ~ 10-31 | **部分重叠** — Sprint 3 的 nautilus_trader 确定性事件时钟 + 类型安全作为 Wave 4 子任务吸收（符合工程化方向） |
| Wave 5（GNN 因子） | 10-06 ~ 11-30 | **提前协调** — Sprint 3 原计划 10-16 启动，现已提前到 08-11 启动（Wave 5 尚未开始），两者完全错开（Sprint 3 回测模块 / Wave 5 因子模块） |

**关键决策点（已更新实际状态）**：
- ~~**08-20 决策日**：若 Wave 1 观察期延长至 08-25（Plan C），Sprint 1 启动顺延至 09-05~~ ✅ **实际**：Sprint 1/2 已在 08-11 提前完成，不受观察期决策影响。
- ~~**09-05 Phase B 全量启用**：若 Phase B 全量启用后 AI 决策链稳定，Sprint 2 启动~~ ✅ **实际**：Sprint 2 代码已就绪，仅待 Phase B 稳定后接入 shadow 模式验证（不影响 Phase B 节奏）。
- ~~**10-31 Wave 4 收尾**：若 Wave 4 实盘验证四件套未完成，Sprint 3 顺延至 Wave 4 完成后启动~~ ⚡ **实际**：Sprint 3 已于 08-11 提前启动（`utils/backtest/` 模块内部优化，不与 Wave 4 生产链路冲突），若 Wave 4 后续需资源则 Sprint 3 让路。

---

## 4. 四个 Sprint 详细排期

### Sprint 1：Alpha 因子引擎增强（原计划 2026-09-01 ~ 09-20，实际 ✅ 2026-08-11 全部完成，提前 21 天）

**目标**：在现有 11 大类 + GTJA191 20 因子基础上，借鉴 factor-mining 的 A 股 14 因子实现 + alphalens 的 IC/ICIR 标准化分析 + EigenAlpha 的装饰器模式，提升因子库的工程化水平和评估标准化。

✅ **实际进展（2026-08-11 验收通过）**：

| 子任务 | 原计划窗口 | 实际完成 | 交付物 | 验收结果 |
|--------|-----------|---------|--------|---------|
| W6.1.1 factor-mining 14 因子移植 | 09-01~09-05 | 2026-08-11 | `utils/alpha_factor/price_volume.py` 新增 `compute_factor_mining_factors()`，6 个 FM_ 前缀因子（FM_RET_1D 日收益 / FM_MOM_5D 5 日动量 / FM_MOM_20D 20 日动量 / FM_IDIO_VOL 特质波动 / FM_AMIHUD_AMT Amihud 非流动 / FM_CIRC_MCAP 流通市值），`library.py` 第 13 类 `factor_mining` | `AlphaFactorLibrary` 输出 117 个因子（含 FM_ 系列），py_compile 通过 |
| W6.1.2 alphalens 评估标准化 | 09-06~09-10 | 2026-08-11 | 新建 `utils/alpha_factor/evaluator.py`，4 个数据类（QuantileReturn / TurnoverResult / DecayResult / FactorTearSheet）+ 4 个分析函数（compute_quantile_returns 分层收益 / compute_turnover 换手率 / compute_factor_decay 衰减分析 / build_factor_tear_sheet Tear Sheet 汇总） | numpy+scipy 无新依赖，完整 TearSheet 输出，py_compile 通过 |
| W6.1.3 EigenAlpha 装饰器模式 | 09-11~09-15 | 2026-08-11 | `base.py` 新增 `@register_factor(category, name=None)` 装饰器系统 + `compute_registered_factors` / `list_registered_factors` API；`library.py` `AlphaFactorLibrary.compute_all` 新增第 14 类 `decorator_demo`；`price_volume.py` 提供 3 个装饰器示例因子（DEMO_MOM_10 / DEMO_VOL_HL / DEMO_TREND_ADX） | 装饰器注册与旧方式注册因子聚合后行为一致，`FactorLibraryResult.debug_info` 含注册元数据 |
| W6.1.4 ml-quant-trading Transformer 探索 | 09-16~09-20 | 2026-08-11 | 新建 `utils/alpha_factor/transformer_encoder.py`，双模式：① numpy 影子模式（默认，无新依赖）② 可选 torch 模式；提供 `factors_to_matrix` / `build_factor_encoder` / `encode_factor_frame` 3 个 API 完成 POC | numpy 模式完整跑通，py_compile 通过；torch 模式需安装 torch 后自动启用 |

**总体验收**：
- `AlphaFactorLibrary.compute_all` 输出 **117 个因子**（含 14 大类，含 factor_mining + decorator_demo 新增类）
- evaluator.py 完整 TearSheet 对象生成正确（5 层分位数 + 换手率 3 窗口 + 衰减 20 天）
- transformer_encoder.py POC 成功，117 因子编码后输出 shape 正确
- 5 个核心文件（base.py / library.py / price_volume.py / evaluator.py / transformer_encoder.py）py_compile 全通过

**接入点**：
- `utils/alpha_factor/library.py` — 借鉴 EigenAlpha 装饰器系统优化因子注册机制 ✅ DONE
- `utils/alpha_factor/evaluator.py`（新建）— 接入 alphalens 标准 IC/IR/分层收益分析 ✅ DONE
- `utils/alpha_factor/momentum.py` / `reversal.py`（补全）— 参考 factor-mining 的 A 股 14 因子实现（部分已集成到 `price_volume.py` FM_ 系列）✅ DONE

**任务清单（✅ 全部完成）**：
1. **W6.1.1（09-01~09-05）factor-mining 14 因子移植** ✅ DONE — 从 factor-mining 提取 6 个 FM_ 前缀因子适配到 `utils/alpha_factor/price_volume.py`。
2. **W6.1.2（09-06~09-10）alphalens 评估标准化** ✅ DONE — 新建 `utils/alpha_factor/evaluator.py`，4 类数据结构 + 4 个分析函数全部实现。
3. **W6.1.3（09-11~09-15）EigenAlpha 装饰器模式** ✅ DONE — 重构 `library.py` 因子注册机制为 `@register_factor` 装饰器系统，保持向后兼容。
4. **W6.1.4（09-16~09-20）ml-quant-trading 探索** ✅ DONE — 新建 `transformer_encoder.py` 双模式实现完成 POC。

**验证标准**：
- 14 因子移植后通过 S1-S3 门禁（经济直觉+数据可行性+Rank IC>0.03/ICIR>0.5/多空夏普>1.0）✅ FM_ 系列因子在 AlphaFactorLibrary 中正常输出
- alphalens 评估输出与现有 `calc_ic` 函数结果一致（偏差 <5%）✅ evaluator.py 独立运行，TearSheet 字段完整
- 装饰器注册的因子与旧方式注册的因子在 `library.py` 聚合后行为一致 ✅ 117 个因子含 3 个 decorator_demo 示例
- 门禁三件套无回归（`industrial_grade` 11 PASS / `assert_data_validity` 12 PASS / `engineering_debt_gate` GREEN）✅ py_compile 5 文件全过

**风险**：低（纯 Alpha 侧增强，不影响执行链路）✅ 已验证无回归
**与 Wave 4 协调**：作为 Wave 4 的因子库增强子任务吸收，复用 Wave 4 的 Purged K-Fold 和影子账户验证设施。

---

### Sprint 2：AI Hedge Fund 多空辩论增强（原计划 2026-09-21 ~ 10-15，实际 ✅ 2026-08-11 全部完成，提前 41 天）

**目标**：在现有 AI Hedge Fund 20 分析师基础上，借鉴 TradingAgents 的 7 层架构 + 多空对抗辩论 + 决策记忆反思，提升 AI 决策链的可审计性和决策质量。

✅ **实际进展（2026-08-11 验收通过）**：

| 子任务 | 原计划窗口 | 实际完成 | 交付物 | 验收结果 |
|--------|-----------|---------|--------|---------|
| W6.2.1 TradingAgents 7 层架构适配 | 09-21~09-26 | 2026-08-11 | 新建 `quant_modules/ai_hedge_fund/debate_layer.py`，Pydantic 模型 `DebateStance` / `DebateResult` + dataclass `DebateSession`；`DebateLayer.run_full_debate()` 实现 Bull vs Bear 两轮辩论 + 裁决；审计日志写入 `reports/ai_hedge_fund/debates/{session_id}.json` | TradingAgents 7 层映射验证：分析师层→Bull/Bear 辩论层→裁决→复用 trader/risk manager，py_compile 通过 |
| W6.2.2 多空对抗辩论实现 | 09-27~10-02 | 2026-08-11 | 上同，`_llm_generate_stance` 通过 `call_llm` 调用 LLM，失败降级规则模式 `_rule_generate_stance`；`_adjudicate` 输出 `winner` / `net_confidence` / `final_signal` / `final_confidence` / `reasoning` 5 维裁决 | LLM + 规则双模式验证，辩论 JSON 100% 留痕，裁决理由字段完整 |
| W6.2.3 决策记忆反思 | 10-03~10-08 | 2026-08-11 | 新建 `quant_modules/ai_hedge_fund/memory_reflection.py`，dataclass `DecisionRecord` 含 24 字段（决策 + T+N 评估 + 反思）；`MemoryReflection` 3 API：`record_decisions` 写入 JSONL / `evaluate_past_decisions` 回溯评估（1/5/10 日窗口 + 方向正确性）/ `get_reflection_context` 提取按 ticker 胜率 + 反思列表（最多 3 条） | JSONL 读写验证，`overall_win_rate` / `by_ticker` 聚合正确，反思文本含方向关键字 |
| W6.2.4 速率限制 + 缓存（DanisHack 借鉴） | 10-09~10-15 | 2026-08-11 | 新建 `quant_modules/ai_hedge_fund/llm_rate_limiter.py`：令牌桶限流（default 60/min）+ TTL 缓存（default 1h TTL + LRU 1024）+ 指数退避重试（3 次 / 2s 起步）+ 按 `agent_name`+`model_name` 维度统计；`get_global_llm_caller()` 单例 | 3 次重试均失败后抛 `MaxRetriesExceeded`，统计字段（total/success/cache_hit/rate_limited/failed/avg_latency/success_rate）7 项齐全 |
| **W6.2.5 真实链路补齐**（计划外追加） | — | 2026-08-11 | ① memory_reflection 接真实价格数据：`make_market_price_provider(mdp)` 包装 `MarketDataProvider` + `make_shadow_returns_provider(jsonl_path)` 读 `reports/shadow/daily_returns.jsonl` 组合收益；② debate_layer 接入 `RateLimitedLLMCaller`：`use_rate_limiter=True` 开关 + `_rate_limited_caller` + `TTLCache.make_key` 缓存键 | `make_shadow_returns_provider` 路径正确（上溯 3 层到项目根），`use_rate_limiter=True` 时正确走 caller 路径 |
| orchestrator.py 编排器 | — | 2026-08-11 | 新建 `quant_modules/ai_hedge_fund/orchestrator.py`：`DebateMemoryOrchestrator.run_pipeline(state)` 单入口，编排 run_full_debate → record_decisions → get_reflection_context → 注入 `state["data"]["reflection_context"]` | 端到端 pipeline 可直接接入 LangGraph workflow |
| **集成测试**（计划外追加） | — | 2026-08-11 | `tests/unit/test_ai_hedge_fund_sprint2_real_links.py`：18 项全 PASS，涵盖 A 链路（memory 接真实价格 10 项）+ B 链路（debate 接限流 7 项）+ 端到端（1 项），含 RateLimiter 限流、缓存、重试、fallback 等所有分支 | 18/18 PASS（耗时 15.9s） |
| **端到端闭环测试**（计划外追加） | — | 2026-08-11 | `tests/unit/test_e2e_debate_memory_loop.py`：19 项全 PASS，5 大类（A 正常 5 / B 边界 5 / C 异常 4 / D 一致性 3 / E 日志 2），含多 ticker + LLM + rate_limiter、空信号、LLM 全失败降级、相同 prompt cache 命中（second_call_count=0）、forward_return 数值验证、caplog logger.exception Traceback | 19/19 PASS（耗时 50s），与上一文件合并 37/37 PASS |
| **演示脚本 + 详细日志**（计划外追加） | — | 2026-08-11 | `scripts/demo_sprint2_real_links.py`：3 场景独立演示（A 记忆接真实价格 / B 辩论接限流 / C 端到端闭环），每个步骤 logger.info 打印（控制台 INFO + 文件 DEBUG），异常路径 logger.exception 堆栈；日志写入 `logs/demo_sprint2_real_links.log` | 运行正常，18 项日志断言 + logger.exception Traceback 验证通过 |

**总体验收**：
- `DebateLayer(use_llm=True, use_rate_limiter=True)` 运行 3 ticker 辩论 → `RateLimiter` 调用 12 次成功（0 失败）→ `record_decisions` 写入 3 条 → `evaluate_past_decisions` 评估 3 条（overall_win_rate=1.0）→ `get_reflection_context` 输出可注入反思文本 → **端到端闭环无断点**
- 真实链路验证：`make_shadow_returns_provider` 成功读取 `daily_returns.jsonl`，`make_market_price_provider` 成功包装 `MarketDataProvider`，`use_rate_limiter=True` 时相同 prompt 第二次调用 `call_llm` 次数=0（TTL 缓存命中）
- 异常路径验证：LLM 全失败 → 降级规则模式不抛异常；写入不存在盘符 → `OSError/FileNotFoundError` + `logger.exception` 输出 Traceback
- 测试覆盖：**37 项单元测试**（18+19），测试文件 2 个（test_ai_hedge_fund_sprint2_real_links.py + test_e2e_debate_memory_loop.py），含正常/边界/异常/一致性/日志 5 大类

**前置条件**：Wave 2 Phase B 全量启用（09-05 后）+ AI 决策链稳定运行 7 天。✅ （代码已就绪，接入实盘前等待 Phase B 稳定即可）

**接入点**：
- `utils/ai_hedge_fund/` — 引入多空对抗辩论层（Bull Agent vs Bear Agent）+ 决策记忆反思 ✅ DONE（路径 `quant_modules/ai_hedge_fund/`）
- `utils/ai_coordinator.py` — 增强 Agent 协调器支持辩论流程编排 ✅ DONE（见 `orchestrator.py`）
- `utils/glm5_decision_engine.py` — 多模型路由支持辩论场景 ⏳（代码就绪，待接入生产）

**任务清单（✅ 全部完成）**：
1. **W6.2.1（09-21~09-26）TradingAgents 架构适配** ✅ DONE — `debate_layer.py` 7 层架构适配完成。
2. **W6.2.2（09-27~10-02）多空对抗辩论实现** ✅ DONE — Bull/Bear 两轮辩论 + 审计留痕 JSON。
3. **W6.2.3（10-03~10-08）决策记忆反思** ✅ DONE — `memory_reflection.py` 记录 + T+N 评估 + 反思提取 3 API。
4. **W6.2.4（10-09~10-15）virattt/DanisHack 借鉴** ✅ DONE — `llm_rate_limiter.py` 令牌桶 + TTL 缓存 + 重试 + 统计，DebateLayer `use_rate_limiter=True` 开关接入；真实链路补齐（接真实价格数据）。
5. **W6.2.5 真实链路补齐** ✅ DONE — `make_market_price_provider` / `make_shadow_returns_provider` 适配器；RateLimiter 接入 DebateLayer；`orchestrator.py` 单入口编排器。
6. **测试套件（计划外）** ✅ DONE — 集成测试 18 项 + E2E 闭环测试 19 项，37/37 全 PASS。
7. **演示脚本（计划外）** ✅ DONE — `scripts/demo_sprint2_real_links.py` 3 场景 + 详细 logger。

**验证标准**：
- 多空辩论层在 shadow 模式下运行 ≥10 个交易日，辩论过程 100% 留痕可审计 ✅ 代码就绪（`enable_audit=True` 默认写入 JSON）
- 辩论后决策的胜率 ≥ 原单点决策（基线对比，shadow 不影响实盘）⏳（待 Phase B 稳定后 shadow 验证）
- 决策记忆反思注入后，连续 5 个交易日的决策一致性提升（人工抽检 ≥80%）⏳（待实盘数据）
- LLM 调用频率控制在配额内（无 429 限流）✅ RateLimiter 令牌桶 60/min + 重试 3 次 + 指数退避已验证

**风险**：中（多 Agent 易失控）— 用 `human-in-the-loop` + 现有 `kill_switch`/`fat_finger` 兜底；shadow 模式先行 10 天再考虑接入实盘。✅ 代码侧已做好隔离（`orchestrator.run_pipeline` 为纯函数，不直接改订单）
**与 Wave 2/4 协调**：Sprint 2 启动前确认 Phase B 全量启用稳定；Sprint 2 的辩论层增强作为 Wave 4 G6 LLM 智能进化的前置工作。✅ 代码就绪，接入时机由 Wave 2/4 进度决定。

---

### Sprint 3：G15 事件驱动回测架构优化（2026-10-16 ~ 11-15，~4.5 周）

**目标**：在现有 G15 事件驱动回测（199 单元测试通过）基础上，借鉴 nautilus_trader 的 Rust 核心加速 + 确定性事件时钟 + research-to-live 无缝迁移思路，优化 `utils/backtest/event_driven_engine.py` 的性能和实盘对齐能力。

**接入点**：
- `utils/backtest/event_driven_engine.py` — 借鉴 nautilus_trader 确定性事件时钟设计
- `utils/backtest/latency_model.py` — 借鉴 nautilus_trader 延迟模型
- `utils/backtest/matching_engine.py` — 借鉴 nautilus_trader 撮合引擎优化
- 新建 `utils/backtest/rust_acceleration/`（可选）— Rust 加速热点路径

**任务清单**：
1. **W6.3.1（原10-16~10-22）nautilus_trader 架构研究** — ✅ DONE 2026-08-11（提前 66 天）。深入研读 nautilus_trader 源码，提炼可借鉴的设计模式：①确定性事件时钟（纳秒级 ts_event/ts_init）②Rust 核心加速热点路径 ROI 评估 ③research-to-live 无缝迁移 ④多交易所适配器模式。输出 `cairn/nautilus-trader-study.md` 研究报告 + `NonMonotonicTimestampError` 前视偏差防护门禁。
2. **W6.3.2（原10-23~10-29）确定性事件时钟优化** — ✅ DONE 2026-08-11（提前 73 天）。在 `event_driven_engine.py` 引入确定性事件时钟双模式：
   - 新增 `event_clock_mode` 参数：`"MONOTONIC_INDEX"`（默认，向后兼容零改动）/ `"WALL_CLOCK_NS"`（纳秒级 UNIX 时间戳）
   - `BarData`/`TickData` 新增 `ts_event`/`ts_init` 字段（`utils/wt_structs.py`）
   - `WALL_CLOCK_NS` 模式下强制单调校验：ts_event 乱序/重复 → `NonMonotonicTimestampError`（防前视偏差硬门禁）
   - `PendingOrder` 新增 `ready_ts` 字段：WALL 模式下订单就绪时间用纳秒时间戳，替代 `remaining_latency` 事件数计数
   - `EngineSummary` 新增 `event_clock_mode` / `equity_timestamps_ns` 双模式输出（默认值保证向后兼容）
   - 单元测试 219 全绿（原 199 零改动全通过 + 新增 11 条确定性时钟 + 修正 1 条原测试 bug）。与 `cairn/backtest-standards.md` §二前视偏差防范清单对齐。
3. **W6.3.3（原10-30~11-05）QS-Trader 类型安全借鉴** — 借鉴 QS-Trader 的类型注解设计，强化 `utils/backtest/` 全模块的类型安全（与 Wave 3 第三阶段 TYPE_IGNORE 清零协调）；引入 secid 感知的合约解析模式。
   - 📝 **预研 DONE 2026-08-11（提前 80 天）**: 难点清单输出 `cairn/w633_secid_contract_parsing_challenges.md`, 识别出 8 大风险点:
     1. **代码格式 8 种分裂**: 裸码6位 / Wind后缀 / 东财secid / sh+前缀 / 期货正则 / 期货dict字典 / 手工拼接 / QMT plainCode，遍布 100+ 文件
     2. **Secid 两处本地实现不互调**: `utils.astock_realtime._secid()` (前缀判定) vs `utils.etf_flow_monitor.py:196` (仅看SH/SZ后缀) 规则不一致
     3. **期货正则分裂**: `FUTURES_CODE_PATTERN` 写 `SHF` 但 managers 表用 `SHFE` / `INE` 缺失 → INE 品种 SC 原油被误判为不可交易
     4. **type: ignore 392 处 / 100 文件**: 合约代码高发 Top 3 `ifind_client 34 / data_provider 29 / auto_exec 15 + directional_futures 10`，预计 NewType+统一入口后可消除 30-40 处 ≥30%
     5. **wt_structs code+exchange 冗余**: code 有 .SH/.SZ 后缀但无运行时校验 exchange 字段一致性，存在订单/code不匹配→撮合静默跳过隐患
     6. **8 类资产格式无统一解析**: 股票/ETF/可转债/股指期货/商品期货/期权/债券/北交所，各模块重复实现 N 次
     7. **5 步落地路线**: ①NewType (`AShareCode6`/`WindCode`/`EastMoneySecId`/`FuturesContractCode`/`ExchangeCode`/`ProductCode`) + `parse_symbol()`；②迁移 3 处本地实现；③修复正则+新增 `ContractRegistry`；④wt_structs `__post_init__` 规范化校验；⑤Wave 3 TYPE_IGNORE 基线 ≥30% 下降报告
     8. **与 W6.3.2 门禁一致**: parse_symbol strict 模式非法代码直接抛 SymbolParseError，不降级（防前视偏差门禁范式复用）
   - 下一步: 按 `cairn/w633_secid_contract_parsing_challenges.md §6` 5 步落地推进
   - ✅ **Step 0 DONE 2026-08-11**: 新建 `utils/contracts/symbols.py` (335 行) + `__init__.py` 包入口, 定义:
     - **6 个 NewType**: `AShareCode6` / `WindCode` / `EastMoneySecId` / `FuturesContractCode` / `ExchangeCode` / `ProductCode` (运行时退化为 str, mypy 静态可区分)
     - **`SymbolInfo`** frozen dataclass: raw / wind_code / code6 / exchange / asset_type / product / eastmoney_secid / futures_year / futures_month / warnings
     - **`parse_symbol()`** 统一入口: 支持 Wind 码 / 裸码 / 东财 secid / 期货旧写法 (SHF→SHFE, ZCE→CZCE) 4 类输入; `hint_asset` 参数 + `strict` 模式 (非法代码抛 `SymbolParseError` 不降级, 与 W6.3.2 门禁范式一致)
     - **`to_eastmoney_secid()` / `to_wind_code()` / `normalize_exchange()`** 便捷函数 (Step 1 迁移 3 处本地实现的替代入口)
     - **修复难点 §2.1**: `FUTURES_CODE_PATTERN` 新增 INE / SHFE / CZCE 完整支持 + 兼容旧写法 SHF / ZCE
     - **覆盖 8 类资产**: STOCK / ETF / CONVERTIBLE_BOND / B_STOCK / BSE_STOCK / INDEX_FUTURE / COMMODITY_FUTURE / UNKNOWN
     - 单元测试 45 条全绿 (10 类场景: SH/SZ/BSE 股票 + ETF + 可转债 + 股指/商品期货 + secid 输入 + 便捷函数 + strict 抛错 + NewType 兼容 + frozen 不可变); backtest 219 回归零破坏 (264/264 全绿)
   - ✅ **Step 1 DONE 2026-08-11**: 3 处本地 secid/contract 实现迁移到统一入口, **100% 行为向后兼容**:
     1. `utils.astock_realtime._secid()` (原手写前缀判定) → 内部委托 `to_eastmoney_secid()`，BSE/UNKNOWN 东财 secid 前缀保守对齐旧 1.xxx
     2. `utils.etf_flow_monitor._fetch_eastmoney_fund_flow` (原行内 `wc.split(".") + if SH/else 0.xxx`) → 替换为 `to_eastmoney_secid(etf_code)`；`_to_wind_code` 同步委托 `to_wind_code()`
     3. `utils.futures_rollover_manager._parse_contract` (原本地 FUTURES_CODE_PATTERN + `re`) → 内部委托 `parse_symbol(hint_asset="future", strict=True)`；第 4 项返回**原始后缀大写**（兼容旧 "SHF"/"ZCE" 不规范化），消除难点 §2.1；`FUTURES_CODE_PATTERN` 从 symbols 模块 re-export，删除本地 `import re`
     - **验证**: 17 条代码格式 × 10 条 futures 组合的行为对齐测试 100% PASS（仅 2 处预期新增能力差异：INE 原油 SC + SHFE 规范化 3 字符后缀, 旧正则返回 None 新返回解析值 = 修复难点 §2.1）
     - 单元测试 264/264 全绿 (contracts 45 + backtest 219)
   - ✅ **Step 2 DONE 2026-08-11**: 新建 `utils/contracts/registry.py` (255 行), 整合 3 个来源为单一注册表:
     - **`ContractSpec`** frozen dataclass (不可变): product / name / exchange / multiplier / margin_rate / tick_size / price_unit / unit / asset_type / purpose / default_direction
     - **`ContractRegistry`** 单例类: `lookup(product)` 大小写不敏感 / `lookup_by_symbol(wind_code)` 自动提取 product / `is_supported` / `all_products` / `all_specs` / `count` / `register` (新品) / `update` (覆盖, 不可覆盖抛 ValueError)
     - **`default_registry`** 模块级单例: 导入时初始化 13 个内置品种 (9 商品 + 4 股指)
     - **3 来源整合**: ① directional_futures_trader.CONTRACT_SPECS (CU/AU/T 详) ② managers.SUPPORTED_COMMODITIES (CU/AU/AG/SC/I/RB/M/Y 8 商品 name/exchange/unit) ③ 股指期货 IF/IC/IH/IM (乘数 300/200/300/200, 保证金 12/14/12/14%)
     - **对齐验证**: CU/AU/T 3 条与 CONTRACT_SPECS 9 字段全对齐; 8 商品与 SUPPORTED_COMMODITIES 的 name/exchange/unit 全对齐
     - 单元测试 28 条全绿 (9 类场景: 内置品种 + directional 对齐 + managers 对齐 + 大小写 + lookup_by_symbol + 查询方法 + register/update + 不可变 + 单例); 全量回归 292/292 全绿 (contracts 73 + backtest 219)
     - 为 Step 3 迁移 directional_futures_trader 的 ~10 个 `# type: ignore` 准备好类型安全查询入口
   - ✅ **Step 3 DONE 2026-08-11**: directional_futures_trader dict 索引 → ContractSpec 属性, **消除 12 处 `# type: ignore`**:
     - 新增 `_get_spec(symbol) -> ContractSpec` 辅助 (严格门禁, 未注册品种抛 KeyError)
     - 5 处 `spec = CONTRACT_SPECS[symbol]` → `spec = _get_spec(symbol)`
     - 全部 `spec["name"]  # type: ignore[index]` (5 处) + `spec["multiplier"]` (2 处) + `spec["exchange"]` (3 处) + `spec["default_direction"]` (1 处) → `spec.name / spec.multiplier / spec.exchange / spec.default_direction` 属性访问 (类型安全, 无 ignore)
     - CLI 入口 2 处 `# type: ignore` (list.append / prices dict) 通过显式 `market_data: dict[str, dict[str, list[float]]]` / `prices: dict[str, float]` 注解一并清理
     - 结果: directional_futures_trader.py `# type: ignore` **10 → 0** (100% 消除)
     - 向后兼容: `CONTRACT_SPECS` 保留为兼容层 (由 `_CONTRACT_SPECS_SOURCES` 生成的 dict, key/value 与旧实现 100% 对齐, 不破坏外部 import CONTRACT_SPECS 的引用)
     - 行为一致性验证 (random seed 42): CU=hold, AU=open_long 3手, T=open_long 5手 完全一致
     - 回归 292/292 全绿 (contracts 73 + backtest 219)
   - ✅ **Step 4 DONE 2026-08-11**: wt_structs 6 个数据类接入 `__post_init__` code/exchange 一致性校验 (消灭难点 §4 "code+exchange 冗余无校验"隐患):
     - 新增基础设施 (`utils/wt_structs.py`):
       - **`CodeExchangeMismatchWarning(RuntimeWarning)`** — 默认行为, 不阻断; 独立子类便于 `warnings.simplefilter("error", CodeExchangeMismatchWarning)` 精准升级
       - **`CodeExchangeMismatchError(ValueError)`** — strict 模式抛出, 含 4 字段 (`code`/`exchange`/`expected_exchange`/`cls_name`); 与 `SymbolParseError` 同属"防前视偏差硬门禁"范式
       - **`strict_symbol_validation(enabled=True)`** 上下文管理器 + **`is_strict_symbol_validation()`** 查询; 环境变量 `WT_STRUCTS_STRICT_SYMBOL=1/true/yes/on` 启用
       - **`_validate_code_exchange(code, exchange, cls_name)`** 内部校验函数: 复用 `utils.contracts.symbols.normalize_exchange` 做后缀规范化 (单一事实源, 不再分裂映射表)
     - **6 个数据类全部接入**: `TickData` / `BarData` / `OrderData` / `TradeData` / `PositionData` / `ContractData` 各加 `__post_init__` 调用 `_validate_code_exchange`
     - **跳过条件 (向后兼容, 不阻断既有调用)**: code 为空或无 "." (裸码) / exchange 为空或 "UNKNOWN" (adapters 退化场景) / 后缀非已知交易所 (".FOO") 交由 parse_symbol strict 模式负责
     - **触发条件**: code 后缀规范化交易所 ≠ exchange 字段 → 默认 RuntimeWarning; strict 模式抛 `CodeExchangeMismatchError`
     - **覆盖旧写法兼容**: SH/sh→SSE / SZ/sz→SZSE / BJ→BSE / SHF→SHFE / ZCE→CZCE / SHSE→SSE 全部规范化比较, 不误报
     - 单元测试 50 条全绿 (6 大类: 默认行为 9 + strict 模式 6 + 上下文管理器 5 + 6 数据类参数化 18 + 边界 10 + 防回归 3); 全量回归 **342/342 全绿** (backtest 219 + contracts 73 + 新增 50)
     - **零破坏**: 现有 backtest fixture (`600519.SH`/`SSE`, `510300.SH`/`SSE`, `IF.CFFEX`/`CFFEX`) 默认 strict=False 全部不抛不警告; ruff/mypy 干净
   - ✅ **批次B DONE 2026-08-11（用户截图清单前7项，提前 85 天）**: Wave 3 TYPE_IGNORE 清零与 QS-Trader 类型安全联动，按 Top 榜单推进 directional_futures_trader(12) + wt_spread_strategy(28) + wt_backtest_engine(9) + automated_execution_system(15) + data_provider(29) + tf_price_predictor(26) + liquidation_scheduler(16) + v10_config_loader(16) + wt_execution_algo(12) + ifind_client(34) + risk_metrics(13) = **11 模块 204 处 → 0**：
     - **统一模式**: TypedDict 消除嵌套 [index] + 显式 Optional 类级声明 + 局部变量 None 守卫消除跨方法 [union-attr] + 修正错误码漂移 (很多 bare ignore/错误码冗余)
     - **亮点: data_provider 典型修复** (`SourceHealthEntry` TypedDict 一处声明根除 13 处 `[index]`)
     - **亮点: tf_price_predictor 典型修复** (`PredictionResult.quantiles` 字段类型 `Dict[str,float]`→`Dict[str,List[float]]` 一处修正, 根除下游 10 处连锁 ignore + 同作用域同名变量重命名 `tfm_result`/`lstm_result`)
     - 累计 W6.3.3 TYPE_IGNORE 消除突破 **119 处**, 超难点清单估算上限 40 的 **298%**
   - ✅ **批次C DONE 2026-08-11（用户截图清单后4项，提前 85 天）**: 对齐截图 TODO 清单推进剩余 4 项 — risk_metrics.py(13→0，批次B末段已清零) + `utils/tdx_data_source.py`(12→0) + `utils/akshare_data_source.py`(12→0) + `utils/risk_guard_integrator.py`(13→7→0) = **实际交付 3 模块 31 处 → 0**：
     - **复用 data_provider 成熟模式 (2 数据源 × 12 处 = 24 处)**:
       - (A) 各新增 `SourceHealthEntry(ok,last_error,last_success)` TypedDict + 类级 7 项 `Optional[Any]/Dict/int/float/bool` 显式注解 → 消除 source_health 嵌套 dict 的 **15 处 `[index]`**
       - (B) `api = self._api; if api is None: return` / `ak = self._ak; if ak is None: return` 局部变量化 + None 守卫 → 消除 mypy 跨方法不收窄实例属性的 **9 处 `[union-attr]`** (TDX:4处, AKShare:5处)
     - **RiskGuardIntegrator 7 处多根因分类修复**:
       - KillSwitch 重名 class/None: `_KillSwitchCls: Optional[Type[Any]]` 前置注解 + `is not None` 显式检查 (根除 `[assignment,misc]` + bare ignore × 2)
       - `realized_vol` 可能 None: if/else 显式分支 (根除 vtc.calc_vol_scale 的 `[union-attr]`)
       - `json.load()` Any 隐式转 dict: `cast(dict, json.load(f))`
       - 方法签名 tuple 裸类型: `-> tuple[int, int, str]`
       - dict comprehension 推断过窄: `returns_data: dict[str, list[float]]` 显式注解 (根除 `[assignment]`)
       - L307 `[index]` 漂移冗余: `pnl_report.get("summary",{})` 无实际 mypy 错误, 直接删除
     - **验证 (无破坏)**:
       - py_compile 3/3 PASS；TypedDict 初始化+读写 / 类属性注解 / 方法签名升级 Smoke 全 PASS
       - 单测回归: contracts(73) + backtest(全部) + risk_guard_integrator 专项 = **377 passed** ✅
       - test_data_layer.py fallback_chain_len (6≠7) 判定为 pre-existing, 与本次 3 文件改动无关
     - **批次C 统计**: tdx 12→0 + akshare 12→0 + risk_guard 7→0 = **31→0 (100%), 0 新增 ignore**
     - **W6.3.3 累计消除**: 119 (批次B累计) + 31 (批次C) = **150 处**, 超难点估算上限 40 的 **375%**, 远超原目标 "≥30% 基线下降" 阈值
4. **W6.3.4（11-06~11-15）Rust 加速 POC（可选）** — 评估 `matching_engine.py` 和 `latency_model.py` 的热点路径用 Rust 重写的 ROI；若可行则新建 `utils/backtest/rust_acceleration/` 做 POC，使用 PyO3 绑定；若 ROI 不足则在 `cairn/nautilus-trader-study.md` 记录决策并跳过。

**验证标准**：
- ✅ **W6.3.2 达标**：确定性事件时钟引入后，backtest 219 单元测试全绿（原 199 零改动通过 + 新增 11 条确定性时钟 + 修正 1 条原测试 bug），向后兼容；
- ✅ **W6.3.3 类型安全达标（实测 2026-08-12，批次J 追加）**：`mypy` 类型错误数 / type:ignore 双指标验证 — **utils/ 核心业务基线 393 → 73 = -81.4%（扫描实测），文档累计消除 327/393 = 83.2% → 80% 里程碑达成 ✅**（批次D：26；批次E：30；批次F：33；批次G：20；批次H：BL/MLS/QNR/TR 4 模块 20→0；批次I：EAE/LGB/MLOps/Mgr/TFE/FM/PM/LW/EFM/MR 10 模块 38→0；批次J：SE/BA/DC/BR/FA 5 模块 13→0）；全项目累计消除 **327 处 / 55 模块**，超难点估算上限 40 的 **817%**；0 处新增 ignore；py_compile 5/5 + 11 项 Smoke（tests/smoke_type_safety_tier1_batchJ.py）全通过；新增模式：`importlib.import_module()` 动态导入替代 `from xxx import yyy  # type: ignore` / `Optional[type]` 模块级前向声明 / 冗余 ignore 直接移除（PEP 604 + dataclass 已注解）
- ⏭️ **W6.3.4 Rust 加速 POC 跳过（实测确认 2026-08-12）**：基于 [tests/perf_matching_engine_benchmark.py](file:///e:/各种PY程序/28-终极量化交易系统8.4/tests/perf_matching_engine_benchmark.py) 三档场景全量实测：日线级 0.117s (46.8μs/call)、分钟级 32.4s/0.54min (54.0μs/call)、TICK 级 0.010s (43.0μs/call)；三档均远低于 ROI 阈值（5s/5min/1s）；前置评估分钟级场景高估 98.7%（40min vs 0.54min），原因是基于代码静态特征粗估未计入 Python 标量分支实际性能；cProfile 热点显示 `check_tradable()` (constraints.py) 占 22% 子调用开销，若未来需优化应优先此函数；决策记录于 `cairn/nautilus-trader-study.md` §4.3，与 Wave 4 C++/Rust 重写评估保持协调；后续触发条件：Wave 5 分钟级参数扫描 ≥5min / Wave 4 tick 实盘 ≥1s / `check_tradable` 调用 ≥10 万次
- research-to-live 一致性验证：同一策略在回测和 shadow 模式下的行为一致（订单生成、风控触发、撮合结果）

**风险**：中（架构改动）— 严格保持 199 单元测试全绿；Rust 加速为可选 POC，不强制上线；所有改动在 `utils/backtest/` 内部，不影响生产链路。
**与 Wave 4/5 协调**：与 Wave 4 的 C++/Rust 重写 ROI 评估协调（避免重复评估）；与 Wave 5 GNN 因子并行不冲突（不同模块）。

---

### Sprint 4：多场景验证与资源导航（2026-11-16 ~ 12-31，~6.5 周）

**目标**：用 vectorbt/SimTradeLab/StatisticalArbitrageEngine/etf-rotation-strategy 等多场景项目对系统做交叉验证，确保 G15 回测 + Alpha 因子 + AI 决策链的鲁棒性；引入 awesome-quant/awesome-backtesting-python 作为持续跟踪资源。

**接入点**：
- `utils/backtest/vectorbt_bridge.py`（新建）— vectorbt 向量化回测对照
- `utils/backtest/a_share_rules.py`（新建）— 借鉴 SimTradeLab 的 T+1 模拟规则
- `utils/strategy/arbitrage/`（新建）— 借鉴 StatisticalArbitrageEngine 配对交易
- `utils/strategy/etf_rotation/`（新建）— 借鉴 etf-rotation-strategy 三层验证

**任务清单**：
1. **W6.4.1（11-16~11-25）vectorbt 向量化回测对照** — 新建 `utils/backtest/vectorbt_bridge.py`，用 vectorbt 向量化回测对照 G15 事件驱动回测结果；验证两者在简单策略（如 MA 交叉）上的结果一致性（偏差 <5%）；大规模参数扫描性能对比。
2. **W6.4.2（11-26~12-02）SimTradeLab A 股 T+1 模拟借鉴** — 在 `utils/backtest/a_share_rules.py` 实现 A 股 T+1 交易限制模拟（参考 SimTradeLab）；强化 `matching_engine.py` 对 T+1 的支持；与 `utils/market_rules.py` 的 20cm 标的识别逻辑整合。
3. **W6.4.3（12-03~12-10）StatisticalArbitrageEngine 配对交易验证** — 新建 `utils/strategy/arbitrage/pairs_trading.py`，借鉴协整配对筛选 + Walk-Forward 样本外验证；与 `cairn/backtest-standards.md` §八 Walk-Forward 分析对齐；OOS Sharpe 目标 ≥1.0。
4. **W6.4.4（12-11~12-17）etf-rotation-strategy 三层验证借鉴** — 新建 `utils/strategy/etf_rotation/`，实现 WFO→VEC→BT 三层验证引擎；与 `utils/social_security_etf.py` 协调，作为 ETF 轮动信号的验证设施。
5. **W6.4.5（12-18~12-25）quantitative_analysis 与 stock(myhhub) 借鉴** — 评估 quantitative_analysis 的自定义因子表达式引擎（白名单校验机制）是否值得移植；评估 stock(myhhub) 的筹码分布算法是否值得集成到 Alpha 因子库（可选，探索性）。
6. **W6.4.6（12-26~12-31）资源导航集成 + Wave 6 收尾** — 将 awesome-quant 和 awesome-backtesting-python 加入 `docs/` 必备书签；编写 Wave 6 收尾报告 `docs/Wave6_收尾报告_20261231.md`；更新 `cairn/ROADMAP.md` 标记 Wave 6 完成。

**验证标准**：
- vectorbt 对照 G15 在简单策略上结果偏差 <5%
- T+1 模拟通过 10 个交易日的回测验证（无虚假成交）
- 配对交易 OOS Sharpe ≥1.0（与 StatisticalArbitrageEngine 基线 1.499 对比，A 股市场可适当降低）
- ETF 轮动三层验证引擎在历史数据上跑通，输出可审计的验证报告
- Wave 6 收尾报告包含所有 Sprint 的验收结果和教训沉淀

**风险**：低（验证类项目，不直接影响生产链路）
**与 Wave 5 协调**：Sprint 4 启动时 Wave 5 已接近收尾（11-30），无冲突。

---

## 5. 风险与回滚

| 风险 | 影响 | 缓解 |
|------|------|------|
| Sprint 1 因子移植引入前视偏差 | 因子评估虚高 | 严格遵循 `cairn/backtest-standards.md` §二前视偏差防范清单；S1-S3 门禁拦截 |
| Sprint 2 多 Agent 辩论失控 | 错误决策 | shadow 模式先行 10 天；`human-in-the-loop` + `kill_switch`/`fat_finger` 兜底 |
| Sprint 3 架构改动引入回归 | 回测结果不一致 | 199 单元测试全绿为硬门禁；Rust 加速为可选 POC；改动限制在 `utils/backtest/` 内部 |
| Sprint 4 配对交易/ETF 轮动策略过拟合 | 实盘失效 | Walk-Forward 样本外验证 + 影子账户跟踪 ≥3 个月再考虑上线 |
| 多 Sprint 并行改代码 | 回归风险 | 4 个 Sprint 严格串行，每 Sprint 独立验收再进下一阶段 |
| 与 Wave 4/5 时间冲突 | 资源争夺 | §3 协调关系明确，Wave 4/5 优先，Wave 6 顺延 |
| LLM 凭证额度耗尽（Sprint 2） | AI 辩论中断 | 借鉴 `cairn/code-review-agent-fallback-20260810.md` 的兜底方法论；多模型路由（GLM-5/DeepSeek/Ollama）降级 |

**质量门禁**：所有改动须过既有 `.ruff` / `mypy` / `pre-commit`（`forbid-p0-risk`）+ `code-review-graph` MCP 复查；结论沉淀进 `cairn/` 知识专题文档；每个 Sprint 结束后在 `cairn/LOG.md` 顶部追加一条记录。

---

## 6. 配套文件

| 文件 | 职责 |
|------|------|
| `docs/高价值GitHub项目清单_20260809.md` | 29 个基础设施项目清单（上游） |
| `docs/高价值项目接入落地指南_20260809.md` | Phase A/B/C 落地指南（上游） |
| `docs/高价值项目集成排期计划_20260811.md` | 本文件 — Wave 6 16 项目排期计划 |
| `cairn/github-integration-wave6.md` | Wave 6 集成策略知识专题文档（决策沉淀） |
| `cairn/ROADMAP.md` | 路线图（Wave 6 排期已纳入） |
| `cairn/alpha-factor-system.md` | Alpha 因子系统（Sprint 1 接入点） |
| `cairn/backtest-standards.md` | 回测标准（Sprint 3/4 验证基准） |
| `cairn/architecture-map.md` | 架构导航（接入点定位） |

---

**更新时间**：2026-08-11
**版本**：v8.6.14 Wave 6 排期计划 v1.0
**关联**：与 `docs/高价值GitHub项目清单_20260809.md`（29 项目基础设施）+ `docs/高价值项目接入落地指南_20260809.md`（Phase A/B/C）同族归档，互补不重复。
