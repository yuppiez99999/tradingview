# 8/18 前工作计划（2026-08-08 ~ 08-17）

> 目标：在 github_trending Phase 1（loopx，8/18 起）之前，完成 v9.2 工业级 Phase 2 执行闭环补齐、观察期收尾、遗留项清理，为 Phase 1 和 Phase 3 做好前置准备。
> 依据：`docs/WORK_PLAN_v9.2_工业级达标_20260806.md`、`docs/计划同步对齐_20260807.md`、`docs/今日工作总结_20260807.md`。

---

## 一、总览甘特

```
任务\日期         8/8  8/9  8/10  8/11  8/12  8/13  8/14  8/15  8/16  8/17
观察期自动推进     █    █    █    █(满14)
G1 QMT接线                ██████████████(3天)
G2 再平衡撮合                          ████████(2天)
G4 成交回报PnL                                    ████████(2天)
TDAM固化+增量      █
C2导入路径加固     █
loopx调研/方案                                        ██████
Phase3前置(G5调研)                                          ████
```

---

## 二、分块任务清单

### A. 观察期自动推进（每天，无需手动）
| 日期 | 预期进度 | 说明 |
|------|----------|------|
| 8/8 | 11/14 | 任务 `v84_ObservationBriefing` 16:10 自动写入（已注册） |
| 8/9 | 12/14 | 同上 |
| 8/10 | 13/14 | 同上 |
| 8/11 | **14/14 观察期满** | 达到 `min_required=20` 下的 14 天观察下限，`observation_complete=true` |
| 8/12 起 | 进入演化阶段 | `next_action` 从"等待数据"切换为可评估 |

**注意**：`data_sufficient` 当前 false（shadow_data.total_days=10，min_required=20），但观察期以 `observation_total=14` 为界。8/11 后 `USE_DRIFT_DETECTOR`/`USE_STRATEGY_EVALUATOR` 可实际触发。

### B. v9.2 Phase 2：执行闭环补齐（08-09 ~ 08-15，核心工作）
> 目标：从"生成订单但不执行"→"订单→撮合→成交→持仓→PnL"完整闭环。**建仓期结束后执行**。

| 子任务 | 排期 | 问题 | 关键步骤 | 验收 |
|--------|------|------|----------|------|
| **P2.1 G1 QMT 真实下单接线** | 8/9-8/11（3天） | `qmt_broker.py` 已实现但 `broker.enable=false`，主链路用 MockBroker | ①确认 QMT 客户端/xtquant/账号 ②`daily_trade_executor.py` 用 `QmtBrokerAPI` 替换 MockBroker ③QMT 不可用降级 MockBroker ④dry_run 3天→10%资金3天→全量 ⑤成交回报写 `reports/fills/{date}.json` | 真实下单 1 笔（最小手数），成交回报回写 positions.json + fills JSON |
| **P2.2 G2 再平衡执行撮合** | 8/12-8/13（2天） | `rebalance_execution_orders.py` 只有 generate 无 execute/fill/place_order | ①新增 `RebalanceExecutor` ②经 QmtBrokerAPI/SimulatedBroker 撮合 ③成交回报写 `reports/rebalance_fills_{date}.json` ④更新 positions.json ⑤集成 `daily_workflow.py` 再平衡阶段 | generate→execute→positions 更新→fills 落盘完整闭环 |
| **P2.3 G4 成交回报驱动 PnL** | 8/14-8/15（2天） | PnL 从行情估算，非成交回报驱动 | ①定义 `FillsStore` 接口（`reports/fills/{date}.json` 唯一事实源）②`daily_pnl.py` 从 FillsStore 计算已实现 PnL ③未实现 PnL 仍行情估算 ④`assert_data_validity.py` 加断言 `sum(fills.pnl)==daily_pnl.realized_pnl` | 已实现 PnL = fills PnL 之和，无偏差 |

**Phase 2 里程碑（08-15）**：工业级判据 C1 执行闭环从 WARN→PASS；`industrial_grade_check.py` 无 FAIL。

### C. 今日遗留项清理（8/8，优先级高）
| 项 | 说明 | 状态 |
|----|------|------|
| **TDAM team/agent 固化** | `.env` 固化 `TDAM_TEAM_ID=team-n5phx61z0a` / `TDAM_AGENT_ID=agt-n5ppojc8rj` / `TDAM_BASE_URL=http://127.0.0.1:8420`；`phase1p_generate_cache.py` + `import_cairn_to_tdam.py` 加载 .env 并修正 base-url 默认端口（8125→8420） | ✅ **已完成**（TDAMConfig 自动读取，端到端检索验证通过） |
| **C2 导入路径加固** | `automated_execution_system.py` 由 sys.path 兜底改为 importlib 从 `_PROJECT_ROOT` 显式加载 `hedge_execution_orders.py`，不再污染 sys.path | ✅ **已完成**（签名/返回元组验证 + sys.path 未污染确认） |
| **H2 iFinD 残留清理** | `data_layer.py` docstring、`connectors.py` P0 注册块、统一入口注释、`data_cleaning.py` 多源校验 | ✅ **已完成**（附带修复 G14 死代码，见下） |

**H2 清理详情（8/7 完成）**：
1. `quant_modules/connectors.py`：删除 iFinD MCP（优先级 100）注册块及其 docstring，序号重排为 Wind MCP(200)→通达信(300)→新浪(400)→本地缓存(500)。实测注册链 `[通达信(300)]`，无 iFinD。
2. `quant_modules/data_layer.py`：两处 docstring 示例由 `'iFinD MCP'` 改为 `'Wind MCP'`。
3. `量化策略系统_统一入口_v8.6.py`：4 处注释（模块头 ×2、连接器注册、主连接器激活）更新为真实优先级链。
4. **`utils/pipeline/data_cleaning.py`（附带修复真实缺陷 G14）**：`_fetch_multi_source_prices` 原引用 `DataProvider.get_price` / `get_ifind_price` / `get_akshare_price` **三个符号全部不存在**，ImportError 被 except 静默吞掉，导致 `prices` 恒为空、`n_sources` 恒为 0，多源交叉校验实际从未生效。改为遍历 `MarketDataProvider` 上真实存在的 `_try_wind_mcp_realtime` / `_try_tdx_realtime` / `_try_akshare_realtime` / `_try_sina_http_realtime` 四通道逐源取数（`index_price` 字段），单源失败不影响其余源。
   - 实测（600900 长江电力）：`{'wind_mcp': 27.75, 'tdx': 27.75, 'sina': 27.75}`，`n_sources=3`，偏离度 0%。
   - 该修复直接推进 v9.2 差距项 **G14「多源交叉校验弱」**，根因确认为死代码而非源数量不足。

**未纳入本次清理（有守卫，非死代码）**：`utils/etf_flow_monitor.py:101`、`ms_strategy/src/monitoring/intraday_monitor.py:404` 引用的 `IFindClient` 类真实存在且由 `IFIND_TOKEN` 环境变量守卫（未设置则跳过），行为安全；`_archive/`、`research/`、`realtime_monitor/` 下的引用属独立重构范围。

### D. loopx 接入准备（8/16-8/17）
> github_trending Phase 1 前置调研，为 8/18 接入 `live_scheduler` 做准备。
- 调研 loopx 的「持久目标 + 配额感知自动唤醒 + 可验证交接」机制如何增强长跑健壮性
- 评估接入 `live_scheduler` 的 shadow 运行方案（feature flag，不干扰实时调度）
- 产出接入方案设计 + shadow 运行验收标准（7 天长跑无状态丢失）

### E. Phase 3 前置准备（8/16 起）
- **G5 调研**：定位 `portfolio_optimizer.py` L460、`auto_factor_factory.py` L400/434/440/584 的 6 处 `import research.*` 具体引用
- 为 8/16-8/22 的 Phase 3（环境隔离）做前置识别

---

## 三、依赖与风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| QMT 客户端/xtquant 环境未就绪 | P2.1 阻塞 | 8/8 先做环境核查；不可用则先用 SimulatedBroker 验证逻辑，QMT 后补 |
| 观察期 8/11 前数据不足 | 演化阶段延后 | 观察期以 14 天为界，8/11 自动满足；`min_required=20` 为 shadow 数据下限非硬阻塞 |
| G4 改 PnL 事实源 | 与现有行情 PnL 报告冲突 | 分阶段：已实现 PnL 切 fills，未实现 PnL 暂留行情，逐步迁移 |
| loopx 提前调研超出范围 | 8/18 排期冲突 | 只做方案设计不落地，验收标准明确为 8/18 后 shadow 运行 |

---

## 四、验收清单（8/18 前）

- [ ] 观察期满 14/14（8/11）
- [ ] G1 QMT 接线：真实下单 1 笔 + fills JSON 落盘（8/11）
- [ ] G2 再平衡撮合：RebalanceExecutor 闭环（8/13）
- [ ] G4 成交回报驱动 PnL：已实现 PnL = fills PnL（8/15）
- [ ] `industrial_grade_check.py` C1 PASS、无 FAIL（8/15）
- [x] TDAM team/agent 固化到盘后环境（8/8 已完成，TDAMConfig 自动读取 + 端到端验证）
- [x] C2 导入路径加固（8/8 已完成，importlib 显式加载 + sys.path 未污染）
- [x] H2 iFinD 残留清理（8/7 已完成，4 文件；附带修复 G14 多源交叉校验死代码，实测 3 源生效）
- [ ] loopx 接入方案设计完成（8/17）

---

*计划由 AI 辅助生成。每完成一项更新本文档。与 `docs/WORK_PLAN_v9.2_工业级达标_20260806.md`、`docs/计划同步对齐_20260807.md` 配套。*
