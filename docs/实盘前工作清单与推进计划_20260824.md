# 实盘前工作清单与推进计划 (2026-08-24)

> 计划: go-live-prerequisites-20260824
> 状态: 全部 10 条工作线核查/落地完成
> 目标: 梳理量化系统 v8.6 上实盘前的全部前置工作, 按优先级推进 (不排严格时间表)

## 0. 执行摘要

本轮完成 10 条实盘前工作线, **落地 8 项关键修复/接线, 发现 1 个实盘前 P0 阻断项 (影子无真实撮合), 记录 3 项 P1 结构债**。全部修复通过回归测试 + ruff 门禁 + assert_data_validity (12 PASS/0 FAIL) 验证。

## 1. 工作线完成状态总表

| # | 工作线 | 状态 | 关键成果 |
|---|---|---|---|
| C1 | 影子真实性核查 | ✅ 完成 | **发现 P0: 影子账户 trade_log 空, NAV 回算无真实撮合** |
| G1 | QMT 真实下单接线 | ✅ 配置层完成 | system_config broker 段补齐 + 四重门控验证 (xtquant 待装) |
| UE-1 | 统一实盘门控 | ✅ 完成 | --hedge/--rebalance-execute 加 TRADING_ENV 双签 + --yes |
| DTE-1 | 建仓接入撮合链 | ✅ 首步完成 | 建仓成交落盘 FillsStore (strategy=build) |
| EX-1 | broker 契约统一 | ✅ 完成 | SmartOrderRouter 运行时契约校验 (fail-fast) |
| B | 风控 fail-close 完整化 | ✅ DTE-6 完成 | --auto-confirm 环境护栏 (RK-1/DTE-8 记录后续) |
| D | 工程 CI/质量 | ✅ 核查完成 | 0 新增违规; 14 存量 ANN 债记录 |
| E | 运维监控部署 | ✅ 核查完成 | 告警通道/EOD/数据降级就绪 |
| F | 灰度发布流程 | ✅ 核查完成 | 3 阶段配置就绪; 推进建议明确 |
| Z | 最终汇总 | ✅ 本报告 | — |

## 2. 关键发现 (实盘前 P0 阻断项)

### 影子观察期无真实撮合 (audit-shadow-realness)
影子账户 (launch_shadow_account) 的 NAV 是**回算** (daily_workflow Phase 10 从 daily_returns.jsonl 累加),
**trade_log 恒空, 从不执行真实策略撮合**。23 天观察期 + 无 fail-fast, 但绩效基于回算 NAV,
**无法验证策略在真实行情下的滑点/成交率/流动性冲击**。

**修复方向**: 影子账户消费 DTE-1 落盘的 strategy=build fills 作为真实成交 (本轮已落地首步),
积累 1-2 周真实成交后再评估准入/推进灰度。

### 两套影子系统并存
- `launch_shadow_account.py` (NAV 回算, 运行中, trade_log 空)
- `shadow_admission_launcher.py` (T2.4 准入, **从未启动**, admission_state.json 不存在)

准入流程未打通, 需启动 shadow_admission_launcher 使准入覆盖真实成交数据。

## 3. 落地修复清单 (8 项)

| 项 | 文件 | 说明 |
|---|---|---|
| system_config broker 段 | config/system_config.json | 补 broker 配置 (enabled=false 安全默认) |
| UE-1 门控函数 | 量化策略系统_统一入口_v8.6.py | `_enforce_live_gate` + --yes 参数 |
| DTE-1 建仓落盘 | daily_trade_executor.py | `_record_build_fill` → FillsStore (strategy=build) |
| EX-1 契约校验 | smart_order_router.py | REQUIRED_BROKER_METHODS 运行时校验 |
| DTE-6 auto-confirm 护栏 | daily_trade_executor.py | TRADING_ENV ∈ {shadow,production} 才允许自动确认 |
| DTE-6 os import | daily_trade_executor.py | 补 os 导入 |

## 4. 新增回归测试 (3 文件 12 测试)

| 文件 | 覆盖 |
|---|---|
| test_dte1_fills_store_20260824.py | DTE-1 建仓落盘 (FILLED/SKIPPED/qty=0) |
| test_ex1_broker_contract_20260824.py | EX-1 契约校验 (合法/非法/部分) |
| test_risk_failclose_20260824.py | DTE-6 auto-confirm 护栏 (5 场景) |

全部通过 + ruff_incremental_gate 通过 + assert_data_validity 12 PASS/0 FAIL。

## 5. P1 结构债 (后续批次, 非实盘阻断)

| 项 | 说明 |
|---|---|
| RK-1 | risk_manager 与 hedging 对冲阈值硬编码统一到配置 |
| DTE-8 | 建仓结果写回 trade_plan 消除双轨 |
| 影子准接入 | shadow_admission_launcher 启动 + trade_log 真实撮合 |
| QC-3 | 14 个存量 ANN 注解 + 覆盖率 80% |
| Docker | 实盘部署时评估封装 |

## 6. 实盘前完整 checklist

### 执行链
- [x] G1 QMT 接线 (配置层) — **待装 xtquant + 配账号 + dry_run 影子期**
- [x] UE-1 实盘门控 (TRADING_ENV 双签 + --yes)
- [x] DTE-1 建仓落盘 FillsStore
- [x] EX-1 broker 运行时契约校验

### 风控
- [x] DTE-6 auto-confirm 护栏
- [x] 本轮审查已修风控 fail-open (DTE-2/3/4, UE-1)

### 影子验证
- [ ] 影子账户积累真实成交 (strategy=build fills) ≥ 1-2 周
- [ ] 启动 shadow_admission_launcher T2.4 准入
- [ ] 偏离回测 ≤ 30% (memory 45164388)

### 灰度
- [ ] stage_2 (50%) 运行 ≥ 1 周, 无 fail-fast
- [ ] stage_3 (100%) 运行 ≥ 2 周, 偏离回测 ≤ 30%

### 环境
- [ ] xtquant 安装 + QMT 账号 (环境变量) + TRADING_ENV=production
- [ ] 告警通道生产可送达

## 7. 方法论沉淀

- **影子观察期必须真实撮合**: NAV 回算 ≠ 策略验证, trade_log 空是重大缺口。
- **实盘门控分层**: UE-1 (撮合入口) + DTE-6 (自动确认入口) 互补, 都需 TRADING_ENV 门控。
- **事实源消费**: 影子账户应消费 strategy=build fills, 而非独立回算。
