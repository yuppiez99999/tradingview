# 09-11 EOD 生产机复跑执行包

> **发起**: Issue #13 排期计划（用户 安然，2026-09-11）
> **上游材料**: `docs/sprint1_收尾判定材料_20260910.md` §附二「运行时基础设施状态」（含 3 行待终态项）
> **目标**: 把附二待终态行（09-10 EOD 三 cron / 09-10 T3 时点性 FAIL）与 B4 首 EOD 预期，一次复跑全部转为**终态**
> **性质**: 执行包 + 结果记录。**证据分级**：`【R】`= 仓库内可复现（本包已实测）；`【P】`= 生产机运行时产物（`reports/` 不入版本库，须生产机重跑）

---

## 0. 一句话

三件事一次做完：**① B4 首 EOD 应跳 warmup ② T3 七项无回归 ③ 三 shadow cron 三线实跑**。本包把每一项所需的**证据字段、判定口径、复现命令**固定下来，避免又变成"9/10 那种凭时点猜 PASS/FAIL"。

---

## 1. 前置（09-11 收盘后、wind 取数不可用时的替代路径）

09-10 已确认数据源劣化：Wind MCP **余额不足**、东财间歇断连、新浪抖动、TDX 通但 K 线空。
若今日 EOD 再遇取数失败 → 先用替代路径喂数（口径见 `cairn/LOG.md` 09-11 条目）：`feed_910_tencent.py`（腾讯 qfq）。

```powershell
cd "28-终极量化交易系统8.4"

# 0.1 flag 双签留痕核对（应 enabled: true + 双签）
Get-Content reports\flag_overrides\USE_MLOPS_PIPELINE.json
Get-Content reports\flag_audit\USE_MLOPS_PIPELINE.jsonl -Tail 2
python -X utf8 -c "from utils.infra.feature_flags import is_enabled; print('B4 =', is_enabled('USE_MLOPS_PIPELINE'))"

# 0.2 三 shadow 计划任务排期（应 SYSTEM + LogonType5 + wrapper，Next Run = 16:30/35/50）
schtasks /Query /TN S12_Shadow_EOD      /FO LIST /V | Select-String "Last Run|Last Result|Next Run"
schtasks /Query /TN Shadow30Day_EOD     /FO LIST /V | Select-String "Last Run|Last Result|Next Run"
schtasks /Query /TN GNN_S6_Paper_EOD    /FO LIST /V | Select-String "Last Run|Last Result|Next Run"
```

---

## 2. 复跑主流程（16:30 ~ 17:30）

```powershell
# ① 16:30 / 16:35 / 16:50 三 shadow cron 由计划任务自动触发（无需人工）
#    wrapper 逐次落盘: logs\task_logs\<Tag>_<ts>.log，透传 python exit code

# ② EOD 全链（B4 首 EOD）
python -X utf8 "15_每日工作流\run_daily_eod_workflow.py" --date 2026-09-11

# ③ 门禁三件套聚合（T3 ② 依赖 gate_daily_2026-09-11.json）
python -X utf8 scripts\gate_check_daily.py --date 2026-09-11

# ④ 离线核对（本包新增，只读/不触网）
python -X utf8 scripts\_eod_verify_911.py --date 2026-09-11 --md-out reports\operations\eod_verify_2026-09-11.md
```

> 注：T3 脚本自 2026-09-07 起不再把核对表写 stdout，**只落盘** `reports/operations/t3_check_<date>.md`，读取该文件即得七项明细。

---

## 3. 验收 A — B4 首 EOD 应跳 warmup

**预期行为**（`docs/b4_mlops_enable_checklist_20260909.md` §三 第 1 项）：阶段 4.86 读 `system_config.json` 判 `USE_MLOPS_PIPELINE=True` → 写 `skipped: true` + `reason: "B4 already enabled, warmup complete"`，**不再调用** `phase_b_b4_shadow_runner.py`（其不变式硬要求 flag=False，调用即每日 FAIL）。

| # | 证据 | 路径 | PASS 判据 |
|---|---|---|---|
| A1 | EOD 摘要 | `每日报告归档/2026-09-11/eod_workflow_summary_2026-09-11.json` → `phases.phase4_86_b4_shadow` | `skipped == true` 且 `reason` 字面量匹配 且 `flag_invariant == "USE_MLOPS_PIPELINE=True (post-warmup)"` |
| A2 | EOD 日志 | `logs/daily_eod_20260911.log` | 含「B4 已启用 … 跳过 shadow 预热」；**不含**「B4 shadow 每日预热 (USE_MLOPS_PIPELINE=False 不变式)」 |
| A3 | warmup 状态文件 | `reports/shadow/b4_shadow_status.json` → `last_run` | **不以 2026-09-11 开头**（未被当日刷新 = runner 确实没跑）＋ `consecutive_failures < 3` |
| A4 | flag 终态 | `system_config.json` / `config/feature_flags.yaml` / `reports/flag_overrides/` | 三方一致 = True（信息项，不参与 PASS/FAIL） |

**【R】契约已加回归护栏**：`tests/unit/test_daily_workflow_unit.py::TestB4SkipWarmupContract`（4 用例，纯 AST、无副作用）
—— 断言"跳过判定必须在 runner 调用**之前**"。负向实证：把跳过块移到 runner 之后 → 该用例立即红（`test_skip_branch_precedes_runner_call` FAILED）。

---

## 4. 验收 B — T3 七项无回归

```powershell
python -X utf8 scripts\t3_post_market_check.py --date 2026-09-11   # 落盘 reports\operations\t3_check_2026-09-11.md
```

对照基线（09-08 / 09-09 两日 T3 均 **PASS**）逐项比对，**重点是非回归而非绝对达标**：

| # | 检查项 | 09-09 PASS 基线口径 | 09-11 关注点（B4 首 EOD 的差异面） |
|---|---|---|---|
| ① | 数据源四查 | eod_guard 存在 + health_score 存在 + 数据源类 degradation 0 | 09-10 因 TDX 故障曾击穿 → 已修 `7d4fb2fd`；看是否复发 |
| ② | EOD 任务执行 | health_score + gate_daily 均存在 | **必跑 `gate_check_daily.py`**，否则此项天然 FAIL（沙箱已踩过） |
| ③ | 风控配置降级 | degradation 全为 `config_manager` + kill_switch level 0 | 关注 trade_execution 类降级条数 = 0 |
| ④ | 账户对账 | S12 NAV > 1.0 + 等权权重 + fail-fast 未触发 | 与 09-09 NAV 1.008397 对比 |
| ⑤ | 双 cron 产出 | mvsk ≥1 条 + S6 ≥1 条且非全 skeleton | qlib 当日 0 条为**预期**（R-6 停跑归档） |
| ⑥ | C10 fills 新鲜度 | 当日 fills 落盘 或 无 trade_plan | 若"无 fills 但有 trade_plan" = FAIL，查执行链 |
| ⑦ | Phase B / D11 进度 | D11 stable 7/7 + samples 15/20；B4 warmup 8/7 连败 0 | **B4 warmup 当日不应再 +1**（与验收 A3 互证） |

> **非回归判据**：七项 PASS/FAIL 组合与 09-09 一致；⑦ 的 B4 warmup 由递增转为**冻结**（这正是 B4 启用生效的第二个指纹）。

---

## 5. 验收 C — 三 shadow cron

**三线**：`S12_Shadow_EOD` 16:30（第三线，09-11 材料首次要求显式核对）/ `Shadow30Day_EOD` 16:35（MVSK P5-2）/ `GNN_S6_Paper_EOD` 16:50。

| # | 证据 | PASS 判据 |
|---|---|---|
| C1 | `schtasks /Query /V` 三任务 | Last Result = 0×3；Last Run 落在 16:30/35/50 ± 10 分钟内（错过则由 `StartWhenAvailable` 补跑，须记备注） |
| C2 | `reports/shadow/mvsk_p5_daily_diff.jsonl` | 当日 ≥1 条 |
| C2 | `reports/gnn_factor/s6_paper_trading.jsonl` | 当日 ≥1 条且 `status != skeleton`（全 skeleton = sys.path 修复失效，立即排查） |
| C2 | `reports/shadow/qlib_lgb_v2_daily.jsonl` | 当日 0 条 = **预期**（R-6 停跑归档，不判 FAIL） |
| C3 | `python scripts/run_s12_shadow.py --status` | rc=0 + NAV / 权重 / 再平衡次数 / fail-fast 可解析 |
| C4 | `logs/task_logs/<Tag>_*.log` | 三线当次 wrapper 日志存在，python exit code 透传 = 0 |

---

## 6. 结果记录（终态行，复跑后填写）

### 6.1 附二待终态行 → 终态（替换 `docs/sprint1_收尾判定材料_20260910.md` §附二表格）

| 日期 | 触发情况 | exit_code | 备注 |
|---|---|---|---|
| 09-10 16:30 / 16:35 / 16:50 | 【P】待填 | 待填 | 09-10 当日触发结果（材料待终态行） |
| **09-11 16:30 / 16:35 / 16:50** | 【P】待填 | 待填 | B4 首 EOD + T3 七项终态 |

### 6.2 T3 七项（补齐 09-10「时点性 FAIL」）

| 目标日 | 核对时间 | 总体 | 说明 |
|---|---|---|---|
| 09-10 | 09-11（**EOD 后补跑**） | 待填 | 09-10 为盘前核对，时点性 FAIL；须以当日 EOD 后结果为准 |
| 09-11 | 09-11（当日盘后） | 待填 | B4 首 EOD 对照 |

### 6.3 B4 首 EOD

| 项 | 结果 | 证据 |
|---|---|---|
| A1 摘要 skipped/reason | 待填 | |
| A2 日志跳过行 | 待填 | |
| A3 warmup 状态未刷新 | 待填 | |
| B4 连续 2 EOD 复验（09-14） | 待填 | checklist §五 第 4 项 |

---

## 7. 回滚触发条件（任一 FAIL 即执行）

判据见 `docs/b4_mlops_enable_checklist_20260909.md` §四：**主链路回归 / MLOps 真实路径阻断异常 / 审计留痕缺失 / 三方状态不一致** → 单签 `disable`。

```powershell
python -X utf8 -c "from utils.infra.feature_flags import disable; disable('USE_MLOPS_PIPELINE', signer='<人>', reason='09-11 首 EOD 验证 FAIL: <原因>')"
python -X utf8 -c "import sys; sys.path.insert(0,'.'); from scripts.phase_b_progressive_enabler import _sync_flags_to_system_config; print(_sync_flags_to_system_config({'USE_MLOPS_PIPELINE': False}))"
```

回滚事件当日记入 `cairn/LOG.md`（根因 + 重试计划）。

---

## 8. 交付物清单（本包）

| 文件 | 性质 | 说明 |
|---|---|---|
| `scripts/_eod_verify_911.py` | 新增【R】 | 离线只读核对脚本：A/B/C 三段，缺失记 MISSING 计 INCOMPLETE（fail-closed），不写 reports/、不触网 |
| `tests/unit/test_daily_workflow_unit.py` | 修改【R】 | 新增 `TestB4SkipWarmupContract`（4 用例，含顺序强约束 + 负向实证） |
| `docs/eod_复跑执行包_20260911.md` | 新增本文档 | 执行步骤 + 判定口径 + 结果记录表 + 回滚触发 |

**为什么不做**：不新增 reports/ 运行产物、不改 flag、不重跑 EOD（沙箱无生产数据源，强行重跑只会产出假绿）。
