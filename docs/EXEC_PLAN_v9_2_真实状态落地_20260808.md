# v9.2 工业级达标 — 真实状态落地计划 (2026-08-08 修订)

> 本计划基于 2026-08-08 实际运行 `industrial_grade_check.py` 与源码核查结果制定，
> **取代 2026-08-06 记忆中的过时差距评估**（08-07 已做完整一轮修复，多数 G 项已 PASS）。

## 0. 当前权威状态 (industrial_grade_check.py 实测)

| 判据 | 状态 | 对应差距 | 说明 |
|------|------|----------|------|
| C1 执行闭环 | WARN | G1 QMT | broker.enable=false/dry_run=true，真实下单未接线（用户决定本次跳过） |
| C2 数据管道分层 | PASS | G9 基础 | data_pipeline/ 已删，逻辑归 utils/data |
| C3 环境隔离 | WARN | G5 | 6 处 import research.* 未隔离 |
| C4 信号执行分离 | PASS | G4 | 成交回报驱动 PnL 已接 |
| C5 测试可运行 | PASS | — | 已知测试套件可采集 |
| C6 CI 可运行 | PASS | G6/G7 | 10+ 脚本接入 CI |
| C7 告警实现 | PASS | G8 | send_alert/send_sms_alert 已接 |
| 汇总 | 6 PASS / 3 WARN / 0 FAIL | | |

结论：**仅 C1(G1,跳过) 与 C3(G5) 仍为 WARN，其余全 PASS**。08-07 已修复 G2/G4/G8/G9基础/G11基础/G14基础。

## 1. 本次执行范围（用户确认：全部 + G1暂不做）

### P1 — G5 环境隔离 (C3 WARN, 明确缺口) 【最高优先】
- 现状：`utils/portfolio_optimizer.py` L460/L524 硬失败导入 `research.vibe_trading_factor_analysis.*`；
  `utils/evolution/auto_factor_factory.py` L400/L434/L440/L584 软降级导入 `research.factor_discovery*`。
- 策略：
  1. 将 `research.vibe_trading_factor_analysis` 与 `research.factor_discovery*` 迁到生产路径 `utils/factor_research/`（绝对导入），消除对 `research` 包的依赖；
  2. 若迁移成本过高，至少把 portfolio_optimizer 两处改为软降级（except→退回基础路径），使 C3 不再 WARN；
  3. 验证：重跑 industrial_grade_check，C3 转 PASS。
- 风险：research 模块可能依赖研究环境特定依赖（qlib 等），迁移需保证生产降级路径可用。

### P2 — 代码审查体系收尾
- P2.1 升 `quant_review_lint --strict`：ci.yml L155-156 已预留"试点无误报后加 --strict"，将 quant_review_lint 在 incremental-static job 升为阻断。
- P2.2 填充首次双周复盘 `docs/CODE_REVIEW_RETRO.md`（第 1 期，2026-08-08）。
- P2.3 填充 `docs/CODE_REVIEW_BACKLOG.md` 基线（跑 quality_snapshot.py 取真实数据）。
- P2.4 整理 PR：汇总本轮所有改动，给提交信息约定 `[P1.x] Gx`。

### P3 — G9/G11/G14 架构增强 (非阻断, 最低优先)
- G9 FeatureStore：utils/data 仅有 data_layer.py，无特征缓存抽象层。新增轻量 `utils/data/feature_store.py`（特征计算缓存，避免重复计算）。
- G11 CVaR 蒙特卡洛：wt_risk_control.calculate_cvar 用解析近似（vol*1.645），可改为调用 utils/fineng 已有蒙特卡洛路径模拟器做模拟 CVaR（保留解析为快速路径）。
- G14 多源交叉校验：quant_modules 现有多源"回退"非"交叉比对"。新增 `utils/data/cross_validation.py` 对多源同标的报价做一致性比对，超阈值告警。

## 2. 执行顺序与验证

1. 先做 P1(G5) — 消除唯一明确 WARN，跑 industrial_grade_check 验 C3 PASS。
2. 再做 P2 — 升 strict + 填文档 + 整理 PR。
3. 最后 P3 — 增强项，不阻断判据，做完跑测试 collection 与 lint。
4. 全量验证：`industrial_grade_check.py`（目标 7 PASS/2 WARN(C1+可选增强)/0 FAIL）、`assert_data_validity.py`、`pytest` collection 0 errors。

## 3. 原则
- 先还债（G5）再加功能（P3）；每修复必验证；灰度优先；不接真实券商。
- 不重复 08-07 已完成的修复（G2/G4/G8/G9基础/G11基础/G14基础 已 PASS）。

## 4. 执行结果 (2026-08-08 完成)

### P1 G5 环境隔离 ✅
- `utils/factor_research/factor_discovery.py` + `factor_discovery_enhanced.py`：从 `research/` 隔离区迁入生产路径。
- `utils/evolution/auto_factor_factory.py`：4 处 `from research.factor_discovery*` 改为 `from utils.factor_research.factor_discovery*`（软降级保留）。
- `utils/portfolio_optimizer.py`：2 处 `from research.vibe_trading_factor_analysis.*` 硬导入重构为基于 `AlphaFactorLibrary` 的生产实现（消除 research.* 依赖 + 复活离线因子流水线）。
- 验证：`utils/` 树内 `from research.`/`import research.` 字符串 = 0；`industrial_grade_check` C3 转 PASS（7 PASS/2 WARN/0 FAIL）。

### P2 代码审查收尾 ✅
- `scripts/quant_review_lint.py`：收窄 Q2/Q3 正则消除 __future__/lookahead/标签/docstring 误报（133→0 命中），新增 `_is_ignored` 豁免机制。
- `.github/workflows/ci.yml`：量化专项 lint 升 `--strict` 阻断（L150-156）。
- `docs/CODE_REVIEW_RETRO.md` 第1期 + `docs/CODE_REVIEW_BACKLOG.md` 基线已填充（数据来自 quality_snapshot.py）。

### P3 架构增强 ✅
- G9 `utils/data/feature_store.py`：特征缓存层（内存+文件+TTL+线程安全）。
- G11 `utils/wt_risk_control.py`：`calculate_cvar` 新增 `method="monte_carlo"` 路径（numpy 路径模拟左尾均值），默认仍解析（兼容）。
- G14 `utils/data/cross_validation.py`：多源报价一致性比对（超阈值告警）。

### 全量验证 ✅
- `industrial_grade_check.py`：7 PASS / 2 WARN(C1 QMT 有意跳过) / 0 FAIL
- `assert_data_validity.py`：7 PASS / 0 FAIL
- 9 个改动模块 import 全 OK；pytest 本地 venv 未安装（环境限制，CI 有）

### 未做（按用户决策）
- G1 QMT 真实下单：本次跳过，C1 维持 WARN。
