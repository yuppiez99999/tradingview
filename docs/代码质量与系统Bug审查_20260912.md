# 代码质量与系统 Bug 审查（0912 续批）

> 日期：2026-09-12（周六）｜ 执行：集群扫描第二轮（昨日报告 `代码质量与系统Bug审查_20260911.md` 的续批）
> 性质：只读审查，不含代码修改。发现项延续 SC 编号（接昨日 SC-1~14，今日新增 SC-15 起）。
> 本批两条线：**① 昨夜修复批次回归验证**（PR #20~#27，验证修复本身没引入新 bug）；**② 昨日覆盖缺口补扫**（信号/权重侧策略文件）。

---

## 00. 结论速览

| # | 结论 |
|---|------|
| ✅ | 昨夜修复批次质量总体**过关**：SC-1 / SC-3 / SC-4 / SC-5 / SC-6 / P1-2(资金口径) / P2-1(约束顺序) / P0-4(熔断链) / regime max_weight 均主线复核通过，未发现修复引入的新逻辑 bug |
| ✅ | `hedge_execution_engine._resolve_stock_etf_budget_base()` 三级解析（腿显式值 > total 折算+WARNING > 静态兜底）实现正确，与 P1-2 拍板口径一致 |
| ✅ | `executor/risk_feed.py`（P0-4 新增 127 行）fail-open 喂数 + fail-closed 阻断的组合语义正确，且对老指令文件向后兼容 |
| ❌ | **新发现 SC-15（P1）**：`vol_regime_weighter.enforce_constraints` 约束链存在**现金缓冲破坏** bug——裁剪产生的赤字全部塞给现金、现金可为负再归零、最终总和可偏离 1 达 +30%，且无任何告警 |
| ✅ | **SC-16 已修（2026-09-12）**：`enhanced_signal_fusion` softmax max-shift 防溢出 + capped-simplex 投影使约束在归一化后仍成立；8 例回归先红后绿 |
| ⚠️ | **遗留确认**：昨日 P2-5（情绪链 Ling 判定吞掉规则关键词命中）**仍未修复**，`_compile_rows` 结构未变 |
| ❌ | **新发现 SC-25~27（P1/P2/P3）**：S4 成本解析链三处静默降级 —— 尾部保护缺配置**静默兜底且来源串冒充配置值**、非法值静默兜底、**成本率无区间护栏**（单位错写 0.3% → 0.3 使成本率放大 ~100 倍且零告警）|
| ✅ | **SC-25~27 已修（2026-09-12）**：`_tail_pct` 改三元组返回 + 新增 `_cost_rate_guard` + `validate_configs` 护栏10；5 例回归先红后绿 |
| 📋 | 覆盖缺口转下批：correlation_regime 数据源失败路径、并行会话 WIP 15 文件（提交后） |

---

## 01. 昨夜修复批次回归验证（全部通过）

以下均为 HEAD 已提交代码的主线独立复核（证据 = file:line 实读，非转述 commit message）：

| 昨日项 | 修复 commit | 验证证据 | 结论 |
|--------|------------|----------|------|
| SC-1 建仓流水静默短路 | `b8eff0aa` | `executor/premarket.py:53-87` 新增 `TradePlanUnavailableError`；文件缺失/不可读/根节点非对象三路全部抛错，`load_trade_plan` 不再返回空计划 | ✅ fail-loud 落地 |
| SC-3 factor_scorer 适配器 | 昨夜批次 | `utils/alpha/factor_scorer.py:86-106` 改用 `get_adapter` + try/except + getattr 防护 | ✅ |
| SC-4 IC_IR 哨兵 | 昨夜批次 | `drift_shadow_integrator` 哨兵值显式化，不再用 0.0 冒充真实 IC_IR | ✅ |
| SC-5 GTJA191 显式降级 | `4ac0f961` | `gtja191_factors.py:37-58,93-134` 构造期能力探测 + `GTJA191UnavailableError` 显式异常 + `record_degradation` 降级审计；配套 160 行单测（抽查形态正确，逐行复验转下批） | ✅ |
| SC-6 trendcast 裸 now | 昨夜批次 | 裸 `now=0` 处已修 | ✅ |
| P1-2 资金口径 5M→300万 | `04da219c`+`051209dd` | `rebalance_execution_orders.py:62` `TARGET_TOTAL = get_stock_etf_capital()`（证券腿 200w）；11 处消费点接线 capital_base 单一事实源 | ✅（消费点逐点复验转下批） |
| P1-2 对冲腿解析 | `162a3796` | `hedge_execution_engine.py:202-260`：①`meta.stock_etf_capital` 显式值直接采信；②仅 `total_capital` 时按静态腿占比折算 + WARNING（防 5M 旧头高估 82%）；③均缺失走静态兜底。禁止静默把总口径当腿口径 | ✅ 三级解析正确 |
| P2-1 约束/归一化顺序 | 昨夜批次 | `utils/universe/portfolio_builder.py:235-330`：「先砍帽再全局归一化」改为「层内迭代收敛 + 全组合行业上限」，注释明确归一化后上限仍成立 | ✅ |
| P0-4 熔断链闭环 | `c7bf0704` | 见 §02 专节 | ✅ |
| regime max_weight 双失效 | `7f451478` | `utils/regime_aware_allocator.py:347-395`：可行性按**活跃资产数**判定（`max_w * n_active < 1` → 不可行）；不可行返回活跃等权 + 显式 WARNING；可行时真实裁剪 | ✅ 语义正确（含 n_active=0 边界、非有限 max_w 防护） |

**修复批次未发现引入型新 bug**（就主线复核范围而言；D1 子任务对 capital_base 其余接线文件的逐行复扫未完成，转下批，见 §05）。

### 02. P0-4 熔断链专节（新文件 `executor/risk_feed.py` 全文审读）

两段组合语义核验：

- `_feed_risk_control_equity()`（L82-127）：fail-open。持仓估算不出权益 → 只告警跳过喂数，**不用 0 冒充权益**；真正的阻断判定交给 `risk_checks.circuit_breaker`。与 `config/risk_thresholds.yaml` 的 fail-open 不变量一致。
- `check_circuit_breaker_gate()`（L105-127）：fail-closed。`passed is False` 一律阻断（不区分真超限与数据不可用）；**老指令文件无 circuit_breaker 段 → 返回 None 不阻断**，向后兼容正确。
- 原缺陷（假 PASS → 执行原样放行，缺陷链路位移）被两点同时堵住：premarket 侧 UNKNOWN 显式化 + 执行侧 gate 消费。

⚠️ 观察项（SC-18，P3）：`_compute_positions_equity` 用 `est_price`（premarket 落盘的估计价）估值。若多数持仓 `est_price` 缺失/过期，权益会被**系统性低估**，回撤类熔断阈值可能误触发。建议在 risk_checks 里附带 `equity_source` 与估值覆盖率，便于事后审计。

---

## 03. 新发现

### SC-15（P1）vol_regime_weighter：约束链现金缓冲破坏，输出权重总和可偏离 1 达 +30%

- **位置**：`utils/alpha/vol_regime_weighter.py:659-733`（`VolRegimeWeighter.enforce_constraints`）
- **问题链**（四步互相放大）：
  1. **步骤 2（现金下限 L692-699）**：现金 < floor 时无条件抬到 floor —— 直接**凭空注入权重**，不做对应压缩；
  2. **步骤 3（差额归现金 L701-708）**：裁剪后总和若 >1，赤字 `delta` **全部塞给现金**——现金可能变负（如 -0.30），且无下限保护；
  3. **步骤 4（负值保护 L710-716）**：现金归零后**不再重新归一**——总和 ≠ 1 直接输出；
  4. **步骤 5（最终校验 L718-726）**：只把 `final_total` 写进日志字符串，**无断言、无告警、无修正**。
- **数值例**（CASH_OFFENSIVE regime，矩阵 ±20%，7 风格各 0.135 + 现金 0.05）：
  - 原始和 = 0.9448 → 抬现金到 floor 后 = 1.3448 → 四个 0.27 风格裁到 0.30 后 ≈ 1.35 → delta = -0.35 → 现金 = 0.05 − 0.35 = **-0.30** → 负值归零；
  - **最终输出**：四个 0.30 + 周期 0.10 + 现金 0 = **总和 1.30（超发 30%）**，现金 = 0（违背 cash_floor=0.05），权益类被从 0.135 抬到 0.30（**+122%**）。全过程零告警。
  - 常规情形（非极端）下同样失真：裁剪产生的赤字不再按比例分摊回风险资产，而是先冲现金——现金被无声挪用，最坏时现金缓冲完全消失。
- **影响**：
  - `etf_option_hedge_rebalancer.py:98,305-313` 真实接入 VolRegimeWeighter（对冲调仓的 regime 适应路径）——启用即踩；
  - `utils/auto_trading_system.py:242,417-460` 走 `USE_VOL_REGIME_WEIGHTER` feature flag，**默认 False**（当前未启用，无实盘影响）——开 flag 前必须先修本条。
- **修复建议**：约束执行改为单次收敛循环——①抬现金时按比例压缩风险风格；②sum>1 的赤字优先从**最高权重风格**扣除（或全风险风格按比例分摊），不得单冲现金；③任何负值归零后必须**重跑整条约束链**；④步骤 5 由日志改为告警+强制修正（fall back 到安全等权+现金 floor）。
- **置信度**：高（代码路径直接推演，多组数值复算一致）。

### SC-16（P3）enhanced_signal_fusion：softmax 无溢出防护 + 上限约束在归一化前  ✅ 已修 (2026-09-12)

- **位置**：`utils/enhanced_signal_fusion.py:535-553`（`_apply_weight_constraints`）
- **问题 ①**：`np.exp([adjusted_scores[s] * 10 ...])` 无 max-shift 减法。`adjusted_scores` 可为负（相关性惩罚相乘），score ≈ -7 时 exp(-70) 无碍，但 score 为大正值时 exp(700) 直接 `OverflowError`/inf → softmax 输出 NaN。异常分数无钳位。
- **问题 ②**：L545-551 先逐源 clip 到 `[min_weight_per_source, max_weight_per_source]`，**然后**才归一化——归一化会把刚压到 0.5 上限的源重新抬超上限（与昨日 portfolio_builder P2-1 同型：约束必须在归一化后仍成立）。极端时单一源可被抬到 ~1.0。
- **消费方现状**：全仓库无生产调用——`signal_fusion.py:1327,1405,1481` 仅注释宣称"由 EnhancedSignalFusionEngine 动态调整"（**实际未接线**，注释与代码不符）；`scripts/_coverage_recalc.py` 为工具脚本。故降级 P3。
- **修复建议**：softmax 加 `exp((s - max(s)) * 10)`；clip 与归一化迭代收敛或裁剪后重分配。**若未来把权重机制真正接线，本条升级为 P1 前置必修**。
- **✅ 修复落地（2026-09-12，PR 见 `cairn/LOG.md` 顶部条目）**：
  - ① 抽出 `_softmax_weights`：max-shift（`exp((s - max) * 10)`，平移不变性）+ 非有限值 `nan_to_num` 清洗 → 溢出/NaN 不可能发生（实测 score=100 与 1e3 均得合法分布）；
  - ② 抽出 `_project_to_capped_simplex`：把权重投影到 `{sum=1, min<=w<=max}`（water-filling，逐轮钉住越界源、余量按基准比例分给未固定源），归一化后约束恒成立（单源主导实测 0.833→0.5）；不可行时 fail-safe 退化等权（不静默放行违规权重）；
  - 终检不变量：非有限值/越界/总和≠1 一律退化等权 + WARNING。
  - **验证**：新增 8 例回归（修复前 **6 failed** → 修复后全绿）；该文件 12→20 passed；相关面 4 文件 **226 passed**；ruff All checks passed。

### SC-17（P3）mvsk_regime_detector / correlation_regime：无显著逻辑 bug（记录性核验）

- `utils/mvsk_regime_detector.py`（198 行）：判定方向正确——滚动波动分位 > 0.75 **或** 超额峰度 > 3.0 → HIGH_VOL_FAT_TAIL（用 MVSK），否则 LOW_VOL_NORMAL（用 MV）；置信度"距阈值越远越高、模糊区 0.5"合理。
- `utils/correlation_regime.py`（239 行）：滞回状态机 enter=0.65 / exit=0.45 方向正确，因果平滑窗口；文件头明确"Descriptive risk context — not a trading signal"（描述性上下文，非交易信号），风险等级自洽。
- 观察项：两者共享外部数据源（`_fetch_price_series`）失败路径未深审，转下批。

### 遗留确认：P2-5 情绪链 CRITICAL 吞词（仍未修）

- `nlp/sentiment_hub.py:269-311` `_compile_rows`：Ling `direction` 非空时规则关键词（kw_neg/kw_pos）**完全不参与**——Ling 把"暴跌/退市/立案"级标题判成中性/混合时，条目落入 mid 仅提示不预警；CRITICAL_NEGATIVE_KEYWORDS（L27-43）在判定环节只用于展示统计（L446-455），**没有 forced 提升逻辑**。
- 与昨日报告结论一致：维持 P2 遗留，等 Ling 判定与规则词的融合策略拍板（方案：CRITICAL 词命中 → 至少 mid+预警，或强制复检）。

---

## 04. 风险评级汇总

| 编号 | 级别 | 一句话 | 状态 |
|------|------|--------|------|
| SC-15 | **P1** | vol_regime_weighter 约束链破坏现金缓冲，输出总和可 1.30、现金可归零、零告警 | 新发现，启用 flag/对冲 rebalancer 前必修 |
| SC-16 | P3 | enhanced_signal_fusion softmax 溢出 + 约束顺序同型旧病（无生产调用方） | **✅ 已修 (2026-09-12)** — 8 例回归先红后绿，接线前必修项已前置完成 |
| SC-17 | P3 | mvsk/correlation regime 判定核验通过，数据源失败路径留观察 | 记录性 |
| SC-18 | P3 | risk_feed 权益估值依赖 est_price，缺失时熔断回撤基准被低估 | 观察项，建议附估值覆盖率 |
| SC-19 | **P1** | `hedge_execution_orders` 空组合市值回退 5M ⇒ 对不存在的敞口做空（裸空 IF 名义 ~114 万） | ✅ 已修（fail-closed + degraded_reason），7 例回归 |
| SC-20 | P2 | `daily_hedge_update` 硬编码 `portfolio_value=5_000_000.0`，对冲金额与真实市值脱节 | ✅ 已修（传 None 由引擎按真实持仓算） |
| SC-21 | P2 | `launch_shadow_account` 缺 `capital_config` 时兜底 5M 旧口径 | ✅ 已修（唯一事实源） |
| SC-22 | P2 | `next_day_planner` + `generate_daily_report` 资金兜底 5M/3M 双口径残留（分母错放大占比） | ✅ 已修（唯一事实源） |
| SC-23 | **P1** | `run_daily_eod` Guard2 的 peak/current 同值回退 ⇒ 回撤判定恒 0 级（守卫静默失效） | ✅ 已修（腿口径兜底 + 缺字段显式告警） |
| SC-24 | P3 | `scripts/calc_2030_annual_return` 兜底 3M/5M 旧口径 | ✅ 已修（唯一事实源） |
| SC-25 | **P2** | S4 尾部保护成本缺配置时**静默兜底 0.5%** 且来源串**冒充配置值**（口径不可审计，S4 差异化出处不明） | ✅ 已修（兜底显式标注 + 配置护栏10） |
| SC-26 | P3 | `_tail_pct` 对字符串/布尔非法值静默兜底（bool 是 int 子类，`True` 会被当 1.0 成本率） | ✅ 已修（落兜底并标注「非法」） |
| SC-27 | **P1** | 保护成本率**无区间护栏** ⇒ 单位错写（0.3% 写成 0.3）使 S4 成本 31.25%（年净值 ×0.73）**零告警** | ✅ 已修（越界 fail-closed 归零 + WARNING + 配置护栏10） |
| P2-5 | P2 | 情绪链 CRITICAL 吞词未修（昨日遗留） | 等拍板 |

## 05. 本批覆盖缺口（转下批）

1. ~~**capital_base 消费点逐点复验**：昨日 P1-2 修复涉及 11 处消费点，主线已抽查核心 3 处（rebalance_execution_orders / hedge_execution_engine / automated_execution_system）；`rebalance_order_executor.py`、`alpha_hedge_engine.py`、`institutional_pipeline_runner.py` 细节未逐行过。~~ **✅ 已闭环 2026-09-12（Issue #13 续批）**：三项目标文件逐行复验**本身均合规**（腿/总口径语义正确）；但顺同一张消费面扫出 **6 处漏网硬编码 SC-19~24 并全部修复** —— SC-19 `hedge_execution_orders` 空组合市值回退 5M（实测产出裸空 IF 1 张，名义 ~114 万）→ 改 fail-closed；SC-20 `daily_hedge_update` portfolio_value=5M → 交引擎自算真实市值；SC-21 `launch_shadow_account` 兜底 5M；SC-22 `next_day_planner` + `generate_daily_report` 5M/3M 双兜底；SC-23 `run_daily_eod` Guard2 的 peak/current 同值回退致**回撤判定恒 0 级**；SC-24 `calc_2030_annual_return` 3M/5M。新增 7 例回归（先红后绿：旧码 6 failed → 修复后全绿），全量 tests/unit 失败集与基线逐项一致。详见附录 B 台账 SC-19~24。
2. ~~**S4 假策略修复质量**：`scripts/run_200w_etf_backtest.py`（commit `96739a83`）未复审。~~ **✅ 已闭环 2026-09-12（Issue #13 续批）**：S4 主体修复（tail 在 collar 之上叠加）**独立复核通过**——实测 S1 0% / S2 1.50% / S3 1.25% / S4 1.55%，四腿数值互异，假策略已消除。但**顺同一张成本解析链扫出 SC-25~27 三处静默降级**（与 SC-19~24 同族：主路径改对、兜底路径继续静默）：① SC-25 `_tail_pct` 缺配置时返回裸 float `0.005`，调用方来源串**恒宣称** `tail_protection.budget_annual_pct` —— 用的是兜底值却宣称是配置值（对比 `_protective_put_pct` 会明确写「内置兜底 1.5%」，口径可审计性不一致）；② SC-26 非法值（字符串 / 布尔）静默兜底；③ **SC-27 保护成本率无区间护栏** —— 实测单位错写（0.3% → 0.3）时 S2 成本率 50%、S4 成本率 31.25%，仅成本项一年把净值打到 0.73（腰斩再腰斩）且**零告警**，回测会据此产出「策略不可交易」的假结论。修复 = ①`_tail_pct` 改为返回 `(值, 来源串)` 三元组（与 `_protective_put_pct` 同构）+ ②新增 `_cost_rate_guard` 区间护栏（越界/非有限值一律归零 + WARNING）、+ ③`validate_configs.py` 新增**护栏10**（尾部保护成本量级 + enabled 但缺值）。新增 5 例回归 + 2 例自测变异（先红后绿：旧码 **4 failed / 2 passed**，其中 S2 实测输出 50% 未被拦；修复后全绿）。详见附录 B 台账 SC-25~27。
3. **correlation_regime / mvsk 数据源失败路径**。
4. **并行会话 WIP 已于 0912 上午提交**（`72f17e3b` / `52583dd7` / `4ac0f961` / `1154ca92` 等，其中 `4ac0f961` 即 SC-5 修复）：新落地代码（EOD Wind 回测日报接线、sync busy-guard）复扫转下批。

## 06. 证据与过程

- 主线复核均为 HEAD 实读（file:line 见各条）；数值例为手工推演复算。
- 两路扫描子任务（修复批次复扫 / 信号侧缺口）均超时未完整回传；D2 抢救出的 vol_regime_weighter 约束层线索已由主线独立复核确认（即 SC-15），D1 清单由主线代审 4/6 项（risk_feed / hedge 解析 / premarket / regime），余 2 项转下批。
- 工作台存档：`.cluster/scan-20260911/subagent_D1.md`、`subagent_D2.md`。


---

## 07. Issue #30 安全专项·二次复扫（2026-09-12 晚，本文档补充）

> 性质：**首方代码 bandit MEDIUM+ 归零的第二轮**（第一轮见 Issue #30 首条评论 / PR #31）。
> 触发：用户 Issue #30 二次派单「安全11个提示 解决」。

### 07.1 为什么会有第二轮

第一轮以 CI 门禁口径（`bandit -c bandit.yaml -r <changed> -ll -ii`）把当时扫出的 11 项清了零，并把 `# nosec` 逐点标注。**但那次修复存在两个结构性缺口**：

1. **口径不一致**：`bandit.yaml` 的 `exclude_dirs` 排除了 `tests/ scripts/ tools/ research/ ms_strategy/`，而首轮复扫时把 `ms_strategy/cloud_train/*.py` 纳入了 → 首轮「11 项」与门禁口径实测并不完全对齐（用户侧看到的「11 个提示」正是这个错位）。
2. **`# nosec` 逐点标注 ≠ 收口**：首轮把每个 urlopen / ET.parse / pickle.load 都加了 `# nosec` 并写理由。这类修复的失效模式很典型 —— **新增调用点照抄旧写法就产生裸告警**，且同源逻辑被复制成多份（一处修、另处漏）。**实测已发生**：09-12 14:40 新入库的 `ms_strategy/cloud_train/debug_backtest.py`（B301）与 `backtests/etf200w_opt_20260911/fetch_klines.py`（B602）就以裸写法进入 main —— 这两项正是用户侧扫描器此刻报出的告警。

### 07.2 本轮定位（三口径实测）

| # | 口径 | 修复前 | 修复后 |
|---|------|--------|--------|
| ① | CI 门禁 `bandit -c bandit.yaml`（全部 1274 首方文件，无路径排除） | **2**（B301 `debug_backtest.py` / B602 `fetch_klines.py`） | **0** |
| ② | 全仓 `bandit -r . -ll -ii`（排除第三方，含 nosec 豁免） | 11（B301×5 / B310×13 / B314×11 / B608×3 / B602×1 去重后首方 11） | **0** |
| ③ | 全仓 `--ignore-nosec`（真实豁免面） | 33 | **9**（全部落在三个收口模块内，见下） |

### 07.3 本轮改为「收口」而非「逐点 nosec」

| 收口模块 | 取代 | 说明 |
|---|---|---|
| **`utils/safe_pickle.py`（新增）** | 5 处各自的 `pickle.load` + 各自复制的 SHA256 侧车校验 | 唯一反序列化入口 `load_pickle`；侧车比对 + **「无侧车」策略显式化**：`QUANT_REQUIRE_PICKLE_INTEGRITY=1` → fail-closed（默认关＝保持既有 warning 行为，逐字节兼容） |
| **`utils/safe_url.py`（复用，升级）** | 13 处裸 `urlopen` + 3 份重复 scheme 校验副本 | `safe_urlopen` 成为**全项目唯一允许调用 `urllib.request.urlopen` 的位置**；`_urlopen_raw` 承载豁免点 |
| **`utils/safe_xml.py`（复用）** | 11 处 `ET.parse` + 逐点 `# nosec B314` | coverage.xml 全部改走 `safe_xml_parse`（defusedxml 优先 / DTD 与实体在解析阶段拒绝）；4 个脚本补齐 CLI 直跑所需的 `sys.path` 引导 |
| **`backtests/.../fetch_klines.py`** | `subprocess.run(..., shell=True)` | **真收窄**：参数已全部列表传入，无 shell 解析需求；保留 shell 等于把参数再交给 `cmd.exe` 解释一遍（元字符可越权） |

顺带扫出并修复的**实质缺陷**（非告警消音）：

1. **`utils/alpha/omni_route_client.py::_safe_urlopen` 无限递归** —— `timeout is not None` 分支调用的是**自己**而非 urllib（本模块此前无调用方，缺陷长期潜伏，一旦接线即 `RecursionError`）。
2. **「安全」加载器在无侧车时静默放行** —— 5 个 `load_model_safe`/`load_pickle` 入口中，**「侧车不存在」分支全部是 warning 后继续反序列化**。即：投毒者只要**不带侧车**即可绕过全部 SHA256 校验。覆盖面看着有，实质防护缺位（典型「审计口径达标、攻击路径畅通」）。本轮改为显式策略开关。
3. **`ms_strategy/cloud_train` 三份 `load_model_safe` 完全重复** —— 同源校验逻辑复制 3 份，正是「一处修、另处漏」的温床；已统一委托收口。

### 07.4 验证（【R】本机实测）

- **bandit 三口径**：① 2 → **0**；② 11 → **0**；③ 33 → **9**（且 9 项全部位于 `safe_pickle` / `safe_url` / `safe_xml` 三个收口模块内部，带 docstring 说明的实质豁免）
- **回归测试**：`tests/unit/test_security_hardening_20260912.py` 由 21 → **42 passed / 4 skipped**（新增 `TestSafePickleUnification` / `TestSafeUrlUnification` / `TestCoverageXmlHardenedParse` / `TestSubprocessShellDisabled` / `TestBanditMediumZeroInvariant`）
- **行为等价实测**：`safe_urlopen` 对本机 100ms HTTP 服务 GET 成功（`Request` 对象与 str 两条路径）；`load_pickle` 四态（有正确侧车／侧车错／强制校验下无侧车／显式哈希）行为符合设计；`research` 缓存三守卫（路径越界 / 空文件 / 合法加载）实测行为不变
- **全量 tests/unit**：失败/错误集与基线**逐项 diff 为空**（94 vs 94，零新增、零丢失）
- **门禁**：`ruff_incremental_gate` 通过；改动文件 ruff 与基线**逐项一致**（残留 3 处 T201 为 pre-existing 既有项）；`f821_fullscan_gate` 0/0 PASS；`check_utf8_mojibake` exit 0；`check_no_print_p0` OK；`check_prod_research_isolation` 31 路径通过；`industrial_grade_check` **10 PASS / 2 WARN / 0 FAIL**（与 09-11 基线逐项一致）
- **测试适配**：`test_tradingagents_bridge_unit.py` 4 处 `patch("...tradingagents_bridge.urlopen")` → `...safe_urlopen`（该符号已不存在于模块命名空间，属收口的预期副作用）

### 07.5 未做（如实声明）

- `defusedxml` 仍未列入 `requirements*.txt`（沿用首轮设计：软依赖，缺失时回退「解析阶段拒绝 DTD/实体」的 stdlib 解析器）
- `QUANT_REQUIRE_PICKLE_INTEGRITY` **默认关闭**（保持既有行为）；**生产机是否开启需用户拍板** —— 开启后任何缺侧车的旧模型文件都会被拒绝加载
- 三个收口模块内部保留 9 处 `# nosec`（`pickle.load` ×3 / `urlopen` ×1 / `ET.parse` ×2 / SQL 拼接 ×3），均为**实质豁免**：调用点唯一、前置校验在函数体内可读、docstring 写明为何安全
- 第三方目录（`external/`、`unsloth_compiled_cache/`、`qlib/`）不计入范围
- **【P】生产机运行时行为未观测**

### 07.6 方法论沉淀（与 SC-19~24、SC-25~27 同族）

本轮把前两轮反复出现的同一种失效模式**用同一把尺子**量了第三次：**「主路径改对 / 兜底路径继续静默」**。此次的具体形态是：

- **「逐点 nosec」冒充「收口」**：`# nosec` 让扫描器安静，但没有消除**新增调用点照抄旧写法**的可能 —— 而仓内多处重复实现（3 份 scheme 校验、3 份 load_model_safe）使「照抄哪个」本身就是风险。**判据：同一安全语义在全仓出现 >1 份实现，即视为未收口。**
- **「校验存在」冒充「校验生效」**：5 个加载器都有 SHA256 侧车校验代码，但「侧车不存在」这一分支全部放行 —— 而攻击者恰恰**不需要**提供侧车。**判据：安全校验必须逐个分支问「攻击者能否选择走这条路」，尤其是「输入缺失」这条。**
- **「审计口径」冒充「门禁口径」**：首轮 11 项与 CI 门禁的 2 项在**路径排除集**上并不一致。**判据：报数时必须写明扫描命令与排除集，否则「11」和「2」是两个不可比的数字。**
