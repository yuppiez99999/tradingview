# 明日系统自我升级工作计划（2026-08-07）

> 基于 2026-08-06 实际进展更新，聚焦 Wave6 修复落地、TDAM Phase 0b、观察期推进与对冲配置。

---

## 今日完成情况复盘（2026-08-07 日终更新）

| 主线 | 目标 | 完成状态 | 证据 |
|------|------|----------|------|
| **Wave6 修复验证** | 代码级验证 7 个修复，确保无回归 | ⏳ **代码级验证完成，实盘验证待定时任务** | 7 项（C1-C4/H1/H2/W6-1）代码实读 + py_compile + 运行时导入/签名/返回元组验证全部通过；`build_orders` 5 参↔`load_positions()` 4 元组调用匹配。实盘级验证待 15:30 `v84_PostMarket` 日终确认 |
| **TDAM Phase 0b** | 数据导入链路跑通 | ✅ **已完成** | 29 wiki skill + 114 chat_memory 共 143 文档导入成功，端到端 BM25 检索全通过；修复 `tdam_client.py` 3 个"假成功"缺陷。详见 `docs/今日工作总结_20260807.md` §四 |
| **观察期推进** | 第 10 条样本写入 | ⏳ **等待 16:10 定时任务** | `daily_returns.jsonl` 最新到 2026-08-06（共 9 条）；`status.json` 显示 `observation_progress: "9/14"`，第 10 条由 `v84_ObservationBriefing` 16:10 自动写入 |
| **对冲配置** | 降低 Beta 1.300 偏高敞口 | ✅ **已完成** | `decisions.jsonl` 已于 2026-08-07 01:30 写入决策：`A_no_adjustment` |

**总体完成度：75%**（Wave6 代码级验证 + TDAM Phase 0b + 对冲配置完成；观察期等待定时任务写入）

---

## 一、系统自我升级主线

| 主线 | 目标 | 验收标准 | 优先级 |
|------|------|----------|--------|
| **Wave6 修复验证** | 在真实交易环境中验证 7 个修复，确保无回归 | 实盘/模拟盘日志无 TypeError/NameError；`build_orders` 正常生成对冲单 | P0 |
| **TDAM Phase 0b** | 数据导入链路跑通 | `export_cairn_for_tdam.py` 适配新端点，成功导入 `cairn/` 核心知识 | P1 |
| **观察期推进** | 收集第 10 天真实数据 | Shadow 账户写入成功；样本数 10/20 | P1 |
| **对冲配置** | 降低 Beta 1.300 偏高敞口 | 完成 Put 期权保护建仓或明确“暂不建仓”的决策日志 | P1 |

---

## 二、Wave6 修复验证（P0）

### 2.1 今日修复详情（2026-08-06 已完成）

> 以下 7 个缺陷已在今日完成代码级修复，明日进入**实盘/模拟盘验证**。

| 严重度 | 编号 | 文件 | 问题描述 | 修复动作 |
|--------|------|------|----------|----------|
| **致命** | C1 | `utils/hedge_rebalance_integrator.py` | 未导入 pandas 却调用 `pd.read_parquet`，导致 VaR/协方差风控完全失效 | 补 `import pandas` + 异常收窄 |
| **致命** | C2 | `utils/execution/automated_execution_system.py:1970` | 以 3 个参数调用 `build_orders`（定义要求 5 个必填参数），导致对冲单永不生成 | 改用 `load_positions()` 补全 5 参 + except 上抛 |
| **高** | C4 | `utils/hedge_execution_orders.py:338` | 期权名义金额计算错误：`remaining_notional * weight` | 改用 `option_notional * weight` |
| **中** | C3 | `daily_trade_executor.py:629,1078` | 建仓日期硬编码 | 改为 `ACCUMULATION_START/END` 动态生成 |
| **中** | H1 | `utils/hedge_engine.py:306` | `except Exception: pass` 静默吞错，无法区分“无数据”与“数据源故障” | 改为 `logger.warning` 显式告警 |
| **低** | H2 | `quant_modules/data_layer.py` | 误导性 docstring，新开发者易误解系统行为 | 重写 docstring |
| **低** | W6-1 | `utils/data/data_layer.py:1-98` | docstring 重复注入 4 遍 | 安全清理为单份完整 docstring |

**修复验证结果**：
- 7 文件 `py_compile` 通过
- `read_lints` 0 错误
- 子代理待复核项经 AST + 逐行复核**全部确认为误报**（`is None is False`、`in ['LISTED']`、`df.empty` 死代码等不存在）

### 2.2 验证清单

| 编号 | 修复项 | 验证方法 | 通过标准 |
|------|--------|----------|----------|
| C1 | `hedge_rebalance_integrator.py` 补 pandas | 运行风控模块，检查 VaR/协方差输出 | 无 NameError；数值正常 |
| C2 | `automated_execution_system.py:1970` `build_orders` 参数修复 | 触发对冲执行，检查生成的 orders | 非空；参数完整；日志无 TypeError |
| C4 | `hedge_execution_orders.py:338` 期权名义金额 | 回测/模拟盘期权对冲计算 | 名义金额与总额占比一致 |
| C3 | `daily_trade_executor.py` 日期动态化 | 运行建仓流程 | 无硬编码日期；日志显示动态区间 |
| H1 | `hedge_engine.py:306` 异常告警 | 模拟无数据场景 | 出现 `logger.warning`，不再静默 |
| H2 | `quant_modules/data_layer.py` docstring | 阅读代码 | 描述与实际行为一致 |
| W6-1 | `utils/data/data_layer.py` docstring 清理 | 阅读代码 | 仅一份完整 docstring |

### 2.2 验证步骤
1. **单元级**：对修复文件单独运行相关函数/类方法。
2. **集成级**：启动 Shadow 账户日终流程，观察完整链路。
3. **日志审计**：重点搜索 `TypeError`、`NameError`、`except Exception` 静默块。

---

## 三、TDAM Phase 0b 数据导入（P1）

> **前置条件**：Phase 0a Windows 部署已于 2026-08-06 完成，交付物如下：

| 交付物 | 路径/标识 | 状态 |
|--------|-----------|------|
| `tdam_client.py` v3 | `utils/tdam_client.py` | ✅ 端点全面修正（`/v3/skill/search`、`/v3/knowledge/list` 等）+ 自动加载凭据 + 5 端点全通 latency 8-9ms |
| 后台服务包装脚本 | `scripts/tdam/tdam_service_wrapper.ps1` | ✅ 后台启动 + 日志 7 天轮转 + PID 管理 |
| Windows 任务计划脚本 | `scripts/tdam/register_tdam_task.ps1` | ✅ 3 个任务已注册：AutoStart / StopIntraday / StartPostMarket |
| admin 凭据 | `.admin-credentials.json` / `E:\tdam-data\memory\` | ✅ user_id=`usr-mbiyz1q0x7` |
| 部署方案文档 | `TDAM_cairn_对接方案.md` v3 | ✅ 端点 + 鉴权 + 服务化 + 任务计划 |

### 3.1 关键发现（Phase 0a 源码核查）

| 问题 | 原文/文档 | 实际源码 | 影响 |
|------|-----------|----------|------|
| README 端点错误 | `/v3/tools/list`、`/v3/tools/search` | 不存在 | 若按文档开发会浪费大量时间 |
| 真实端点 | - | `/v3/skill/search`、`/v3/conversation/search`、`/v3/knowledge/list` | JSON body，非 query string |
| 鉴权机制 | 单层 Bearer | 两层：网关级 `TDAI_GATEWAY_API_KEY`（本地禁用）+ 用户级 `user_key` | 必须同时携带 `Authorization: Bearer` + `x-tdai-service-id: default` |
| 本地状态 | 文档暗示需服务端 | 本地可直连（`base_url=http://localhost:2025`） | 无需额外服务部署 |

### 3.2 Phase 0b 任务分解

| 任务 | 输入 | 输出 | 验收标准 |
|------|------|------|----------|
| 3.2.1 更新 `export_cairn_for_tdam.py` | Phase 0a 端点文档、`.admin-credentials.json` | 适配 `/v3/skill/search`、`/v3/knowledge/list` 的导出脚本 | 脚本能读取 `cairn/*.md` 并构造正确 JSON body |
| 3.2.2 端到端测试导入 | 脚本 + TDAM 服务 | `cairn/` 核心文档进入 TDAM | `/v3/knowledge/list` 能看到新增条目；搜索返回预期结果 |
| 3.2.3 日志与错误处理 | 测试输出 | 稳定的导入流程 | 失败有明确 error message；支持断点续传 |

### 3.3 关键决策
- **导入范围**：先导 `AGENTS.md`、`ROADMAP.md`、`LOG.md`、`code-quality-review-wave6-20260806.md` 等核心文档，验证通路后再全量导入。
- **频率**：观察期内每日增量导入；观察期结束后改为每周全量同步。

---

## 四、观察期推进（P1）

### 4.1 今日目标
- **数据样本**：第 10 条（当前 9 条）
- **写入时间**：16:10 左右（跟随 `v84_ObservationBriefing` 定时任务）
- **验证**：`reports/evolution/daily_briefing_latest.md` 更新；`reports/shadow/daily_returns.jsonl` 新增一行

### 4.2 风险监控
| 指标 | 阈值 | 动作 |
|------|------|------|
| 单日回撤 | > -2% | 触发 `v84_ShadowAdmissionWatchdog` 复核 |
| 连续亏损 | ≥ 3 天 | 暂停观察期新增策略，启动复盘 |
| 数据缺失 | 任意 | 补录或标记为无效样本 |

---

## 五、对冲配置（P1）

### 5.1 当前状态
- **现货组合 Beta**：1.300（偏高）
- **期货对冲**：未配置（名义价值 ¥0）
- **期权保护**：未配置（权利金预算 ¥0）
- **现金余额**：¥2,328,895（46.6%）

### 5.2 今日决策结果（2026-08-07 已完成）

> **决策记录**：`decisions.jsonl` 已于 2026-08-07 01:30 写入

| 决策 | 选项 | 理由 | 失效条件 |
|------|------|------|----------|
| **A_no_adjustment** | A. 暂不建仓 | Beta 0.3558 已低于目标 0.80（实际已超对冲）；VIX=7.24 低波动环境；Unwind cost > benefit | If VIX > 15 or daily drawdown > -2%, reconsider unwinding or adding futures hedge |
| **期权头寸** | 3 Put + 2 Covered Call | 已于 2026-08-06 全部 FILLED | 持续监控 Delta decay |

### 5.3 明日待办
1. **早盘前**：评估市场状态（VIX、趋势、情绪），在 `v84_PostMarket` 或手动触发前给出明确决策。
2. **记录**：将决策写入 `decisions.jsonl`，包含理由、预期效果、失效条件。
3. **若选择 B/C**：更新 `positions.json` 中的期货期权计划头寸，并在 `daily_pnl_report` 中反映。

---

## 六、定时任务检查清单

| 任务 | 时间 | 预期结果 | 检查方法 |
|------|------|----------|----------|
| `v84_PostMarket` | 15:30 | 生成日终报告 | `daily_pnl_report_2026-08-07.md` |
| `v84_EvolutionEval` | 16:05 | 演化评估更新 | `reports/evolution/status.json` |
| `v84_ObservationBriefing` | 16:10 | 观察期播报 | `daily_briefing_20260807.md` |
| `v84_ShadowAdmissionDaily` | 16:15 | Shadow 数据写入 | `daily_returns.jsonl` 新增 |
| `v84_ShadowAdmissionWatchdog` | 17:00 | 数据质量检查 | 日志无异常 |

---

## 七、明日（2026-08-08）里程碑

- [ ] **Wave6 修复验证（实盘级）**：确认 C1、C2 两项致命修复在 15:30 日终链路中无 TypeError/NameError，对冲单正常生成
- [ ] **TDAM Phase 0b（已代码级完成）**：固化 `TDAM_TEAM_ID=team-n5phx61z0a` / `TDAM_AGENT_ID=agt-n5ppojc8rj` 到盘后作业运行环境；验证增量导入
- [ ] **观察期推进**：第 10 条样本成功写入（当前 9/14 天，16:10 定时任务）
- [ ] **对冲监控**：复核 `A_no_adjustment` 决策是否仍有效，若 VIX > 15 或回撤 > -2% 则重新评估
- [x] **后期排期 Phase 0（code-review-graph）已提前完成（08-07）**：版本 2.3.7=最新无需升级；图谱增量更新（8819节点/92531边/641文件）+ guide 8 项查询回归验证（7 正常）。报告 `docs/code-review-graph_升级回归验证_20260807.md`。发现未跟踪文件不入图盲区，新增文件须先 `git add` 再 `update`
- [ ] **后期排期 Phase 1（loopx 接入 live_scheduler）**：W34（8/18 起）执行，shadow 运行验证 7 天长跑无状态丢失；pdf-inspector/skills/cloudflare/TencentMemory 依次按 W36 起执行，详见 `docs/github_trending_高价值统计与升级计划_20260807.md` §后期排期

---

## 八、风险与阻塞

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Wave6 修复引入回归 | 交易中断 | 先在模拟盘验证，再小额度实盘 |
| TDAM 端点再次变更 | Phase 0b 阻塞 | 保留 Phase 0a 的源码核查方法论，随时 re-check |
| 观察期数据延迟 | 评估启用推迟 | 与数据源团队同步，确保交易日数据完整 |
| 对冲建仓时机不当 | 增加成本/亏损 | 严格按决策日志执行，避免盘中冲动 |

---

*本计划基于 2026-08-06 实际进展制定，已于 2026-08-07 日终复盘后更新。*
