# 代码质量 Bug 扫描与修复方案（2026-09-01 第二轮）

> 审查方式：ruff 静态检查 + 4 路并行深度审查（回测/训练/执行风控/数据层）+ 运行时复现验证 + Windows 计划任务巡检。
> 范围：2160 个 Python 文件 + 调度脚本（.bat/.ps1）+ 10 个计划任务。
> 与同日《代码质量审查报告_20260901.md》（CodeReviewExpert，总评 B+）互补：本轮聚焦**新发现的运行时/运维层 P0** 与核心链路逻辑 bug，均经人工验证或实机复现确认。

---

## 一、总评

| 维度 | 状态 |
|---|---|
| 静态质量（ruff） | ✅ 全库 0 error（2026-09-01 清零） |
| 防泄漏机制 | ✅ 扎实（Purged K-Fold + embargo + validate_embargo + DSR 日频口径正确） |
| 核心算法正确性 | ✅ 回测引擎无前视模式，绩效指标公式正确 |
| **生产调度** | ✅ P0 全部修复 + 健康监控告警上线（`v84_TaskHealthCheck` 每日 09:05，16/16 任务 OK） |
| 信号链路 | ✅ P1-1 已修复：LGB 信号改用真实最新行（TDD 回归覆盖） |
| 风控状态 | ✅ P1-2 已修复：止损水位线持久化 + 重启恢复（TDD 回归覆盖） |

> **处置总结（2026-09-01）**：P0×4 / P1×2 / P2×4 / P3×2 全部修复完毕（含 CI/本地 Python 版本统一至 3.14）；同日补做防复发基建（第八章）。无遗留项。

---

## 二、P0 — 立即修复（生产故障）

### P0-1 每日早8点晨报工作流计划任务连续失败（≥08/26，退出码 9009）✅ 已修复 (2026-09-01)

**证据**：
- `logs/daily_morning8_log.txt`：08/26、08/27、08/29、08/30、09/01 全部 `退出码: 9009`
- 计划任务 `V84_DailyMorning8Report` LastResult=9009（今日 08:00）
- 08/26–08/30：bat 回退到裸 `python`（任务会话 PATH 中无 python）→ 9009
- 09/01：已选中 `.venv\Scripts\python.exe`，仍在 10ms 内 9009，且无任何 Python 输出进日志

**根因定位**（已实机排除法验证）：
- ✅ 交互式手动运行同一 bat + 同一命令行 → 完整工作流成功（exit 0）
- ✅ 其他直接调用 `.venv\Scripts\python.exe` 的任务（GateCheckDaily / ShadowAdmission / ShadowS10 / IntradayLLM / DailyPnlReport）全部 LastResult=0
- ✅ bat 为 CRLF、UTF-8 无 BOM、无行尾空格；无 trailing-space 变量污染
- ⇒ 故障锁定在「**任务调度会话 → cmd → bat**」层（调度会话的 cmd 环境/chcp 65001 下 UTF-8 bat 解析问题），而非 Python 或脚本本身

**修复方案**（参照已验证成功的 v84_GateCheckDaily 模式，绕过 bat 层）：
```
Execute:   e:\各种PY程序\28-终极量化交易系统8.4\.venv\Scripts\python.exe
Arguments: "e:\各种PY程序\28-终极量化交易系统8.4\15_每日工作流\run_daily_morning.py" --phase all
Start in:  e:\各种PY程序\28-终极量化交易系统8.4
```
bat 保留为手动入口。另建议给该任务加失败告警（Task Scheduler → 发邮件/事件触发），避免再静默失败 6 天。

> **处置结果**：任务已按上述方案重注册，今日 LastResult=0（见 `reports/scheduled_tasks_health.json`）；失败告警已由第八章 `v84_TaskHealthCheck` 每日巡检覆盖。

### P0-2 多个注册/修复脚本硬编码不存在的解释器路径 ✅ 已修复 (2026-09-01)

`C:\Users\Administrator\AppData\Local\Programs\Python\Python311\python.exe` **实测不存在**（实际 3.11 位于 `C:\Users\Administrator\py311\python.exe`）。全库扫描实际发现 **10 个**代码文件（超出初审 4 个）：
- `register_intraday_task.ps1` / `repair_scheduled_tasks.ps1` / `service_manager.ps1` / `register_daily_trading_tasks.ps1`(DEPRECATED) / `register_trade_executor_tasks.ps1`(DEPRECATED)
- `15_每日工作流\run_eod_workflow.bat` / `setup_eod_scheduled_task.ps1` / `setup_retrain_scheduled_task.ps1`
- `scripts\deploy_trading_schedule.bat` / `scripts\trading_scheduler.bat`

**修复**：全部统一改为 `.venv\Scripts\python.exe`（3.14.4）。`repair_cn_tasks.ps1` 为外部项目（11_量化策略）一次性修复脚本，保留原样。复扫确认代码文件零残留。

### P0-3 v84_EOD_Fallback 任务失败（LastResult=1，今日 16:00）✅ 已修复 (2026-09-01)

**根因**（日志 `logs/eod_fallback.log` + 手动复现确认）：任务以 **SYSTEM 账户**运行 `C:/Users/Administrator/py311/python.exe`，SYSTEM 会话无 Administrator user site-packages → py311 缺 `urllib3`/`typing_extensions`（torch DLL 亦损坏）→ EOD 重跑 4 阶段失败（收盘报告 DeepSeek / LLM 决策 / Shadow Real Data Feeder `No module named 'urllib3'` / Drift Integrator 级联）→ exit 1。08-26、08-28、09-01 三次均此模式。脚本头部 08-21 的 `ADMIN_USER_SITE` PYTHONPATH 补丁依赖环境变量，但任务注册时从未设置 → 补丁失效。

**修复**：任务重注册为 `.venv\Scripts\python.exe scripts\eod_health_check_and_rerun.py`（SYSTEM 账户/工作日 16:00 触发保留，超时 72h→1h，加失败重试与 StartWhenAvailable）。脚本 `VENV_PYTHON=sys.executable` 自动让重跑子进程也用 .venv（依赖齐全）。

**验证**：手动触发 LastResult=0（原 1），健康检查 `✅ shadow 最新日期 2026-09-01 >= 目标`。下次调度 2026/9/2 16:00。

### P0-4 `.bat` 文件 LF-only 行尾 ✅ 已修复 (2026-09-01)

cmd 解析 LF-only bat 会产生命令拼接错乱（本轮已用最小复现 bat 确认：`'...python.exe" -c "print' 不是内部或外部命令` → 正是 9009 症状）。全库扫描发现 6 个异常文件（3 个全 LF + 3 个混合）：`run_eod_workflow.bat`、`run_eod_with_env.bat`、`15_每日工作流\run_weekly_report.bat`、`research\run_weekly_report.bat`、`scripts\cleanup_v7x_legacy_tasks.bat`、`scripts\trading_scheduler.bat`。

**修复**：全部统一转 CRLF（字节级替换，保持原编码），复扫确认全库 .bat 行尾清零。建议将 .bat 行尾检查加进 `scripts/pre_commit_check.py` 防回归。

---

## 三、P1 — 高优先（本周内）

### P1-1 LGB 训练「当前信号」滞后 label_horizon 个交易日 ✅ 已修复 (2026-09-01)

**位置**：`lgb_trainer/trainer.py` + `lgb_tscv_trainer.py`（同构 bug，horizon=1）

**Bug**：`dropna()` 丢弃 target 为 NaN 的最后 label_horizon 行后，`iloc[-1:]` 取的是 **T-h 行的特征** —「当前信号」实际预测的是 T-h→T 这段已经实现的历史收益，信号系统性滞后。tscv trainer 同构（滞后 1 日）。

**修复**（TDD：先写回归测试 `tests/unit/test_lgb_latest_signal_fix.py` 复现 3 红 → 修复 → 17 绿含既有 callback 测试）：
```python
latest_raw = df.iloc[[-1]]   # dropna 前保存真实最新行
df = df.dropna()
...
latest_features = np.asarray(latest_raw[selected_features].values, dtype=np.float64)
```
测试设计要点：末行在 dropna 后被丢弃不参与训练，因此修改末行特征跑两次训练（固定种子），模型相同而信号必须不同 — bug 场景下两次信号完全相同，判别力有保证（首轮测试曾因预测值容差过宽假绿，改为敏感性设计）。

### P1-2 移动止损水位线不持久化，重启后风控弱化 ✅ 已修复 (2026-09-01)

**位置**：`stop_loss_monitor.py`（`_high_water_mark`/`_low_water_mark` 纯内存）

**Bug**：水位线纯内存，进程重启后丢失。持仓曾涨 +20% 后监控重启，移动止损线从 `high×0.92` 回落到 `entry×0.92` —— 盈利持仓的止损保护大幅下移，恰在最需要锁盈的位置失效。`get_monitoring_status()` 已导出该数据但从不恢复。

**修复**（TDD：6 红 → 修复 → 17 绿，测试追加于 `tests/unit/test_stop_loss_monitor_unit.py` TestWaterMarkPersistence）：
- 水位线持久化到 `reports/stop_loss_water_marks.json`（复用 `atomic_write_json` 原子写）
- `__init__` 新增 `water_mark_file` 参数，启动时 `_load_water_marks()` 恢复；文件缺失/损坏容错为空 dict，不阻断启动
- 3 个更新点（多头 HWM / 空头 LWM / 清仓清除）更新即落盘
- 测试覆盖：多/空头重启恢复后 trailing stop 不回落、落盘存在性、清仓清除持久化状态、损坏文件容错、首次运行无文件；真实 `__init__` 冒烟通过（19 条规则加载）

**顺带发现**（未修，归入 P2-4 关联）：`_create_mock_broker` 的 `BrokerFactory.create()` 调用缺 `config` 参数 → MockBroker 降级路径本身也失败（broker=NoneType），但降级告警路径能工作，监控主流程不受影响。

---

## 四、P2 — 中等（下周排期）

### P2-1 OHLCV 缓存完整性校验双重缺陷 ✅ 已修复 (2026-09-01)

**位置**：`cache/data_downloader.py`

- **阈值口径错**：`days=730` 是日历天数，实际只产生约 490 个交易日行，永远 < 620.5 阈值 → `skip_if_exists` **从不生效**，每次全量重下。
- **无新鲜度校验**：行数碰巧达标的陈旧文件会被直接复用。

**修复**：双校验 — 行数 ≥ 预期交易日数（`days * 252/365 * 0.85`）且最后一行距今 ≤ 7 天历日；不达标时记录诊断日志后重下。

### P2-2 建仓下单：预算守卫价格基准不一致 + 强制最小手数 ✅ 已修复 (2026-09-01)

**位置**：`daily_trade_executor.py`

- 数量按 `max_buy_price`（价格带上限）估算，预算守卫却按 `ref_price` 验证 → 按带内高价成交时实际成本可超预算。
- `est_qty<=0`（预算不够一手）时强制 100 股，属超预算下单。

**修复**：预算守卫改用 `est_qty * max_buy_price`（最坏成本口径）；`est_qty<=0` 时区分两种情况 — 预算足够一手（上方高价股路径，`allocated >= min_lot_cost`）按一手执行，预算不足一手则跳过并告警。既有测试 173/173 通过。

### P2-3 benford_check 死代码 / 逻辑丢失（mypy 崩溃点）✅ 已修复 (2026-09-01)

**位置**：`utils/value_investing/financial_rigor.py`

**修复**：重建数字分布明细表（observed/expected/deviation/flag）并入返回 dict（附 `n_samples`）；删除 `is_ok` 双空分支；`_ALLOWED_BINOPS`/`_ALLOWED_UNARYOPS` 显式注解（UNARYOPS 为单参 `Callable[[Any], Any]`）。功能验证：500 样本 lognormal 数据 mad=0.0069，分布表 9 位完整。

### P2-4 测试套件健康度 ✅ 已修复 (2026-09-01)

- ✅ pytest-timeout 已加（`timeout=300, timeout_method=thread`，插件 timeout-2.4.0 已装）— 防挂起用例拖垮全量跑
- ✅ `network` marker 已注册（建议跑套件用 `-m "not network"`）；核查确认网络依赖用例已有 `integration` 标记隔离（`test_regression_bugfixes.py` 的 MarketDataProvider 实例化用例）；pytdx 单例测试已 mock
- ✅ MockBroker 降级失效已修（`BrokerFactory.create("mock")` 双重错误 → `create("simulated", config={})`，冒烟验证 broker=SimulatedBroker）
- ✅ CI/本地 Python 版本统一（2026-09-01 二批）：实测本地生产 `.venv` 为 **Python 3.14.4**（当日全部修复与 173 项测试在其上验证通过），而 CI 存在 3.11（ci/tdd-guard）+ 3.10（quality-gate/mmr-deep/mmr-judge）双重漂移。决策：**CI 统一升 3.14 对齐生产**（升 CI 纯文本零风险 vs 降级 .venv 需重装依赖且中断生产任务）。5 个工作流已改，ocr-review/ocr-nightly 无 Python 步骤不动；7 个 YAML 语法验证通过。requires-python>=3.10 无需改动。

---

## 五、P3 — 轻微 ✅ 已修复 (2026-09-01)

1. ~~ruff 36 处：全部位于 `backtests\_etf_rotation_2014\`~~ → 按项目 per-file-ignores 惯例豁免（研究性回测代码，与 `research/` 同类）。顺带清理全库其余 4 处：P1-1 新测试的 F841（真修）、`utils/system_check.py` 的 UP036/UP042（Py3.8 polyfill 有意设计，按惯例豁免）、`financial_rigor.py` 的 UP035（P2-3 引入，typing.Callable 保兼容，豁免）。**全库 `ruff check .` 现已 All checks passed。**
2. ~~`hedge_order_executor.py` fallback 去重键不含日期~~ → 键补 `date` 前缀（`{date}-{instrument}-{contracts}-{strike_rule}`），不同交易日的同参数订单不再误去重。

---

## 六、健康面（正面确认，无需动作）

- **防泄漏链路扎实**：`lgb_trainer/metrics.py`（P3-1 Purged K-Fold + embargo）+ `utils/purged_kfold.py`（`validate_embargo` 断言）+ `ci_lookahead_guard.py`（CI 扫描 bfill/shift(-N)/全样本标准化）。
- **DSR 实现正确**：`utils/alpha/fast_backtest.py` L244-256 日频口径转换 + `kurt()+3`（pandas 超额峰度转原始）处理无误。
- **回测引擎干净**：`utils/wt_backtest_engine.py` 无 bfill / shift(-N) / fillna(method) / 全样本标准化等前视模式。
- **绩效指标公式正确**：年化、最大回撤（expanding max）、Sharpe/Sortino/Calmar 与 metrics.py 对齐，除零/NaN 边界处理完备。
- **期权订单收集**：`_collect_pending_orders` 的 seen_ids 去重 + active_orders 嵌套结构兼容处理正确。
- **关键状态已原子写**：进度文件、触发日志均走 `atomic_write_json` / `_atomic_write_json`。
- 计划任务 7/10 健康（DailyShadowSample / TDAM / GateCheck / Shadow×2 / IntradayLLM / DailyPnl 均正常）。

---

## 七、修复排期建议

| 批次 | 项 | 内容 |
|---|---|---|
| 今天 | P0-1/2/3/4 | 重注册晨报任务（直调 python）；统一解释器路径；查 EOD fallback；LF→CRLF |
| 本周 | P1-1 / P1-2 | 训练信号改用真实最新行；HWM 持久化+恢复 |
| 下周 | P2-1~4 | 缓存双校验；下单守卫；benford 修复；测试套件治理（marker+timeout+CI 统一 3.14 ✅） |
| 顺手 | P3 | ruff exclude / fallback order_id 加日期 |

---

## 八、防复发与告警基建（同日补做）

### 8.1 pre-commit 新门禁：Windows 脚本健康检查 ✅

- 新增 [scripts/check_windows_scripts.py](file:///e:/各种PY程序/28-终极量化交易系统8.4/scripts/check_windows_scripts.py)：① git-tracked .bat/.cmd 无裸 LF（CRLF 校验）② .bat/.cmd/.ps1 引用的 python.exe 路径必须真实存在（绝对路径直接校验 / .venv 相对按项目根校验 / 裸 python 不查）
- 挂入 [scripts/pre_commit_check.py](file:///e:/各种PY程序/28-终极量化交易系统8.4/scripts/pre_commit_check.py) 第零道门禁（毫秒级，命中阻断）
- 首跑即抓到漏网之鱼 `repair_cn_tasks.ps1` 引用不存在的 Python311 → 已按 DEPRECATED 惯例修为 .venv

### 8.2 计划任务健康检查告警 ✅

- 新增 [scripts/check_scheduled_tasks_health.py](file:///e:/各种PY程序/28-终极量化交易系统8.4/scripts/check_scheduled_tasks_health.py)：v84_* 任务 FAILED（退出码≠0，容忍 0x41303 尚未运行）/ STALE（日频超 36h 未跑）判定，快照原子落盘 `reports/scheduled_tasks_health.json`，异常经 `utils.notify` 发钉钉/飞书告警（无配置降级日志）
- 注册计划任务 `V84_TaskHealthCheck`（每日 09:05，晨报后），注册脚本 [scripts/register_task_health_check.py](file:///e:/各种PY程序/28-终极量化交易系统8.4/scripts/register_task_health_check.py) 用 pywin32 COM 直传 XML（规避 schtasks UTF-16 与 PowerShell CIM 两个已知坑）
- 设计细节：排除自身（否则自我告警死循环）、跳过禁用任务、1999-11-30 零值时间视为从未运行
- 端到端验证：手动触发 → 正确检出异常 exit 1 + 告警发出 → 处置后 16/16 OK exit 0

### 8.3 顺带发现并处置：v84_UniverseScan 从未工作过

- 健康检查首跑即抓到该任务每天 exit 1。深挖：`factor_scorer.py` 引用的 `VibeFactorAdapter` **从未存在过**（git 历史确认引入版本即无此类；外部 Vibe-Trading 项目亦无对应接口）— 属"对冲基金级全市场选股"计划中预期但未实现的功能
- 其输出无任何生产代码消费。处置：**任务已禁用**（不再每天失败）；5 处 `get_vibe_adapter` 悬挂 import 修正为真实接口 `get_adapter`（延迟导入躲过了 dangling_refs 门禁 — 已知盲区）
- 若未来要恢复此功能：需实现完整因子引擎（list_factors/compute_single_stock，458 因子），属独立工程

---
