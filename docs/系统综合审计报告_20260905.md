# 系统综合审计报告（代码质量 / bug / 排期完成度 / 工业级差距）— 2026-09-05

> **审计范围**: `28-终极量化交易系统8.4` 全仓（1827 个 git 跟踪 .py）
> **证据链**: 门禁三件套实测 + ruff 全量统计 + py_compile 全仓 + unit 四分片证据链运行（`reports/test_runs/20260905_111209_unit-split4`）
> **审计性质**: 只读体检 + 修复方案；除标注"已顺手修"外未改生产代码

---

## 〇、总评

| 维度 | 评级 | 一句话 |
|------|------|--------|
| 语法健康 | 🟢 A+ | 1827 文件 py_compile **0 错误** |
| 测试健康 | 🟢 A | **15563 passed / 2 failed**（2F = 已知限制复现，非新回归）/ 64 skipped |
| 数据有效性 | 🟢 A | assert_data_validity **12 PASS / 0 FAIL**（D1 压力测试遗留已消除） |
| 工业级判据 | 🟢 A- | industrial_grade_check **10 PASS / 2 WARN / 0 FAIL** |
| 工程债务 | 🟡 B+ | debt gate **YELLOW**（唯一 XX：T6 fail-safe 宽捕获 31 处） |
| lint 卫生 | 🟡 B | ruff 全量 126 条（其中 60 条可自动修复） |
| 排期完成度 | 🟢 A- | v8.7 发布门禁 5 项 Blocker 中 3 项已达标、1 项 ON_TRACK、1 项待执行 |

**结论**: 系统处于"发布准备健康态"——无阻断性缺陷，剩余工作全部集中在**已知且已排期的项**（C1 QMT 接线、D11 样本积累、T6 渐进清理）。12-31 发布路径无新增风险。

---

## 一、代码质量

### 1.1 语法与编译
- **py_compile 全仓 1827 文件: 0 错误**（首跑 27 个 "OSError" 系 git quotePath 八进制转义假象，修正编码后实测 0）

### 1.2 ruff 全量统计（126 条）

| 规则 | 数量 | 性质 | 处置建议 |
|------|------|------|---------|
| T201 print | 22 | 非阻断（per-file-ignores 管理范围外残留） | 逐个转 logger 或豁免标注 |
| I001 import 排序 | 20 | 可自动修 | `ruff check --fix` 批次 |
| F401 未用导入 | 18 | 可自动修（防 F821 前先核对副作用导入） | AUTO-4 任务已覆盖 |
| W292 文件尾缺换行 | 15 | 可自动修 | 同上 |
| UP009 utf8 声明 | 7 | 可自动修（py3.8+ 冗余） | 同上 |
| ANN 系列缺注解 | 14 | 渐进补全 | AUTO-5 |
| N806/N803 命名 | 14 | 人工判断（可能有对外契约） | 低优 |
| E741 歧义变量名 | 6 | 人工改名（今日 merge 器已清 3 处） | 顺手修 |
| B905 zip 无 strict | 2 | 显式 strict= | 顺手修 |
| C901 复杂结构 | 1 | 重构候选 | 单列 |

**小计**: 60 条 `[*]` 可自动修复，建议开一个 `ruff --fix` 批次 commit（冻结窗前完成，属于"清理"非新功能）。

### 1.3 类型与债务
- **mypy**: 基线 957 持平（pre-commit 门禁三次全过，无新增退化）
- **T6 fail-safe 宽捕获**: **31 处**（阈值 30，YELLOW）——注意：已从 08 月初的 222 处降至 31，剩余为真实需要逐个人工判断的降级点，建议不再压数量而是逐处标注豁免理由
- **T7 裸 except**: 0（清零保持）
- **覆盖率**: 基线 0.833 ≥ 0.80（D9 门禁 PASS）

---

## 二、bug 排查（全量测试证据链）

### 2.1 运行结果
- **15563 passed / 2 failed / 0 errors / 64 skipped**（四分片，254s）
- 证据目录: `reports/test_runs/20260905_111209_unit-split4/`（failures_shard2.txt）

### 2.2 2F 归类（failures_shard2.txt）

| 失败 | 现象 | 归类（已做因果实验） |
|------|------|---------------------|
| `test_execution_bridge::test_execution_audit_contains_escalation_field` | 审计文件未生成（assert 0 > 0），执行计划本身生成正常 | **已知限制复现**，非新回归 |
| `test_execution_bridge::test_tca_audit_record_contains_tca_fields` | 同上（TCA 审计） | 同上 |

**因果实验**: 临时移除今日 conftest Tier-2 新增 3 条目 → 仍 2F → **排除本日改动**。根因 = 09-04 治理批次 3 已登记的"ai_decision 8 常量不可 patch"已知限制的表现面扩大：`ai_decision.execution_bridge` 在 import 时**值拷贝**了 `execution_audit._EXEC_AUDIT_DIR`，conftest Tier-2 patch 后者时前者仍持固化路径 → 审计写入与测试读取路径分叉。
**注**: 09-04 全量时该 2 测试通过（当时导入时序不同），属**导入顺序敏感的既有缺陷**，非功能性回归（执行计划/风控 veto 行为全部正常，仅审计落盘位置断言失败）。

### 2.3 修复方案（见 §五 F-1）

---

## 三、排期完成度（对照 ROADMAP Release Control Board 2026-09-05 版）

### 3.1 v8.7 发布门禁（Blocker 白名单）

| Gate | 状态 | 证据 |
|------|------|------|
| G-1 D11 双条件 | 🟡 ON_TRACK | stable 7/7 ✓（08-31 达标），samples 11/20（09-04 EOD，每交易日+1）；达标 09-17 EOD、复验 09-18 |
| G-2 门禁三件套 0 FAIL | 🟢 进行中 | 今日实测 industrial 10P/2W/0F + assert 12P/0F + debt YELLOW（无 FAIL）；21 天观察窗 12-10 起 |
| G-3 daily_workflow ≤3000 | ✅ | 2159 行 |
| G-4 覆盖率 ≥0.80 | ✅ | 0.833（基线冻结） |
| G-5 12-10 功能冻结执行 | ⏳ 待执行 | Change Budget 机械检查（D12）已就位待激活 |

### 3.2 各轨道完成度

| 轨道 | 完成度 | 关键节点 |
|------|--------|---------|
| Phase B（B1-B3） | ✅ 100%（08-27 全启用） | 阶段轨最终阶段 orchestrator |
| Phase B（B4） | 🔄 shadow 4/7 交易日 | ~09-09 达标 → 评估 USE_MLOPS_PIPELINE |
| Wave 7 Sprint 1 | 🟢 收尾就绪 | 09-11/12 判定材料（B1+B2 稳定 + 行数 + R10，不含 D11） |
| Wave 7 Sprint 2-4 | ⏳ 按排期 | T15 QMT paper（09-13~26）/ G9 FeatureStore / W7.4.6 发布 |
| ERL | ER-1.x ✅ / ER-2.x 双签待执行（09-13~18，checklist 已备）/ ER-3.x 冻结观察 | D-3 |
| shadow 三线 | cron 已注册（09-07 首触发，窗口 09-13~10-12） | MVSK preflight 9/9；**qlib 线已判无效（见 §四）**；S6 骨架风险已暴露 |
| ETF 子模型 | P3.2 运行中（NAV 1.0117） | P3.3 评估 ~10-10 |
| Wave 8-LIT / Wave 9-GH | ✅ 全部提前完成 / ⏳ 2027-01-04 | — |
| v8.7 发布（W7.4.6） | ⏳ 12-31（软件）+ 01-02~09 生产切换窗（R-3） | RC-1 12-11 起 |

### 3.3 本周（NEXT 14 DAYS）剩余动作
- 09-07: 双 cron 首触发验证 ｜ 09-11/12: Sprint 1 收尾材料 ｜ 09-13~18: ER-2.x 双签（checklist 已备）｜ 09-18: D11 复验（复验清单 A~E 已备）｜ 09-19 前: R-4 已完成 ✅

---

## 四、工业级差距（industrial_grade_check 12 判据 + v8.7.1 对照）

### 4.1 判据现状（10 PASS / 2 WARN / 0 FAIL）

| 判据 | 状态 | 差距说明 |
|------|------|---------|
| C1 执行闭环 | ⚠️ WARN | QmtBrokerAPI 已实现但 broker.enable=false / dry_run=true —— **物理前置 = xtquant 未安装 + 账号未配**（环境问题非代码缺口）；按排期 Phase 4（冻结窗后）接线 |
| C10 fills 新鲜度 | ⚠️ WARN | 最近 fills 2026-09-04（周六非交易日无成交，属正常节奏）；需关注周一 09-07 是否恢复当日落盘 |
| C2-C9/C11/C12 | ✅ PASS | 数据管道/环境隔离/信号执行分离/测试/CI/告警/成交回报/期权执行链/PnL 桥接/集成 全部达标 |

### 4.2 v8.7.1 计划项对照（发布后窗口，不阻塞 12-31）

| 项 | 状态 |
|---|---|
| P0 归因真实性（real_inputs_builder） | ✅ 09-02 完成（20 单测） |
| P0 Chaos 灾难演练 | ✅ 09-02 完成（40 用例） |
| P1 风险预算 regime 打通（G3） | ⏳ 2027-02-15~03-07 shadow → 03 月决策 |
| P1 Alpha Registry 统一化（G4） | ⏳ 2027-01 |
| Hardening: LLM 权限规范 + CI 隔离门禁（I-02 ◆） | ⏳ 未启动（AUTO-2 可提前做文档部分） |
| Hardening: 生产端成本模型核验（P3） | ⏳ 2027 |
| C++/Rust 核心路径 | ✅ 08-26 ROI 证伪搁置（重启条件：TICK 级延迟成瓶颈） |

### 4.3 新发现的研究线有效性缺口（今日审计新增）
- **qlib W7.2.9**: 双设计缺陷（接线错配 + 信号静态）→ 10-13 评估判 FAIL（设计口径），决策材料 `docs/qlib_w729_gap_analysis_20260905.md`
- **GNN S6 骨架风险**: s6 jsonl 当前全 skeleton（因子数据不可用）——若 09-13 cron 后仍全骨架，S6 30 天观察将无效。**建议 09-08 前排查 S6 runner 因子数据链路**（C4 合并器已内置骨架比例暴露）

---

## 五、修复方案（按优先级）

### P0（阻塞/临近节点，本周内）

| # | 项 | 方案 | 估工 | 窗口 |
|---|---|------|------|------|
| P0-1 | **S6 因子数据链路排查** | 检查 `s6_paper_trading_runner.py` 因子数据不可用的根因（CHAIN_MOM_60D 数据源/计算依赖），确保 09-13 cron 起产出真实记录而非骨架；若数据源本质不可得 → 提前判定 S6 无效，避免 30 天空跑 | 0.5d | 09-08 前 |
| P0-2 | **D11 样本积累守护** | 每日 EOD 后确认 daily_health_log +1 且 healthy=true（已由 T3 核对覆盖）；**严禁**任何"加速达标"操作（改 min_samples 属违规） | 0（例行） | 至 09-17 |
| P0-3 | **C10 fills 新鲜度复核** | 09-07（周一）EOD 后确认当日 fills 落盘；若仍无 → 排查 DTE-1/执行链当日是否有真实成交预期 | 0.2d | 09-07 |

### P1（冻结窗前，09-19 前）

| # | 项 | 方案 | 估工 | 窗口 |
|---|---|------|------|------|
| P1-1 | **execution_bridge 审计路径收口（2F 根治）** | `ai_decision/execution_bridge.py` 的审计写入改动态引用 `execution_audit` 模块属性（`import ai_decision.execution_audit as _ea; _ea._EXEC_AUDIT_DIR`）替代 import 时值拷贝；或测试 helper 改用被测模块常量（治理批次 3 既有修法）。修复前会失败/修复后通过的回归测试各 1 | 0.5d | 任意非交易日 |
| P1-2 | **ruff 可自动修批次** | `ruff check --fix`（60 条 I001/F401/W292/UP009）+ 人工复核 F401 副作用导入；单 commit | 0.3d | 冻结前 |
| P1-3 | **E741/B905 顺手清** | 8 处人工改名 + zip strict= | 0.2d | 同上 |
| P1-4 | **AUTO-2 LLM 权限边界规范** | 文档 + CI 检测性门禁（I-01 成文锚点） | 0.5d | 冻结前 |

### P2（冻结窗内 shadow 期 / 2027）

| # | 项 | 说明 |
|---|---|------|
| P2-1 | T201 print 22 处 | 逐个转 logger 或加豁免（每处需人工判断输出通道） |
| P2-2 | ANN 注解 14 处 | AUTO-5 渐进 |
| P2-3 | C901 复杂结构 1 处 | 重构候选单列评估 |
| P2-4 | C1 QMT 真实接线 | xtquant 安装 + 账号配置 + dry_run 影子期（Phase 4，冻结豁免评估后） |
| P2-5 | G3/G4（v8.7.1） | 2027 排期不变 |
| P2-6 | docs 08 月中旬报告类 ~8 文档归属核对 | 交云端 NPC AUTO 类 |

---

## 六、验收判据（修复方案完成的定义）

- P0-1: S6 jsonl 出现 status != "skeleton" 的真实记录，或 S6 无效判定已登记
- P0-3: 09-07 fills 当日新鲜（C10 转 PASS）或无成交证据留档
- P1-1: test_execution_bridge 全绿（分片内 + 单跑双通过）
- P1-2/3: ruff 全量 ≤ 60 条且无 [*] 可自动修项残留
- 全程: 门禁三件套无回归（工业级 ≥10P/0F、assert 12P、debt 无新 FAIL）

---
审计执行: 2026-09-05 ｜ 证据: `reports/test_runs/20260905_111209_unit-split4` + 门禁三件套实时输出 ｜ 指针: `cairn/LOG.md` 2026-09-05
