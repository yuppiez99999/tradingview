# A1 — codebase-memory-mcp 接入脚本

> 2026-08-21 | 纯工程提效, 不改主系统代码
> ⚠️ 涉及下载二进制 + 修改全局 Codex 配置, 需用户手动执行

## 价值
- tree-sitter 知识图谱, 158 语言, 14 MCP 工具, 120x 省 token
- 主系统 136+ py 文件导航/impact 分析提速
- 零依赖单二进制 (macOS/Linux/Windows)

## 接入步骤 (Windows PowerShell)

### 1. 下载安装
```powershell
# 一键安装 (自动配置 Codex/Claude Code/Gemini CLI 等 11 agent)
Invoke-WebRequest -Uri https://raw.githubusercontent.com/DeusData/codebase-memory-mcp/main/install.ps1 -OutFile install_cbm.ps1
# 建议先检查脚本
notepad install_cbm.ps1
# 执行 (会修改全局 agent 配置)
.\install_cbm.ps1
```

### 2. 验证安装
```powershell
# 重启 Codex 后, 在主系统目录执行
cd "e:\各种PY程序\28-终极量化交易系统8.4"
# 对 Codex 说 "Index this project"
```

### 3. 验证提效
- 一次结构查询 (如 "哪些文件 import了 hedge_engine") vs grep 的 token 消耗对比
- 预期: ~3400 tokens vs ~412000 tokens (120x 省)

## 安全确认
- SLSA 3 签名 + VirusTotal 扫描 + 100% 本地处理 (代码不离开机器)
- 完整源码: https://github.com/DeusData/codebase-memory-mcp

## 不自动执行的原因
本脚本会修改全局 Codex/Claude Code 配置文件, 属于用户环境变更, 需用户确认后手动执行。