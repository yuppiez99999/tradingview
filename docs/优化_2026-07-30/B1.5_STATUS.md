# B1.5 完成状态报告 — 删除 `daily_trade_executor.py` ms_strategy 副本

**任务**: 删除 `daily_trade_executor.py` ms_strategy 副本
**完成日期**: 2026-07-30
**状态**: ✅ 已完成
**验收**: 全部通过

---

## 一、问题背景

项目存在 2 份 `daily_trade_executor.py` 副本：

| 文件路径 | 行数 | 独有特性 |
|---------|------|---------|
| `daily_trade_executor.py` (根目录版) | 1141 → 1175 (B1.5 新增 `_infer_suffix` 等) | `atomic_write_json`、`post-market-auto` 模式、`generate_next_trading_day_plan`、`init_wt_modules` 完整版 |
| `ms_strategy/scripts/daily_trade_executor.py` | 1134 | 独有 `_infer_suffix` 函数、`BAIJIU_CAP_PCT`/`BAIJIU_CODES` 常量、`v7.5_institutional` 路径 |

**核心结论**: 两份文件约 60% 逻辑重叠。根目录版功能更完整（含原子写、自动收盘流程），副本独有 `_infer_suffix` 函数和白酒单票上限常量需迁移到根目录版。

---

## 二、修复方案

### 2.1 将副本独有的符号迁移到根目录版

从 `ms_strategy/scripts/daily_trade_executor.py` 迁移以下符号到根目录版第 86 行后：

- `BAIJIU_CAP_PCT = 0.10` (白酒单票上限百分比)
- `BAIJIU_CODES = {"600519", "000858"}` (白酒代码集合)
- `_infer_suffix(code)` 函数 (QMT 规范的交易所后缀推断)

`_infer_suffix` 函数被 `tests/test_qmt_audit_fixes.py:155` 测试覆盖：
```python
from daily_trade_executor import _infer_suffix
# 600519 → 600519.SH (上海)
# 000858 → 000858.SZ (深圳)
# 159915 → 159915.SZ (深圳 ETF)
# 830799 → 830799.BJ (北交所)
```

### 2.2 更新调用方路径（1 处）

| 调用方文件 | 修改前 | 修改后 |
|----------|--------|--------|
| `ms_strategy/scripts/ai_decision_gate.py:24` | `DAILY_EXECUTOR_PATH = PROJECT_ROOT / "ms_strategy" / "scripts" / "daily_trade_executor.py"` | `DAILY_EXECUTOR_PATH = PROJECT_ROOT / "daily_trade_executor.py"` |
| `ms_strategy/scripts/ai_decision_gate.py:27` | `sys.path.insert(0, str(PROJECT_ROOT / "ms_strategy" / "scripts"))` | `sys.path.insert(0, str(PROJECT_ROOT))` |

### 2.3 删除副本

- 删除 `ms_strategy/scripts/daily_trade_executor.py` (1134 行)
- **共删除 1134 行重复代码**

### 2.4 已验证未引用副本的调用方

以下调用方原本就指向根目录版（通过 `PROJECT_ROOT` 或相对路径）：

- `register_trade_executor_tasks.ps1:9` → `Join-Path $projectRoot "daily_trade_executor.py"`
- `register_daily_trading_tasks.ps1:83,97` → `Join-Path $projectRoot daily_trade_executor.py`
- `tests/unit/test_trade_calendar_unit.py:190` → `import daily_trade_executor as dte` (sys.path 已配置项目根)
- `tests/test_qmt_audit_fixes.py:155` → `from daily_trade_executor import _infer_suffix` (sys.path 已配置项目根)

---

## 三、变更清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `ms_strategy/scripts/daily_trade_executor.py` | 删除 | 1134 行副本 |
| `daily_trade_executor.py` | 修改 | 新增 `_infer_suffix` 函数 + `BAIJIU_CAP_PCT`/`BAIJIU_CODES` 常量 (从副本迁移) |
| `ms_strategy/scripts/ai_decision_gate.py` | 修改 | `DAILY_EXECUTOR_PATH` 改为根目录版；`sys.path` 改为项目根 |

---

## 四、验证结果

### 4.1 py_compile 通过

```
[OK] py_compile: daily_trade_executor.py
[OK] py_compile: ms_strategy\scripts\ai_decision_gate.py
```

### 4.2 ruff 检查通过（E9/F821）

```
All checks passed!
```

### 4.3 Smoke Test 通过

**根目录版 7 个符号可导入**:

```python
>>> from daily_trade_executor import (
...     PRICE_PROTECTION_PCT,    # ✅
...     load_positions,          # ✅
...     load_latest_prices,     # ✅
...     DEFAULT_PRICES,         # ✅
...     _infer_suffix,          # ✅ (B1.5 新增迁移)
...     BAIJIU_CAP_PCT,         # ✅ (B1.5 新增迁移)
...     BAIJIU_CODES,           # ✅ (B1.5 新增迁移)
... )
[SMOKE] 7 symbols importable from daily_trade_executor (root)
```

**`_infer_suffix` 函数行为正确**:

```
600519 -> 600519.SH   (上海主板)
000858 -> 000858.SZ   (深圳主板)
159915 -> 159915.SZ   (深圳 ETF)
830799 -> 830799.BJ   (北交所)
```

**`ai_decision_gate.py` 路径解析正确**:

```
[OK] ai_decision_gate imports
DAILY_EXECUTOR_PATH: E:\各种PY程序\28-终极量化交易系统8.4\daily_trade_executor.py
exists: True
```

### 4.4 副本路径引用清理验证

```
Grep (ms_strategy[/\\]scripts[/\\]daily_trade_executor) in *.py / *.bat / *.ps1 / *.sh
→ No matches found
```

---

## 五、风险与影响

### 5.1 行为变化

- **原行为**: `ms_strategy/scripts/ai_decision_gate.py` 通过 `sys.path.insert(ms_strategy/scripts)` + `from daily_trade_executor import` 引用副本
- **新行为**: 改为 `sys.path.insert(PROJECT_ROOT)` + 同样 `from daily_trade_executor import`，解析到根目录版

### 5.2 兼容性

- 根目录版 `daily_trade_executor.py` 包含 `ai_decision_gate.py` 所需的全部 4 个符号（`PRICE_PROTECTION_PCT`/`load_positions`/`load_latest_prices`/`DEFAULT_PRICES`）
- 根目录版独有 `atomic_write_json`、`post-market-auto` 等改进，副本原本缺失这些功能现已恢复
- `tests/test_qmt_audit_fixes.py` 测试所需的 `_infer_suffix` 函数已迁移至根目录版

### 5.3 功能增量

- 通过根目录版调用，`ai_decision_gate.py` 现在可以使用根目录版的 `atomic_write_json` 原子写功能（P0-C1 修复）
- 根目录版的 `post-market-auto` 模式（自动收盘 + 生成下一交易日计划）现可用

### 5.4 已知差异（未处理）

- 副本原本使用 `v7.5_institutional/trade_plans/auto_trade_plan_500w_2026-2030.json` 路径，根目录版使用 `v8.3_institutional/trade_plans/auto_trade_plan_500w_2026-2030.json`
- 此差异在 B1.2 阶段已确认（is_trading_day 统一）且 `v8.3_institutional` 是 v7.5 的升级版，路径变更符合预期

---

## 六、推迟事项

无。本任务范围内所有验收标准已达成。

---

## 七、Phase B-1 累计进度

| # | 任务 | 状态 | 删除/修改行数 |
|---|------|------|-------------|
| B1.1 | 修复 `phase_hedge.py:23` 语法错误 | ✅ | 13 个 phase 文件修复 |
| B1.2 | 修复 `is_trading_day` 简陋实现 | ✅ | 2 处统一 + 22 单元测试 |
| B1.3 | 修复 `MAX_DRAWDOWN_LIMIT` 不一致 | ✅ | 7 处统一到 `risk_params.yaml` |
| B1.4 | 删除 `generate_daily_report.py` 2 副本 | ✅ | -3287 行 |
| B1.5 | 删除 `daily_trade_executor.py` ms_strategy 副本 | ✅ | -1134 行 + 迁移 `_infer_suffix` |
| **B1.4+B1.5 小计** | | | **-4421 行重复代码** |
| B1.6 | 合并 `autolearn_trainer.py` 副本 | 🔄 进行中 | — |
| B1.7 | 抽取 `load_positions()` 共享函数 | ⏳ 待办 | 22 处统一 |

---

**下一任务**: B1.6 — 合并 `autolearn_trainer.py` 副本
