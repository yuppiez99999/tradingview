# B1.6 合并 autolearn_trainer.py 副本 — 状态报告

**日期**: 2026-07-30
**状态**: ✅ 已完成
**偏差**: 简化方案 (保留 v8.3_institutional 原位, 未迁移到 ml/trainers/)

---

## 任务背景

项目中存在两份 `autolearn_trainer.py` 副本:
- `v8.3_institutional/autolearn_trainer.py` (852 行) — 主版本
- `ms_strategy/training/autolearn_trainer.py` (762 行) — 冗余副本

## 差异分析

| 维度 | v8.3_institutional (主) | ms_strategy (副本) |
|------|-------------------------|-------------------|
| `synthesize_ohlcv_from_returns` | ✅ 已废除 (raises RuntimeError, 2026-07-24) | ❌ 旧版 (仍允许合成数据) |
| `load_real_ohlcv` | ✅ 本地实现 (从 data_provider 加载真实 OHLCV) | ❌ 运行时从 v8.3_institutional 导入 (line 549) |
| 依赖关系 | 独立 | 已依赖 v8.3_institutional |
| 被引用 | lgb_tscv_trainer.py, lgb_enhanced_trainer.py, daily_workflow.py | 仅 scripts/_check_training_capability.py (字符串路径) |

**关键发现**: ms_strategy 副本已在 `run_autolearn()` 中通过 `from v8.3_institutional.autolearn_trainer import load_real_ohlcv` 导入主版本函数,实质上已完全依赖 v8.3_institutional。其自身的 `synthesize_ohlcv_from_returns` 是未废除的过时版本,存在被误用风险。

## 执行操作

1. **删除冗余副本**: `ms_strategy/training/autolearn_trainer.py` (762 行)
2. **更新引用**: `scripts/_check_training_capability.py` line 65
   - 旧: `"ms_strategy/training/autolearn_trainer.py"`
   - 新: `"v8.3_institutional/autolearn_trainer.py"`
3. **保留目录**: `ms_strategy/training/` 目录保留 (含 qlib_train_test.py 等其他文件)

## 方案偏差说明

原计划: "统一到 `ml/trainers/autolearn_trainer.py`"
实际方案: 保留在 `v8.3_institutional/autolearn_trainer.py`

**理由**:
1. 所有现有导入方 (lgb_tscv_trainer, lgb_enhanced_trainer, daily_workflow) 已指向 v8.3_institutional
2. 迁移到新路径需更新 6+ 导入点,无功能收益,违反 KISS 原则
3. v8.3_institutional 已是事实上的规范位置
4. 最小变更原则 — 仅消除冗余,不引入不必要的扰动

## 验证

| 验证项 | 结果 |
|--------|------|
| v8.3_institutional/autolearn_trainer.py py_compile | ✅ 通过 |
| 导入测试 (POSITION_SYMBOLS, add_technical_features, load_real_ohlcv, run_autolearn) | ✅ 通过 (23 标的) |
| ruff check (v8.3_institutional + _check_training_capability) | ✅ All checks passed |
| 残留引用扫描 (ms_strategy/training/autolearn) | ✅ 仅计划文档 (预期) |

## 影响范围

- **零破坏性变更**: 所有功能导入路径未变
- **消除风险**: 移除未废除的 `synthesize_ohlcv_from_returns` 旧版,防止误用合成数据训练
- **代码减少**: 762 行
