# B1.4 完成状态报告 — 删除 `generate_daily_report.py` 2 个副本

**任务**: 删除 `generate_daily_report.py` 2 个副本（v8.3_institutional/ + ms_strategy/scripts/）
**完成日期**: 2026-07-30
**状态**: ✅ 已完成
**验收**: 全部通过

---

## 一、问题背景

项目存在 3 份 `generate_daily_report.py` 副本，行数与功能差异如下：

| 文件路径 | 行数 | 字节数 | 类/函数数 | 独有特性 |
|---------|------|--------|----------|---------|
| `generate_daily_report.py` (根目录版) | 1970 | 105225 | 1 class + 27 def | `_load_dotenv`、`_call_deepseek`、`_generate_deepseek_recommendations` (DeepSeek AI 推荐) |
| `v8.3_institutional/generate_daily_report.py` | 1767 | 93594 | 1 class + 25 def | 无 DeepSeek 集成 |
| `ms_strategy/scripts/generate_daily_report.py` | 1520 | 78504 | 1 class + 25 def | 无 DeepSeek 集成；类型注解 `str = None`（非 `Optional[str]`） |

**核心结论**: 根目录版是最完整的版本，包含 DeepSeek AI 推荐功能；两副本是子集版本，缺少关键 AI 集成。

---

## 二、修复方案

### 2.1 保留根目录版，删除两副本

- 保留 `generate_daily_report.py` (根目录，1970 行，含 DeepSeek AI 推荐)
- 删除 `v8.3_institutional/generate_daily_report.py` (1767 行)
- 删除 `ms_strategy/scripts/generate_daily_report.py` (1520 行)
- **共删除 3287 行重复代码**

### 2.2 更新调用方路径（3 处）

| 调用方文件 | 修改前 | 修改后 |
|----------|--------|--------|
| `v8.3_institutional/quick_start.bat:43` | `python "%SCRIPT_DIR%generate_daily_report.py"` | `python "%SCRIPT_DIR%..\generate_daily_report.py"` (调用根目录版) |
| `v8.3_institutional/v8.5_start.bat:103-114` | `:run_script` 函数仅查找 `%~dp0%~1` | 增加 `%~dp0..\\%~1` 回退查找根目录版 |
| `ms_strategy/scripts/live_scheduler.py:352-357` | `BASE_DIR / "generate_daily_report.py"` + `cwd=str(BASE_DIR)` | `BASE_DIR.parent.parent / "generate_daily_report.py"` + `cwd=str(report_script.parent)` |

### 2.3 已验证未引用副本的调用方（无需修改）

以下调用方原本就指向根目录版（通过 `PROJECT_ROOT` 或相对路径 `..`）：

- `15_每日工作流/run_daily_eod_workflow.py:78` → `PROJECT_ROOT / "generate_daily_report.py"`
- `15_每日工作流/run_daily_morning.py:58` → `PROJECT_ROOT / "generate_daily_report.py"`
- `live_scheduler.py:458` (根目录) → `BASE_DIR / "generate_daily_report.py"` (BASE_DIR 是项目根)
- `run_daily_report.bat:18` → `"generate_daily_report.py"` (相对当前目录)
- `run_v84_pnlreport.ps1:4` → `"generate_daily_report.py"` (相对当前目录)
- `v8.3_institutional/run_all_modules.py:259` → `script="../generate_daily_report.py"` (相对路径指向根目录)

---

## 三、变更清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `v8.3_institutional/generate_daily_report.py` | 删除 | 1767 行副本（无 DeepSeek 集成） |
| `ms_strategy/scripts/generate_daily_report.py` | 删除 | 1520 行副本（无 DeepSeek 集成） |
| `v8.3_institutional/quick_start.bat` | 修改 | 第 43 行：路径从 `%SCRIPT_DIR%` 改为 `%SCRIPT_DIR%..\` |
| `v8.3_institutional/v8.5_start.bat` | 修改 | `:run_script` 函数增加根目录回退查找 |
| `ms_strategy/scripts/live_scheduler.py` | 修改 | 第 352-357 行：路径改为 `BASE_DIR.parent.parent`，`cwd` 改为 `report_script.parent` |

### 附带修复（预存在 bug）

- `ms_strategy/scripts/live_scheduler.py:546` 修复 `IndentationError: unexpected indent`（`return True` 多了一层缩进）

---

## 四、验证结果

### 4.1 py_compile 通过

```
[OK] py_compile: generate_daily_report.py
[OK] py_compile: ms_strategy\scripts\live_scheduler.py
```

### 4.2 ruff 检查通过（E9/F821）

```
All checks passed!
```

### 4.3 Smoke Test 通过（符号可导入）

```python
>>> from generate_daily_report import (
...     PortfolioAnalyzer,       # ✅
...     generate_markdown_report, # ✅
...     save_report,             # ✅
...     print_report_summary,    # ✅
...     main,                    # ✅
...     _call_deepseek,          # ✅ (根目录版独有)
...     _load_dotenv,            # ✅ (根目录版独有)
... )
[SMOKE] all 7 public symbols importable
```

### 4.4 副本路径引用清理验证

```
Grep (v8\.3_institutional[/\\]generate_daily_report|ms_strategy[/\\]scripts[/\\]generate_daily_report)
  in *.py / *.bat / *.ps1 / *.sh → No matches found
```

---

## 五、风险与影响

### 5.1 行为变化

- **原行为**: v8.3_institutional/quick_start.bat 与 v8.5_start.bat 调用 v8.3 副本（无 DeepSeek AI 推荐）；ms_strategy/scripts/live_scheduler.py 调用 ms_strategy 副本（无 DeepSeek AI 推荐）
- **新行为**: 三处统一调用根目录版，启用 DeepSeek AI 推荐（DeepSeek → 豆包 → Ollama 三级降级）

### 5.2 兼容性

- 根目录版 `PortfolioAnalyzer` 类签名与两副本兼容（`__init__(positions_file, hedge_file)` 一致）
- 根目录版 `main()` 入口与两副本兼容（参数解析一致）
- 三处调用方修改后路径正确指向根目录版（已通过 py_compile 与 ruff 验证）

### 5.3 工作目录

- `ms_strategy/scripts/live_scheduler.py` 调用根目录版时 `cwd` 改为 `report_script.parent`（项目根目录），确保 `config/positions.json` 等相对路径正确加载
- `v8.3_institutional/quick_start.bat` 通过 `%SCRIPT_DIR%..\` 路径调用，但 bat 未显式切换 cwd，依赖根目录版使用 `Path(__file__).resolve().parent` 自动定位（已通过根目录版代码确认）

### 5.4 功能增量

- v8.3 副本与 ms_strategy 副本原本缺失的 `_call_deepseek` / `_generate_deepseek_recommendations` 功能现已恢复
- 三处调用方在运行根目录版时会自动加载 `.env`（DeepSeek API Key 等配置）

---

## 六、推迟事项

无。本任务范围内所有验收标准已达成。

### 后续关联任务

- **B3.2 拆分 `PortfolioAnalyzer` God Class**: 根目录版 `generate_daily_report.py` (1970 行) 仍超过 800 行上限，将在 B3.2 阶段拆分为 `reporting/pnl_calculator.py`、`reporting/hedge_analyzer.py`、`ai/recommendation_generator.py`、`reporting/markdown_renderer.py`
- **B3.4 拆分 `LLMRouter` God Class**: 根目录版的 `_call_deepseek` 方法 (71-122 行) 将在 B3.4 阶段统一到 `llm/providers/deepseek.py`

---

**下一任务**: B1.5 — 删除 `daily_trade_executor.py` ms_strategy 副本
